# Diář 2026-06-01

## Sezení 55 — lanovka/vlek → 510 (sloučeno do `--powerlines`) + korekce „katalog vyčerpán"

Stroj: **ntbhej21**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (clean), žádný neuzavřený %END.
Audit cadence: všechny prahy daleko (CODE Sez. 50 +4, DOCS Sez. 46 +8, pruning Sez. 50 +4, CALIBRATE
Sez. 51 +3). Stale Příště: „oplocené volné terény" viselo 4× (blíží se práh 5).

### Volba fokusu — uživatel zpochybnil „katalog vyčerpán"
Navrhl jsem fokus (oplocené terény / velké plochy s děrami / posun k UC5), uživatel místo toho:
**„Které ZABAGED ještě nemáme pokryty?"** → revize katalogu. Tvrdil jsem „vyčerpán, zbytek 0-prvkový",
ale uživatel oponoval: *„Lanovka/vleky mohou být na dvou z pěti map. Také na LS máme kamenolom…"*

**Censure já (`verify-data-not-assume`):** můj „0 prvků" byl **neměřený odhad** — ○ kandidáti lanovka/lom/
brod/podjezd/hráz nesli v katalogu jen „hory / Σ1", NIKDY se neproměřili jako Sez. 43. Probe
`temp/probe_remaining_layers.py` (`returnCountOnly` 5 lokalit) dal tvrdá čísla:

| ISOM | Vrstva | Σ (5 lokalit) |
|---|---|---|
| 510 | Lanová dráha / vlek | 3 (NL 2, LS 1) + stožáry 6 |
| 208 | Skupina balvanů (linie) | 14 (SV 7, HS 3, NV 4) |
| 528 | Přehradní hráz, jez | 13 (LS 7) |
| 519? | Podjezd (linie) | 12 (LS 11) |
| 519? | Brod | 6 |
| 313 | Vodopád (bod) | 2 |
| 201 | Lom | 1 (LS, kamenolom) |
| 210 | Suť | 1 |

Uživatelova intuice (lanovka 2/5, lom LS) **přesně seděla**. „Vyčerpáno" tvrdit až po měření.

### Foundations PŘED kódem (verify-against-source)
- **Template id=121:** ISOM **510 = „Power line, cableway _or skilift_"** = JEDEN symbol pro vedení i
  lanovku/vlek; „bars show exact location of pylons" = příčky na stožárech.
- **Probe atributů (NL):** `Lanová dráha, lyžařský vlek` id=72 = `Polyline`, atribut `typ_ldv_k=VLL`
  „lyžařský vlek" (ISOM nerozlišuje typ); `Stožár lanové dráhy` id=61 = `Point`, bez atributů (jako
  stožár el. vedení) → **dokonalý mirror** `Elektrické_vedení` + `Stožár_elektrického_vedení`.

### Rozhodnutí (AskUserQuestion) — sloučit do `--powerlines`
ISOM 510 nerozlišuje zdroj → render/maska/omap/meta identické. Uživatel zvolil **sloučit** (KISS, DRY,
precedent --rocks/--landmarks: 1 ISOM doména = více ZABAGED zdrojů). GT maska 510 = „kde je symbol 510"
(UC5 čte mapu, ne zdroj) → rozlišení lanovka/vedení v masce nemá hodnotu.

### Kód (4 edity, čisté rozšíření)
- `zabaged.LAYER_IDS` += `Lanová dráha, lyžařský vlek`:72 / `Stožár lanové dráhy`:61 (REST jména s mezerou).
- `POWERLINE_LAYERS` + `POWERLINE_MAST_LAYERS` += lanovka / stožár lanovky.
- `map_powerline_to_isom` docstring (vždy 510; `typ_ldv_k` nerozlišuje) — **logika 0 změn** (už `return 510`).
- `generator.POWERLINE_NAME` → oficiální ISOM „Power line, cableway or skilift".
- batch beze změny (lanovka pod `--powerlines`, který je v obou batch větvích off; B1 OK).

### Verify
- **proc baseline byte-identický:** git-stash diff → proc `.omap` md5 `a76af84…` s edity i bez nich
  (noise větev `powerlines='off'` nedotčena; edity izolované do real powerline bloku). Behavior-preserving
  PROKÁZÁN, ne tvrzen (vzor Sez. 54 holes).
- **Lanovka integrována PŘESNĚ dle probe** (meta items per lokalita): NL 3 vedení + 2 lanovky = 5;
  LS 19 + 1 = 20; SV/HS/NV lanovka 0. Delta omap objektů na SV/HS (+5/+6, lanovka=0) = nezávislý drift
  reálných ZABAGED dat, ne má změna (můj příspěvek = přesně 3 lanovkové linie NL+LS).
- **Vizuál NL:** rgb crop = 2 paralelní vleky (Ještěd) symbolem 510 s kolmými příčkami; GT maska
  `mask_powerlines.png` nese vleky s příčkami na stožárech (fáze 1). **Žádný nový ISOM symbol** → OOM
  verify netřeba (510 už ověřeno u el. vedení; lanovka jen přidává linie téhož symbolu).
- 5 lokalit regen s ortofotem (SV 6036 / NL 1833 / LS 35649 / HS 7555 / NV 2302 omap), STATISTICS regen.

### Propagace
katalog (lanovka/stožár ○→✓ + čísla; **korekce „vyčerpán" Sez. 52** + tvrdá čísla k ◐/○ kandidátům;
**oprava driftu strom ◐→✓** — implementace `LANDMARK_POINT_LAYERS_417` od Sez. 43), architecture (výčet
zabaged), spec §4.9c (vedení+lanovka), GLOSSARY (termín), README root+generator+connectors, TODO
(`[x]`→`[~]`: katalog NENÍ vyčerpán), DONE, STATISTICS.

### Audit cadence
%AUDIT:CODE Sez. 50 (+5; malá feature). DOCS Sez. 46 (+9 — **zralý příště**, +1 nad práh za sezení). pruning
Sez. 50 (+5). %CALIBRATE Sez. 51 (+4). Stale Příště: oplocené terény stále nevybráno (viselo by 5× → příště rozhodnout DO/DROP).

### Kudos / Censure
- **Kudos uživatel:** zpochybnění „katalog vyčerpán" datovou intuicí (lanovka 2/5, lom LS) — trefil, že
  ○ kandidáti nebyli změřeni; jasná volba sloučení do `--powerlines`.
- **Censure já (`verify-data-not-assume`):** tvrdil jsem „0 prvků / katalog vyčerpán" z neměřeného
  katalogového odhadu, místo abych proměřil jako Sez. 43. Oprava: probe všech zbylých ○/◐ kandidátů.
- **Proces dobře:** foundations PŘED kódem (template 510 spec + probe atributů); behavior-preserving
  prokázán git-stash md5 diffem; lanovka ověřena per-lokalita proti probe (ne celkové counts, kvůli data driftu).

### Příště (Sez. 56)
1. *(změřeno, dobíratelné)* **balvany-linie → 208 Boulder field** (Σ14, nový area pattern trojúhelníky) —
   nebo **podjezd → 519** (LS 11, verify ISOM 519 spec před kódem).
2. *(Stale, rozhodnout DO/DROP — viselo by 5×)* **oplocené volné terény** (RÚIAN {5,13} ~80 % limit, %THINK).
3. *(zralý)* **%AUDIT:DOCS** (+9/10, za sezení překročí; po vlně Sez. 52–55 zkontrolovat drift docs/katalogu).

---

## Sezení 56 — přechod ntbhej→mrkla + kamenolom → 520

Stroj: **mrkla**. `%BEGIN` krok 0 odhalil **klon 15 commitů pozadu** za origin/main (ntbhej odjel Sez. 49–55).
ff-sync PŘED prací (přesně scénář, před kterým varuje projektový %BEGIN). Mezi novinkami i přesun docs
root→`docs/` (Sez. 48). Kontinuita přechodu OK: diár Sez. 55 úplný, žádný neuzavřený %END.

### Regen 5 lokalit s ortofotem (žádost uživatele)
Po přechodu stroje nejsou lokální rendery (`output_*`/`maps/` gitignored, nepřenáší se). Regen všech 5
(SV/NL/LS/HS/NV) s defaultním ortofotem. **omap counts byte-shodné se Sez. 55** (SV 6036 / NL 1833 /
LS 35649 / HS 7555 / NV 2302) → reprodukovatelnost potvrzena datově (stejný kód + zachovaná cache = týž výstup).

### Kamenolom → ISOM 520 (návrh uživatele)
Pokračování probe Sez. 55 (lom Σ1). Uživatel: „kamenolom = většinou oplocený areál se zákazem vstupu → olivová 520".

- **Foundations (verify-against-source):** katalog vedl lom jako odložené **201 Impassable cliff**, ale 201 je
  **LINIE** (hrana stěny s ticky) a ZABAGED `Povrchová těžba, lom` (id 118) je **PLOCHA** → to byl důvod odložení
  (Sez. 44). Uživatelových **520 ten nesoulad ruší** (plocha→plocha, raw kresba jako hřbitov), sémanticky sedí.
- **Probe LS** (`temp/probe_quarry.py`): lom Σ1, `druhtez_p='kámen'`, centroid (-685545,-971453); areály 114 Σ177,
  nejbližší od lomu **469 m** → **0 překryv** (potvrdilo uživatelovo pozorování A2: lom teď nevykreslený).
- **Rozhodnutí (AskUserQuestion A1/A2):** A1 = čistá 520 bez dotahování stěny 201 (Σ1, KISS); kamenné/zemní útvary
  zůstávají v z-orderu NAD olivovou (řeší se existujícím z-order, nová color-table varianta „olivová pod černou
  a hnědou" až kdyby reálný překryv vadil). A2 = probe ověřit, ale uživatel vidí lom nevykreslený.

### Kód (6 editů, izomorfně s hřbitovem)
- `zabaged.py`: `LAYER_IDS` += `"Povrchová těžba, lom": 118`; `QUARRY_LAYERS`; `fetch_quarries`
  (`_collect_features`); `map_quarry_to_isom` → 520.
- `generator.py`: lom = 5. zdroj olivové do surfaces kanálu (`_generate_real_surfaces`: import + pár).
- Lom pod existujícím `--surfaces` (ne nová CLI flag) → batch validace beze změny (B1 OK).

### Verify
- LS regen: pokryv **20159→20160**, `.omap` **35649→35650** = přesně **+1** (lom, sedí s probe Σ1).
- Ring lomu px bbox `x[1597..1742] y[46..244]` **přesně padl** na vykreslenou olivovou plochu (138 vrcholů, raw obrys).
- **A1 z-order ověřen vizuálně** (`temp/ls_quarry_crop.png`): hnědé vrstevnice + černé liniové útvary kreslené NAD olivovou.
- Lom jen na LS (probe Sez. 55) → ostatní 4 lokality bez lomu, dnešní rendery platné, regen netřeba.

### Propagace
katalog (lom ○→✓ + souhrn ◐/○ ~10→~9 + akční seznam bod 5), architecture (výčet zabaged + Sez. 56),
spec §4.9 („pět zdrojů 520"), GLOSSARY (pět zdrojů), README root (EN ∪ quarry) + generator + connectors.

### Audit cadence
%AUDIT:CODE Sez. 50 (+6; malá feature). **%AUDIT:DOCS Sez. 46 (+10 — práh dosažen, zralý příště).** pruning
Sez. 50 (+6). %CALIBRATE Sez. 51 (+5). **Stale Příště: „oplocené volné terény" viselo 5× → příště ROZHODNOUT DO/DROP.**

### Kudos / Censure
- **Kudos uživatel:** návrh 520 pro lom byl lepší než odložené 201 — intuitivně trefil, že plocha→plocha je věrnější
  než dotahování liniové hrany; A2 vizuální pozorování („vidím ho nevykreslený") = správné, potvrzeno probe.
- **Proces dobře:** foundations PŘED kódem (katalog důvod odložení + probe překryvu); verify per-lokalita
  (+1 LS, ring bbox na olivovou plochu); izomorfní kód (mirror hřbitovu, 0 nového renderu).

### Příště (Sez. 57)
1. *(zralý, +10/10)* **%AUDIT:DOCS** — po vlně Sez. 52–56 (lom/lanovka/zeleň/holes) zkontrolovat drift docs/katalogu.
2. *(Stale 5× — ROZHODNOUT DO/DROP)* **oplocené volné terény** (RÚIAN {5,13} ~80 % limit, %THINK před implementací).
3. *(změřeno, dobíratelné)* **balvany-linie → 208 Boulder field** (Σ14, nový area pattern trojúhelníky).

---

## Sezení 57 — %AUDIT:DOCS + balvany-linie → 208 Boulder field + parkoviště → 501.1

Stroj: **mrkla**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (clean), žádný neuzavřený %END.
Audit cadence: %AUDIT:DOCS **+11/10 → zralý** (fokus), CODE Sez. 50 (+7), pruning Sez. 50 (+7), CALIBRATE Sez. 51 (+6).
**Stale Příště „oplocené volné terény" viselo 5× → rozhodnuto DROP** (volba uživatele; zůstává v TODO jako nález Sez. 42,
přestane se navrhovat). Fokus = %AUDIT:DOCS (uživatel zvolil úklid před featurou — foundations before curtains).

### %AUDIT:DOCS — 4 nálezy (3 fan-out agenti → ověřeno ručně proti zdroji)
Přečteno 21 projektových `.md`. Precedent Sez. 46: agentí nálezy **kriticky profiltrovat**. Agent 3 (jazyk) = 0 jistých
chyb (konzervativní, kontrast Sez. 46 halucinace). Agent 1 (vrstvy) chtěl N2 bagatelizovat jako „historii" — chybně.
- **N1 🔴 broken links (53 řádků DIARY.md):** přesun `DIARY.md` root→`docs/` (Sez. 48) neopravil odkazy `](docs/diary/…)`
  → z `docs/DIARY.md` mířily na `docs/docs/diary/…` = neexistuje. První %AUDIT:DOCS po přesunu → chyceno teď. Fix
  replace_all `](docs/diary/` → `](diary/` (jen Sez. 48/49 byly správně).
- **N2 🟡 „katalog vyčerpán" drift:** SSoT katalog už koriguje (Sez. 55), ale 3 živé docs zaostaly — README.md:57
  („now exhausted"), architecture.md:70 (**vnitřně rozporné** — ř. 74 pokračuje Sez. 56!), generator-procedural.md:426.
  Opraveno (diáře/DONE/index-hook Sez. 52 = historie, ponechány).
- **N3 🟡 DIARY pořadí** 56→55→54 (rozbité přechody strojů) → číselně 54→55→56.
- **N4 ⚪ generator/README** `--paved` nezmiňovalo 501.1 → doplněno.
Příčiny driftu: (a) přesun root→docs neopravil relativní odkazy; (b) korekce dorazila do SSoT, ne do 3 konzumentů.
Propagační checklist Sez. 34 tyto dva typy nepokrývá (poznámka pro %CALIBRATE).

### Balvany-linie → ISOM 208 Boulder field (`--rocks`, 4. rock vrstva)
Foundations PŘED kódem (paměti `isom-spec-before-render` + `geometric-selfcheck-before-oom`):
- **Template id=38:** 208 = `area_symbol` s pattern výplní (náhodné plné trojúhelníky, density 0,8-1/mm², rotatable).
  Klíč: `.omap` stačí `area_object` symbol 208 → **OOM vyplní trojúhelníky sám** (jako 402/308). Rastr = px-aproximace.
- **Probe layer 13** (`?f=json`): `Skupina balvanů (linie)` = `esriGeometryPolyline`, atributy jen `jmeno` → KISS vždy 208.
- **Geometrie nesoulad** zdroj LINIE → ISOM 208 PLOCHA: řeší buffer (osa→úzký pás 1,5 mm) = **přesný mirror stromořadí
  406** (Sez. 45 `Liniová vegetace`). Šířka pásu 1,5 mm = volba uživatele (AskUserQuestion). 208 „generally will not
  impact runnability" → tvrdý kamenný objekt, vegetace gate neporušuje.
- **Kód:** zabaged `BOULDER_FIELD_LINE_LAYERS`+`fetch_boulder_field_lines`+`map_boulder_field_to_isom`→208 (mirror
  rock_areas/tree_rows); generator 4. blok v `_generate_real_rocks`, `_draw_boulder_field_area` (maska = celý pás,
  rastr = deterministicky seedované náhodné trojúhelníky uvnitř pásu, `_point_in_ring` ray-casting), konstanty
  `BOULDER_FIELD_*`; omap_export `USED_CODES`+**`AREA_CODES`** += 208 (close-flag, jinak by se nevyplnil — lekce 501.1
  Sez. 54). batch beze změny (208 pod `--rocks`, off obě větve → B1 OK, jako lom Sez. 56). ROCK_CLASS 4-class.
- **Verify:** py_compile OK; SV 7 / HS 3 / NV 4 = **Σ14 přesně dle probe** (rastr=meta=omap symbol 38=7 na SV);
  **proc baseline byte-identický** (md5 git-stash diff `d42dbf72…` před=po → proc nedotčen); vizuál rgb crop = náhodné
  trojúhelníky v pásech; **OOM Test OK** (uživatel: „Pásy balvanů vypadají dobře"). STATISTICS +208 řádek.

### Plot ISOM 516–518 → doložený SKIP (dotaz uživatele)
Uživatel: „a co ploty? ISOM 516–518?" Verify ze spec/template: **516 Fence / 517 Ruined fence / 518 Impassable fence**
existují. ALE probe MapServeru (149 vrstev, filtr plot/oplocení/ohrazení/fence/drát) → jen Zeď (39), Hradba (38),
Zábrana (54). **ZABAGED Polohopis plot jako vrstvu NEVEDE** → mapovat 516 = vymýšlet (porušení verify-against-source,
mirror vegetace gate logiky). Jediný zdroj = OSM `barrier=fence` (mimo ČÚZK doménu = backlog). Zapsáno jako doložený
SKIP do katalogu sekce 11 (mirror „kótovaný bod SKIP" Sez. 43). Bariérová sekce ZABAGED tím vyčerpaná.

### Parkoviště → 501.1 (oprava 501, nález uživatele v OOM)
Uživatel při OOM kontrole: „Parkoviště LS.omap je mapování 501, správně 501.1 (průchozí)." Verify template: 501 =
„Paved area, **with bounding line** *where they have a distinct boundary*", 501.1 = bez obrysu. ISOM nechává volbu na
„distinct boundary" → 501 nebyla ISOM-chyba, ale **konvenční volba**; uživatelova (parkoviště = průchozí, splývá
s okolím → bez obrysu; kolejiště zůstává 501 = vymezený prostor) je praktičtější (OB praxe). Sez. 41 sloučil parkoviště
s kolejištěm na 501 (DRY) → rozděleno: `map_paved_to_isom` parkoviště → 501.1. **Důsledek z-order:** filtr
`urban_base` řadí podle KÓDU → 501.1 jde do spodního průchodu (base výplň, mirror ostatní plochy v sídlech) — žádoucí
(splývající plocha). Generator 501.1 už plně podporoval (Sez. 54) → změna jen mapování + komentáře. Verify: LS
parkoviště **51× → 501.1** (kolejiště 1× a areály 408/409 7× zůstaly 501 správně); STATISTICS 501.1 LS 60.

### Propagace
katalog (208 ◐→✓ + 516-518 SKIP sekce 11 + parkoviště 501→501.1 + souhrn/akční seznam), architecture (Sez. 57),
spec §4.9f (208 + plot SKIP) + asfaltové areály (parkoviště 501.1), GLOSSARY (Boulder field termín), README root+generator
+connectors (208), STATISTICS (stats.py +208 řádek), DONE, DIARY index.

### Audit cadence
%AUDIT:DOCS **reset Sez. 57**. %AUDIT:CODE Sez. 50 (+7; net přírůstek 208+parkoviště malý). pruning Sez. 50 (+7).
%CALIBRATE Sez. 51 (+6). Stale: oplocené terény DROPnuto (už nevisí).

### Kudos / Censure
- **Kudos uživatel:** dotaz na ploty (vedl k doloženému SKIP, ne tichému vynechání); nález parkoviště 501→501.1
  z OOM kontroly (praktická OB znalost — obrys = vymezení, parkoviště splývá). Obojí zpřesnilo katalog.
- **Proces dobře:** foundations PŘED kódem (208 spec+probe, 516-518 probe MapServeru, 501 vs 501.1 template);
  proc baseline git-stash md5 (behavior-preserving prokázán); OOM verify nového area patternu 208 před uzávěrem.
- **Censure 0** (verify-against-source dodržen u všech tří: 208 / plot / parkoviště).

### Příště (Sez. 58)
1. *(změřené katalog kandidáti)* **podjezd → 519** (Σ12, LS 11; verify ISOM spec před kódem) / **brod → 519** (Σ6) /
   **vodopád → 313** (Σ2). Hráz 528 blokovaná (legenda mapy, sporné).
2. *(posun k aplikacím)* UC5 první střípek / prohloubení `compare_real_vs_gen.py` (vektor-na-vektor crosswalk).
3. *(podmíněně)* %AUDIT:CODE se blíží (+7; spustí LOC práh, ne počet sezení).

---

## Sezení 58 — ZABAGED fáze I VYTĚŽENA (doloženo měřením) → compare prohloubení na sbírce 6 reál. map

Stroj: **mrkla**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (clean), žádný neuzavřený %END.
Audit cadence: %AUDIT:CODE Sez. 50 (+7; net ~+581 LOC v generator/connectors → přes práh 500, ne ≥2×,
zralý kandidát ne vynucený), DOCS reset Sez. 57, pruning Sez. 50 (+7), CALIBRATE Sez. 51 (+6). Stale: čisto.

### Strategická otázka uživatele: „ZABAGED je 100% vytěžený?"
Místo featury uživatel zpochybnil stav: *„Už z ZABAGED nemůžeme pro ISOM nic získat?"* Verify-against-source
proti SSoT katalogu (paměť `verify-data-not-assume`, Sez. 55 mě „vyčerpáno" už spálilo) → **tři roviny**:
- **Extenzivní (~98 %):** zbývá 5 ZMĚŘENÝCH marginálních vrstev (podjezd Σ12, hráz Σ13 blok., brod Σ6,
  vodopád Σ2, suť Σ1) — diminishing returns, ale „100 %" fakticky ne.
- **Intenzivní:** atributy uvnitř použitých vrstev (crossability vody) — nabídl jsem jako protiargument.
- **Strop = vegetace gate:** ZABAGED nese druh porostu, ne průchodnost → ISOM 400 zelené škály jen UC5.

### Crossability vody (volba uživatele) → probe vyvrátil i tu (verify PŘED kódem)
`temp/probe_water_crossability.py`: (1) `Vodní_tok` **NEMÁ pole šířky** — jediný signál velikosti = `typtoku_k`
(001 nesplavný / 099 splavný / 004 podzemní); (2) široká řeka (Labe) = **LINIE** `Vodní_tok` typtoku 099, ne
`Vodní_plocha`; (3) **distribuce na 5 DEV lokalitách: splavný 099 = 0 na VŠECH** (splavné řeky = nížiny, ne OB
lesy). → crossability TOKU je marginálie jako podjezd; PLOCHA→301 už dnes správně. **Můj protiargument (rovina 2)
měřením padl** — uživatelův instinkt „vytěžen" platí silněji. Lekce: i protiargument změř, než ho naprogramuješ.

### Rozhodnutí: fokus UC2→compare (měř, než investuješ do UC5)
AskUserQuestion: ZABAGED vytěžen → směr = **prohloubit `compare_real_vs_gen.py`** (změř recall/precision po
vrstvách, než se sype do UC5). Volba B (přijmout vytěžení) → compare.

### Sbírka 6 reálných map (TrainsLab/resources) — uživatel přinesl
`probe_map_collection.py` (georef, verze, poloha): **kritické vyřazení** — `SampleMap` = **UTM zone 10 = Severní
Amerika** (ZABAGED ji nepokrývá → gen nikdy neporovná). 5 ČR (Liberecko): Soví vrch/Bedřichovka/Blatná (Křovák) +
Slovanka (UTM33) + Velbloud (Křovák, .pgw chyběl). Všechny grivace 3,75–17° (magnetic-north; .pgw rotace =
−grivace, ověřeno na 5/5 → PNG export použil grivaci z .omap). Zkopírováno do `resources/` (gitignored).

### Tracer Bedřichovka E2E (volba „1 lokalita první")
**Matched výsek** (`probe_matched_extent.py`): gen generuj na S-JTSK axis-aligned obal rotované reálné mapy
(4 rohy přes `.pgw`) → střed → WGS84 (pyproj) → `--lat/--lon/--width-km/--height-km`. Footprint = celá mapa
(ne ořezek velkých DEV výseků). Gen 2,88×3,57 km, 3616 omap objektů. **Compare parametrizován** (`_map_paths(name)`,
STAT 1 crosswalk podmíněn na kalibrovaný „Soví vrch", STAT 2 univerzální; `_stat1_crosswalk` vyčleněn SLAP).
**Vizuál odhalil layout kontaminaci:** reálné PNG nesou rám/north-lines/legendu → padají do rohů gen mřížky →
green nadhodnocen (zelený rám), blue (modré poledníky). Volba uživatele: **re-export čistého pole z Mapperu**
(autoritativní georef + .pgw; čistý vstup > softwarová záplata). Po re-exportu: blue real 2,5→1,3 %, green 39,6→30,4 %,
brown prec 53→67 % — kontaminace doložena číselně.

### Generalizace na 3 Křovák mapy + měření gate
Gen matched pro Soví vrch/Blatná/Velbloud (Velbloud dostal .pgw z Mapperu). **Soví vrch OUTLIER** (white 94 %,
footprint 11,85 km²) — diagnóza: reálná mapa **domapována jen ~1/4**, zbytek bílý papír (NE export bug; korekce mé
hypotézy). Vyřazen z agregátu (čeká na domapování — pak STAT 1 crosswalk + terénní GT). **3 plně domapované cizí mapy
konzistentní:**

| metrika | Bedř | Blatná | Velbl | čtení |
|---|---|---|---|---|
| white (les) IoU | 50 | 65 | 66 | gen projektuje les dobře |
| brown (vrstevnice) prec/rec | 67/75 | 69/74 | 56/67 | terén věrně |
| black (cesty/stavby) prec | 60 | 73 | 75 | vysoká prec, rec ~½ |
| green (vegetace) real%→gen% | 30→0 | 34→0 | 29→0 | **mezera ~30 % = gate** |
| open (žlutá) real%→gen% | 26→28 | 15→2 | 16→7 | gen PODkresluje (gate z druhé strany) |

### Hodnocení fáze I (otázka uživatele „shodujeme se ~60 %?")
Vážená **precision** (co umístíme & sedí, váha = gen plocha, 3 mapy): vrstevnice+cesty/stavby **~65 %**, +voda ~58 %,
+žlutá ~52 %, +les ~76 %. **~60 % je férový souhrn** — ale tři výhrady: (1) precision ≠ úplnost (recall vegetace 0 →
„správné, ale neúplné"); (2) barevná pozice + tol 4 m, NE shoda symbolů (vektor-na-vektor = budoucí hloubka);
(3) žlutá slabina ~45 % = gate z druhé strany. **Verdikt: fáze I projektuje TVRDOU geometrii ~65 % věrně a
nevymýšlí si (vysoká precision); ~třetinu profíkovy mapy (vegetace/běhatelnost) vědomě nekreslí — strop ZABAGED,
skok je UC5, ne další symbol.**

### Kód (1 soubor)
`generator/compare_real_vs_gen.py`: `_map_paths(name)` + `CROSSWALK_CALIBRATED`, `main(name=…)` parametrizace,
`_stat1_crosswalk(real_stat)` vyčleněn (podmíněn na kalibrovanou mapu), argv `sys.argv[1]`. STAT 2 beze změny
(univerzální). Probe skripty v `temp/` (gitignored). resources/maps gitignored → necommitují se.

### Audit cadence
%AUDIT:CODE Sez. 50 (+8 — práh sezení dosažen; LOC ~+590 s compare, zralý). DOCS Sez. 57 (+1). pruning Sez. 50 (+8).
%CALIBRATE Sez. 51 (+7). Stale: čisto.

### Kudos / Censure
- **Kudos uživatel:** zpochybnění „ZABAGED vytěžen" (správně — měření potvrdilo i crossability prázdnou); přinesl
  sbírku 6 reál. map (otevřela compare do šířky); re-export čistého pole z Mapperu (vyřešil layout); upřesnění
  „Soví vrch 1/4 domapováno" (korigovalo mou export-bug hypotézu); přesná otázka na precision („co umístíme").
- **Censure já (2×):** (a) crossability nabídnut jako protiargument bez měření — `verify-data-not-assume` platí i na
  vlastní oponenturu (probe ji vyvrátil); (b) Soví vrch bílé pozadí jsem dedukoval jako export bug, byla to nedomapovaná
  plocha (měl jsem se zeptat na stav mapy, ne hádat).
- **Proces dobře:** verify PŘED kódem (probe schématu vody / matched výsek / georef sbírky); tracer 1 lokalita odhalil
  layout past PŘED generalizací; měření gate doloženo na 3 nezávislých cizích mapách, ne na jediné.

### Příště (Sez. 59) — volba uživatele: **LIDAR nebo „stonecore"**
1. *(uživatel)* **LIDAR** — LiDAR pipeline LAZ→DMR→vrstevnice (TODO point-cloud větev, naráží na vegetace gate) /
   **„stonecore"** (upřesnit význam — kamenné jádro / UC5 střípek?).
2. *(compare hloubka)* vektor-na-vektor rozpad recall po sémantických skupinách (per-vrstva masky gen + rasterizace
   real .omap) — rozbít „black 49 %" na cesty/stavby/skály; **Slovanka UTM33** dodělat (jiný transformer); **Soví vrch**
   až domapuje (STAT 1 crosswalk + terénní GT).
3. *(žlutá mezera)* open land precision ~45 % — gen překresluje/podkresluje žlutou vs mapař; %THINK interpretace
   běhatelnosti (= gate, UC5).

### Dodatek (po %END) — vodní plocha 301.1 → 301 (combined s břehovou linií)
Uživatel z compare: mapaři kreslí vodní plochy s **černým okrajem = neprůchodné**. Omap exportoval **301.1**
(area, jen Blue 100% výplň BEZ okraje), ale rastr černý okraj měl od Sez. 18 → **nesoulad omap↔rastr**. Verify
template: 301 = combined „full colour, **with bank line**", 301.2 = „**dominant** (Blue 70%), with bank line"
(zesvětlilo by výplň). **Oponentura:** uživatel navrhl 301.2, ale ověření barev (301.1=Blue 100% color 15,
301.3/301.2=Blue 70% color 16) ukázalo, že 301.2 zesvětlí vodu → uživatel zvolil **301** (zachová sytou modrou,
jen přidá okraj). **Nález:** komentář Sez. 18 „kombinovaný 301 je type 16, nepřiřaditelný objektu" byl MYLNÝ —
vyvrácen kolejištěm 501 (combined area objekt) Sez. 28. Oprava: `omap_export` USED_CODES/AREA_CODES + `generator`
mapování `301.1`→`301` + komentáře (4 soubory). Verify: Bedřichovka regen omap **301×23 / 301.1×0** (= voda 95);
rastr beze změny. **OOM verify: uživatel — Test OK** (modrá výplň + černý břeh).

### Dodatek (po %END) — Rokle/výmol → ISOM 107 Erosion gully (`--linefeatures`)
Uživatel: „rokli a silnici ve výstavbě bychom mohli". Probe DEV: **obě Σ0 na 5 lokalitách** (katalog Σ0 tentokrát
správný, měřeno). **Silnice ve výstavbě → nechána ✗** (sémanticky staveniště ≠ sjízdná 503). **Rokle → 107**
(legitimní OB symbol). Uživatel: „zkus naslepo, linie→linie ISOM 107 nemá být problém". Foundations: id 94 polyline
(✓ linie→linie), jen `fid_zbg` → KISS 107 (ne drobné 108); template 107 = plná hnědá width 375 µm bez ticků.
Impl. mirror sráz 104: `zabaged` LINE_FEATURE_LAYERS_107/fetch/map, `generator` LINEFEAT dicty (107 hnědá solid
2 px bez ticků), `omap` USED_CODES. **Verify > naslepo:** rokle Σ0 na DEV, ale v ČR 1388 (řídká); staženy všechny,
nejhustší shluk = Moravská Třebová (49.838,16.962) → gen výsek → omap **107 × 32**, **OOM Test OK** (uživatel).
Katalog rokle ○→✓. Na 5 DEV se nezobrazí (úplnost, mirror vodojem věžový Sez. 43). Censure 0 (verify-against-source
dodržen: spec+schéma před kódem, výskyt změřen, reálný výsek ověřen místo čistě naslepo).

---

## Sezení 59 — UC5 „stonecore" = zelená věrnost → LiDAR/lasertool probe → vegetace gate DOLOŽENA měřením

Stroj: **mrkla**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (clean), žádný neuzavřený %END.
Audit cadence: %AUDIT:CODE Sez. 50 (+9 sez / ~+600 LOC — **LOC práh ≥500 překročen, zralý kandidát ne vynucený**),
DOCS Sez. 57 (+2), pruning Sez. 50 (+9), %CALIBRATE Sez. 51 (+8). Stale: čisto.

### Volba fokusu (2 AskUserQuestion) — UC5 střípek = věrná zelená
Z Příště Sez. 58 uživatel zvolil **UC5 první střípek** (= „stonecore", upřesněno jako UC5, ne kamenné jádro).
Pak zásadní vstup: *„Dokud nebude v lese spousta zelené, nebude to orienťácké mapy připomínat."* → „stonecore"
konkretizován na **zelenou vegetaci (runnability)**, = `green real 30 % → gen 0 %` mezera ze Sez. 58. Druhá volba:
**věrnost realitě** (ne plausibilita) → zelená má sedět na skutečný hustník, zvednout compare recall.

### Foundations PŘED kódem (verify-against-source, discuss before code)
- **ISOM zelená = runnability (průchodnost podrostu), NE land-use.** ZABAGED dá *kde je les* + druh porostu,
  ne *jak hustý podrost*. Vzrostlý les bez podrostu = bílá; mladý hustník = tmavě zelená 410. Z land-use polygonu
  runnability neodvodíš (= vegetace gate Sez. 3).
- **Karttapullautin/lasertool metoda** (RESEARCH): vegetation density z poměru **multi-echo** ech (penetrace pod
  koruny). `lasertool/` = lokální OpenOrienteering nástroj (Thomas Schöps 2011, LAS/ASC → vegetation height image).
- **Web ověření (2026):** čeští OB mapaři dělají vegetaci z **DMP 1G** (LiDAR, CC BY 4.0) „klasifikací výšky
  vegetace" přes OCAD — ale jako PODKLAD, finální zelenou dolaďují v TERÉNU (my automat nemáme). Nový **DMP OK**
  (2024-25, RGB+NIR+klasifikace vč. vysoké vegetace) = **obrazová korelace, NE LiDAR** → jen koruna, žádná penetrace.

### Probe lasertool / DMP 1G (uživatel: „nyní použijeme lasertool")
**Stažení mračna PLNĚ automatizováno** (UC2 cesta, mirror zabaged.py):
- Nomenklatura listu: REST query na `KladyMapovychListu/MapServer/24` (klad SM5, tentýž ags.cuzk.gov.cz server)
  bodem Soví vrch (S-JTSK −714223,−964505 přes pyproj) → **NBOR52** („Nový Bor 5-2", list 2,5×2 km).
- ATOM `get.ashx?theme=DMP1G-SJTSK` → URL vzor **`openzu.cuzk.cz/opendata/DMP1G/epsg-5514/<MAPNOM>.zip`**.
  Staženo `NBOR52.zip` (2,7 MB) → `NBOR52.laz`. `pip install laspy[lazrs]` do venv (čistý Python LAZ reader).
- **lasertool SEGFAULT na Win11** (exit 139, 13 let starý Win32 Qt4) → funkci (vegetation height) nahrazena přímým
  výpočtem CHM z laspy (`temp/lidar_probe/chm_probe.py`, max−min z per buňka 5 m).

**🔴 TVRDÝ NÁLEZ (doloženo, ne hádáno):** DMP 1G NBOR52 = **100 % single-return** (0 % multi-echo), klasifikace jen
**GROUND 27,8 % / HIGH VEG 68,4 % / building 3,8 %** — žádná low/med veg třída, **nulová penetrace pod koruny**.
→ vegetation height = **výška korun (CHM)**, ne hustota podrostu. Vizuál `chm_green.png` = „kde je vysoký les",
ale vysoký zralý les je často PRŮCHODNÝ (na mapě bílá) → mapovat CHM na zelenou = fyzikálně zavádějící.

### Verdikt + dvojitá vazba
**Věrná ISOM runnability z open ČÚZK dat NEJDE** (doloženo měřením, ne KB odhadem). Surové multi-echo LLS mračno
ČÚZK **nevydává jako open/standardní produkt** (ověřeno na geoportálu — jen odvozené DMR/DMP single-return), jen
přes **ZÚ Praha/Pardubice na vyžádání** (cena individuální). **Dvojitá vazba:** multi-echo (podrost) existuje jen
jako **archiv LLS 2009-13 = 13+ let stará vegetace** (nejdynamičtější prvek mapy); **aktuální DMP OK 2024-25 =
single-surface** (bez podrostu). Co je multi-echo je staré, co je aktuální nemá podrost → **strukturální strop**,
ne jen administrativní.

### Rozhodnutí uživatele — zkusit jiný podklad PLOCHY hustníku, jinak protokol gate
Volba: *„Jestli nevymyslíš jiný reálný podklad (alespoň plocha obecného hustníku), zaprotokolujeme, že vegetace
nebude v real části generátoru."* → dnešek vyloučil **LiDAR větev**; **3 neprozkoumané kandidáty na jiné ose**
(ne plná škála, jen plocha hustníku) → příště probnout, teprve při selhání zavřít gate definitivně:
1. **ÚHÚL lesnická data** (věk + zakmenění porostu — mladý/řídký = hustník; jen hospodářský les + licence verify).
2. **Copernicus HRL** (Tree Cover Density / Forest Type — EU open, hrubé rozlišení).
3. **Multi-temporal ortofoto** (ČÚZK archiv — paseka→zapojený porost = mladý hustník; CV na časové řadě).

### Vedlejší (žádost uživatele) — regen 5 lokalit po dodatku 301
Po přechodu na mrkla `maps/` (gitignored) neměly černý břeh vodních ploch (dodatek Sez. 58 `301.1→301`). Regen
všech 5 s ortofotem (counts sedí: SV 6043 / NL 1833 / LS 35650 / HS 7558 / NV 2306). Voda → 301 combined (OOM verify uživatel).

### Kód / produkční změna
**Žádná** — celý probe v `temp/lidar_probe/` (gitignored), jen `laspy[lazrs]` do venv. Generátor nedotčen.

### Audit cadence
%AUDIT:CODE Sez. 50 (+9; **LOC práh zralý**, příště zvážit jako fokus po zelené rozhodnutí). DOCS Sez. 57 (+2).
pruning Sez. 50 (+9). %CALIBRATE Sez. 51 (+8). Stale: čisto.

### Kudos / Censure
- **Kudos uživatel:** konkrétní nástroj (lasertool) místo vágního směru; jasná volba věrnost > plausibilita; tvrdá
  podmínka („vymysli podklad, nebo protokol") = drží laťku na verify-against-source, ne dohad.
- **Proces dobře:** **measure before invest** (Sez. 58 lekce) — probe za 2,7 MB dat doložil single-return strop
  → ušetřil stavbu zavádějícího CHM→zelená konektoru; verify-against-source (změřeno reálné mračno, ne KB odhad).
- **Censure 0:** oponentura „věrná zelená nejde" prokázána daty (single-return), ne tvrzena; nezavřel jsem gate
  natvrdo (3 kandidáti na jiné ose) místo ukvapeného „nikdy".

### Příště (Sez. 60)
1. *(podmínka uživatele)* **Probnout 3 kandidáty plochy hustníku** — ÚHÚL věk/zakmenění (licence+pokrytí verify),
   Copernicus HRL TCD, multi-temporal ortofoto. **Když všechny selžou → zaprotokolovat vegetaci mimo real část**
   (data-sources „Vegetace gate" + spec + GLOSSARY: doložené NE jako kótovaný bod SKIP Sez. 43).
2. *(zralý)* **%AUDIT:CODE** (+9 / LOC práh) — po vlně Sez. 52-58 (holes, 501.1, 402, lom, 208, 107, compare).
3. *(carry)* compare hloubka vektor-na-vektor / Slovanka UTM33.

---

## Sezení 60 — %AUDIT:CODE (úklid driftu po vlně Sez. 50→59)

Stroj: **mrkla**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (clean), žádný neuzavřený %END.
Audit cadence: %AUDIT:CODE Sez. 50 (+9 sez / net **+616 LOC** generator+connectors → **LOC práh ≥500
překročen**). Fokus = uživatel zvolil **bod 2 (%AUDIT:CODE)** místo bodu 1 (kandidáti hustníku) —
úklid zralého dluhu před další featurou (foundations before curtains).

### Metoda (vzor Sez. 50): hlavní soubor sám + agenti na okraj, nálezy ověřit proti zdroji
`generator.py` (3716 ř.) přečten **celý sám**; 2 paralelní agenti na okraj (`zabaged.py` 1222 ř. /
`omap_export.py`+`compare`+`stats`+`batch`). Precedent Sez. 46/50: **agentí nálezy kriticky profiltrovat**.
Ověřeno navíc ručně: `batch.py` **B1 lekce DRŽÍ** (16 real vrstev explicitně v OBOU větvích = validační
smyčka generate_map); `stats.py` má 208, ne 107.

### Nálezy: 0 kritických, 0 mrtvého kódu — dominanta = drift komentářů po featurové vlně
Refaktory Sez. 50 (`_collect_features`, `_layer_meta_section`) i DRY helpery a izomorfismus drží.
Symboly 402/402.1 (Sez. 53), 208 (Sez. 57), 107 (Sez. 58) se přidaly, ale výčty/komentáře nedohnaly.

**Funkční (2):**
- **N1 `stats.py` SYMBOLS postrádal 107** Erosion gully → `main()` iteruje jen SYMBOLS → STATISTICS.md
  rokli nesledoval (gen ji kreslí, `USED_CODES` ji má). Doplněn `("107","Erosion gully")` do liniové sekce.
- **N2 `compare` GEN_CAPABILITIES zastaralý** (chyběl 208/501.1/523/412/402/402.1) → STAT 1 hlásil falešné
  mezery. Synchronizováno na existující klíče; komentář přepsán „kalibrovaný řez pro STAT 1, NE SSoT schopností
  (ten je `USED_CODES`)". **Censure-catch:** přidal jsem omylem nový klíč `line-feature` bez CROSSWALK protějšku
  (= falešné „○ real 0") → zachyceno a staženo PŘED dokončením (verify-against-source na vlastní opravu).

**Drift komentářů/docstringů (9, KOSMETICKÉ):** `_generate_real_rocks` docstring (208 chyběl, „tři vrstvy"→čtyři);
maska surfaces komentář (3→5 tříd, +402/402.1); `_generate_real_surfaces` docstring (5 tříd, park→402);
z-order výčty (skály +208, liniové +107); `zabaged` boulder-cluster docstring („odložena 3 prvky"→realizováno 208
Σ14) + „Each item"→„Každý prvek"; `omap_export` docstring zobecněn na `USED_CODES` (zrušen zastarávající taxativní
výčet); `stats` 510 popisek „Power line"→oficiální „Power line, cableway or skilift"; omap 530 popisek.

### Verify
- **py_compile OK** (5 souborů: generator/compare/stats/omap_export/zabaged).
- **STATISTICS.md regen** (`stats.py`): řádek **107 Erosion gully** je v tabulce (`·` všude = Σ0 na DEV, ale
  sledován); 510 = „Power line, cableway or skilift"; 208 = SV 7/HS 3/NV 4 = Σ14 (sedí Sez. 57).
- **Behavior-preserving**: všech 11 oprav jsou komentáře + 2 TISKOVÉ seznamy (stats SYMBOLS, compare
  GEN_CAPABILITIES) → žádná logika generátoru dotčena, proc baseline 65 triviálně drží (regen map netřeba).

### Kód / produkční změna
6 souborů, samé komentáře/docstringy + 2 datové seznamy. Žádný runtime kód generátoru.

### Audit cadence
**%AUDIT:CODE reset Sez. 60.** DOCS Sez. 57 (+3). pruning Sez. 50 (+10). %CALIBRATE Sez. 51 (+9). Stale: čisto.

### Kudos / Censure
- **Proces dobře:** hlavní soubor čten sám, agentí nálezy ověřeny proti zdroji (N1/N2 potvrzeny, falešný
  „line-feature" klíč zachycen na vlastní opravě); behavior-preserving doložen (komentáře + tiskové seznamy).
- **Censure 0** (verify-against-source dodržen i na vlastní edit).
- **Vedlejší nález:** Sez. 59 chyběl v `DONE.md` (probe bez kódu) → doplněn při tomto %END (mirror Sez. 34 audit).

### Příště (Sez. 61)
1. *(podmínka uživatele, carry Sez. 59)* **Probnout 3 kandidáty plochy hustníku** (ÚHÚL / Copernicus HRL / multi-temporal
   ortofoto); když selžou → zaprotokolovat vegetaci mimo real část.
2. *(carry)* compare hloubka vektor-na-vektor / Slovanka UTM33.

---

## Sezení 61 — probe 3 kandidátů plochy hustníku → K1 ÚHÚL věk porostu zvolen

Stroj: **mrkla**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (clean), žádný neuzavřený %END.
Audit cadence: %AUDIT:CODE **reset Sez. 60** (+0), DOCS Sez. 57 (+4), pruning Sez. 50 (+11), %CALIBRATE
Sez. 51 (+10). Stale Příště: čisto (kandidáti hustníku visí teprve 2×). Žádný práh překročen o ≥2.

### Volba fokusu (AskUserQuestion) — probe 3 kandidátů
Z Příště Sez. 60 uživatel zvolil **bod 1 (probnout 3 kandidáty plochy hustníku)** — tvrdá podmínka ze Sez. 59
(„vymysli jiný reálný podklad, nebo zaprotokoluj gate"). Metoda: **measure-first** (lekce Sez. 58/59), s klíčovým
verify-against-source testem pro každého kandidáta: *„měří runnability podrostu, nebo zase jen korunový zápoj /
výšku shora?"* (= past CHM Sez. 59). Většinu lze rozhodnout z dokumentace zdroje PŘED stažením dat.

### K1 — ÚHÚL věk porostu (porostní mapa) — PROMĚŘENO, ZVOLEN
- **Desk:** porostní mapa ukazuje **stáří + zakmenění** (věkové třídy po 20 letech, odlišené barvou; zakmenění =
  šrafování). OPRL WMS (`geoportal.uhul.cz/wms_oprl`) i DPZ WMS = jen plánování / kůrovcový monitoring, **žádný věk**.
- **Strojový zdroj nalezen:** **AOPK `gis.nature.cz/arcgis/rest/services/Aplikace/Les_Mapy_20nn/MapServer`** —
  „Lesnická obrysová mapa a mapa porostních skupin z LHP a LHO Lesy ČR + ÚHÚL", vrstva **19 „Porostní skupiny 2022"**
  (esriPolygon, **371 236** polygonů celostátně, S-JTSK 5514). Mirror ZABAGED REST (znovupoužitelný `arcgis.py`).
- **Atribut `BARVA` = věková třída** (20-letý interval — DOLOŽENO dokumentací porostní mapy, ne hádáno;
  `ZNAČKA`/šrafa = zakmenění; `DBID`/`DBVAZBA` = cizí klíč do neveřejné LHP DB; `P_DRUH`/`P_VYUZ` = kategorie pozemku).
  Distribuce `BARVA` na NL = 9 hodnot v rozsahu 2–15 (konzistentní s věkovými stupni). **Číselník `BARVA`→věk NENÍ
  ve službě** (renderer „simple", legend prázdná) → vzít z **Informačního standardu LH** (NLI) — foundations Sez. 62.
- **Slabiny (změřeno):** (a) věk = **hrubý proxy**, ne runnability (mladý porost=hustník ANO, ale vzrostlý les
  s ostružiním=neprůchodný „starý"=NE; čerstvá řídká výsadba=„mladý" ale průchodná); (b) **pokrytí DĚRAVÉ 3/5 DEV**
  (okno ±2,5 km: NL 2381 / LS 990 / NV 2243 ✓; **SV 0, HS 0** — Soví vrch je terénně mapovaný flagship! — sada je
  sešitá z LHP/LHO různých roků platnosti, jakýkoli jeden snímek má mezery); (c) data 2022 statická; (d) licence OK
  (LHP/LHO veřejná dle z. 106/1999, AOPK open).
- **Krajská alternativa** `mapy.kr-vysocina.cz/.../LHC/Porostni_Mapa` existuje (klasifikovaný renderer dle věku),
  ale byla po celé sezení **down** („Could not access any server machines") + jen regionální → AOPK je lepší celostátní.

### K2 — Copernicus HRL Tree Cover Density — DESK VERDIKT (SLABÝ)
TCD = **korunový zápoj shora** (0–100 %) v 10 m → **strukturálně tatáž zeď jako CHM Sez. 59** (single-return = jen
koruny): vysoký zápoj může být průchodný zapojený les i neprůchodný hustník, TCD je nerozliší. **Neproměřováno
znovu** — opakovalo by změřenou lekci Sez. 59. 10 m navíc hraniční pro 1:10000.

### K3 — multi-temporal ortofoto — DESK (NEJSILNĚJŠÍ koncepčně, ODLOŽEN)
Jediný kandidát **bez pasti korunového zápoje**: časová změna paseka→zapojený porost detekuje **mladý hustník přímo**
(jako věk, ale celostátní open, 2-letý cyklus ČÚZK). Ale je to **CV projekt na časové řadě** = velká investice, vlastní UC.

### Rozhodnutí uživatele (2× AskUserQuestion)
- **Postavit K1 jako hrubý proxy** (ne zavřít gate, ne probíhat K3) — i přes slabiny. „Částečná zelená lepší než žádná."
- **Odstupňovaná zelená dle věku** (ne KISS jedna): mlazina (1. stupeň ~1-20 let, nejhustší) → **410 Veg: walk**
  (tmavší); tyčkovina (~21-40) → **406 slow** (světlejší); starší → bílá (průchodný les).
- **Implementace příští sezení** (Sez. 62) — toto bylo %BEGIN + hloubkový probe; konektor + číselník = čerstvé sezení.

### Foundations PŘED kódem — co Sez. 62 potřebuje
Číselník `BARVA`→věkový stupeň z Informačního standardu LH (verify-against-source; PDF `KSLH021114.pdf` na uhul.cz
vrací HTML chybu, pypdf je ve venv pro jiný zdroj). Mapování věkový stupeň → ISOM zelená. **Označit vrstvu jako PROXY**
(GLOSSARY/spec) — zelená z věku ≠ terénní runnability, neporušit „negenerovat pod hlavičkou věrnosti" (paměť
[[pseudorealistic-phases]]). Pokrytí 3/5 — na SV/HS vrstva nebude (mirror řídkých vrstev Sez. 43).

### Kód / produkční změna
**Žádná** — celý probe v `temp/uhul_probe/` (gitignored; AOPK JSON cache pro Sez. 62). Generátor nedotčen.
Jediná docs změna: TODO řádek hustníku `[ ]`→`[~]` (výsledek + plán), data-sources „Vegetace gate" (3 kandidáti
→ probnuto), DONE/DIARY/diár.

### Audit cadence
%AUDIT:CODE reset Sez. 60 (+1). DOCS Sez. 57 (+4). pruning Sez. 50 (+11 — **zralý za 1 sezení**). %CALIBRATE Sez. 51 (+10). Stale: čisto.

### Kudos / Censure
- **Kudos uživatel:** tvrdá podmínka ze Sez. 59 („vymysli podklad, nebo protokol") drží laťku; jasná volba
  postavit hrubý proxy odstupňovaně místo zavření gate (částečná zelená > žádná).
- **Proces dobře:** **measure-first** (Sez. 58/59 lekce) — desk verify odhalil, že OPRL/DPZ WMS věk nenesou (ušetřil
  slepé stahování); `BARVA`=věk **doloženo dokumentací** (ne přijato naslepo, paměť `verify-data-not-assume`);
  pokrytí změřeno na všech 5 DEV (odhalilo děravé 3/5, SV/HS chybí — nepředpokládal jsem); K2 neproměřován protože
  = strukturálně už změřená zeď Sez. 59 (neopakovat měření).
- **Censure 0** (verify-against-source dodržen u všech tří kandidátů).

### Příště (Sez. 62)
1. *(fokus, rozhodnuto)* **Implementovat K1** — číselník `BARVA`→věk z IS LH (foundations), konektor na AOPK
   (`connectors/arcgis.py` reuse), odstupňovaná zelená (mlazina→410 / tyčkovina→406), omap/maska/stats kanál,
   **označit jako PROXY**. Pokrytí 3/5 DEV.
2. *(carry)* compare hloubka vektor-na-vektor / Slovanka UTM33.
