# Diár — 2026-05-30

## Sezení 44 — Katalog dávka 4 (mokřady/pramen/jeskyně/nádrž) + audit věrnosti renderu

Stroj: **mrkla** (přechod z ntbhej21; `%BEGIN` krok 0 pullnul 24 commitů Sez. 41–43). Fokus z „Příště Sez. 44" bod 1 (akční seznam katalogu, dávka 4 vodní/mokřady/terén).

### Dávka 4 — 4 čisté symboly, 2 odložené
Foundations před kódem (verify-against-source): nejdřív ověřeny ISOM symboly v template, pak REST názvy vrstev přes `?f=json`. **Nález (paměť `zabaged-rest-naming-conventions` opět platí):** katalog má WFS-styl s podtržítky, REST má mezery/čárky/závorky — `Bažina, močál` / `Rašeliniště (plocha)` / `Zdroj podzemních vod` / `Vstup do jeskyně` / `Ústí šachty, štoly` / `Nadzemní zásobní nádrž`. Bez probe by fetch selhal.

- **`--marsh real` (nový flag):** bažina + rašeliniště → **308 Marsh** (KISS vždy crossable, NE 307 uncrossable — data nenesou překonatelnost). Render `_draw_marsh_area` = modrá vodorovná **scanline šrafa** (rozestup 0,45 mm dle template patternu) ořezaná na polygon; `.omap` = area_object 308.
- **Do `--landmarks` (bodové):** pramen → **312** (modré „U"), jeskyně+šachta → **203.2 Cave**, nádrž (plocha→centroid) → **311** (modrý čtverec).
- **Odloženo, doloženo v katalogu:** hráz → 528 (vyžaduje legendu mapy + sporné mapování), lom → 201 (plocha ≠ hrana srázu, Σ1 marginální).
- **203.2 je necelý ISOM kód → string** napříč flow (`map_landmark_to_isom: int | str`). Bug chycený při verify: `sorted()` na smíšené množině int+str padá `TypeError` → `key=str` na 2 místech.

Počty sedí na data-driven probe Sez. 43 **na kus**: pramen Σ65, nádrž Σ8 (LS6+HS2), jeskyně Σ9, mokřady NV15/HS10/NL9/SV5/LS0.

### Cave sága — 3× špatně, než správně (a poučení o autoritě uživatele)
Uživatel: „Ledovou jeskyni pod Suchým vrchem ISOM nevidím." Probe ukázal, že **data byla vždy v pořádku** — `Vstup do jeskyně` OBJECTID 157 `jmeno="Ledová jeskyně"` se fetchla i umístila správně. Problém = **nečitelný symbol** (kreslil jsem plný černý trojúhelník hrotem nahoru).
- Uživatel: „není to trojúhelník, ale otočené V (stříška)". → překreslil jsem na chevron, ale **hrot dolů (V)**.
- Uživatel: **„Víš, že 203.1=V a 203.2=Λ? Zkontroluj!"** → odhalil, že mám **obrácenou konvenci osy y**.

### Audit věrnosti renderu (na žádost uživatele)
Porovnán template (autorita) vs `_draw_*` u ~35 symbolů. **Root cause systémový:** předpokládal jsem OOM osu **+y = nahoru**, ve skutečnosti **+y = DOLŮ** (rastr NEFLIPUJE, `screen_y = cy + omap_y`). Špatný předpoklad zrcadlil vertikálně asymetrické body. Definitivní ověření: překreslení template coords bez flipu reprodukuje uživatelovo **203.1=V, 203.2=Λ**.

Opraveno (3 reálné nálezy):
- **203.2 cave** → Λ stříška hrot nahoru (= „with a distinct entrance"; můj „fix" na V byl chybný).
- **312 spring** → ∪ ústí nahoru (letos psáno s chybnou konvencí jako ∩).
- **104 sráz** → HNĚDÁ (template color 6 Brown), ne černá; linie i ticky.

**Staženo (falešný poplach):** 111 depression (∪) a 207 boulder cluster (▲) byly **celou dobu správně** — staré code je psal se správnou konvencí. Málem jsem je „opravil" (flipnul) → uživatelovo „Zkontroluj!" to zachytilo včas. Paměť `omap-symbol-y-axis-down`.

Drobnosti (k tvému rozhodnutí, na konvenci nezávislé): 501 paved obrys hnědý vs template černý; 513 wall plná linie bez teček (template má tečku á 3 mm). 502 PoC casing + 510 příčky = doložené záměry.

### 704 verify pomůcka
Na žádost: do SV.omap injektovány **ISOM 704 Control number** (1–5) na pozice 5 jeskyní (post-process, ne generátor; XML validováno, OOM headless nejde — paměť `omap-verify-oom`). Uživatel ověřil v OOM → **„Test OK"**. SV pak regenerován načisto (704 jsou jednorázové).

### Cleanup
Uživatel viděl `map.omap` → byl to **osiřelý `sandbox/`** na stroji mrkla (untracked výstupy z doby PŘED reorgem Sez. 39; `git pull` je nemohl uklidit, protože gitignored). Pravidlo `<lokalita>.omap` (Sez. 42) se propagovalo správně. `sandbox/` smazán.

### Verify
proc baseline **65 drží**; 5 lokalit přegenerováno (počty sedí); STATISTICS 39/39 aktivních; py_compile OK. Vizuál ověřen okem (crop 6–8×): 308 šrafa, 312 ∪, 203.2 Λ, 104 hnědá (pixel sample 160,95,31).

### Audit cadence
%AUDIT:CODE Sez. 41 → +3 sez (dnešní audit renderu byl cílený verify, ne plný %AUDIT:CODE). DOCS/pruning/CALIBRATE od Sez. 34 → +10 sez, prahy 10/12/15 — DOCS přesně na 10, spustí při ≥2 nad (Sez. 46). Stale Příště: čisté.

### Kudos / Censure
- **Kudos uživatel ×3:** Ledová jeskyně (oko=source odhalilo nečitelný symbol), „Zkontroluj!" (zachytil obrácenou konvenci před kaskádou flipů), 704 Test OK.
- **Censure já:** orientace symbolů 3× špatně (cave fill→trojúhelník, pak V místo Λ, audit s obrácenou konvencí). Lekce: u orientace ISOM symbolu je **autorita uživatel z OOM**, ne moje coord-flip uvažování.

### Příště (Sez. 45)
1. *(druhá vlna land-cover)* pole → **412** / sad → **413** (žlutá + pattern; dnes vše 401).
2. *(drobnosti auditu)* 501 paved obrys hnědý→černý; 513 wall tečky á 3 mm.
3. *(carry-over [!])* **416 stromořadí** — uživatel měl vysvětlit, proč je 416 špatně pro `Liniová vegetace`; pořád otevřené.
4. *(carry-over)* oplocené volné terény (RÚIAN {5,13} ~80 %); 115 `Ostatní plocha v sídlech` → 501 dle uživatelova projití map.

---

## Sezení 45 — Stromořadí 416 → 406 „lineární les" (oprava sémantiky + nový `--treerows`)

Stroj: **mrkla**. Fokus z „Příště Sez. 44" bod 3 (carry-over `[!]` 416 stromořadí), odblokovaný vysvětlením uživatele.

### Vysvětlení uživatele: jak ISOM mapuje stromořadí
Otázka z mé strany (verify-against-source PŘED kódem): jak ISOM definuje 416 a čemu jsem ho přiřadil. Ověřeno v template (`id=100 code=416`): **416 Distinct vegetation boundary = ZŘETELNÁ HRANICE mezi dvěma oblastmi** (kraj lesa, předěl smrková monokultura↔smíšený les). Tam, kde hranice není zřetelná (běžný neostrý přechod v lese), se NEkreslí. **Stromořadí (alej) = řada stromů = něco jiného než hranice.** Uživatel: alej se mapuje **(I)** linií bodů 417/418 (musíš znát polohu kmenů) nebo **(II)** area polygonem — tenkou nepravidelnou „špagetou" = lineární les (obecné, nemusíš znát kmeny).

**Data určila volbu II.** ZABAGED `Liniová vegetace` (id 15, výhradně `typveg_k=S` stromořadí) dává jen OSU → cesta I (body) je neproveditelná, cesta II (buffer osy → pás) ano.

### Parametry (rozhodnutí uživatele)
- **Symbol: 406 Vegetation: slow running** (světlá zelená `C_GREEN1` = green1 „pomalý běh"; A1).
- **Min. mapovatelná plocha: 1,0 mm²** — uživatel správně předpokládal ISOM doporučení; ověřeno v `isom-2000-spec.pdf` (MINIMUM DIMENSIONS: „smallest green/blue/yellow **dot screen: 1.0 mm²**"; full colour 0,5 mm²). 406 = zelený screen → práh 1,0 mm² ≈ **21 px²** @ 1:10000 (`PX_PER_MM` 4,5855). Menší úseky se zahodí.
- **Šířka pásu: 0,7 mm ≈ 7 m** (typická alej; A2).
- **Nepravidelnost „špageta" hned** (A3, „doladíme později") — DETERMINISTICKÁ sinusová perturbace šířky (NE random: real nelosuje, Sez. 20).

### Implementace (plná vertikální integrace, mirror vrstev)
- `zabaged.py`: `Liniová vegetace` ven z `LINE_FEATURE_LAYERS_416`/`fetch_line_features` (zůstaly 104 sráz + 513 zeď); nový `TREE_ROW_LAYERS` + `fetch_tree_rows` (vrací jen osu = čistá data) + `map_tree_row_to_isom` (vždy 406).
- `generator.py`: konstanty `ISOM_TREE_ROW`/`TREEROW_*`; `_buffer_polyline_irregular` (osa → nepravidelný prstenec, kolmá normála + sinus perturbace), `_polygon_area_px` (shoelace filtr), `_draw_treerow_area` (plná zelená bez obrysu, jako `_draw_surface_area`); `_generate_real_tree_rows` (px buffer → rastr; px→grid inverze pro .omap = TÝŽ polygon, konzistence). Z-order nad surfaces (401), pod marsh/vrstevnicemi. Maska `mask_treerows.png`, meta blok, CLI `--treerows`, validace.
- `omap_export.py`: 416 ven z `USED_CODES`, 406 do `USED_CODES`+`AREA_CODES`; `treerow_features` emit (area_object). `stats.py` (406 do tabulky, 416 ven; sekce `treerows`). `batch.py` obě větve `treerows="off"` (lekce B1).

**Konceptuální milník:** první zelená vegetační plocha generátoru. Vegetace gate NEporušuje — stromořadí je tvrdý objekt z dat (jako budovy/skály), ne hádaná hustota lesa. Izomorf s 308 Marsh: data nesou *že tam je*, ne *jak je husté* → KISS jedna úroveň 406.

### Verify
proc baseline **65 drží** (noise větev nepadla — B1 OK); SV regen → **83 stromořadí 406** (= bývalých 83× 416 na kus, nic nepropadlo min-area filtrem); linefeatures 87 (jen 104+513); .omap well-formed, 83× symbol 406, **0× objekt 416**; py_compile OK. 5 lokalit přegenerováno (stromořadí SV 83/HS 121/LS 47/NV 18/NL 4 = bývalá 416). STATISTICS regen. **Vizuál (crop SV obec): světle zelené nepravidelné pásy podél cest = věrná alej**; z-order pod budovami/cestami, nad žlutou. **Uživatel ověřil SV.omap v OOM → „Test SV OK".**

### Cleanup
Rezidua „416 vegetace" v komentářích `generator.py` (5 míst: docstringy `_draw_line_feature`/`_generate_real_line_features`, generate fáze, mask save, omap features) a v docs (architecture, spec §4.9m, katalog, oba README, data-sources, root README) sjednocena na 406 / odebrána. Ponecháno (historie/crosswalk): `ISOM2000-ISOM2017-2.crt`, `compare_real_vs_gen.py` (seznam ISOM2000 kódů reálných map), `template_classic.omap` (definice symbolu 416 = nepoužitá část ISOM knihovny), diáře 2026-05-29.

### Audit cadence
%AUDIT:CODE Sez. 41 → +4 sez (práh ≥8 / ≥500 LOC; +~120 LOC dnes). DOCS od Sez. 34 → +11 (práh 10, ≥2 nad = Sez. 46 → příště zralý). pruning +11/12, CALIBRATE +11/15. Stale Příště: čisté (416 vyřešeno, nebyl ≥5).

### Kudos / Censure
- **Kudos uživatel:** přesné vysvětlení I vs II (kartografická expertíza odblokovala fokus); správný odhad existence ISOM min-area doporučení (1,0 mm² ověřeno); Test SV OK.
- **Proces dobře:** verify-against-source PŘED kódem (spec 416 + min. plocha z PDF), ne hádání; foundations (přečíst vzor surfaces/marsh) → integrace bez zacyklení.

### Příště (Sez. 46)
1. *(zralý audit)* **%AUDIT:DOCS** (od Sez. 34 +11, práh 10 → ≥2 nad). Souběžně zvážit %AUDIT:CODE (+4, LOC roste).
2. *(druhá vlna land-cover)* pole → **412** / sad → **413** (žlutá + pattern; dnes vše 401).
3. *(drobnosti auditu Sez. 44)* 501 paved obrys hnědý→černý; 513 wall tečky á 3 mm.
4. *(carry-over)* oplocené volné terény (RÚIAN {5,13} ~80 %); 115 `Ostatní plocha v sídlech` → 501.
5. *(drobnost Sez. 45)* „špageta" nepravidelnost doladit (uživatel „později"); ověřit prahy `TREEROW_*` na víc lokalitách.

---

## Sezení 46 — %AUDIT:DOCS (zralý audit dokumentace, +11 sez od Sez. 34)

Stroj: **mrkla**. Spuštěn cadence-zralý %AUDIT:DOCS (práh 10, +11). Přečteno 27 `.md` (živé docs důkladně, diáře jen na konzistenci). Fan-out čtení rozděleno na 3 paralelní Explore agenty (konzistence kód↔docs / jazyková korektura / TODO-DONE-DIARY-IDEAS), nálezy poté **kriticky profiltrovány** — část byla šum/halucinace.

### Výsledek: docs v dobrém stavu, 0 kritických
Propagace Sez. 44/45 proběhla čistě (důsledek propagačního checklistu Sez. 34). Ověřeno správné: CLI flagy (`--treerows`/`--marsh` všude), ISOM 39/39 (406 ano / 416 ne, kód↔STATISTICS shoda), masky, počty stromořadí konzistentní napříč 6 docs (SV 83/HS 121/LS 47/NV 18/NL 4), 416 rezidua v živých docs jen historická/vysvětlující, DONE↔TODO konzistentní, terminologie (S-JTSK / ZABAGED® / desetinné čárky) jednotná, GLOSSARY wikilinks bez broken cílů.

### Opraveno (5 nálezů, vše kosmetické/doporučené)
1. **`DIARY.md` index přeskládán do číselného pořadí** (byl 1–15,17,18,16,…,44,45,43 = pořadí psaní přes přechody strojů → teď 1…45; skript `temp/sort_diary.py` se sanity-asserty, žádný řádek neztracen).
2. **spec §4.9 sekvenční číslování** — ad-hoc „4.9k-bis" (mokřady Sez. 44) → řádné: k-bis→l, l→m, n nově stromořadí (k,l,m,n souvislé); opraveny 2 odkazy (`viz §4.9n`, diár §4.9m).
3. **`TODO.md`** — redundantní inline poznámka o 416→406 v `[!]` bloku smazána (stromořadí má vlastní „STROMOŘADÍ HOTOVO" blok; není liniové).
4. **`README.md`** (anglický exports řádek) — mezery kolem `+` (čitelnost).
5. **`DIARY.md:8`** — kotva sezení 1 doplněna (konzistence s 2–45).

### Zamítnuto (agentí šum / halucinace)
„prohlubeň → prohlubň" (halucinace — `prohlubeň` je správně), GLOSSARY pořadí „form line / pomocná vrstevnice" (stylistická preference, klíčujeme ISOM termínem), „chybí centrální CLI tabulka" (feature request, ne vada).

### Audit cadence (reset)
%AUDIT:DOCS **reset na Sez. 46**. %AUDIT:CODE Sez. 41 → +5 (práh ≥8 / LOC roste). pruning +12/12 (na prahu, příště zralý). CALIBRATE +12/15. Stale Příště: čisté.

### Příště (Sez. 47)
1. *(druhá vlna land-cover)* pole → **412** / sad → **413** (žlutá + pattern; dnes vše 401).
2. *(drobnosti auditu Sez. 44)* 501 paved obrys hnědý→černý; 513 wall tečky á 3 mm.
3. *(zralý)* IDEAS/TODO pruning (+12/12).
4. *(carry-over)* oplocené volné terény (RÚIAN {5,13}); 115 `Ostatní plocha v sídlech` → 501.
5. *(drobnost Sez. 45)* „špageta" doladit; ověřit prahy `TREEROW_*` na víc lokalitách.

---

## Dovětek — ⚠ přechod mrkla → ntbhej21 (Sez. 47 jede na ntbhej21, FETCH FIRST)

Sezení po Sez. 46 proběhlo na stroji **mrkla** jen jako `%BEGIN` (sync + shrnutí stavu) —
**žádná kódová ani docs práce** kromě tohoto handoffu. Další reálné sezení (Sez. 47) poběží na
**ntbhej21**. Handoff přes git (jediný kanál mezi stroji, paměť `two-machines-git-sync`).

**Pro ntbhej21 — `%BEGIN` krok 0 BEZPODMÍNEČNĚ:** `git fetch` + porovnej `HEAD` s
`origin/main` PŘED jakoukoli prací. Klon na ntbhej21 bude pozadu **min. o tento handoff commit**
(reálně o víc — Sez. 44/45/46 vznikly na mrkla). „clean" working tree ≠ up-to-date. Stalo se už
dvakrát (Sez. 12 = 20 commitů pozadu, Sez. 33 = 33 pozadu) → redundantní/zbytečná práce.

**Co se NEpřenáší přes git** (regeneruj dle potřeby): `maps/<lokalita>/` rendery (gitignored),
`temp/` scratch (uklizen), cache `.dmr_cache`/`.zabaged_cache`/`.ruian_cache` (regenerovatelné,
jen zrychlují — první fetch z ČÚZK bude pomalejší). Kód jede ve `.venv` v kořeni (paměť
`generator-poc-venv`).

**Stav k handoffu:** working tree čistý, vše hotovo do Sez. 46 včetně. proc baseline 65, ISOM
39/39 aktivních. Docs po %AUDIT:DOCS (Sez. 46) v dobrém stavu, 0 kritických.

**Audit cadence pro Sez. 47** (no-work handoff cadence NEposouvá): %AUDIT:DOCS reset Sez. 46;
%AUDIT:CODE Sez. 41 → +5 (práh ≥8 sez / ≥500 LOC, LOC roste — hlídat); **pruning +12/12 = zralý**;
CALIBRATE +12/15. Stale Příště: čisté.

**Fokus Sez. 47 (návrh, viz „Příště" výše):** přirozené pokračování land-cover vlny —
pole → **412** / sad → **413** (čistá projekce z dat, dnes vše v 401); nebo nejdřív odbavit
**zralý pruning** (rychlý úklid TODO/IDEAS). Verify-against-source PŘED kódem: ověřit symboly
412/413 + pattern v `template_classic.omap` a crosswalk podle `typveg_k`/druhu kultury.

---

## Sezení 48 — Druhá vlna land-cover: pole 412 / sad 413 (kód hotov, verify nedokončen) + přesun docs

Stroj: **ntbhej21**. `%BEGIN` krok 0 srovnal klon (6 commitů pozadu — Sez. 44–46 + handoff,
přesně dle dovětku Sez. 46; fast-forward na `a8b244f`). Fokus z „Příště" bod 1 (pole→412/sad→413).

### Verify-against-source PŘED kódem (foundations)
Template `template_classic.omap` (autorita): **412 Cultivated land** = `type=16` combined = 401 žlutá
+ **412.1 černý tečkový pattern** (tečka color 31 r 0,15 mm, grid 1,2 mm), min. plocha 3×3 mm.
**413 Orchard** = `type=4` area = žlutá inner_color 33 + **ZELENÝ tečkový pattern** (color 29
undergrowth, r 0,338 mm, grid 1,2 mm), min. plocha 2×2 mm. ZABAGED crosswalk (dnes vše→401):
`Orná půda…` (138)→412, `Ovocný sad, zahrada` (135)→413, louka (139)+park (134)→401.

### Rozhodnutí uživatele (AskUserQuestion)
- **Min. plocha:** pod ISOM prahem (412: 9 mm², 413: 4 mm²) → spadne na **401** (izomorf se stromořadím Sez. 45).
- **Vrstva 135 celá → 413**; privátní zahrady přemaže olivová 520 z RÚIAN (kreslí se NAD surfaces,
  z-order téma 2 Sez. 42) → reálně zůstanou 413 jen ne-privátní sady.

### Implementace (vertikální integrace, mirror surfaces)
- `zabaged.py`: `map_open_land_to_isom` rozlišuje vrstvu (138→412, 135→413, zbytek→401); komentář OPEN_LAND_LAYERS.
- `generator.py`: konstanty `ISOM_CULTIVATED`/`ISOM_ORCHARD` + `SURFACE_*` rozšířeny (NAME/CLASS 3,4/FILL/DOT/MIN_AREA);
  nový `_draw_dotted_surface_area` (žlutá výplň + tečkový scanline-grid pattern kotvený globálně, odvozen z marsh);
  `_generate_real_surfaces` degraduje kulturu pod min. plochou na 401 (přes `_polygon_area_px`).
- `omap_export.py`: 412.1+413 do USED_CODES/AREA_CODES; **412 rozbal na 401+412.1** (combined type 16 nepřiřaditelný objektu,
  precedent most 1→2 / voda 301); 413 = jeden area objekt.
- `stats.py`: 412/413 do tabulky SYMBOLS.

### Verify — DÍLČÍ, NEdokončen (poctivě)
HOTOVO: py_compile všech 4 modulů OK; **izolovaný `write_omap` test prošel** (412→401+412.1, 413→1 objekt);
**data-probe potvrdil vrstvy** (SV 18 polí/167 sadů; NL 7/4; LS 23/703; HS 61/159; NV 12/52 — všude existují);
**proc baseline 65 drží** (noise větev nespadla); chyba `C_GREEN`→`C_GREEN1` opravena (paleta nemá C_GREEN).
**NEHOTOVO:** plný render-verify SV (vizuál teček + .omap objekty v reálné mapě) — render se opakovaně zasekl
(omylem 2 paralelní běhy do `maps/Soví vrch` + default ortofoto/20 vrstev viselo na síti >10 min). **→ dotáhnout Sez. 49.**

### Regrese 416 — PROVĚŘENA, NENÍ (na dotaz uživatele)
Grep celého stromu: `map_tree_row_to_isom` vrací **406**, omap_export 0× kód 416, všechny „416" jen vysvětlující
komentáře. Stromořadí (špageta 406) netknuté. „Rozbití" = zaseklý render, ne kód.

### Přesun root → docs/ (žádost uživatele)
`git mv` 6 pracovních `.md` → `docs/`: TODO, DONE, DIARY, IDEAS, RESEARCH, GLOSSARY (zachována historie).
V rootu zůstaly jen `README.md` + `CLAUDE.md` (GitHub/harness konvence). Odchyluje se od root-level konvence
globálního CLAUDE.md (volba uživatele pro AzimutLab). Odkazy opraveny v živých dokumentech: README (layout blok +
Docs sekce → `docs/…`), PROMPTS %BEGIN kontext. Diáře needitovány (historický záznam stavu v té době).

### Kudos / Censure
- **Censure já:** opakovaně spuštěny 2 paralelní rendery do téže složky → zasekly se navzájem; honění shellu
  duplicitními voláními (parallel tool-call chaos). Lekce: jeden render, blokující čekání, ne paralelní bombardování.
- **Kudos uživatel:** včasný dotaz na 416 regresi (Censure-prevence) + jasné %END rozhodnutí.

### Příště (Sez. 49)
1. **[!] Dotáhnout verify kultury 412/413** — čistý render SV (`--no-ortho` pro rychlost), vizuál teček
   (pole černé / sad zelené), .omap objekty 401+412.1/413, 5 lokalit regen, STATISTICS.
2. *(propagace po verify)* kultura 412/413 → architecture UC2, spec §4.9*, GLOSSARY, README status, oba README, katalog
   (NEpropisovat „hotovo" dokud verify nestojí — conceptual integrity).
3. *(drobnosti auditu Sez. 44)* 501 paved obrys hnědý→černý; 513 wall tečky á 3 mm.
4. *(zralý)* IDEAS/TODO pruning (+13/12).

---

## Sezení 49 — Verify kultury 412 (root cause zaseklého renderu) + sad/zahrada → 520 (oprava 413)

Stroj: **ntbhej21**. `%BEGIN` krok 0: git fetch, `HEAD == origin/main` (0/0), clean — žádný handoff
pozadu. Fokus z „Příště Sez. 49" bod 1 (`[!]` dotáhnout verify kultury). Sezení přeteklo přes půlnoc
na 2026-05-31 (záznam ponechán v 2026-05-30.md = kontinuita číslování + den startu).

### Root cause „render nedoběhne" — nekonečná smyčka (NE paralelní běhy)
Render kultury se v Sez. 48 opakovaně zasekl; Censure Sez. 48 to svedl na „2 paralelní běhy + ortofoto
viselo na síti". **Diagnostika ukázala jinou příčinu:** spustil jsem render na pozadí s unbuffered logem
a sledoval. CPU **monotónně rostlo** (123→272 s), RAM konstantní 113 MB, log zaseknutý přesně mezi „terén"
a „plošný pokryv" = **spin v smyčce, ne síť**. V kódu: `_draw_dotted_surface_area` (nový Sez. 48) má vnější
`while y <= y1:`, který **nikdy neinkrementoval `y`** (chybělo `y += sp`). `_draw_marsh_area` (vzor) má
`for y in range(...)` → auto-inkrement, nezamrzl; při přepisu na `while` u teček inkrement vypadl. SV má 185
kultur → narazí hned. **Po fixu render SV ~15 s** (předtím >10 min „viselo"). Lekce → diár Censure níže.

### Verify kultury 412 (5 lokalit)
Pole 412 = černé tečky (pixel-sample C_BLACK), žluté pozadí, tečky lícují na globální mřížce; .omap
401 + 412.1 (16 objektů SV z 18 polí, 2 pod min. 9 mm² → 401); degradace funguje. Vizuál ukázán (crop).

### Sad/zahrada → 520 (oprava chybného 413 Orchard)
Uživatel z vizuálu poznal: „Ovocný sad, zahrada" jsou v ČR **zahrady u rodinných domů a chalup —
oplocené, nepřístupné** → musí být **520 olivová** (out-of-bounds), ne běhatelný 413 Orchard. 413 byl
konceptuální omyl Sez. 48. **Změna:** `map_open_land_to_isom` 135 → 520. **413 úplně smazán** (mrtvý kód:
`ISOM_ORCHARD`, `SURFACE_DOT/CLASS/FILL/MIN_AREA[413]`, omap_export USED/AREA_CODES, stats; grep 0 živých
reziduí — paměť `slap-symbol-rewrite-comments`). Verify: 413 = 0 napříč 5 lokalit, sady přesunuty do 520
(SV 2040 olivová), 412 pole beze změny, maska 3 třídy (1=open/2=oliv/3=pole). Vizuál: olivové plochy
kolem zástavby SV. **Uživatel → „Test OK".**

### Ortofoto podklad vrácen
`--no-ortho` (rychlý verify, paměť `one-render-blocking-wait`) vyřadil připnutý ortofoto podklad, který
uživatel používá ke kontrole. 5 lokalit přegenerováno s ortofotem (`ortofoto.png` + `<template TemplateImage>`
v `.omap`, opacity 0.5, visible). (Pozn.: `.ortofoto_cache` 749 MB pokrývá 5 dev lokalit → fetch cache hit.)

### Audit cadence
%AUDIT:CODE Sez. 41 → **+8 (práh ≥8 sez DOSAŽEN)**; net LOC dnes spíš záporné (smazán 413 > přidán fix).
Doporučit Sez. 50 jako první bod. DOCS reset Sez. 46 → +3. **pruning +14/12 (≥2 nad, zralý)**. CALIBRATE +14/15.
Stale Příště: **501 paved obrys/513 wall tečky** visí Sez. 44→49 = **6× → ⚠ Stale, rozhodnout DO/DROP Sez. 50.**

### Kudos / Censure
- **Kudos uživatel:** vizuál-verify odhalil koncepční chybu (413 sad = ve skutečnosti zahrady → 520);
  „Test OK"; všiml si chybějícího ortofota (oko = source).
- **Censure já (retro Sez. 48):** mis-diagnostika root cause — zaseknutí svedeno na paralelní běhy/ortofoto
  (symptom), skutečná příčina = nekonečná smyčka v mém čerstvém kódu. Lekce: **mířit na root cause, ne na
  nejbližší symptom** — a u „visícího" procesu změřit CPU/RAM/log (spin vs síť), ne hádat. Druhý drobný:
  `--no-ortho` při verify omylem odstranil podklad, který uživatel potřebuje.
- **Proces dobře:** systematická diagnostika (background + unbuffered log + CPU/RAM poll) přesně lokalizovala
  bug; grep-ověření 0 reziduí po smazání 413 (paměť `slap-symbol-rewrite-comments` ctěna).

### Příště (Sez. 50)
1. *(první bod)* **%AUDIT:CODE** (práh ≥8 dosažen) — `generator.py` monolit roste; souběžně zvážit pruning.
2. *(⚠ Stale 6×, DO/DROP)* drobnosti auditu Sez. 44: 501 paved obrys hnědý→černý; 513 wall tečky á 3 mm.
3. *(zralý)* IDEAS/TODO pruning (+14/12).
4. *(carry-over)* oplocené volné terény (RÚIAN {5,13}); 115 `Ostatní plocha v sídlech` → 501.
5. *(katalog PŘÍŠTĚ)* zábrana (54)→519, komín (31)→524.