# Diář 2026-06-06

## Sezení 92 — Rozšíření pokrytí generátoru: 403 Rough open (první vegetační třída ze separace)

Hlavní tah po Sez. 91 (DoD ≥90 % ISOM, pokrytí = strop tréninku): **rozšiřovat pokrytí > ladit
model**. První přírůstek: **403 Rough open land** ze separace (bledá žlutá).

### %BEGIN + měření pokrytí přes 5 vzorových map
Stroj mrkla (RTX 5070 + korpus 268 → plná dostupnost). `temp/coverage_union.py` (scratch):
union ISOM kódů z 5 reálných `.omap` vs schopnosti generátoru (`omap_export.USED_CODES`), TÁŽ
integer-prefix logika jako `compare_isom`. **Pokrytí 46–52 %** (schopnostmi; přísnější matched
číslo Sez. 91 = 38 %, penalizuje i symboly co umíme ale data je na výseku nedala). Top chybějící
žádané VŠEMI 5 mapami: **210 Stony ground (4641×)**, 416 veg hranice (2383, linie), **403 Rough
open (2321, plocha)**, 419/418 veg body, 112 mikroformy, 409/407 hustníky, 507 footpath, 404.

### Rozhodnutí (uživatel)
- **Balík:** vegetační rodina přes separaci. ALE rozpadá se dle geometrie+metody — realisticky
  teď jen **403** (plošně-odstínová): 404/407/409 = PATTERN (separace slepá, nález Sez. 90), 416 =
  linie (Png2Line), 418–420 = body (Png2Point). 403 = přesně start, který navrhla Sez. 91.
- **DoD metoda:** separaci pustit i na `resources/*.png` (obarvené skeny) — sjednotí měření s
  tréninkovou cestou. **Q2 carry — dnes neimplementováno** (samostatný krok).
- **Architektura:** vlastní area-color klasifikace v `separate.py` (SLAP: separate = area GT pro
  živý Png2Area, map_gt = runnability GT pro archiv; reframe Sez. 79). NE rozšiřovat map_gt.

### Doložení separability 403 (verify-against-source)
Georef resources `.omap` je „Local" (jen scale+grivation, bez S-JTSK ref) → paper→png přes omap
nejde. Reference odečteny z RASTRU (osvědčená metoda Sez. 71/72 olivová/purpurová), ne přes georef:
- `temp/yellow_probe.py` k-means žlutavých: **bimodalita na všech 5 mapách** (sytá B~54–74 vs bledá
  B~148–155). Sedí na ISOM: 401 = Yellow 100 % (sytá), 403 = Yellow 50 % (bledá).
- `temp/yellow_overlay.py` vizuál Velbloud: bledá žlutá padá i na **oranžové cesty** + **404 pattern
  tečky** → nutná 3-way reference. k=3 oddělil tři konzistentní clustery: **403 (254,222,154)** /
  401 sytá (255,200,58) / road (227,168,118). Road oddělitelný (nižší R i G než 403).

### Kód — 403 E2E napříč vrstvami
1. **`palette.py`** — swatch `yellow_pale` (254,228,139) = Yellow 50 % render barva + `C_YELLOW_PALE`.
2. **`separate.py`** — `AREA_CLASSES` refaktor na CODE-CENTRIC schéma ({kód: {gt, vis, [pale_yellow]}}).
   `YELLOW_REFS` (4 scan reference: 403/401/road/BÍLÁ) + `_is_pale_yellow`. **403 = (gt==4 open) ∩
   bledá žlutá** — staví na OČIŠTĚNÉM gt z map_gt (median+ignore+layout crop), jen rozštěpí žlutou
   uvnitř open (sytá 401 = real část, neseparuje se). `separate_areas(label_map, rgb, src_mpp)` +
   `_downscale_mask`. Zelené 406/408/410 beze změny.
3. **`pairs.py`** — `_separate_to_sjtsk` načte map.png, ořízne rgb STEJNĚ jako gt, předá do separace;
   code-centric iterace.
4. **`generator.py`** — `PREDICT_AREA_*` registr (superset FOREST_AGE_* o 403 bledá žlutá);
   `_draw_predict_areas` kreslí 403 přes `_draw_area_symbol` (ne zelené `_draw_forest_age_area`);
   meta sekce `FOREST_AGE_NAME/CLASS`→`PREDICT_AREA_*` (jinak KeyError na 403). 403 NENÍ míchán do
   „forest_age" názvosloví (legacy úklid TODO Sez. 90).
5. **`omap_raster.py`** — `AREA_ZORDER` +403 (za 401) + `LABEL_VIS`. **`omap_export.py`** —
   `USED_CODES` + `AREA_CODES` +403 (template HÁ area_symbol 403 jako 401).

### Verify
- Izolovaná separace 1088447: 403 = 163 polygonů (0,7 %). **NÁLEZ — bug bílého papíru:**
  `_fill_ignore` zaplnil velký okrajový IGNORE (papír mimo mapu, který `_detect_map_area`
  nezachytil) jako open(4); bílý papír je BLÍŽ bledé žluté než syté → falešné 403 na papíru
  (vizuál: žlutý pruh nad mapou). **Fix: 4. reference BÍLÁ** v `_is_pale_yellow` → papír padne na
  bílou, ne 403. Po fixu vizuál čistý.
- Crop full-res: sytá žlutá (401/404 scattered) správně NEoznačena jako 403 (jen antialiasing); RAW
  403 maska roztříštěná (drobky), vektorizace `MIN_AREA_PX`+closing scelí (overlay = souvislé plochy).
- **`build_pair(1088447)` E2E prošel** (542 ploch vč. 403): render rgb→scan + `area_labels.png` (Y).
  **403 v Y label rastru (label 4, 0,52 %, 27313 px)**, konzistentně v X i Y. Pár self-konzistentní.
- **Regrese:** proc/noise cesta behavior-preserving (246=246, git-stash diff — `_draw_predict_areas`
  se v noise nevolá). py_compile 6/6.
- **Dopad:** `coverage_union` po přidání 403 → **pokrytí +1 symbol na všech 5 mapách** (Bedřichovka
  48→49 %, Blatná 52→54 %, Slovanka 48→49 %, Soví vrch 47→49 %, Velbloud 46→47 %).

### Příště (Sez. 93, mrkla)
1. *(hlavní tah, `[!]`)* **Q2 — separace na `resources/*.png` → tvrdý DoD.** map_gt na resources
   sken → separace → matched gen `.omap` → `compare_isom` se 403. Dnešní `coverage_union` je
   schopnostní míra, ne matched DoD.
2. *(pokrytí, pokračování rodiny)* další odstínové/snadné: 404 (pattern — vyžaduje rozhodnout přístup),
   507 Small footpath (linie, ZABAGED?), pak 210 Stony ground (#1 četností, ale ZABAGED ho nevede —
   rozhodnout zdroj).
3. *(úklid)* `%AUDIT:DOCS` (zralý, práh 10) + legacy `forest_age` názvosloví v predict cestě.
4. *(model 1b, odsunuto za pokrytí)* class-balanced expansion (208/501/301.1).
5. *(nový směr)* saský konektor `connectors/saxony.py` → Jonsdorf demo.
