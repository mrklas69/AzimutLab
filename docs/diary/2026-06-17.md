# Diář — 2026-06-17

## Sezení 136 — pseudo body 417/419 do generátoru (princip kamenů) + ROADMAP + fázová závora (HAL3000)

Sezení dvojí povahy: **vytěžovací práce** (nová pseudo vrstva 417/419) + **procesní korekce**
(ROADMAP, zakázaná slova). KPI generátoru **58,6 → 61,1 %** (KOMPAS se zaktualizoval sám,
viz níže).

### Censure (otevřeně) — sezení ZAČALO ve špatné fázi
%BEGIN navrhl jako fokus **A2b geometrickou augmentaci (warp) + „degrade-lite" experiment** =
degenerace podkladů. Uživatel to **dvakrát odmítl** („nebudeme deformovat") a ukázal vizuálním
verify, že eval_real skeny jsou **čisté/ploché/nedeformované** → augmentace by modelovala
neexistující vady (Goodhart). Otočil směr na **vytěžení** (pseudo 417/419). Je to ~6. výskyt
téhož sklouznutí → uživatel vytvořil `ROADMAP.txt`. Lekce zakotvena (viz ROADMAP + %BEGIN).

### Hlavní tah — pseudo body 417/419 do generátoru (vytěžení, plnění KOMPASu)
Asymetrie: Png2Point **čte** 417/419, ale generátor je do `.omap` skoro nekreslil (417 jen řídce
z ZABAGED `Významný_strom`, 419 vůbec). Rozhodnutí uživatele: **princip kamenů** (Sez. 107) —
doložené ZABAGED stromy + dosypat na reálnou hustotu; 419 čistě pseudo.

- **Měření first:** reálná hustota z kartografových `.omap` (eval_real GT/plocha, crosswalk-aware
  `parse_carto_points`): medián **417 ~27/km², 419 ~18/km²** (rozptyl 4–46 dle terénu).
- **`_generate_pseudo_veg_points`** (izomorf `_generate_pseudo_boulders`): maska = pole MINUS voda
  MINUS 206 skály (dilatace); hustota losovaná per mapa; 417 = cíl − reálné ZABAGED (doplnění, ne
  zdvojení). Render přes `_draw_landmark` (417 zelený kroužek; **419 nový — zelený X**, mirror
  inject `_stamp_cross`). `ISOM_VEG_FEATURE=419` + `LANDMARK_NAME/CLASS[419]`.
- **6 míst propsáno:** konstanty + render 419 + funkce + call site (gated `pseudorealistic`,
  `landmark_features`+`landmarks_info`) + `omap_export` „419" do `USED_CODES` + meta. Template má
  417/418/419 (type=1) → export nespadne.
- **Verify:** 5 DEV map regen; `.omap` 417=237/419=206 (HS) … korelace se skalnatostí (HS 847 skal
  → málo bodů; NL 0 skal → plná hustota). Meta + `.omap` propsané.

### Body 1+2 (dotažení vrstvy — nálezy uživatele {A})
1. **ISOM rozestup** (NL {A}: dva symboly přes sebe) → **rejection sampling** (nový bod zahozen,
   je-li blíž než r_a+r_b+mezera k umístěnému). Verify: 0 párů pod floor.
2. **Nekreslit na budovách/cestách/zpevněných** (SV {A}: kameny na 501 pásech podél cest, zakryté).
   První pokus přes **grid masku SELHAL** (tenké cesty na hrubém gridu zmizí → 160 px na cestách) →
   oprava na **px rozlišení** + dilatace o poloměr: nový sdílený helper `_build_forbid_px`
   (DRY, ≥2 konzumenti). Aplikováno na **veg 417/419 I kameny 204/210** (uživatelova zpráva).
   Verify z `.omap` (paper→px): **VŠECHNY čtyři třídy 0 na cestách/budovách/zpevněných**.

### Nález 418 — měl jsem se mýlit
Tvrdil jsem „418 není KPI díra". KOMPAS to **vyvrací**: 418 orig 178 / gen 0 = díra 1,3 pb (v
žebříčku, protože 417/419 z něj vypadly). 418 = `Prominent bush or tree`, tvar z templatu =
malý zelený **plný bod** (`inner_radius 75 / outer_width 300 color 3`). **Kandidát na přidání**
(volba Příště) — plní KOMPAS, ne jen vizuální úplnost.

### KPI/KOMPAS — sám se zaktualizoval
`measure_dod` má cache invalidaci na čas kódu (no silent fallback) → detekoval změnu generátoru a
**přegeneroval compare mapy s pseudo body**. KOMPAS Png2Point: **417 gen 0→199, 419 gen 0→230**
(z `·` na `✓`). KPI **58,6 → 61,1 %** (bod sub 52,8 → 59,2). Pozor: proporčně Goodhart-citlivé
(poloha vymyšlená), ale jako **pokrytí** (ROADMAP definice) = legitimní (2 nové typy).

### Proces — ROADMAP + fázová závora (governance)
- `docs/ROADMAP.md` (z uživatelova `.txt`, formalizováno + antidrift): cíl, dvě etapy, **fázová
  závora**, mechanismy proti driftu. `ROADMAP.txt` smazán (nahrazen).
- **Zakázaná slova (rozhodnutí uživatele):** `Rekonstruktor()` + „degradace" — jsme v etapě
  `Generator()`, energie do vytěžení/KOMPASu.
- `%BEGIN` krok 0.5: číst ROADMAP + self-check „vytěžení/KOMPAS, nebo degradace/leštění/rekonstruktor?".

### Příště (po Sez. 136)
1. **418 Prominent bush or tree** — přidat (doložená KOMPAS díra, stejná mašinérie jako 417/419).
2. **KOMPAS sloupce** — `zdroj · věrohodnost · provedení` do `measure_dod --table` (ROADMAP g2 SSoT).
3. Další KOMPAS díry (vytěžení): 508 / 403 / 409 / 202 — pozor na Png2Line scope a vegetace-gate.
4. (Zmrazeno do naplnění KOMPASu: rekonstruktor ladění, degradace/augmentace.)


## Sezení 137 — pseudo bod 418 Prominent bush or tree do generátoru (vytěžení) (HAL3000)

Čisté vytěžovací sezení (Příště #1 ze Sez. 136). Žádné sklouznutí do špatné fáze — self-check
(ROADMAP 0.5) prošel: nový ISOM typ v generátoru = pokrytí KOMPASu. KPI **61,1 → 61,7 %**.

### Hlavní tah — 418 jako třetí pseudo veg třída
Izomorfní rozšíření vrstvy 417/419 ze Sez. 136. 418 = `Prominent bush or tree`, template id=103
(`inner_radius=75` bílý / `outer_width=300` zelený, color 3) → vizuálně **plný zelený disk** ⌀ ~0,375 mm,
odlišný od 417 (dutý kroužek) i 419 (X). Keře/buše ZABAGED nemapuje → **čistě pseudo** (jako 419).

- **Měření first** (doménová zásada): reálná hustota 418 z kartografových `.omap` toutéž crosswalk-aware
  metodou jako Sez. 136 (`temp/measure_418_density.py`). Metoda **zkalibrována** — 417 mi vyšlo 26,7/km²
  (Sez. 136 uvádí ~27 ✓), 419 21,8/km² (~18 ✓). **418: medián 17,8/km², rozsah 6,6–25,3/km²** →
  `PSEUDO_BUSH_PER_KM2 = (8.0, 26.0)` (uniform průměr ~17 = medián, pokrývá řídké i bohaté mapy).
- **7 míst propsáno** (vše izomorf 417/419): `generator.py` konstanty (`ISOM_PROM_BUSH=418`,
  `LANDMARK_NAME/CLASS[418]=9`, `LANDMARK_BUSH_R_PX`, `PSEUDO_BUSH_PER_KM2`) + `_draw_landmark` plný disk +
  `_generate_pseudo_veg_points` (418 do `r_of`/losovací smyčky, čistě pseudo bez odečtu reálných) +
  call-site log + docstringy + `omap_export.USED_CODES += "418"`. Sdílí `_build_forbid_px` + rejection sampling.

### Verify (Nová Louka, seed 0)
- `.omap`: 418 **0 → 398 objektů** (sym id 103). Hustota 16,7/km² ≈ měřený medián.
- **Forbid** (`temp/verify_418_forbid.py`, px rozpad): budovy=0, zpevněné=0; cesty jen 3 px (1 antialiased
  okraj X), voda 38–112 px = hrana vyloučená na grid rozlišení (očekávané, jako 204/210/417/419). Středy
  rejection-samplingem garantovaně mimo forbid.
- **Vizuál** (zoom): tři rozlišitelné zelené symboly — plný disk 418 / kroužek 417 / X 419; rozložení přirozené.
- **KPI/KOMPAS** (`measure_dod`, cache invalidace přegenerovala compare mapy): KPI 61,1 → **61,7 %**
  (bod 59,2 → 62,4); KOMPAS 418 z díry `·` (orig 178/gen 0) na `✓` (orig 178/gen 90). Gen podstřeluje
  vědomě — hustota kotvena na reálný medián, ne na KPI číslo (ROADMAP/Goodhart A3); na skalnatých mapách
  (Velbloud) maska veg body ukrajuje.

### Poznámka — 418 mimo Png2Point scope
Png2Point detekuje jen 204/210/417/419. 418 přidáno **jen do generátoru** → zvyšuje POKRYTÍ/KPI a plní
ROADMAP zásadu „pokrytí = strop" (generátor musí kreslit, aby se reconstructor jednou mohl naučit).
Detekce 418 (rozšíření Png2Point jako Sez. 128 pro 417/419) = samostatný budoucí krok.

### Příště (po Sez. 137)
1. **KOMPAS sloupce** — `zdroj · věrohodnost · provedení` do `measure_dod --table` (ROADMAP g2 SSoT).
2. Další KOMPAS díry (vytěžení): **508 / 403 / 409 / 202** — pozor Png2Line scope + vegetace-gate.
3. (Audit zralý: `%AUDIT:DOCS` práh ≥10 dosažen Sez. 137 — kandidát, prioritně ntbhej.)
4. (Zmrazeno do naplnění KOMPASu: rekonstruktor ladění, degradace/augmentace.)


## Sezení 138 — podklady (ortofoto+DMR) + E1/E3 boulder fixy + #1 KOMPAS sloupce + E2 neatline border + duplikáty (HAL3000)

Dlouhé sezení vedené **testováním uživatele** — regenerace map → série nálezů a feature requestů.
Většina práce = **verify-against-source tooling + polish výstupu** (vyžádané uživatelem pro testování),
plus jeden vytěžení-SSoT krok (#1 KOMPAS sloupce, ROADMAP g2). KPI **61,7 → 60,7 %** (E3 rejection).

### Podklady (background templates) — verify-against-source tooling
Na přání „co nejvíc vrstev pro testování": nový `gen_backgrounds.attach_dmr_hillshade` (ESRI hillshade
z DMR 5G, slunce SZ 315°/45°, 2,5 m/px) + `attach_ortho` (ČÚZK ortofoto obdélník) + `omap_export.append_image_templates`
(přidá podklad k UŽ existujícím `<templates>`, na rozdíl od `inject_image_templates` který chtěl prázdné count=0).
Všech **9 map**: ortofoto + DMR (5 DEV); +sken (3 KPI); plný set scan/mobile/ortho/gt/dmr (Buschdörfl). OOM *Templates* toggle.
Architektura: post-process (ne do jádra `generate_map`, SLAP); pracuje jen z `rgb.pgw`+`meta.json` (společný jmenovatel 3 cest).

### E1 ({A} Novina) — balvany na železničním koridoru
`_build_forbid_px` měl jen budovy/cesty/zpevněné, **chyběla železnice** (`railway_mask_img`, ř. 4022) → balvany 204/210
padaly na úzký železniční pruh. Fix: přidána do `forbid_imgs`. Verify (měření z `.omap`): 204 na železnici **0**, 210 **0**.

### E3 ({C} Novina) — dva překryté balvany 204
`_generate_pseudo_boulders` **neměl rejection sampling** (veg 417/419 ho dostaly Sez. 136, balvany vypadly) → pseudo 204
se překrývaly se sebou i s reálnými. Fix: rejection vůči seed = reálné 204/207 i už umístěným (izomorf veg, 2·r_b+mezera).
**Rešerše (WebSearch ISOM 2017-2 + O-Map Wiki):** bodové symboly se NESMÍ překrývat (legibilita); těsná skupina → cluster
**207** / pole **208**. Verify: min vzdálenost 5,0 px ≥ práh 4,9, **0** dvojic pod prahem. Pozn.: rejection-while dotahuje na
cílovou hustotu (retry místo skip) → na skalnatých mapách o něco víc balvanů (SV 2998→3375), bez překryvů.

### #1 KOMPAS sloupce (Příště #1 ze Sez. 137; ROADMAP g2 SSoT)
`measure_dod --table` + `zdroj · věrohodnost · provedení`. Registr `KOMPAS_SOURCE` (51 kódů, ukotveno proti
`USED_CODES`): zdroj (ZABAGED/DMR/separace/pseudo/mix) + věrohodnost (data/odvoz/separ/pseudo/mix). `_provedeni` = AUTO
ze **share** orig:gen (Goodhart-aware, memory `kpi-overshoot-dilutes-not-counts`): ok/přestřel/podstřel/chybí/jen gen.
Starý `✓/·/+` mark nahrazen `proved.` (subsumuje). **Rozlišení:** `chybí`+`–` = strukturální díra (neumíme); `chybí`+zdroj
= mechanismus máme, ale 0 (datová mezera, např. 308 marsh). Čtení tabulky odhalilo díru registru (416 separace) → doplněno.

### E2 ({A}-{X} Novina) — neprůnikový border na řezné hraně plochy
Voda 301 (kombinovaný symbol: modrá výplň + černý břeh) přesahuje horní hranu papíru → `cut_box` ořízne → OOM kreslí
černý břeh i podél umělé řezné neatline. **Probe (uživatel = OOM arbiter, OOM headless není):** v2 ověřil primitiv — `16`
(hole, BEZ close bitu 2) na **POSLEDNÍM** vrcholu = plná výplň + border bez uzavíracího segmentu. Fix `cut._emit_area`:
detekuje neatline úsek (`_on_clip_edge`), přerotuje na **uzavírací segment** + flag 16; `omap_raster` dělí ringy na bitu 16
→ area_labels netknuté. **Single-run HOTOVO** (verify: 48 ploch suppressed vč. horní vody 301; 5/5 cut testů OK).
**Multi-run NEJDE** (probe v3: mid-ring hole rozseče path → žádná výplň; OOM hole = oddělovač pod-cest, ne border-gap):
{X} = voda dotýkající se horní hrany na 2 místech → potlačí se jen nejdelší úsek. Robustní = oddělit fill/border na samostatné
objekty (template změna) → **odloženo jako nepoměrné** (volba uživatele: přijmout single-run zisk; multi-run → TODO limit).

### Duplikáty (úklid maps/)
- **Jonsdorf SMAZÁN** (volba uživatele): jen reference (README + Livelox sken Oybin/Hölle, re-download-recoverable);
  rationale saské cesty je v `data-sources.md`, classId **1138425** doplněn tamtéž před smazáním.
- **Buschdörfl** `gen.omap` → `Buschdörfl.omap` (konsolidace): byly identické (Sez. 132), ale regeneracemi se rozešly —
  `gen.omap` čerstvý (E1/E3+podklady), `Buschdörfl.omap` stale → ponechán čerstvý pod konvenčním názvem, starý smazán.
- **Pravidlo uživatele „vyhnout se duplikátům platí pro všechny mapy"** → každý `maps/<name>/` = JEDEN `<name>.omap`,
  žádný `gen.omap`; Livelox mirror přejmenuj. Paměť `maps-single-omap-no-duplicates`. Regen-all to teď respektuje.

### KPI/měření
KPI **61,7 → 60,7 %** (Bedř 53,6 / Blatná 61,5 / Velbloud 67,0; plocha 69,5 / linie 58,9 / bod **62,0**). Pokles = vědomý
důsledek E3 (ISOM-korektní nepřekrývání balvanů; Velbloud −2,4 jako nejskalnatější; plocha/linie beze změny — E1/E3 jen body).
E2 nesahá na počty (border flagy) → KPI beze změny. Žebříček děr: **508 (2,9 pb) / 403 (2,7 pb)**. Všech 9 map regenerováno s E2.

### Censure
Žádný drift do špatné fáze (ROADMAP). Side-quest (podklady/E1/E2/E3 = tooling + polish, ne vytěžení) byl **celý vyžádaný
uživatelem** (testování) — legitimní; #1 KOMPAS = ROADMAP g2 SSoT. Procesní chyba: regeneroval jsem Novinu pro E1/E3 verify
DŘÍV, než jsem vytáhl uživatelovy {A}/{B}/{C} markery → přepsal je (přiznáno; markery byly deterministicky reprodukovatelné).
Sandbox false-positive (`\d+` v Remove-Item) + Bash `cd` přepsal sdílený cwd PowerShellu (`Set-Location` reset).

### Příště (po Sez. 138)
1. Další KOMPAS díry (vytěžení): **508 / 403 / 409 / 202** — pozor Png2Line scope + vegetace-gate.
2. `%AUDIT:DOCS` zralý (≥10 od Sez. 127) — prioritně ntbhej.
3. (Backlog: E2 multi-run = oddělit fill/border (template); KOMPAS do trvalého souboru jako STATISTICS.md.)
4. (Zmrazeno do naplnění KOMPASu: rekonstruktor ladění, degradace/augmentace.)

### Předání na ntbhej (Sez. 139 tam)
- **Sync:** `git pull` → kód+docs na Sez. 138 (podklady / E1/E2/E3 / #1 KOMPAS sloupce). Pak `%BEGIN`.
- **Hlavní fokus tam: `%AUDIT:DOCS`** — zralý (≥10 od Sez. 127, **prioritně ntbhej** per `docs/PROMPTS.md`: úklidové
  audity nepotřebují CUDA/korpus, vzácné HAL3000 okno patří CUDA práci). Docs jsou čerstvě sahané (Sez. 138 diář/DONE/
  DIARY/TODO + KPI blok) → dobrý čas na audit konzistence/jazyka/úplnosti.
- **Constraints ntbhej** (paměť `two-machines-git-sync`): bez CUDA + bez Livelox korpusu (`resources/livelox/`) →
  trénink, `Png2*`, `pairs.build_pair` **NEJDOU**; `measure_dod` jen **částečně** (chybí `resources/Velbloud.pgw` →
  změří Bedř/Blatná). `maps/` je gitignored → **NEpřenáší se** (kdyby audit potřeboval výstup, regeneruj přes ČÚZK —
  funguje všude). KOMPAS k nahlédnutí: `python generator/measure_dod.py --table` (regeneruje dostupné mapy).
- **Carry zpět na HAL3000/mrkla:** další KOMPAS díry (508/403/409/202) běží kdekoli (generátor = ČÚZK), ale verify
  reálného transferu + jakýkoli trénink = CUDA → mrkla/HAL3000. E2 multi-run (template fill/border split) = backlog.


## Sezení 139 — %AUDIT:DOCS (5 agentů) + sladění architektury s ROADMAP + IDEAS/TODO pruning (ntbhej)

Úklidové sezení na ntbhej (per `docs/PROMPTS.md` bod 4: audity prioritně sem). Fokus zvolen `%AUDIT:DOCS`
(práh ≥10 překročen o 2 od Sez. 127 → vynucený první bod), pak pruning (≥12). Žádný kód, jen docs.

### Diskuse
- **Sync:** lokál byl **45 commitů pozadu** za `origin/main` (přesně past paměti `stale-clone-fetch-first`;
  `git status` clean ≠ up-to-date) → ff na `6b1604a`. Stroj NTBHEJ21.
- **Dostupnost (ověřeno `ls`, ne předpoklad):** `resources/livelox/` má **264 map** — rozpor s předáním Sez. 138
  („bez korpusu"). Korpus tu JE (bez ověřené kurace/splitu); bez CUDA stejně trénink nejde. `resources/*.pgw`:
  Bedř/Blatná/Soví vrch/Slovanka/SampleMap ✓, **Velbloud.pgw chybí** (předání správně). Paměť
  `gitignored-availability-verify-not-assume` opět platí.
- **KPI rekapitulace:** 60,7 % (Sez. 138) beze změny — generátor se od HEAD neměnil (jen docs).

### Rozhodnutí
- **%AUDIT:DOCS rozdělen na 5 paralelních agentů** (hlavní dokumenty / TODO-DONE-DIARY / KB / modulová README /
  jazyk-struktura) → konsolidováno: **4 kritické + 9 doporučených + kosmetika**. Konstanty v kódu (N_AREA=18,
  N_LINE=2, peak_thr, USED_CODES vč. 418) **sedí** — drift byl jen v prozaickém popisu stavu.
- **Klíčový nález C0 (nadřazený, žádný agent ho neviděl — odhalil ho dotaz uživatele „četl jsi ROADMAP?"):**
  dva soupeřící „zdroje pravdy" — `architecture.md:6` se prohlašuje za SSoT a drží 5-UC DAG s UC5 = „palette
  separation", zatímco novější `ROADMAP.md` (Sez. 136, čtený každým `%BEGIN`) drží osu `Generator()`→`Rekonstruktor()`.
  **Rozhodnuto (volba uživatele): sladit architekturu s ROADMAP** — ROADMAP je nadřazený směr, architecture popisuje
  statické vrstvení. **Tím se rozpustil A4 dluh** (~37 sezení): taxonomie není „k rozhodnutí", ROADMAP ji dal.
- **Opravy: vše včetně velkých přepsů** (volba uživatele). Výjimka C2/UC5 taxonomie = sladěna s ROADMAP,
  ne vymýšlena znovu (fait-accompli zakázáno A4 → ale rozhodnutí už padlo v ROADMAP).

### Kód
Žádný — čistě docs. **16 souborů (+163 / −123 ř.):**
- **C0+C2 `architecture.md`:** status hlavička → ROADMAP jako SSoT směru/etap; DAG diagram + UC5 sekce přepsány
  z „palette separation" na tři reconstructory (Png2Area/Point/Line); separace = feeder fáze I. **C1:** „Png2Line
  neexistuje" opraveno 3× (architecture 2× + `generator-procedural.md`) + plný popis 3. reconstructoru. **C3:** KPI
  60,7 % do README/architecture/GLOSSARY (bod 52,8→62,0). **C4:** vložen chybějící nadpis `## Sezení 135` do DONE.
- **Doporučené (9/9):** `generator/README` dotažen (cut.py / gen_backgrounds.py / pseudo veg 417/418/419 / railway
  forbid / LOC); Png2Point mF1 **0,827** 4-třídový všude (README+GLOSSARY); ROADMAP+DIARY-archive do README layout/Docs;
  `tools-models.md` stack sync s `requirements.txt` (scikit-image/scipy/pygeomag) + tři reconstructory; railway 509
  do spec §4.9k; [x] položky (A7+balvany) z TODO; HANDOFF_v2 + oba 260614 audity archivní baner; IDEAS hotová práce
  (Png2Line/poledník, −56 ř.) zhuštěna na pointer. TODO A4 přeznačen `[~]`.
- **Pruning (≥12):** ChatGPT-audity visící hlavička → 1 řádek; `[×ARCHIV]` Krok 5 zkrácen; Backlog/Rozhodnutí
  sladěny s ROADMAP (palette-separation rámec → `Rekonstruktor()` etapa). Aktivní backlog (drobnosti, DRY dluhy
  fáze A, GT kvalita) ponechán — nejsou mrtvé.
- **Záměrně neopraveno:** `generator/STATISTICS.md` (K3) zastaralý (regen 2026-06-10, před rock gate + pseudo veg) →
  vyžaduje běh `stats.py`, ne docs edit (backlog).

### Kudos / Censure
- **Kudos! (uživatel):** „četl jsi ROADMAP?" — jediný dotaz, který otočil mechanickou opravu driftu na
  správné rámování (C0). Bez něj bych byl sladil architekturu naslepo a možná vymýšlel taxonomii, kterou už
  ROADMAP dal. Lekce: **při auditu „zdroje pravdy" nejdřív zjisti, KTERÝ dokument je dnes nadřazený.**
- **Censure! (sám sobě):** %BEGIN jsem odprezentoval ROADMAP jen jako zmínku agentů, nečetl ho celý sám —
  přitom `%BEGIN` krok 0.5 (Sez. 136) ho velí číst. Kdybych ho přečetl hned, C0 jsem viděl bez upozornění.

### Příště
1. KOMPAS díry **508 / 403 / 409 / 202** (vytěžení) — generátor běží kdekoli, ale 508→Png2Line scope (CUDA),
   403/409→vegetace-gate. Měřitelná páka na ntbhej omezená (chybí Velbloud.pgw, korpus bez kurace).
2. `generator/STATISTICS.md` regen `stats.py` + (backlog) KOMPAS→trvalý soubor.
3. Kosmetická revize statického 5-UC DAGu (zbytek A4: umístění UC3 de-purple / UC4-III v `Rekonstruktor()` etapě) — neblokuje.
4. (Zmrazeno fázovou závorou: rekonstruktor ladění, degradace/augmentace.)
