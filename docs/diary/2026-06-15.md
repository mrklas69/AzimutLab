# AzimutLab — deník 2026-06-15

## Sezení 127 — cleanup + bezpečné checkpointy + uzavření auditních dluhů (HAL3000)

### Diskuse

Po `%BEGIN` byl repozitář synchronní s `origin/main`; KPI generátoru se po změně
`generator/cut.py` přeměřilo na **58,6 %** (plocha 69,5 / linie 58,9 / bod 52,8;
největší díry 417/419/508/409/202). Uživatel zvolil úklidový balík
`%CALIBRATE` + TODO/IDEAS pruning + `%AUDIT:DOCS`, poté CODE-C2 a nakonec
auditní dluhy CODE-D3/CODE-K1.

Audit potvrdil, že aktivní docs míchaly historické snapshoty s dnešními kontrakty:
README začínal stosezenovým changelogem, TODO obsahovalo dokončené top-level úkoly,
IDEAS fungovalo převážně jako archiv a DIARY znovu překročil roli lehkého indexu.
Opraven byl také drift Png2Area: správný kontrakt je 17 ISOM kódů + pozadí
(`N_AREA=18`), X je čistý `rgb.png` a degradace se aplikuje on-the-fly.

### Rozhodnutí

- Každý trénink Png2Area/Png2Point má vlastní `run_id`; běh nikdy automaticky
  nepřepisuje kanonický model.
- `unet_best.pt` se mění pouze explicitním `train.py --promote <run_id>` po
  dokončeném testu. Overfit nebo přerušený běh nelze povýšit.
- Fingerprint popisuje obsah splitu a kanonických manifestů, ne absolutní cestu
  checkoutu. Velké PNG se při startu tréninku zbytečně celé nehashují.
- Stringový `.omap` post-process smí podporovaný formát buď bezpečně zpracovat,
  nebo hlasitě odmítnout; žádný návrat `(0, 0)` na chybu formátu.
- Fialová trať má podle deklarovaného kontraktu opravdu 5–12 kontrol, vedle
  samostatného startu a cíle.
- Před každým krokem, po kterém agent čeká na vstup uživatele, se přehraje
  `C:\Users\mrkla\.codex\resources\servant-bell-ring.mp3`.

### Kód

- Nový `model/checkpoints.py`: izolované `runs/<run_id>/`, `manifest.json`,
  `best.pt`, atomické save/promote, metadata a fingerprint.
- `model/png2area/train.py` a `model/png2point/train.py`: `--run-id`,
  `--promote`, per-run historie/křivka; Area nově také reprodukovatelný `--seed`.
- `generator/cut.py`: `OmapFormatError`, striktní parsování `<objects>/<coords>`
  s cestou a identitou objektu.
- `model/purple.py`: helper `_course_points`, `n_controls + 2`.
- Nové CPU testy `tests/test_{checkpoints,cut,purple}.py`.

### Dokumentace a cleanup

- README status zkrácen na aktuální snapshot; UC5 a repository layout aktualizovány.
- TODO nemá dokončené top-level položky; IDEAS drží pouze nezralé směry.
- DIARY drží posledních 10 sezení, starší index je v `DIARY-archive.md`.
- Doplněny chybějící DONE sekce a sjednoceny AGENTS/CLAUDE/PROMPTS,
  architecture, GLOSSARY, RESEARCH a generator README.
- Lokální `.claude/settings.local.json` zkrácen na 11 obecných allow pravidel;
  soubor je záměrně gitignored. Uživatelský `.codex/hooks.json` zůstal netknutý.

### Ověření

- `python -m unittest discover -s tests -v`: **8/8 OK**.
- `python -m compileall -q connectors generator model tests`: OK.
- Přísný parser: **6 referenčních `.omap`, 35 904 objektů, 0 chyb**.
- Markdown link check: **72 souborů, 0 rozbitých odkazů**.
- Session coverage: DIARY index i DONE pokrývají všechna sezení.
- `git diff --check`: čisté.

### Příště

1. **Png2Line — `%THINK` + rešerše před implementací.** Porovnat
   segmentaci+skeletonizaci, deep vectorization a polyline regresi; výstupem má
   být rozhodnutí architektury a GT kontraktu, ne kód.
2. **A2b geometrická augmentace** až po Png2Line rozhodnutí: warp musí
   transformovat X i Y současně, Y nearest-neighbor.
