# 2026-06-25

## Sezení 165 — Png2Area 409 retrain (pipeline běží) + %AUDIT:DOCS + isom_scan %AUDIT:CODE + Copilot benchmark (HAL3000)

**Stroj:** HAL3000 (CUDA RTX 5070 + Livelox korpus + `.pgw`). **Etapa (ROADMAP):** `Generator()`.
Dlouhé multi-task sezení: jeden CUDA fokus běží autonomně na pozadí, mezitím dva ne-CUDA úklidové audity
ve fronte (cadence) + ad-hoc benchmark od uživatele.

### Fokus B — Png2Area retrain na 409 GT (volba uživatele; CUDA; PIPELINE BĚŽÍ)
Follow-up Sez. 164 (409 gate fix): separace teď 409 kreslí → páry by měly nést 409 GT → retrain naučí
reconstructor 409 číst (dnes trénovaný na párech s ~0 409).
- **Anti-A1 probe PŘED 3,4h tréninkem** (`temp/probe_409_pairs.py`): měření „409 0→114" bylo na KPI mapách
  (resources/), NE na tréninkovém **Livelox korpusu**. Staré páry (Sez. 156) regenerovány se STARÝM rozbitým
  gate → 409 ~0 (3 vzorky: label 15 = 0/0/94 px). Probe regeneroval 13 reprezentativních párů s novým gate:
  **409 PŘED Σ470 px → PO Σ18389 px (39×), 10/13 map, per-mapa až 0,84 %**; 407 9×. = trénovatelný signál
  (řádově nad Sez. 156 datovým stropem) → retrain obhájený. Caveat: 0,14 % agregát + Sez. 156 „slabý ČR reálný
  transfer" → eval_real 409 může zůstat skromné; reálný zisk = trénink na opravených datech (407 i obecně).
- **Autonomní pipeline** `temp/retrain_409_pipeline.py`: regen 207 (`build_pairs skip_existing=False` — KLÍČ,
  jinak skip past Sez. 156) → tiles (`allow_missing=True`) → train `s165_area409_s42`.
- **DVA pády, oba recovery (ne data loss):** (1) regen+tiles (3,6h) doběhly OK (206/1 fail = čekaný 1192962),
  ale **můj buggy assert** `tj["n_area"]` (správně `tj["_meta"]["n_area"]`) shodil STAGE 2 PO postavení tiles →
  train spuštěn přímo. (2) Train doběhl **ep 38/40** (best **ep 26 val mIoU 0,560**), pak crash **Windows commit
  limit (error 1455)** u DataLoader worker shared-memory — NE bug, systémový resource limit; `best.pt` uložený,
  model validní (plateau, cosine LR ~0).
- **eval_real verdikt — REGRESE → NEPROMOTOVÁNO, revert na `.bak`:** s165 vs baseline (Sez.156): Bedř soft
  **0,520** (z 0,525) / acc 0,857 (z 0,887); Blatná strict **0,208** (z 0,232) / soft **0,301** (z 0,363) / acc
  0,818 (z 0,865). **Příčina (confusion):** model MASIVNĚ halucinuje 409 — Bedř pravé 407→**89 % jako 409**,
  404→56 %, 310→58 %; Blatná 407→82 % jako 409. Vysoká median-freq váha vzácného 409 → krade pixely ostatním
  odstínům. **Honest negativní výsledek fokusu B:** 409 coverage fix (Sez.164) zvedl GENERÁTOR (KPI 67,5%), ale
  trénink reconstructoru na 409 datech **POŠKODIL reálný transfer** (Sez.156 nález „halucinuje 409", jen horší s víc
  daty). Kanonický model = baseline (`.bak` restore); s165 zůstává v `runs/` jako záznam. 409 val IoU plateau ~0,08.

### %AUDIT:DOCS (cadence +2, vynucený; 5 agentů + verify) — 16 oprav, 9 souborů
Dominantní vzorec: **expanze Png2Point 5→10 tříd (Sez. 158-162) + přidání Png2Line se NEÚPLNĚ propagovaly**;
README (root) jediný plně aktuální = de-facto SSoT, ostatní z něj nederivují.
- **K1 Png2Point scope/metrika drift v 5 docs** (každý jiné stale číslo): AGENTS (5-class/0,811), GLOSSARY:588
  (4-class/0,827), tools-models:48 (204/210/417/419/0,827), model/README:16 (5-class/0,811), architecture
  detail (končil Sez.158) → vše sladěno na **10 tříd 204/210/417/419/531/525/527/109/111/112, mF1 0,745**.
- **K2 CLAUDE.md vynechával Png2Line ÚPLNĚ** + „tři podadresáře" → „čtyři" + přidán Png2Line (živý od Sez.130).
- **K3 architecture.md:171-172** týž subdir drift. **D1** CLAUDE png2area 0,683→0,577.
- **D2 root-cause SSoT:** PROMPTS.md %END dostalo propagační pravidlo pro model scope/metriku (headline v 7 docs;
  SSoT = `isom.capabilities.SCAN_LIVE_POINT`). **D3** GLOSSARY 110-nuance. Kosmetika: „jen mrkla"→„mrkla/HAL3000"
  (CLAUDE/AGENTS, drift — trénuje se i na HAL3000), isom-colour PDF, isom-issprom cross-repo, resources/isom→čeština.
- **Verify > slepá aplikace agenta (3 korekce):** GLOSSARY:500 ponecháno (správně, agent se mýlil), architecture
  terminologie ponechána (konzistentní), „Pattern area"/„Kanonický" termíny nepřidány (over-eng, oponoval jsem).

### isom_scan %AUDIT:CODE (4478 LOC, NIKDY auditováno; 4 agenti + verify) — vše opraveno vč. C1
Headline: funkčně solidní PoC (žádný crash bug, dobrá bezpečnost — path-traversal/127.0.0.1/zámky/fail-loud),
těžký DRY dluh. Volba uživatele „vše vč. C1 refactor" (oponoval jsem dělat narychlo → zmírněno behavior verify).
- **C1 DRY refactor (přes fork, behavior-preserving):** nový `points_common.py` (436 ř.) — 5 byte-identických
  helperů + Candidate/component/classify/payload/overlay/contact_sheet/run_detector + schéma kurace. 4 detektory
  → tenké configy, `manmade_omap/review` → `points_omap/review` (git mv, misnomer K2). **Net −420 LOC (−19 %).**
  **Ověřeno 3×:** fork diff (4 detektory byte-identický detections+stats) + má nezávislá runtime smoke (terrain
  běží) + 48 testů. **Fork správně NEAPLIKOVAL D2/D3 leak-fix + D4 name-SSoT** (MĚNÍ výstup) → měřený opt-in.
- **C2 (bug-class):** `build_tile_set.py` silent-fallback při chybějícím `--src-mpp` → hlasité varování.
- **Phase A:** ps1 mrtvý dvojník smazán, future-imports pryč, README file-tree +7 souborů +seskupení +governance
  („ledger prahů, 1/12 recall" — A2 potvrzeno daty: validátor 28 WARN), mark_isoms CODE_LABELS divergence
  (311/418→capabilities SSoT) + **APP_HTML extrakce 1330→704 ř.** (`mark_isoms.html`, byte-preserving, 11 testů),
  overlay with-open, review_ui SSoT komentáře (lehká kopie ověřena — review_ui stdlib+PIL, points_common táhne numpy).
- **Odloženo → TODO:** D2/D3 leak-fix + D4 detector name-SSoT (measure-first opt-in), D7 „celá paleta" vs
  theme-filtr (UX rozhodnutí), gt_ui→symbol_index (marginální).

### Copilot ISOM-scan benchmark (ad-hoc, uživatel) — iterace v1→v3
Uživatel sdílel Copilot výstup, iterovali jsme se score.py zpětnou vazbou. **Klíčový nález (anti-Goodhart):**
| iterace | point_F1 (headline) | area_fid | class_recall |
|---|---|---|---|
| v1 (špatné kódy: 201=Contour…) | 0,0 | 0,11 | 0,25 (4/16) |
| v2 (kódy opraveny) | 0,0 | 0,16 | 0,44 (7/16) |
| v3 (voda 304/305/308 + vrstevnice 101-104 + body 526/530) | **0,0** | 0,20 | **0,75 (12/16)** |

class_recall 0,25→0,75 (zná KÓDY), ale **headline point_F1 drží 0,0** = bodovou prostorovou lokalizaci coaching
neopravil: 526/530 správný kód, ale halucinované pozice (22/14 bodů vs 1 GT → TP=0). **To je pravá zeď** — ne
znalost ISOM, ale pixel-přesná lokalizace symbolů. ChatGPT 5.5 (point_F1 0,50) ji uměl → tam je rozdíl.
Fér výhrada k coachingu (Copilot iteroval, Opus/ChatGPT ne) → **uživatel: iter3 umožníme VŠEM + brzy re-run.**
Záznam: `runs/2026-06-25_copilot.json` (gitignored) + `results.csv` řádek `build=iter3-coached` (značka asymetrie
do re-runu; headline 0,0 srovnatelný, recall/area coached-inflated).

**ChatGPT iter3** (stejný protokol, zapsán `results.csv` `build=iter3-coached`): class_recall **0,875→1,0→1,0**
(PLNÉ pokrytí 16 score_codes, area_fid 0,43 — nejlepší čtenář ISOM), ale **headline point_F1 0,0** i s feedbackem
„jsi ~1000px vedle": 526 posunul 989→**429 px** od GT (pořád 20× tol), 530 zůstal **1056 px**. **Zeď potvrzena
definitivně.** Pozn.: starý ChatGPT 5.5 (Sez.146) měl point_F1 0,5 (jeden bod LOKALIZOVAL) → zeď NENÍ absolutní,
single-run šum → benchmark chce medián. Leaderboard interim: ChatGPT5.5 0,50 ≫ ChatGPT-iter3/Copilot/Opus 0,0.

**Dva ChatGPT trace mining** (`isom_scan/chatgpt_40min_aktivita1.pdf` + `chatgpt_aktivita2_3.pdf`, analogie Sez.146):
pipeline ≈ naše scan-mining (už máme). **Nové → TODO/IDEAS:** (1) **circularity `4πA/P²` + solidity** + **skeleton-endpoint
topologie** (⊤=3 konce, ×=4) = geometrický diskriminátor bodů (řeší balvany 204/210 vs sráz FP); (2) HoughCircles→IDEAS.
**Klíčový nález uživatele → silné TODO:** headline `point_F1` skóruje JEN 2 body (526/530, data-derivovatelné) →
křehké {0;0,5;1}. **GT factory (`mark_isoms`/`gt_ui`) umí naklikat RUČNÍ GT všech symbolů** → desítky bodů. **+ GT red
flag:** 526/530 jsou ~11 px od sebe (artefakt `only_real`) = headline na ~1 lokaci. ChatGPT s plnou shape-CV trefil
429/1056 px vedle → **tvar SÁM nestačí, je to informační mezera, kterou zaplní jen ruční GT** (klíč k prolomení zdi).

### KPI / KOMPAS
**KPI 67,5 % netknuto** (Sez. 164) — docs-only + isom_scan + benchmark generátor nemění; retrain (až doběhne)
KPI nemění (feeder kvalita, ne pokrytí). Žebříček děr beze změny.

### Censure / Kudos
- **Kudos uživateli:** (1) Copilot benchmark iterace = pěkný anti-Goodhart materiál; (2) „iter3 pro všechny"
  čistě vyřešilo fér výhradu, kterou jsem vznesl. (3) ROADMAP/cadence drželi sezení produktivní (CUDA + úklid paralelně).
- **Censure mírné sobě:** u Copilot v1→v2 fairness jsem nejdřív zatlačil na „uncoached v1 jako oficiální"
  (napsal jsem runs v1) — než uživatel ukázal záměr iterovat; správně jsem výhradu vznesl, ale měl jsem dřív
  rozpoznat iterativní rámec. Smazal jsem fork c1 baseline scratch s důvěrou v diff → doplnil nezávislou runtime smoke.

### Příště NA NTBHEJ (handoff — vše ne-CUDA, vše committed/pushed → `git pull` čistý; paměť `two-machines-git-sync`)
Fokus B UZAVŘEN (negativně, viz výše). Carry jsou ne-CUDA (ntbhej-friendly):
1. **409 reconstructor = uzavřeno NEGATIVNĚ.** Retrain regredoval (halucinace 409), reverted na baseline. **Generator
   409 gate ZŮSTÁVÁ** (zvedl KPI 67,5%). Lekce: vzácnou pattern třídu (jako 208 nenaučitelná) nejde naučit jen víc daty
   — buď **class-balance/oversampling 409**, nebo nechat jako gen-only coverage a NEtrénovat reconstructor naslepo. Ne CUDA okno na to nemrhat.
2. **Ruční benchmark GT přes GT factory** (KLÍČ k prolomení zdi point_F1) — `mark_isoms`/`gt_ui` naklikat
   `task_isom_scan.png` Branžež → desítky skórovatelných bodů místo 2, zvýšit `BENCHMARK_VERSION`. Uživatelova manuální práce, ne-CUDA. TODO detail.
3. **Benchmark re-run iter3 pro Opus + ChatGPT5.5** (fér leaderboard — uživatel pustí modely, já skóruju). Ne-CUDA.
4. **isom_scan opt-iny:** circularity+skeleton filtr (vytěžek ChatGPT trace) · D2/D3 leak-fix + D4 name-SSoT (measure-first) ·
   D7 paleta UX · gt_ui→symbol_index. Vše ne-CUDA kód.
5. Případně 311/312/313 modré pointy (governance A2 first) jako další vytěžení.

*(Lokální artefakty: `runs/s165_area409_s42/` + `unet_best_presez165.bak` = gitignored záznam/pojistka, lze smazat. Kanonický model netknut.)*

## Sezení 166 — KPI vytěžení: kalibrace 210 přestřelu (12→7) + nález RNG cascade + baseline sirotek (ntbhej)

**Stroj:** ntbhej (NTBHEJ21, bez CUDA/korpus-tréninku). **Etapa (ROADMAP):** `Generator()`. **Fokus
(volba uživatele):** KPI vytěžení (measure-first). Self-check ROADMAP ✓ (kalibrace přestřelu = plnění
KOMPASu, ne degradace/rekonstruktor).

### Sync + dostupnost — ntbhej POPRVÉ plná sada
Klon **52 commitů pozadu** (Sez. 148 → 165), FF na `ce7ee8b`, strom čistý (past [[stale-clone-fetch-first]]).
`ls` ověřil (ne předpoklad, [[gitignored-availability-verify-not-assume]]): ntbhej má **poprvé KOMPLETNÍ sadu** —
KPI mapy 6× `.omap`+`.pgw` (vč. Velbloud, dřív blokátor), Livelox korpus **265 classId**, isom_scan task png+GT,
`connectors/.dmr_cache` 2,6 GB. **ntbhej už není jen úklidový stroj** — KPI/KOMPAS měření odblokované (C1 fix Sez. 143).

### Measure-first — KOMPAS `--table` na plné 3-map sadě
Baseline **ntbhej 65,6 %** (Bedř 57,4 / Blatná 65,4 / Velbloud 74,1). **NÁLEZ — baseline sirotek (audit A3):**
65,6 ≠ deklarovaný `KPI_3MAP_CANONICAL` 67,5 (Sez. 164, HAL3000). Per-mapa lokalizuje rozdíl do **Bedřichovky**
(ntbhej 57,4 vs README 58,5 vs TODO 61,9 — i HAL3000 doklady se rozcházejí); Blatná/Velbloud sedí. Kód 164→HEAD
generátoru NEZMĚNĚN (`f535f3e` separate-gate JE Sez. 164) → rozdíl je STROJOVÝ (Bedř = největší sken, separace
RAM-downscale citlivá), ne posun kódu. → carry: prozkoumat + sjednotit headline.

### Páka — 210 Stony ground proporční přestřel
`_provedeni` je share-based: 210 gen_share 11,8 % vs orig 7,3 % (r 1,6) = **přestřel**, ač gen ABSOLUTNĚ
podstřeluje (622 < 975) — gen globálně kreslí míň objektů (Σgen 5278 ≪ Σorig 13407). Přestřel **invertuje
poměr 204:210** (gen 451:622=0,73 vs orig 1064:975=1,09). Simulace (z counts, bez přegenerace, `scratchpad`):
izolovaný 210 sweep optimum faktor 0,5–0,6 → **+1,64 pb**; 417/419/418 marginální (+0,29/+0,19/+0,07).
521 budovy (+1,5) a 503/304 = real-data granularita (NEkalibrováno).

### Kalibrace + verify
`generator.py PSEUDO_STONY_FIELD_PER_KM2 12.0 → 7.0` (proporční match gen=orig share; komentář propsán).
Přeměření (DMR z cache → rychlé): **KPI 65,6 → 67,1 % (+1,5 pb)**, **210 přestřel → ok** (gen 622→351).
Per-mapa: Bedř 57,4→60,2, Blatná 65,4→68,4, Velbloud 74,1→72,8. **48/48 testů OK, smoke OK**, vizuál Velbloud věrný.
**Goodhart-check obstál:** opravuje proporci/inverzi 204:210 (cíl KPI = distribuce); 210 reconstructor stejně
kolabuje (0,00–0,25) → žádné ubrání příkladů; uživatel Sez. 140 sám 210 přestřel hlásil.

### NÁLEZ — RNG cascade (pseudo body sdílí jeden `rng`)
`generate_map` má jeden `np.random.default_rng(seed=1)` sdílený mezi `_generate_pseudo_boulders` (210) a
`_generate_pseudo_points` (417/419/…). Změna `n_fields` (210) posunula RNG stav → ostatní pseudo counts se
DETERMINISTICKY (ale „náhodně") změnily: 419 250→147 (přestřel→ok), 417 279→295, 525 6→20. **Atribuce:** sim
izolovaného 210 = +1,64; reálné s cascade +1,5 → cascade přidal **−0,14 pb** (mírně uškodil) → **+1,5 je robustně
z 210, ne RNG luck.** Reprodukovatelné (seed fixní). Dopad na reconstructor NULOVÝ — Png2Point se trénuje z
`inject.py` (vlastní hustoty), gen .omap pseudo body slouží JEN KPI/KOMPAS distribuci (oddělené, gen.py:168).
→ carry: nezávislé RNG streamy per pseudo symbol (spawn), ať budoucí kalibrace nejsou zašuměné.

### KPI / KOMPAS
**ntbhej KPI_3MAP 65,6 → 67,1 % (+1,5 pb)** (Δ přenositelný na stroje = změna kódu). **Canonical headline
(HAL3000 67,5 %) NEMĚNÍM** — jiný stroj (audit A3: nemíchat ntbhej/HAL3000), přeměření na HAL3000 = carry.
Žebříček děr (po kalibraci): 403 / 416 / 306 / 408 / 109 / 202 / 501 / 308 / 108 / 208 (210 vypadlo).

### Censure / Kudos
- **Kudos sobě:** simulace z counts (bez přegenerace) odhalila past — „přestřel" share-based ≠ absolutní;
  první sim simuloval ZVÝŠENÍ (−4,7 pb), chycen a přesměrován na sweep dolů. Měření před kódem = 0 slepých regenerací.
- **Censure mírné sobě:** RNG cascade jsem nepředvídal — kalibrace jednoho pseudo symbolu zašumí ostatní;
  čistá atribuce vyžadovala sim porovnání. Sdílený rng pro nezávislé vrstvy = latentní past (→ carry).
- **Kudos uživateli:** nález Sez. 140 (210 přestřel) byl přesný; fokus „measure-first" disciplinoval sezení.

### Příště (ntbhej-friendly, ne-CUDA)
1. **521 budovy granularita** — největší zbývající KPI páka (+1,5 pb sim): gen 3,7× víc budov (498 vs 136),
   drobné kůlny/skleníky vs kartografova generalizace. **%THINK min-area filtr PŘED kódem** (ISOM building min size
   ~0,4×0,5 mm; uživatelova doménová expertíza), real-data zásah = opatrně (riziko vynechat legitimní budovy).
2. **Baseline sirotek** — prozkoumat Bedřichovka ntbhej 57,4 vs HAL3000 ~61,9 (separace stroj-rozdíl?); sjednotit
   `KPI_3MAP_CANONICAL` headline (audit A3). Vyžaduje ntbhej↔HAL3000 srovnání téže mapy.
3. **HAL3000 přeměření KPI** po 210 kalibraci (canonical 67,5 → ~69?, carry CUDA-stroj).
4. **RNG nezávislé streamy** pro pseudo body (`rng.spawn`) — odstranit cascade, čisté budoucí kalibrace.
5. Carry Sez. 165: GT factory ruční benchmark, iter3 re-run, isom_scan opt-iny (circularity+skeleton).
