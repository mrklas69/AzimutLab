# 2026-06-22

## Sezení 156 — A1 retrain reconstructorů na nový scope (area 21-class promote, line 5-class regrese→revert) (HAL3000)

Sezení začalo 2026-06-21 večer, hlavní výpočet (regen + 2 tréninky) doběhl 2026-06-22 k ránu.
Vedeno autonomně přes noc (uživatel: „spouštěj další kroky automaticky, neptej se, jdu spát").

### Diskuse
- `%BEGIN` zastihl pracovní strom s necommitnutou prací paralelního vlákna „260621-Buschdörfl"
  (scan-mining 311/312/313: `water_points_poc.py`, `mark_isoms.py`, `markers/`, `calibration_manifest.*`,
  M `separate.py`/`score.py`/`TODO.md`/`.gitignore`). Lokál byl in-sync s `origin/main`.
- Volba fokusu (uživatel): **A1 z `AUDIT_SUPERVISOR_260621`** — KRITICKÁ námitka, že `N_AREA` 18→21
  a `N_LINE` 2→5 jsou v kódu, ale kanonické checkpointy zůstaly staré (18/2) → `eval_real` spadne na
  shape-mismatch → nové typy 404/407/409 a 306/309/508* nikdy neměřeny na realitě.
- Self-check ROADMAP: retrain reconstructoru kvůli **měřicí integritě** (odblokovat `eval_real` na
  aktuálním scope), ne leštění modelu → v souladu s etapou `Generator()`.
- Dvě rozhodnutí uživatele před spuštěním: **(1)** `separate.py` 407/409 GT-gate (cizí necommitnutá
  změna, původ sirotka 66,2 %) → **zahrnout + selektivně commitnout**; **(2)** `png2line` scope →
  **N_LINE=5 retrain, promote PODMÍNĚNĚ** (watercourse nesmí klesnout pod baseline 0,409/0,773).

### Provedení (pipeline, vše na HAL3000 RTX 5070, torch cu128)
1. **Commit `separate.py`** (`4a8712e`) — 407/409 pattern jen na odpovídající zelené GT třídě. Selektivně,
   scan-mining soubory ponechány paralelnímu vláknu.
2. **Sanity regen 1 páru** (1005002, worst-case 93 Mpx) → ověřeno: gen.omap nese 77 nových-scope objektů
   (404=62/407=7/409=8), `area_labels.png` 21-class (labely 5/13/15 přítomné). Doklad, že páry z 12.–17. 6.
   (před Sez. 152) byly 18-class → plná regenerace nutná.
3. **Force-regen 207 párů** (`build_pairs(skip_existing=False)` — pozor: `batch` CLI default `skip_existing=True`
   by všech 207 přeskočil). Výsledek **206 ok / 1 fail** (1192962 „Čertovy mlýny" = DMR 5G noData u hranice,
   hlasité selhání; `gen/` se nevytvořil → žádný zbytkový 18-class soubor).
4. **Rebuild tiles** — první pokus padl správně (no-silent-fallback Sez. 110: tile.py odmítl chybějící pár
   1192962 v test splitu) → `build_tiles(allow_missing=True)` (vědomý resume, 1 nereparovatelná mapa).
   Výsledek: area `n_area=21` (train 10093/val 2077/test 1672), line `n_line=5` (train 10093/val 2085/test 1672).
5. **Retrain `png2area` 21-class** (`s156_area21_s42`, seed 42, 40 ep, ~3,4 h) → **test mIoU 0,577** (best ep 33).
6. **Retrain `png2line` 5-class** (`s156_line5_s42`, seed 42, 40 ep, ~2,3 h) → **test mIoU 0,520**,
   watercourse synt IoU 0,39 (vs starý 2-class 0,55 → multi-class watercourse ředí, signál Sez. 133).
7. **eval_real** na promotnutých checkpointech (backup starých do `.bak` před promote).

### Výsledky
**Png2Area (21-class) — PROMOTNUT** (`unet_best_presez156_18class.bak` pojistka; promoted.json zapsán =
bonus audit B8 provenance):
- Synt test mIoU **0,577** (21 tříd). Hlavní třídy zdravé (401=0,90, 412.1=0,85, 406=0,77, 408=0,75,
  410=0,72, 301=0,70, 520=0,77, 402.1=0,79). Nové: 404=0,36, 407=0,49, **409=0,03** (datový strop, 391 px).
  Pokles vs starý 0,683 = očekávané ředění 3 vzácnými třídami (audit A1 to předpověděl), ne regrese hlavních.
- **eval_real (reálný transfer, JÁDRO A1 — běží na 21-class, žádný shape crash):** Bedř soft mIoU **0,525** /
  pixel-acc **0,887** (les 0,78, open 0,85); Blatná přísná per-shade **0,232** / soft **0,363** / pixel-acc 0,865.
- **Nález (co A1 měl odhalit):** nové area 404/407/409 mají **slabý reálný transfer** — v ČR mapách vzácné,
  model je halucinuje a krade jimi pixely od hlavních odstínů (confusion 401→403 54 %, 403→404 25 %). Soft
  skupinová metrika to pohltí, přísná per-odstín mIoU na Blatné klesla 0,357→0,232. Rozhodnutí: ponechat
  21-class (měřitelnost = A1 primární; soft drží; `.bak` pojistka). Případný revert scope = volba uživatele.

**Png2Line (5-class) — NEPROMOTNUT, REVERT na 2-class** (`unet_best_presez156_2class.bak`):
- **eval_real watercourse na 3 mapách: Bedř IoU 0,265/F1 0,679 · Blatná 0,311/0,746 · Velbloud 0,207/0,581**
  → jasná **REGRESE** vs baseline 0,409/0,773. Nové třídy: **309 narrow_marsh úplný kolaps (F1 0,000)**,
  306 slabé (F1 0,04–0,21), 508 ride slabé (0,13–0,30, doménový gap dle Sez. 133).
- Promote gate porušen → dle volby uživatele („nepromote, revert pak") **vrácen 2-class watercourse
  checkpoint** (`krok1_watercourse_s0`, ověřený 0,774 synt / 0,409 real). Druhé potvrzení Sez. 133:
  multi-class line (zvl. černá dashed 508) zhoršuje watercourse.
- **KPI-bezpečnostní podklad pro revert kódu:** `LINE_CLASSES`/`N_LINE` žije jen v `omap_raster.py` +
  `model/png2line/*`, NE v `measure_dod`/`generate_map` → revert `LINE_CLASSES` na N_LINE=2 by byl
  **KPI-bezpečný** (generátor kreslí 306/309/508 do .omap nezávisle; headline 65,8 % netknut). Sám revert
  kódu NEPROVEDEN (uživatel odložil na „pak" + strukturální změna scope = jeho rozhodnutí).

### Censure / nálezy
- **Falešný poplach (zachycen):** prázdný regen log + rychlý exit jsem podezíral ze skip-existing past
  (Sez. 99) → datum souborů ukázalo reálný přepis. Buffered log ≠ pád (paměť, ověřuj proces).
- Past `code="404"` grep matchoval symbol DEFINICE z template (konstantní „4" napříč mapami), ne objekty
  (paměť `omap-area-code-suffix`) → ověřeno přes parsování objektů + area_labels unique.

### Příště
1. **Line revert kódu (follow-up uživatele, „pak"):** `LINE_CLASSES` v `omap_raster.py` → watercourse-only
   (N_LINE=2), sladit s ověřeným 2-class checkpointem. KPI-bezpečný (doloženo). Pak je A1 line čistě uzavřen.
   Alternativa: nechat N_LINE=5 jako deklarovaný-netrénovaný scope (= stav, co audit A1 kritizoval).
2. **Area scope rozhodnutí:** 404/407/409 mají slabý reálný transfer (vzácné v ČR). Ponechat 21-class
   (pokrytí pro budoucnost) vs revert na 18 (čistší metrika)? Volba uživatele — `.bak` připraven.
3. **A2/A4 scan-mining governance** (zbylé audit námitky): změřit 602-marker recall, commitnout
   `calibration_manifest` + kurační artefakty. Stále untracked v paralelním vlákně.
4. **KPI přeměření** po `separate.py` 407/409 gate commitu (sirotek 66,2 doložit nebo nechat na 65,8).

## Sezení 157 — A1 line revert + ISOM GT factory (human-in-the-loop kurační nástroj) (HAL3000)

### Diskuse
- `%BEGIN`: lokál in-sync `origin/main` (`96dc6e1`). Necommitnutá práce paralelního vlákna
  „260621-Buschdörfl" (scan-mining) ve stromu — necháno vláknu, commituji selektivně.
- Volba fokusu (uživatel): **Q1 line revert** (dokončit A1 z 156), **Q2 ponechat area 21-class**,
  **Q3 = vlastní nápad** → human-in-the-loop UI pro kuraci rozpoznaných ISOM kandidátů ze skenu.
- **Reframe účelu nástroje (klíčové, uživatel):** není to benchmark ani trénink, ale **cvičení,
  které doloží, že KOMPAS je vyplněný oprávněně** — když tabulka tvrdí „umíme symbol", nástroj
  to ověří na reálném skenu (najdu všechny 417, ty potvrdíš/doplníš → recall). Anti-Goodhart (audit A3).
  Self-check ROADMAP: tvorba GT/měřicí kurace ze skenu = legální vytěžení Etapy 1 (`Generator()`).
  **Tvrdá závora:** trénink modelu na tom setu „dokud nedosáhneme úspěšnosti" = Etapa 2 = teď NE;
  „úspěšnost" v nástroji = úspěšnost anotace/rozpoznání, ne reconstructoru.

### Provedení / Kód
1. **A1 line revert** (`generator/omap_raster.py`): `LINE_CLASSES` → watercourse-only (304/305),
   `N_LINE=2`. Ověřeno: tvar hlavy 2-class checkpointu lícuje s `N_LINE` → `eval_real` line už
   nespadne na shape-mismatch (jádro A1). KPI-bezpečné (`LINE_CLASSES` žije jen v `omap_raster` +
   `model/png2line`; generátor kreslí 306/309/508 do `.omap` nezávisle, headline 65,8 % netknut).
   Historie revertu zachována v komentáři (no-silent: proč 306/309/508 padly, Sez. 156).
2. **Review UI MVP1** (`isom_scan/review_ui.{py,html}`): interaktivní náhrada ruční editace
   `review_manifest.json` — stdlib `http.server` (žádná nová závislost) + vanilla HTML, fokus layout,
   klávesy 1/2/3 tp/fp/ignore, crop on-demand ze skenu, `true_code` našeptávač (113 ISOM kódů z
   `resources/isom/*.svg`), auto-FP/precision do `calibration_manifest.json` (`marker_recall=null`
   s no-silent poznámkou bez markerů, audit A2). Smoke headless + HTTP e2e OK.
3. **GT factory MVP2** (uživatelův nápad):
   - **Část A `build_tile_set.py`**: SET dlaždic 512×512 z reálné mapy — mřížka + filtr zeleně
     (téma `green_points` 417/418/419) + `map_cov` maska (largest non-white component + fill-holes →
     ořez bílého okolí, **les uvnitř zachován**), předvyplnění detekcí, resample na `CANONICAL_MPP=1,33`.
   - **Část B `gt_ui.{py,html}`**: dlaždice uprostřed, zdroj nahoře, dole ISOM paleta (SVG ikony,
     filtr na téma), **FN doplňování** klikáním do dlaždice (= recall), počítadlo značek, KOMPAS
     odškrtávání (badge = GT count per kód). Kroužky **mimo ISOM paletu** (magenta/cyan + bílé halo,
     stav detekce výplní) — zelený/modrý kroužek na zeleni/vodě zanikal.
4. **Orientace na sever vyřešena přechodem na Livelox originály** (volba uživatele), ne rotací:
   `bg_scan` (pairs warp do S-JTSK) je pootočený ~12,6° od mg. severu; **korpus `map.png` /
   `source_livelox.png` jsou nativně na mg. sever** (jak mapař), čisté, vysoká kvalita. Proof-of-concept
   přepnut z Buschdörfl na **Branžež** (1005002): 31 dlaždic (6× 417, 121× 418), okraje odfiltrovány.
5. **`.gitignore`**: `!isom_scan/*.html` whitelist (commitnutelný frontend).

### Censure / nálezy
- **bg_scan = S-JTSK warp ≠ mg. sever** (rotace 0 v `rgb.pgw` vs ~12,6° v `source_livelox.pgw`).
  Hough detekce poledníků 601.4 na warpnutém skenu = jen 2 čáry (poledníky ořezané) → orientaci
  řeší zdroj (originál na sever), ne detekce/rotace. Paměť `bg-scan-warp-vs-mg-north`.
- **Souběh gt_ui server × rebuild setu**: server drží stav v paměti, uživatelův POST přepíše můj
  rebuild `tile_gt.json` zpět. + Windows port TIME_WAIT po TaskStop. Pravidlo: nerebuildovat set
  pod běžícím serverem; restart na čerstvém portu. `build_tile_set` teď maže staré `tiles/` (sirotci).
- Detektor `vegetation_points_poc` umí jen 417/418 (ne 419) → potvrzuje uživatelův nález: **color
  separation per ISOM barva** (černé v černé vrstvě) sníží FP z křížení barev (černá pěšina × hnědá
  vrstevnice = falešné 525). Zařazeno jako Část A2.

### Příště
1. **Část A2 — color separation per ISOM barva** před detekcí (`generator/separate.py` navázat):
   černé symboly hledat jen v černé vrstvě → eliminace FP z barevných křížení. Kritické pro černá témata.
2. **Škálování SET** na 100+ dlaždic z Livelox korpusu (skóre kvality dlaždice: barevnost/velikost).
3. **Plná ISOM paleta jako KOMPAS dashboard** (postupné odškrtávání přes témata).
4. Carry z 156: area scope 404/407/409 slabý transfer — ponecháno 21-class (volba uživatele, Q2).
