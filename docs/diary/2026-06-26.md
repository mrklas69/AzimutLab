# 2026-06-26

## Sezení 168 — Podklady SSoT layout (pořadí/opacity/bg_ prefix) + GT factory bodová infrastruktura (ntbhej)

**Stroj:** ntbhej (NTBHEJ21, plná sada — korpus 265, 6× `.pgw`, isom_scan GT, `.dmr_cache`; bez CUDA tréninku).
**Etapa (ROADMAP):** `Generator()`. Self-check ✓ u obou tahů (podklady = verify pomůcka; GT factory = anti-Goodhart
validace KOMPASu na reálném skenu = legitimní scan mining, audit supervisor pravidlo 3). Sezení překlenulo půlnoc
(start %BEGIN 2026-06-25 večer).

### %BEGIN — stav
- Git čistý, `HEAD == origin/main` (b65568b), nejsem pozadu.
- **KPI:** ntbhej KPI_3MAP **67,1 %** (Sez. 166, po 210 kalibraci); canonical HAL3000 67,5 % je STALE (před 210, ~69 po
  přeměření = carry). Generátor se KPI-relevantně neměnil od Sez. 166 → nepřeměřuji. Díry: 403/416/306/408/109/202/501/308/108/208.
- **Cadence:** %AUDIT:CODE sezení-count zralý (10 od Sez. 158), ale **LOC-trigger ≥500 NESPLNĚN** (jádro net +88,
  isom_scan čerstvě auditován Sez. 165) → nevynucený. Ostatní prahy ne. Stale Příště: nic ≥5.
- **Audit supervisor 260621 sekce C** držena (scope bez tréninku ne, kalibrace = recall, KPI feeder ne cíl, jedna metrika).

### Side-task (požadavek uživatele) — podklady SSoT layout
Uživatel poslal `<templates>` blok z OOM (Branžež) + „podklady u map v tomto pořadí a s touto průhledností,
shodně prefix bg_". **Klíčové zjištění:** Branžež na disku už byl v cílovém stavu (jeho ruční OOM úpravy:
pořadí `bg_dmr/ortofoto/bg_scan`, opacity `1,0/0,5/0,25`) — reálný požadavek = aby to **generátor produkoval sám**
pro všechny mapy + sjednotil dvojí název ortofota.
- **Q1 jméno → `bg_ortho.png`** (sjednotí s existujícím helperem `attach_ortho`, zruší dvojí název
  `ortofoto.png`/`bg_ortho.png`). **Q2 rozsah → regen všech maps/.** Navrhl jsem levnější cestu než full-regen
  (re-fetch/separace): **re-aplikace layoutu na existující `.omap`** (post-process, nemění kresbu) — splní cíl bezpečně.
- **Kód (DRY, SSoT):** `gen_backgrounds.BG_LAYOUT = [(bg_dmr,1.0),(bg_ortho,0.5),(bg_scan,0.25)]` (pořadí = z-order,
  index 0 = back). Nová mechanika `omap_export.apply_image_template_layout(doc, layout)` (re-order def bloků +
  přemapování view `<ref>` indexů + opacity dle jména; izomorf `remove_image_templates`). `_write_omap_with_layout`
  ji aplikuje na **každé** připnutí podkladu (attach/add cesty) → jeden bod, kde se layout sjednotí, nezávisle na
  pořadí připnutí. Ortofoto výstup sjednocen na `bg_ortho.png` (`generator.py` + `omap_export.py` default + help).
  CLI `gen_backgrounds.py layout <.omap>` pro ruční re-aplikaci.
- **Migrace 14 map** (scratchpad, reuse `BG_LAYOUT` SSoT): legacy rename reference `ortofoto.png`→`bg_ortho.png`
  (vč. absolutního path) + re-order + opacity + rename souboru. Idempotentní (2. běh `.omap —`). Bez full-regenu.
- **Verify:** nový `tests/test_omap_export.py` (6 testů: reorder/opacity/idempotence/unknown-kept/no-op/geometrie) →
  **54 unittestů OK**, smoke OK, compileall OK. Branžež OOM stav potvrzen (view dmr 1,0 / ortho 0,5 / scan 0,25).
  Proaktivní náhled vrstvení (PIL blend) ukázán — reliéf vespod plný, ortofoto 0,5, sken 0,25 navrchu. **Uživatel
  OOM verify = carry** (jeho oko = finální slovo). `_existing_ortho_templates` docstring sladěn (slap-rewrite-comments).

### Fokus (volba uživatele) — GT factory bodový benchmark (infrastruktura)
Prolomit zeď headline `point_F1 = 0,0`: generátorová GT (`build_gt only_real`) má jen **2 skórovatelné body
526/530 na ~1 lokaci** (11 px od sebe = artefakt; nález Sez. 165). Cesta = **ruční GT všech bodů přes GT factory**.
- **Průzkum nástrojů (Explore agent + verify):** `mark_isoms.py` = klikání na CELÝ sken → `markers/*.json`
  v **absolutních px** (= formát GT) — přímá cesta. `gt_ui.py` = per-dlaždice (lokální px, vyžaduje detektor). Chyběl
  konverzní skript markers→GT.
- **Scope (diskuse, volba uživatele):** „**všechny ISOM, začneme bodovými**" + **Branžež MVP** (1 sken, NCC zarovnání
  Sez. 146 hotové). Doménový nález ze skenu: Branžež je **pískovcová/skalnatá** — dominuje skalní kresba, izolované
  body nejsou dominantní; jasné = hnědé kupky 109 + man-made; **balvany 204 splývají se skalní kresbou** (= proč
  ChatGPT trefil 429 px vedle — informační mezera, ne slabina metody).
- **Infrastruktura (kód):** (1) `mark_isoms.CODE_LABELS` rozšířen z 12 na **21 bodových kódů** (doplněno 204/526/530/
  110/419/207/524/519/203.2; anglické `name` sladěné s capability SSoT). (2) Nový `gt_from_markers.py`: markery →
  **hybrid GT** (ruční body nahradí gen body, gen linie/plochy zůstanou) + **sanity-check** blízkých bodů různých kódů
  (526/530 red flag) + záloha v1. (3) `score.py` čte `_benchmark_version` z GT → hybrid reportuje **2.0**, generátorová
  v1 fallback 1.0 (build_gt README pravidlo: nemíchat tiše). Verify: compileall isom_scan OK, import řetězec OK,
  `test_mark_isoms` 11/11 OK.
- **Klikání = uživatelova carry** (interaktivní, dlouhé). Příkaz připraven (`mark_isoms` na `task_isom_scan.png`
  s 21 bodovými kódy). Po naklikání: `gt_from_markers.py` → `score.py` → první reálný `point_F1`.

### KPI / KOMPAS
**Beze změny — ntbhej 67,1 % / canonical 67,5 %.** Podklady = post-process nad `.omap` (nemění ISOM kresbu);
`generator.py` změna = jen jméno podkladu `bg_ortho.png` (ne kresba); GT factory = benchmark, ne generátor.
Žebříček děr beze změny.

### Censure / Kudos
- **Kudos sobě:** (1) podklady — re-aplikace layoutu místo full-regenu (KISS, bezpečné, splní cíl levně), SSoT
  `BG_LAYOUT` + jeden `_write_omap_with_layout` bod místo per-cesta záplaty; (2) GT factory — scope diskuse PŘED
  kódem odhalila, že Branžež je skalnatá (limit bodového benchmarku) = doménový vhled formoval infrastrukturu.
- **Censure mírné sobě:** podklady jsem zpočátku vedl k full-regenu (uživatelova volba), než jsem nabídl levnější
  post-process; měl jsem to navrhnout rovnou v otázce rozsahu.
- **Kudos uživateli:** (1) přesný `<templates>` blok z OOM = jednoznačné zadání; (2) „všechny ISOM, začneme
  bodovými" = správné fázování GT factory (univerzální cíl, bodový MVP).

### Příště
1. **GT factory klikání (uživatel) → první reálný `point_F1`.** Naklikat bodovou GT Branžež (`mark_isoms`, 21 kódů),
   `gt_from_markers.py` → `score.py`. Ověřit hybrid GT konverzi na reálných markerech (formát). Pak iter3 re-run
   benchmarku (Opus/ChatGPT5.5) na v2 GT = fér leaderboard (carry Sez. 165).
2. Carry Sez. 166/167: **521 budovy granularita** (%THINK, +1,5 pb sim) · RNG nezávislé streamy · baseline sirotek
   (Bedř ntbhej↔HAL3000) · HAL3000 přeměření KPI po 210.
3. Carry: isom_scan opt-iny (circularity+skeleton filtr), GT factory rozšíření na linie/plochy (po bodech).

---

## Sezení 169 — GT factory: ruční bodová GT Branžež (155 bodů / 14 kódů) + detektor tuning na uživatelových diskriminátorech (ntbhej)

**Stroj:** ntbhej. **Etapa (ROADMAP):** `Generator()`. Self-check ✓ — GT factory = scan mining (anti-Goodhart
doklad recallu pro KOMPAS); detektor tuning = uživatelova vize „nálezy → tuning detektoru pro generátor", ne
rekonstruktor/degradace. Navázání na Příště #1 Sez. 168 (prolomit zeď `point_F1`). User-directed celé sezení.

### Fokus — GT factory ruční bodová GT Branžež
**Cíl:** zeď headline `point_F1 = 0,0` (generátorová GT měla jen 2 body 526/530 na ~1 lokaci, artefakt). Cesta =
ruční GT všech bodů přes `mark_isoms`. **Ověření řetězce PŘED klikáním** (anti-ztráta uživatelovy práce): dummy
markery → `gt_from_markers` → `score` end-to-end OK (hybrid GT v2.0, sanity-check blízkých párů, záloha v1 `.bak`,
`benchmark_version` 2.0 do CSV). Coordinate: markery na `task_isom_scan.png` 1655×1868 (lícuje s GT, TOL_PX 20).

**5 setů klikání (uživatel) → spojeno do master `bransez_gt.json`** (workflow: per-set čistá plocha, markery
client-side → **Save nutný** než přepnu paletu, spojení dedup do master):
- 1. terénní hnědé (101.1 spádovka / 102.1 kóta / 103.1 spádovka-form / 104.1 sráz-min / 109 kupka) + klasifikace
  105/106 → **100**; 2. terénní tvary (110/111/112/113.1/115) → **18**; 3. skalní srázy (201.1/202.1; 202.3 = 0,
  mapař nahradil 206) → **2**; 4. balvany (203.1/204/204.5/205) → **31**; 5. vodní (jen 308.1 ×6, Branžež suchá).
- **Finální GT: 155 bodů / 14 kódů** (z 2 šumivých). `point_F1`: **ChatGPT 5.5 0,121 (z 0,0!)**, Opus 0,0.

### Detektor 109 — kalibrace na ruční GT + 3 uživatelovy diskriminátory
Naivní component detektor recall **2 %** (kupky slité s vrstevnicemi → velké komponenty). Opening → **96 %**,
precision 13 % (fragmenty vrstevnic). Uživatelovy doménové insighty postupně vyladily:
- **kupka+černá skála** (jako řopík): hypotéza potvrzena daty (71 % kupek má černou), ale wildcard maska jako CV
  signál ZHORŠILA (skalní textura přebije) — doménově správné, S/N špatné.
- **108 erozní rýha = menší kruhy v řadě**: velikost (medián 7,8 vs 4,8) + linearita (0 vs 2 sousedi) → precision
  13 → **58 %**, návrhy 150 → 28 (klíčový zvrat).
- **110 elipsa (ecc)**: ecc<0,77 odfiltruje elipsy; 865,721 ecc 0,80 ✓. **105 earth wall (průchozí val)**: angular
  měřič nezachytil (4 ramena, 0 protilehlých) = limit classic-CV → oko/ML.
- **ZÁSAH:** detektor našel 1 přehlédnutou 109 (383,4/960,3).

### Detektor balvanů (černá maska) — velikost RELATIVNÍ
Use-case: lokalizace snadná (černé kruhy), velikost subjektivní (204/204.5/205). Mediány ⌀ stoupají (7,2/8,6/9,2),
ale absolutní práh shoda jen **39 %** — **204.5 je definované RELATIVNĚ** (spec „larger than neighboring 204") →
detektor navrhne ⌀ jako vodítko, kategorii určí kartograf. **ZÁSAH:** 3 přehlédnuté balvany; FP = budovy
(pravoúhelník) / skály 206 (plošné).

### Strop classic-CV (doložen — potvrzuje uživatelovu vizi o ML rekonstruktoru)
Výrazné tvary (109 disk, 110 elipsa, 115 △, balvan) detektor lokalizuje. Jemné (111 oblouk 1/10 disk2 → 9/10
disk1; 112 ▽ 1/3) pod rozlišovací schopností na husté Branžeži. Kontext (105 průchozí val, 204.5 relativní
velikost) classic-CV neumí = hranice, kde ML přebírá. Fragmenty vrstevnic (FP): protáhlejší (elong 3,6 vs 2,0) +
tenčí → diskriminátor pro budoucno.

### Kód
`mark_isoms.CODE_LABELS` 12 → **39 kódů** (vrstevnicové 101.1-104.1 / val-rýha 105/106/108 / terénní 110-115 /
skalní-balvany 201.1-205 / vodní 303-313; name verify-against-source z `template_classic.omap`, 311/313 drift fix).
Detektory zůstaly **scratchpad** (opening + size + linearita + ecc + shape_f1 + circularity) — zabudování do
`terrain_points_poc` = carry. KPI/KOMPAS **67,1 % netknuto** (GT factory = benchmark, ne generátor).

### Censure / Kudos
- **Kudos uživateli:** 6 doménových diskriminátorů (108 velikost+linearita = zvrat precision 13→58; 110 elipsa;
  105 průchozí val; budova=pravoúhelník; 206=plošný; fragment vrstevnice=protáhlost) + důkladné klikání (5 setů,
  155 bodů). Meta-feedback rámec (classic-CV dočasná, ML rekonstruktor překoná) potvrzen měřením.
- **Kudos sobě:** ověření řetězce PŘED klikáním (anti-ztráta práce); measure-first u každého diskriminátoru.
- **Censure mírné sobě:** dedup omylem spojil 2 různé 105 body (22,5 vs 972,6) — uživatel opravil; naivní wildcard
  maska (black∩dilate) moc hrubá (215607 px slitá komponenta) než zúžena.

### Příště
1. **Zelené body (417/418/419)** — pokračovat GT factory (volba uživatele).
2. Zabudovat 109 diskriminátory (velikost+linearita+ecc) do `terrain_points_poc` (uživatelova vize „tuning detektoru
   pro generátor").
3. Carry Sez. 166/167: 521 budovy granularita · RNG streamy · baseline sirotek · HAL3000 přeměření KPI.
