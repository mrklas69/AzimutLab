# Diář 2026-05-28

## Sezení 29 — Pomocné vrstevnice (form lines, ISOM 103) z DMR

Dvacáté deváté sezení, mrkla. `%BEGIN`: sync 0/0 (HEAD = origin `30fee76`), čistý strom. Audit cadence
nic due (CODE Sez. 27 = 1 sez / LOC pod 500; DOCS Sez. 19 = 9/10; pruning Sez. 17 = 11/12; CALIBRATE 11/15).
Stale „Příště": katalogové vrstvy se drží od Sez. 24, ale ne stale (menu, odškrtává se po jedné). Fokus
z Příště Sez. 28 = skály/balvany — ale uživatel sezení **odbočil** k symbolu ISOM 103.

### Verify-against-source: co je ISOM 103

Uživatel: „zamyslel bych se nad symbolem ISON 103, umíš pro něj získat podklady?" Ověřeno v
`template_classic.omap` (ne z paměti): **103 = Form line** (pomocná/doplňková vrstevnice), NE slope line
(spádovka je 101.1 „Slope line, contour" / 103.1 „Slope line, formline"). Template: dash 2,0 / break 0,2 mm,
hnědá, čárkovaná. Oficiální ISOM popis (z template `<description>`): *„added only where representation would
be incomplete with ordinary contours; **shall not be used as intermediate contours**; only one form line
between neighbouring contours; excessive use must be avoided."*

**Klíčový nález (odpověď na „podklady"):** form line NENÍ datová vrstva ZABAGED — je to **derivace z DMR
výškopisu**, který už čteme pro vrstevnice 101/102. Žádný nový konektor. ALE naivní „vykresli poloviční
vrstevnice" = přesně to, co spec zakazuje (intermediate contours). Skutečná form line = interpretace, kde
je tvar opodstatněný (paralela s reframe projekce/predikce — [[pseudorealistic-phases]]).

### Heuristika (návrh uživatele, A1 + A2)

Diskuse před kódem (mandát uživatele „nechám na tobě"):
- **A2 (mírný svah):** form line jen kde rozestup vrstevnic > `CONST_LIMIT_103`. Realizováno přes **sklon**
  (ekvivalent, KISS): rozestup = `CONTOUR_STEP / sklon` → „rozestup > limit" ⟺ „sklon < `CONTOUR_STEP/limit`".
  `CONST_LIMIT_103` = `FORMLINE_SPACING_LIMIT_M` (40 m).
- **A1 (složitější polynom):** form line nekreslit tam, kde by byla rovnoběžná s vrstevnicemi (zbytečná).
  Formalizováno jako **křivost terénu** = `|Laplacián výšky|` (druhá derivace). Rovnoměrný svah = lineární
  (polynom 1. stupně) → Laplacián ≈ 0 → vynecháno. Kopeček/deprese/sedlo = nenulový → kreslí se.
- **Maska = (sklon < práh) AND (|Laplacián| > práh).** `elev` se 3× vyhladí (3×3 box `_box_smooth`) před
  derivacemi — tlumí mikro-texturu DMR (Laplacián ji jinak zachytí jako falešné form lines všude).

Implementace izomorfní s 101/102: poloviční hladina (`level + 2,5 m`) přes `cont.lines()`, ořez na masku
(`_clip_line_to_mask` = „část pomocné vrstevnice"), filtr min. délky, render dashed hnědě + GT maska + vektor
+ `.omap`. Jen `terrain == "real"` (reálný terénní detail; noise = umělý perlin → form line nemá smysl + drží
proc baseline).

### Kód

- `generator.py`: `ISOM_FORMLINE=103` + `FORMLINE_*` konstanty; `_box_smooth`, `_formline_mask`,
  `_clip_line_to_mask`, `_polyline_len_px`; render blok po hlavní contour smyčce (jen real); `mask_formlines.png`;
  `_build_meta` sekce `formlines` (+ param `n_formlines`); `contours.geojson` přes `contour_features + formline_features`;
  log „pomocné vrstevnice (103)".
- `omap_export.py`: `103` v `USED_CODES`; `write_omap` param `formline_features` (emit jako 101/102 line_object);
  `n_formlines` v návratu.
- `_write_contours_geojson`: `names[103] = "Form line"`.

### Ladění prahů — verify, ne poslepu

První prahy (`curv_min 0,0015`, 1× smooth, min 1,1 mm) → **1466 form lines** na NL 6×4 km (= 6× víc než
240 vrstevnic). Overlay (maska zviditelněná ×255) ukázal **plošný šum** rozesetý po celé mapě — ISOM
„excessive use". Probe `temp/probe_formline.py` (citlivost passes × curv × spacing + distribuce délek):
- curv 0,008 + 3× smooth + min 1,1 mm → 70 (mezikrok).
- **curv 0,004 + min 3 mm → 108** — uživatel chtěl HUSTŠÍ (nižší curv = víc tvarů), ALE bez „fousků"
  (delší min. délka odřízne krátké úseky; medián délky 1,7 mm → 3 mm odřízne spodní ~60 %). Obě kritéria splněna.
- Min. délka 3 mm = ~30 m terén = ~2× ISOM minimum (1,1 mm) — legitimní „ne fousek".

Render break zvětšen 0,2 → 0,5 mm: template 0,2 mm = 0,9 px = sub-px, v rastru neviditelný (form line splývá
s 101). `.omap` nese věrný symbol 103 (OOM renderuje 0,2 mm autoritativně) — stejný render-vs-omap princip
jako u cest/železnice (Sez. 28).

### Verify (ve `.venv`)

- proc baseline **65 drží** (form line jen real → noise beze změny; ověřeno přes `--terrain noise` přepínače).
- NL 6×4 km: **108 form lines** vs 240 vrstevnic. Overlay (form line červeně přes rgb) potvrdil: form line
  jen v **plochých zakřivených partiích** (řídké vrstevnice), strmé svahy (husté vrstevnice) je nemají —
  heuristika funguje dle záměru. Vpravo nahoře i souvislé smyčky kolem depresí.
- Uživatel ověřil `temp/formline_NL/map.omap` v OpenOrienteering Mapper („super").

### Branžový precedent

**Karttapullautin** (nejrozšířenější LiDAR→OB generátor) dělá form lines stejně v jádru: poloviční vrstevnice
+ filtrace (maže krátké/rovnoběžné segmenty, nechá smyčky v plochých partiích). Náš `elev` rastr na to stačí.

### Kudos / Censure

- **Kudos! (uživatel)** — formalizace „složitější polynom" → křivost (Laplacián) je elegantní řešení ISOM
  zákazu intermediate contours; kombinace „nižší práh křivosti (hustší) + delší min. délka (bez fousků)"
  trefila obě přání jedním tahem.
- **Verify-against-source** (template) potvrdil 103 = Form line (ne slope line) PŘED kódem; probe změřil
  prahy místo hádání (1466 → 108 data-driven).

### Příště

1. **Skály/balvany — `Osamělý_balvan` 204 + `Skalní_útvary` 201** (jádro lesního OB, 2× odložený fokus 28→29).
2. **Most 512** (+ Lávka 512.2), **Lesní_průsek 508**, bodové prvky (kříž 530 / mohyla 526 / věž 524), Zeď 513.
3. *(drobnost)* form line **souvislé smyčky** (ISOM-věrnější tvar; MVP uzavřen) + ověřit prahy `FORMLINE_*` na SV/LS.
4. *(drobnost)* crossability mapování (široký tok → 301 uncrossable); *(dlouhodobě)* ISSprOM sprint; vegetace (UC5).

## Sezení 30 — Skály/balvany (ISOM 204/207/206) ze ZABAGED

Třicáté sezení, mrkla. `%BEGIN`: sync 0/0 (HEAD = `3265b98`), čistý strom. Audit cadence nic due
(CODE Sez. 27 = 2 sez / +154 LOC pod 500; DOCS 10/10; pruning 12/12; CALIBRATE 12/15). Fokus
z Příště Sez. 29 = bod 1 (skály/balvany, **2× odložený 28→29→30, čas DO**).

### Verify-against-source: ZABAGED skály a ISOM 201–214

Před kódem `temp/probe_rocks.py` na **Hrubé Skále** (50,5488°N, 15,1835°E — Hruboskalsko,
lokalita uživatele A4 s ground-truth OB mapami pro kontrolu). Bbox 5,9×4 km. ZABAGED REST
MapServer `?f=json` dohledal layer IDs pro 5 kandidátních vrstev (10, 12, 13, 129, 130):

| Vrstva | Geom | Počet | Atributy | Mapování (po diskusi) |
|---|---|---|---|---|
| `Osamělý_balvan__skála__skalní_suk` | bod | **6** | jen `jmeno` (Kapelník, Kočičí hlava, Osudová + 3 None) | → **204 Boulder** |
| `Skupina_balvanů__bod_` | bod | **168** | jen `jmeno` (vše None) | → **207 Boulder cluster** |
| `Skalní_útvary` | plocha | **411** | jen `jmeno` (6 pojmenovaných: Čertova ruka, Maják, Lebka, Prachovna, Studený, Mariánská vyhlídka); medián 1132 m², max 30 444 m² | → **206 Gigantic boulder** |
| `Skupina_balvanů__linie_` | linie | 3 | jen krátké úseky 33–69 m | odloženo (drobnost) |
| `Sesuv_půdy__suť` | plocha | 0 | — | odloženo (zde nulový dataset) |

**Klíčové zjištění:** žádná vrstva nenese atribut typu/velikosti/výšky (jen `jmeno`). → **KISS:
vrstva → jeden ISOM symbol** (jako budovy → 521, vedení → 510). Hybridní rozhodování by neměo
datový podklad.

### Iterace rozhodnutí — drift po stěně argumentů

Diskuse před kódem vedla přes **dvě otočky** (oba úkroky uživatel iniciativně):

1. **A1+A2+A3+A4 (uživatel po Q1–Q4):** „pro balvan jiná značka než pro skály; podklady proložit
   zaobleným mnohostěnem (smoothing); průchodné = 202; lokalita Hrubá Skála". A2 byl explicitní
   pokyn dělat Chaikin smoothing obrysů.
2. **Q1–Q3 finalizace:** MVP 204+207+202(smoothed), hybridní 202 (≤500 m²) vs 206 (>500 m²),
   Chaikin 2 iterace.
3. **Otočka 1 (uživatel v průběhu kódování):** „Skalní útvary není třeba nějak zvlášť
   generalizovat, polygony jsou pěkné." Atribut `Shape_Area` z ZABAGED (OBJECTID 36552 →
   5289,33 m²) ukázal, že polygony mají ~120 vrcholů na 32×32 m → plynulý obrys už bez kreslení.
   **→ Chaikin ZAVRŽEN** (zachován RAW, jako voda/budovy; izomorfní s Sez. 27 lekcí
   „generalizuj jen s důkazem"). `_chaikin_smooth` smazán.
4. **Otočka 2 (uživatel po verify):** „Proč jsou některé polygony vyplněné černou a jiné jen
   obrysem?" Otázka odhalila, že hybridní rozhodování postrádá datový podklad — práh 500 m²
   byl můj návrh schválený, ale „proč zrovna tahle skála s plochou X je masivní a ta s X-1
   ne" se nedá ze ZABAGED odpovědět. **→ Hybridní 202/206 ZAVRŽEN, vše → 206 (plná plocha).**
   `ISOM_CLIFF_PASSABLE`, `ROCK_AREA_GIGANTIC_THRESHOLD_M2`, `_draw_cliff_line`,
   `_polygon_area_sjtsk` smazány. 202 odstraněn z `USED_CODES`.

Konečný stav: **3 ZABAGED vrstvy → 3 ISOM symboly, vrstva = jeden symbol** (KISS, izomorfní
s budovy→521 / vedení→510 / železnice→509). Symbolický návrat ke stejnému principu, který se
osvědčil u všech ostatních vrstev.

### Kód

- `connectors/zabaged.py`: LAYER_IDS doplnit (10, 12, 130); `BOULDER_LAYERS` /
  `BOULDER_CLUSTER_LAYERS` / `ROCK_AREA_LAYERS`; `fetch_boulders` (body), `fetch_boulder_clusters`
  (body), `fetch_rock_areas` (plochy). `map_boulder_to_isom`→204, `map_boulder_cluster_to_isom`→207,
  `map_rock_area_to_isom`→206.
- `sandbox/generator-poc/generator.py`: ISOM konstanty 204/206/207, `ROCK_NAME`, `ROCK_CLASS`
  (3 třídy v `mask_rocks.png`). `_draw_boulder` (kruh 0,4 mm = 2 px), `_draw_boulder_cluster`
  (trojúhelník 0,8×0,7 mm, vrchol nahoře = template orientace). `_draw_gigantic_boulder`
  = wrapper `_draw_area_symbol` černá výplň + obrys. `_generate_real_rocks` (paralela
  `_generate_real_buildings`+`_generate_real_paths`, vrací `rock_point_features` +
  `rock_area_features` + `rocks_info`). CLI flag `--rocks {off,real}`. Validace
  `--rocks real ⇒ --terrain real`. Logging průběh + souhrn po symbolech. Meta sekce `rocks`.
  Z-order: úplně navrch (po budovách+řopících) — replikuje OOM color priority skály > budovy.
- `sandbox/generator-poc/omap_export.py`: `USED_CODES` doplněn 204/206/207. `write_omap` parametr
  `rock_point_features` (body) + `rock_area_features` (plochy 206 jako budovy). `AREA_CODES`
  doplněn 206. Návrat `omap_counts["rocks"]`.

### Verify (ve `.venv`)

- **Hrubá Skála (5,9×4 km):** 585 skal — **204:6**, **207:168**, **206:411**. Počty sedí
  na probe **přesně**. Skály vizuálně dominují (Hruboskalsko = klasická skalní oblast, pískovcové
  věže Mariánská vyhlídka 30 044 m² → největší 206).
- **NL (6×4 km):** 200 skal — **204:16**, **207:178**, **206:6**. Žádný dominantní masiv, jen
  drobné trojúhelníky kamenných polí rozeseté po krajině (= Jizerky, věrné).
- baseline proc: nedotčen (rocks jen real).
- `temp/rocks_HS/map.omap` a `temp/rocks_NL/map.omap` k otevření v OOM (uživatel; verify vs
  jeho mapy Skalákem).

### Branžový precedent

**Karttapullautin** mapuje skály ze LiDAR identicky podle vrstev: bodové → 204/205, polygonové
→ 206. Kratta také generalizuje plochy do plné výplně (ne hybrid).

### Kudos / Censure

- **Censure (já)** — navrhl hybridní 202/206 s prahem 500 m² jako KISS, ale práh byl hádaný
  (žádný atribut ZABAGED ho neopodstatňoval). Uživatel to odhalil otázkou „proč jsou některé
  plné a jiné jen obrys" → drift bez datového podkladu. **Lekce → memory:** *bez datového
  signálu nedávat per-feature rozhodování*; jeden symbol/vrstva = bezpečné default.
- **Censure (já podruhé)** — navrhl Chaikin smoothing pod hlavičkou A2, ale `Shape_Area`
  v ZABAGED props (~120 vrcholů / 32×32 m polygon) byla jasná indikace „už pěkné". **Lekce
  → CLAUDE.md `generalize-only-with-evidence`** (Sez. 27) potvrzená podruhé.
- **Kudos! (uživatel)** — A4 Hrubá Skála jako verify lokalita: konkrétní místo s reálnými OB
  mapami = ground-truth check pro celé sezení (bez něj bychom hádali, jestli render věrný).
- **Verify-against-source před kódem** (probe → atributy/počty/Shape_Area) zachránilo dvě
  předčasné implementace (hybridní + smoothing) — kdyby šly do kódu, byly by mrtvá zátěž.

### Post-script (po prvním %END) — 3 cleanup commity

Uživatel po `%END` upozornil na tři chyby, které kanonický stav repa narušily — sled tří
opravných commitů:

1. **`0b3c4aa` feat:** `HS` chyběl v `DEV_LOCATIONS` — Hrubá Skála (lokalita pro skály celého
   sezení) měla být shortcut `--location HS`. Doplněno analogicky se Sez. 28 `LS`.
2. **`f6a8316` fix(gitignore):** regen 4 lokalit → `output_SV/NL/LS/HS` (špatná konvence).
   Smazáno, regenerováno do existujících **CZ-named** `Soví Vrch/`, `Nová Louka/`, `Lidové
   sady/`, `Hrubá Skála/`. `.gitignore` doplněn o `Hrubá Skála/` (chyběl, ostatní 3 už byly).
3. **(Censure!) `--no-ortho`** — regen 4 lokalit jsem v rychlosti pustil bez ortofota, čímž
   smazal podklady z předchozího stavu kanonických map. Uživatel *„Censure!"* → druhý regen
   s plnou variantou (4× 160–180 MB ortofoto). Paměť [[scratch-output-naming]] vyjasněna ve
   dvou krocích (Sez. 30): (a) rozlišení 3 hladin (temp/ vs output_X/ vs CZ-named), (b) pravidlo
   **kanonické DEV_LOCATIONS = vždy plný režim, nikdy `--no-ortho`**.

**Lekce:** Po `%END` se nedělají scratch zkratky (`--no-ortho`). Když se sahá na kanonická data
(DEV_LOCATIONS mapy), držet kanonický režim. Paměť to zafixovala.

### Příště

1. **Most 512 (+ Lávka 512.2), Lesní_průsek 508, bodové prvky (kříž 530 / mohyla 526 / věž 524),
   Zeď 513** — další pokrytí katalogu, hlavně liniové/bodové (po skalách jsou poslední „klasické"
   OB vrstvy).
2. *(drobnost)* `Skupina_balvanů__linie_` → 208 Boulder field (jen 3 prvky na Hrubé Skále, buffer
   linie → plocha) + `Sesuv_půdy__suť` → 210 Stony ground (verify v lokalitě s sutěmi —
   Jeseníky/Krkonoše).
3. *(drobnost)* form line **souvislé smyčky** + ověřit prahy `FORMLINE_*` na SV/LS.
4. *(drobnost)* crossability mapování (široký tok → 301 uncrossable); *(dlouhodobě)* ISSprOM
   sprint; vegetace (UC5).

## Sezení 31 — Mosty (ISOM 512) + lávky (512.2) + oprava tramvaje + DEV_LOCATIONS refaktor

Třicáté první sezení, mrkla. `%BEGIN`: sync 0/0 (HEAD = `214bbe9`), čistý strom. Audit cadence
nic due ≥2 přes práh (DOCS 11/10, pruning 13/12 — oba 1 přes; CODE 3 sez. / ~+500 LOC pod 8/500;
CALIBRATE 13/15). Fokus z Příště Sez. 30 = bod 1, uživatel ho zúžil na **Most 512** (lokalita
50.7598686N, 14.9601922E = Novina v Lužických horách, krásný kamenný obloukový most dle uživatele).

### Verify-against-source: probe `Most` + `Lávka`

Před kódem `temp/probe_bridges.py` na bbox 3×2 km kolem Noviny. ZABAGED REST MapServer `?f=json`
listing dohledal layer IDs:

| Vrstva (REST) | id | Geom | Počet (3×2 km) | Atributy non-null |
|---|---:|---|---:|---|
| `Most` | 73 | LineString | **4** | OBJECTID, fid_zbg, premost_p (vodoteč…), material_p (železobeton/„neznámý"), delka_prem, celk_sir_m, v_m_n_ter, silnice/kod_zelez, **jmeno 2/4: Malý viadukt + Novinský viadukt** |
| `Lávka (linie)` | 67 | — | 0 | (nic v bbox) |
| `Lávka (bod)` | 66 | Point | 1 | OBJECTID, fid_zbg |

**Klíčové nálezy (před kódem):**

1. **Most je LINIE** (souhlasí s katalogem Sez. 23, oprava zděděného „bod" omylu) — render = polyline.
2. **Lávka má jméno s MEZEROU v REST** (`Lávka (linie)` / `Lávka (bod)`), NE WFS escape s podtržítky
   (`Lávka__linie_` jak v katalogu). ČÚZK ZABAGED má historicky **dvě konvence** názvů vrstev v REST:
   některé s podtržítky (Skupina_balvanů, Osamělý_balvan, Železniční_trať), jiné s mezerou + závorkou
   (Lávka, Tramvajová dráha). Důsledek: katalog Sez. 23 vyrobený z WFS escape jmen byl částečně
   zastaralý → opravit při doplňování vrstev (verify `?f=json` listing PŘED přidáním do `LAYER_IDS`).
3. **Materiál „kámen" v ZABAGED není** — kamenné viadukty mají `material_p = neznámý`. ISOM 512
   ale nedělí podle materiálu → KISS, jeden symbol.
4. **Železniční vs silniční most rozlišitelný** (`kod_zelez` vs `silnice`), ale ISOM 512 dělí
   jen vozidla (512) vs pěší (512.2) — to nesedí na železniční/silniční (=samostatná osa). Vše
   ze ZABAGED `Most` → 512, `Lávka` → 512.2.

Tři rozhodnutí (Q1–Q3) všechna Recommended: **Most→512, Lávka→512.2** (KISS); **render = polyline +
V-křídla** (template autoritativní); **NV jako 5. DEV_LOCATION**.

### Verify-against-source: ISOM 512 v template

Před kódem renderu kontrola `template_classic.omap`:

- **512 Bridge/tunnel** (id=125, **line_symbol** type=2): linie podél osy (`line_width=180` µm =
  0,18 mm, černá) + `start_symbol`/`end_symbol` = na obou koncích diagonální křídlo z bodu
  (-0,3, -0,44) přes (0,0) na (0,01, 0) — úhel ~35° vůči ose, délka diagonálně 0,53 mm. Symbol je
  symetrický → render kreslí V-křídla na OBĚ strany cesty.
- **512.2 Footbridge** (id=127, **point_symbol** rotatable=true): kolmá čárka 0→±625 µm
  (= 1,25 mm celkem) × 0,25 mm tlusté. Rotace přebíraná z objektu.

**Korekce mého návrhu před verify:** ne „dvě rovnoběžné čáry podél osy" jak jsem řekl v plánu —
je to **jedna středová linie + V-křídla na koncích**.

### Kód

- `connectors/zabaged.py`: `LAYER_IDS` doplnit Most=73, Lávka (linie)=67, Lávka (bod)=66.
  Konstanty `BRIDGE_LAYERS` / `FOOTBRIDGE_LINE_LAYERS` / `FOOTBRIDGE_POINT_LAYERS`. Funkce
  `fetch_bridges` (linie, izomorfní s `fetch_railways`) + `fetch_footbridges` (linie+body
  v jednom volání, paralela `fetch_water`). Mapping `map_bridge_to_isom` → 512 (KISS),
  `map_footbridge_to_isom` → **5122** (= int alias ISOM kódu „512.2", omap_export mapuje na
  string s tečkou).
- `sandbox/generator-poc/generator.py`: ISOM konstanty `ISOM_BRIDGE=512`, `ISOM_FOOTBRIDGE=5122`,
  `BRIDGE_NAME`/`BRIDGE_CLASS` (multi-class maska 1=most, 2=lávka), `BRIDGE_WIDTH_PX=2`,
  `BRIDGE_WING_OUT_PX`/`BRIDGE_WING_BACK_PX` (=0,3 / 0,44 mm × PX_PER_MM), `FOOTBRIDGE_WIDTH_PX=2`,
  `FOOTBRIDGE_HALF_LEN_PX=3`. Render `_draw_bridge` (linie + 4 V-křídla na koncích symetricky;
  tangenta směrem ven z mostu × normála ± wing_out). `_draw_footbridge_point` (kolmá čárka
  rotovaná o `rot_rad`). `_nearest_segment_tangent` helper (paralela `_ropik_outward`, ale
  jednodušší — lávka má jeden tok pod sebou, ne klastr). `_generate_real_bridges` (3-tuple
  návrat: bridge_features+footbridge_features+bridges_info). CLI `--bridges {off,real}` default
  real, validace `bridges=real ⇒ terrain=real`. `mask_bridges.png` 2-class.
- `sandbox/generator-poc/omap_export.py`: `USED_CODES` += `"512", "512.2"`. `ROTATABLE_CODES`
  += `"512.2"` (rotace OBJ). `write_omap` parametry `bridge_features`/`footbridge_features`;
  footbridge = bodový objekt s `rotation="X.XXXX"` v RADIÁNECH (verify v OOM otevřená, předáno
  podle template signatury). `n_bridges` v návratu.
- **Tramvaj (oprava Sez. 28)** — uživatel: „LD u Lidových sadů je točna tramvaje. Nemělo by být
  namapováno jako železnice?" → probe `temp/probe_tramway.py` na LS: `Tramvajová dráha`
  (id=71, jméno **s mezerou** stejně jako Lávka — paralela), 25 LineString prvků v 6×4 km
  výseku. Atributy chudé (jen ID + Shape_Length) → KISS, vše → 509. Sez. 28 ji vynechala jako
  „urbánní", ale na LS chyběla. Přidáno do `RAILWAY_LAYERS`. Mapping `map_railway_to_isom`
  doc-string update: tramvaj nově patří.

### Refaktor `DEV_LOCATIONS` na per-lokalita rozměr

Uživatel: „Rád bych měl v našich testovacích vzorcích právě různé formáty (na výšku, na šířku,
pás, čtverec), abychom otestovali správné dotahování/ořezávání podkladů." Dosud `DEV_LOCATIONS`
mělo společný globální `DEV_W_KM=6, DEV_H_KM=4` (= landscape pro všech 4). Sez. 31 rozšířeno:

- Type: `dict[str, tuple[str, float, float, float, float]]` = (label, lat, lon, w_km, h_km).
- Existující 4 lokality zůstávají **landscape 6×4 km** (zpětná kompat. kanonických výstupů
  Sez. 25/28/30).
- **NV `Novina` 50.7598686, 14.9601922, 3×5 km PORTRAIT** (5. lokalita, kamenné železniční
  viadukty + test ne-1.5:1 poměru).
- **HS `Hrubá Skála` změněna z landscape 6×4 na SQUARE 5×5 km centrované na 50.5481, 15.1762**
  (midpoint Kacanovy ↔ Doubravice; Doubravice = část obce **Hrubá Skála**, NE Turnov — verify-
  against-source wiki). Vzdálenost obcí 4,3 km E-W × 0,6 km N-S → 5×5 km pokryje obě s margem.
- `DEV_W_KM`/`DEV_H_KM` zrušeno; `main()` čte 5-tuple. argparse `--location` help vypisuje per-
  lokalita rozměr (`SV=Sovi vrch 6x4km, NV=Novina 3x5km, …`).

### Verify (ve `.venv`)

- **proc baseline 65 drží** (56 vrstevnic + 2 cesty + 7 bodů, plátno noise → mosty=0, tramvaj=0).
- **NV (3×5 km portrait):** 22 mostů (Bridge:17, Footbridge:5) vč. **Novinský viadukt** a
  **Malý viadukt** (`jmeno` v meta.json). Železnice 1 (= trať 086 Liberec–Bakov), budovy 299,
  voda 130, skály 31. .omap 1384 objektů.
- **HS (5×5 km square):** **639 skal** (459× 206 Gigantic boulder! Hruboskalsko dominantní),
  13 mostů, vrstevnice 524, budovy 1265. .omap 4368 objektů.
- **LS (6×4 km landscape) po tramvaj fixu:** železnice **40** (= 15 trat+vleček + 25 tramvaj),
  76 mostů, 8273 budov, 162 skal. Tramvajová síť včetně **točny Lidové sady** doplněna.
- Verify-against-source na Wikipedia: Hrubá Skála obec má 8 částí (Hrubá Skála, Bohuslav, Borek,
  **Doubravice**, Hnanice, Krčkovice, Rokytnice, Želejov) — pohlcení části obce vyjasnilo, že
  uživatelovo „od Kacanov po Doubravice" zachycuje západní–východní pás severně od Hruboskalska.

### Branžový precedent

**Karttapullautin** mapuje mosty z OSM `bridge=yes` na cestě → ISOM 512 podobně (line symbol s
end decorations). Naše řešení izomorfní, ale data ze ZABAGED (CC BY 4.0, lepší kvalita pro ČR).

### Kudos / Censure

- **Kudos! (uživatel)** — *„LD u Lidových sadů je točna tramvaje. Nemělo by být namapováno jako
  železnice?"* — okamžitý odhalený **Censure Sez. 28** (tramvaj vynechána jako „urbánní"). Lekce:
  „urbánní" nestačí jako vyřazovací kritérium, když ISOM nerozlišuje (jeden symbol, KISS). Doplněno.
- **Kudos! (uživatel)** — návrh různých formátů DEV_LOCATIONS (portrait/square) je *first
  things first* pro testování ořezů: bez něj všechny 4 lokality byly 1.5:1 landscape =
  systematický blind spot pro non-1.5:1 výseky.
- **Censure (já)** — v plánu render mostu jsem napsal „dvě rovnoběžné čáry podél osy", ale
  template ukázalo, že to je „jedna středová linie + V-křídla". Naštěstí verify-against-source
  PŘED kódem chytil (jako Sez. 28 železnice combined symbol).
- **Verify-against-source před kódem** (probe Most + Lávka + Tramvaj, template 512) zachránilo
  3 předčasné implementace: hádané atributy (Lávka má jen ID), špatný symbol (V-křídla ne 2 paralely),
  WFS escape jméno (`Lávka__linie_` v REST nefunguje).

### Příště

1. **Lesní_průsek 508**, bodové prvky (kříž 530 / mohyla 526 / věž 524), Zeď 513 — pokračování
   katalogu (po skalách+mostech jsou poslední „klasické" OB vrstvy).
2. *(drobnost)* liniová lávka jako path se symbolem podél (dnes střed + 1 symbol — KISS MVP).
3. *(drobnost)* verify v OOM: Lávka rotace radiánech? Tramvaj 509 kombinovaný symbol v městské síti?
4. *(drobnost)* `Skupina_balvanů__linie_` → 208 / `Sesuv_půdy__suť` → 210 (Sez. 30 odložené).
5. *(drobnost)* form line souvislé smyčky + ověřit prahy `FORMLINE_*` na SV/LS.
6. *(drobnost)* crossability hranic; *(dlouhodobě)* ISSprOM sprint; vegetace (UC5).

### Post-script 1 — oprava symbolu 512 (Censure! 2× já) + doplnění tunelů

Po prvním commitu uživatel upozornil na DVĚ chyby:

1. **„Značky mostů jsou párové. Patří zrcadlově proti sobě kolem/vně cest. Ty jsi ji použil v jedné
   instanci a vložil si ji na osu linie cesty/železnice."** Censure: původní render kreslil
   středovou linii + V-křídla NA osu — to není ISOM 512. Správný symbol = **4 krátké KOLMÉ čárky
   vně osy cesty**, zrcadlově nad+pod osou na obou koncích mostu, ŽÁDNÁ středová linie. Cesta/
   železnice už je nakreslená pod symbolem. Tvar:
   ```
        |  |              ← horní pár (nad osou: na začátku + na konci mostu)
        |  |
    ====|==|====           cesta
        |  |
        |  |              ← dolní pár (pod osou, zrcadlově)
   ```
   Template start/end_symbol je polovina symetrického páru — OOM je zrcadlí kolem osy linie
   (nemám-li explicitní mid_symbol). Konstanty `BRIDGE_WING_LEN_PX`/`OFFSET_PX` přiřazeny
   správně (300/436 µm = délka kolmé čárky / offset od osy) a zvětšeny pro rastr-čitelnost
   (1,3 / 0,6 mm vs template 0,3 / 0,44 mm — paměť `render-px-tuned-omap-authoritative`).

2. **„V Novině železnice před Karlovem vede dlouhým tunelem, mapuješ to ale po povrchu."** Censure:
   `Most` neobsahuje tunely; ZABAGED má samostatnou vrstvu **`Tunel`** (id=74). Probe na Novině
   (`temp/probe_tunnel.py`): 4 tunely vč. **Karlovský tunel I 314 m** + Karlovský II 48 + Kryštofský
   48 + U myslivny 40 (vše železniční trať 086). Template description: *„Bridges and tunnels are
   represented using the same basic symbols"* → tunel mapován na **stejný symbol 512** (KISS,
   vrstva → symbol). Doplněno do `BRIDGE_LAYERS = ("Most", "Tunel")`. **MVP přijato:** trať pod
   tunelem zůstává plně viditelná (dashed čárkování úseku trati v tunelu = drobnost TODO).
   Také zjištěno: ZABAGED má i `Podjezd (bod)` (64) a `Podjezd (linie)` (77) — odloženo.

Verify: NV 26 mostů (22→26 = +4 tunely), LS 78 (76→78 = +2 tunely), HS 13 (beze změny, tunely
v HS výseku nejsou). Zoom 8x na Novinský viadukt + Malý viadukt + Karlovský tunel I ukázal
**4 kolmé čárky vně železnice** v každém případě — symbol konečně podle ISOM konvence.

### Censure / Kudos (post-script)

- **Censure (já, dvojnásobně)** — (a) interpretace template Sez. 31 v plánu („dvě rovnoběžné čáry
  podél osy" → kontrolováno verify-against-source, opraveno na „středová + V-křídla") byla SOM
  STÁLE špatná: ISOM 512 je **KOLMÝ PÁR** vně osy, NIKOLI cokoli na ose. Verify-against-source měl
  by zahrnovat i porovnání s reálnou OB mapou, ne jen XML template (které je jen polovina symbolu).
  (b) `Most` ZABAGED jsem doplnil bez ověření, zda tunely jsou v stejné vrstvě → nejsou. Uživatel
  musel ukázat na konkrétní reálný tunel (Karlovský), abych ho doplnil. Lekce: u nového ZABAGED
  pole **systematicky zkontroluj sousední vrstvy v MapServer** (Most → Tunel → Lávka → Podjezd
  jsou tematická skupina; nevybírej jen jednu).
- **Kudos! (uživatel)** — okamžitá konkrétní zpětná vazba s ASCII náčrtem (uvnitř AskUserQuestion
  preview) zachránila třetí iteraci. Bez verify-with-user by produkt byl jako po Sez. 31 commit
  (V-křídla na ose) — nesprávný symbol.

### Post-script 2 — most/lávka render NEHOTOV, %END (Sez. 32 znovu a lépe)

Uživatel po 2. iteraci (oprava na 4 kolmé čárky vně osy) řekl *„Mosty v Novině stále špatně"*.
Vytvořil jsem anotovaný přehled (`temp/novina_overview.png`) + zoomy 12× každého z 6 pojmenovaných
mostů/tunelů s tabulkou poloh. Uživatel: *„Tyhle náhledy jsou těžko čitelné (pixelizace), rovnou
proveď do OMAP"* → fokus z rastru na .omap. Verifikace .omap: **21 line objects symbol="125"
(=ISOM 512) + 5 point objects symbol="127" (=512.2)**, coords v paper-space µm sedí. OOM by
mělo renderovat symbol 512 z `template_classic.omap` (line_symbol type=2 + start_symbol +
end_symbol).

Po dotazu „jak v OOM vypadá symbol 512" uživatel: *„Tohle nikam nevede, dáme %END a příště
zkusíme znovu a lépe."* → ukončení sezení s rozpracovaným mostem.

**Co dnes hotovo (přes 2 commits + post-script):**
- ZABAGED `Most` (73), `Lávka (linie)` (67), `Lávka (bod)` (66), **`Tunel` (74)** doplněno do
  `LAYER_IDS`; probe Novina ukázal Novinský viadukt + Malý viadukt + 4 tunely (Karlovský I/II,
  Kryštofský, U myslivny — vše železnice 086).
- Oprava Sez. 28 vynechání: **tramvaj `Tramvajová dráha` (71) → ISOM 509** (LS točna Lidové sady).
- Refaktor `DEV_LOCATIONS` na per-lokalita rozměr (5-tuple); 5. lokalita **NV Novina 3×5 km
  portrait**, HS změněna na **5×5 km square** (midpoint Kacanovy↔Doubravice).
- **`STATISTICS.md` + `stats.py`** — tabulka 24 ISOM symbolů × 5 lokalit s počty objektů +
  mtime poslední regenerace. **Z 24 sledovaných ISOM symbolů reálně používáme 24** (100 %
  pokrytí katalogu pro klasický OB; chybí jen Bridge MVP-správný render).
- Kanonika regenerována: Novina + Lidové sady (s tramvaj) + Hrubá Skála (square). SV/NL stará.

**Co Sez. 32 musí udělat (most/lávka „znovu a lépe"):**
- **Začít v OOM, ne v rastru.** Otevřít template_classic.omap symbol 512 v OOM, ověřit, co
  OOM SKUTEČNĚ renderuje. Možnosti: (a) template má pouze polovinu páru a OOM mirroruje;
  (b) template má pouze polovinu a OOM nemirroruje (= špatně). Verify na známé reálné OB mapě
  z `resources/` (jak vypadá 512 v reálu).
- Pokud OOM render je špatně, dvě cesty: úprava `template_classic.omap` symbol 512
  (přidat zrcadlovou polovinu), nebo přemapování `_draw_bridge` na 2× point_symbol 512.1
  na koncích (Bridge minimum size má v template polyline s 4 šikmými čárkami — M tvar).
- Tunel-specifikum: trať pod tunelem dashed / vymazaná (dnes plně viditelná).

**Censure (já, potřetí):** spoléhání na rastrový zoom jako vize verify ignoruje, že
ISOM-OB mapa = OOM render. Paměť [[omap-verify-oom]] říká „otevírej ručně v OOM"; v Sez. 31
jsem to udělal jen v post-scriptu, ne od počátku. Bridge render = ideální případ kdy začít
v OOM (kde symbol je definován autoritativně).

### Příště (Sez. 32) — znovu a lépe
1. **Most/lávka render** — viz Post-script 2 strategie (OOM-first, template úprava nebo
   přemapování na 512.1 bodový).
2. Tunel dashed pod tunelovým úsekem.
3. Lesní_průsek 508 + bodové prvky (kříž 530 / mohyla 526 / věž 524) + Zeď 513 (původní plán).
4. *(drobnost)* `Podjezd (bod)` 64 + `Podjezd (linie)` 77 → 519 Underpass (verify spec).
5. *(drobnosti dnes ne)* liniová lávka path se symbolem podél / `Skupina_balvanů__linie_` 208 /
   `Sesuv_půdy__suť` 210 / form line souvislé smyčky / crossability / ISSprOM / vegetace.

## Sezení 32 — Rollback mostů/tunelů/lávek + verify-against-source ISOM 512

Třicáté druhé sezení, mrkla. `%BEGIN`: sync 0/0 (HEAD = `a52a5f9`), čistý strom. Audit cadence:
DOCS 12/10 (2 přes), pruning 14/12 (2 přes), CODE 4 sez / pod LOC práh, CALIBRATE 14/15.
Fokus z Příště Sez. 31 = bod 1 mosty/lávky „znovu a lépe", uživatel ho hned přepnul na
**velký ROLLBACK** se zdůvodněním *„Minule jsi se zacyklil a ani napočtvrté nedal správný
výsledek. Přitom je to snadné: Zjisti, jak se mapuje v ISOM. Vypiš!"*

### Verify-against-source: ISOM 512 z oficiální spec

Před rollbackem jsem vypsal symbol 512 ze 3 zdrojů:

1. **ISOM 2017-2 Rev 6 PDF str. 32** (Pic2Omap `/docs/ISOM_2017-2_rev6.pdf`):
   - **`512 Bridge / tunnel (L, P)`** — žádný 512.1 / 512.2 v hlavičce (jsou OOM rozšíření).
   - Description (doslovně): *„Bridges and tunnels are represented using the same basic
     symbols. If it is not possible to get through a tunnel (or under a bridge), it shall
     be omitted. Minimum length (of baseline): 0.4 mm (footprint 6 m). Small bridges
     connected to a track/path are shown by centring a track dash on the crossing.
     Tracks/paths are broken for water course crossings without bridges. A small
     footbridge with no path leading to it is represented with a single dash. Colour:
     black."*
   - **Vizuál spec (str. 32):** **pár závorek `[ ]`** na obou koncích krátké linie =
     **2 šikmé čárky pod úhlem 60° vůči ose**, **symetricky NAD i POD osu**, délka
     0,4 mm, rozestup mezi sloupky 0,5 mm, line_width 0,18 mm.

2. **`template_classic.omap` symbol id=125** (XML):
   - line_symbol type=2, line_width=180 µm, color=2 (černá).
   - **start_symbol** coords `(-300,-436)→(0,0)→(10,0)` = šikmá čárka NAD osou
     (záporné Y = paper-space „nahoru"), úhel ≈ 55,4° vůči ose, délka ≈ 0,53 mm.
   - **end_symbol** coords `(-10,0)→(0,0)→(300,-436)` = zrcadlo NAD osou na konci.
   - **Template má JEN POLOVINU symbolu** (jen jednu stranu závorky, nad osou) —
     proto OOM render nevypadá podle spec `[ ]`.
   - `512.1` (id=126) = point_symbol M-tvar pro minimum-size mosty (taky jen půlka).
   - `512.2` (id=127) = point_symbol kolmá čárka 0→±625 µm (1,25 mm × 0,25 mm) =
     **spec-konformní footbridge** (geometrie OK).

3. **Pic2Omap `spec_check_ISOM-2017-2.md`**: ISOM katalog → 1 řádek `| 512 |
   Bridge / tunnel | L, P |`. Žádné sub-kódy 512.1/512.2 v ISOM spec — jsou OOM only.

### Rollback (žádný kód do oprávy spec)

Uživatel po výpisu: *„Go! Přegeneruj všechny mapy, vymaž temp, začneme mosty, tunely
znovu."* Rollback selektivní (otázka přes AskUserQuestion):

**Smazáno:**
- `connectors/zabaged.py`: LAYER_IDS Most/Tunel/Lávka(linie)/Lávka(bod), konstanty
  BRIDGE/FOOTBRIDGE_LAYERS, `fetch_bridges`, `fetch_footbridges`, `map_bridge_to_isom`,
  `map_footbridge_to_isom`.
- `sandbox/generator-poc/generator.py`: ISOM_BRIDGE/ISOM_FOOTBRIDGE konstanty, BRIDGE_NAME/
  CLASS/WING_LEN/OFFSET/WIDTH + FOOTBRIDGE_WIDTH/HALF_LEN, `_draw_bridge`,
  `_draw_footbridge_point`, `_nearest_segment_tangent`, `_generate_real_bridges`,
  CLI `--bridges`, validace, `mask_bridges.png` save, sekce v `_build_meta`, volání,
  `bridge_omap_features`/`footbridge_omap_features`.
- `sandbox/generator-poc/omap_export.py`: `"512"` + `"512.2"` z `USED_CODES`, `"512.2"`
  z `ROTATABLE_CODES`, parametry `bridge_features`/`footbridge_features`, `n_bridges`.
- `sandbox/generator-poc/stats.py`: řádky 512/512.2 ze `SYMBOLS`, alias `5122=512.2`,
  sekce „bridges" z agregace.

**Zachováno** (nesouvisí s mostem):
- Tramvaj `Tramvajová dráha`→509 (oprava Sez. 28 vynechání, LS točna).
- `DEV_LOCATIONS` 5-tuple per-lokalita rozměr + NV portrait 3×5 + HS square 5×5.
- `STATISTICS.md` + `stats.py` (regenerován).

### Verify (smoke + plný regen)

- **Smoke proc baseline:** `--terrain noise --paths proc --water off ...` → **65 objektů**
  (56 vrstevnic + 2 cesty + 7 bodů) drží přesně. Žádná regrese.
- **Plný regen 5 lokalit** v sandbox/generator-poc/ (každá ~2–3 min, ČÚZK DMR/ZABAGED/ortofoto):

| Lokalita | Σ omap obj. (před→po) | Δ | Pozn. |
|---|---:|---:|---|
| SV Soví vrch  | ~3411→3389 | -22 | žádný most (vesnice) — odečet jen na pomocných |
| NL Nová Louka | 1388       |  0  | žádný most (pomine) |
| LS Lidové sady| ~13634→13558 | -76 | 76 mostů v městské části |
| HS Hrubá Skála| 4368→4355   | -13 | 13 mostů (Sez. 31) |
| NV Novina     | 1384→1362   | -22 | 22 viaduktů+tunelů (Novinský/Malý/Karlovský) |

- **STATISTICS.md regenerován:** 22/22 sledovaných ISOM symbolů aktivních (klesli ze 24 na 22 = -512 -512.2). Tabulka konzistentní.

### Co dál (Sez. 32 pokračování nebo Sez. 33)

Uživatelovo *„začneme mosty, tunely znovu"* implikuje pokračovat **spec-driven**:

1. **Verify-with-OOM** symbolu 512 + 512.1 v `template_classic.omap`: otevřít, nakreslit
   testovací linii, vyfotit/screenshotnout, zjistit jestli OOM zrcadlí start/end_symbol,
   nebo ne. Bez tohoto kroku každá implementace je hádání.
2. **Rozhodnutí mezi (a) editace template + line_symbol** (přidat zrcadlovou polovinu do
   start/end_symbolu) **vs (b) přemapování na 2× bodový 512.1** na koncích každého mostu
   (point_symbol M-tvar, řeší obě strany závorky najednou).
3. Tunel = stejný symbol jako most (spec) + drobnost dashed úsek trati pod tunelem (TODO).
4. Lávka 512.2 single dash = template OK, vrátí se s mostem.

### Kudos / Censure

- **Kudos! (uživatel)** — *„Velký ROLLBACK"* po 3 iteracích zacyklení byla správná volba.
  Spec-driven přístup („Zjisti, jak se mapuje v ISOM. Vypiš!") chrání před další iterací
  bez pochopení autoritativního zdroje. Paměť [[bridge-symbol-render-needs-oom-first]]
  zafixovala tuto lekci ze Sez. 31; tady přidává **PDF-spec-first** vrstva — i OOM
  template může být neúplný (jen půlka symbolu).
- **Censure já 4× (souhrn Sez. 31+32):** (a) interpretace template jako V-křídla na osu;
  (b) interpretace template jako 4 kolmé čárky vně osy; (c) spoléhání na rastr-zoom jako
  verify view; (d) nepřečtení ISOM PDF spec před implementací (template ≠ spec, ukázalo
  se vlastně, že template je defektní). **Lekce → příště:** PDF spec str. konkrétní
  symbol VŽDY PŘED kódem, ne až po 3 iteracích.

### Souhrn Sez. 32 — kompletní průběh (přesun mrkla → NTBHEJ21)

Sez. 32 začalo jako rollback Sez. 31 NEHOTOVÝCH mostů, ale rozrostlo se na **7 iterací**
nového spec-driven přístupu. Klíčové milníky:

| Iter | Commit | Cíl | Výsledek |
|---|---|---|---|
| Rollback | `dde7303` | Smazat Sez. 31 mosty/tunely/lávky po 3× zacyklené implementaci | -307 LOC; verify ISOM 2017-2 PDF spec str. 32 → 512 = jeden symbol pro most i tunel |
| 1. | `0ba7693` | Spec-driven (verze 1, 3 módy): most = 2 paralelní čáry podél celé osy / tunel = endpoint závorky / lávka = single dash | Uživatel Censure × 6 (chyby E1-E6) — všechno špatně |
| 2. | `10bba09` | Fix E1-E6: závorky kolmé+symetrické na koncích osy, ne paralelní; přerušení vrstev pod mostem; tunel kolmé čárky | Uživatel: still wrong (chyby z chyba1.png/chyba2.png) |
| 3. | `d546aec` | Most.omap demo od uživatele odhalilo: most = **2 paralelní 512 linie** s defaultním template, ne 1 line symbol | Uživatel: silnice na mostě cropována (chyba!) |
| 4. | `9009b85` | Crossing strategy (1-2 cuts crop, >2 paralel ignore) + passage strategy pro tunel | Uživatel: A obě nožičky dovnitř / B tunel kreslí nesmyslnou osu |
| 5. | `97e1473` | Reverze SPRÁVNÉ paralely (= druhé, ne první); tunel emit jako most (2 paralelní 512) — prostor uvnitř zůstává prázdný | Verify očekáváno v dalším sezení na NTBHEJ21 |

Kompletní commits Sez. 32 dnes (`a52a5f9..HEAD`):
```
97e1473 fix(generator): mosty/tunely 7. iterace — reverze správné paralely + tunel jako 2 paralely
9009b85 fix(generator): mosty/tunely 6. iterace — reverze paralely + dual crop strategie
d546aec fix(generator): mosty/tunely 5. iterace — 2 paralelní 512 + intersection cropping
10bba09 fix(generator): mosty/tunely E1-E6 oprava (Sez. 32 znovu po uživatelově Censure)
d2a565b docs(session): Sez. 32 dokončeno — mosty/tunely/lávky spec-driven (foto verify)
0ba7693 feat(generator): mosty/tunely/lávky spec-driven (ISOM 512+512.2, Sez. 32)
8098d08 docs(session): Sez. 32 — finalizovaný design mostů/tunelů/lávek pro Sez. 33
80c94e3 docs(session): Sezení 32 — rollback mostů + verify ISOM 2017-2 PDF spec
dde7303 revert(generator): rollback mosty/tunely/lávky (Sez. 31) — spec-driven přepis v Sez. 33
```

### Klíčové architektonické závěry Sez. 32

1. **Template upraven uživatelem** — `template_classic.omap` symbol id=125 (512) zvětšen
   1.5× (line_width 270 µm, coords -450/-654; uživatel sám aktualizoval během sezení).
   start/end_symbol mají JEDNU šikmou čárku na konci (= polovina závorky, OOM nezrcadlí).
   Hranatá závorka `[ ]` vzniká **4 šikmými čárkami ze 2 paralelních linií 512** na koncích.

2. **2 paralelní 512 linie** = uživatelův praktický OB workflow z Most.omap dema:
   - Linie A: REVERSED direction, offset +0,75 mm v levé normále
   - Linie B: ORIGINAL direction, offset +0,75 mm v levé normále
   - Vzdálenost paralel: 1,5 mm (rozestup 0,5 mm + 2 × 0,5 mm šířka)
   - Reverze KLÍČOVÁ: bez ní obě paralely mají stejnou tangentu → obě nožičky stejným směrem
     = jedna ven, druhá dovnitř.

3. **Most i tunel** sdílí emit logiku (2 paralelní 512). Liší se jen v cropping strategii:
   - Most = crossing detection vodu/cesty pod ním
   - Tunel = passage detection železnici/silnici uvnitř

4. **Dvě crop strategie:**
   - **`_crop_line_at_crossings`** (pro mosty): počítá bod-průsečíky line × cutter.
     0 = paralel/mimo → ignore. 1 = bod-křížení → crop ±0,5 mm. 2 = vstup+výstup → crop
     mezi. > 2 = paralel souběh s ZABAGED noise → ignore (silnice/železnice NA mostu).
   - **`_crop_line_at_passages`** (pro tunely): pro každý cutter najde nejbližší body
     line k cutter start/end; pokud oba < 2 mm, cropuje úsek mezi. Funguje i pro paralelní
     souběh (železnice v tunelu sleduje stejnou trasu jako tunel ZABAGED).

5. **Cropping volání per vrstva:**
   - voda: crossings(mosty) — most přes potok
   - paths: crossings(mosty) + passages(tunely) — most nad silnicí + silniční tunel
   - railways: passages(tunely) — železnice v tunelu (NIKOLI most, protože paralel)

6. **`stats.py` aktualizován** s 5122 → "512.2" alias; STATISTICS 24/24 aktivních.

### Censure já 7× souhrn (Sez. 31 + Sez. 32)

1. V-křídla na osu (Sez. 31 plán)
2. 4 kolmé čárky vně osy (Sez. 31 post-script 1)
3. Rastr-zoom jako verify view (Sez. 31 post-script 2)
4. Nepřečtení PDF spec před implementací (Sez. 32 ranní)
5. Interpretace „závorky po celé délce" jako 2 paralelní souvislé čáry (Sez. 32 odpoledne před fotem)
6. Reverze špatné paralelní linie (Sez. 32 7. iterace by se měla otočit Linie A, ne B)
7. Emit tunel jako 1 line objekt 512 (= centrální osa skrz tunel, „nesmyslná čára")

### Lekce → memory bridge-symbol-render-needs-oom-first

Hierarchie verify-against-source pro ISOM symboly:
1. **USER'S DEMO (= Most.omap dema)** — praktický OB workflow = nejvyšší autorita
2. **FOTO reálné OB mapy** — co se v praxi kreslí (= např. závorky vně osy, nožičky ven)
3. **PDF spec** — geometrie + popis (Pic2Omap/docs/ISOM_2017-2_rev6.pdf str. 32)
4. **OOM template** — implementace (může být neúplná / chybná)
5. **OOM ruční render** — co OOM skutečně kreslí
6. **Kód**

Při neshodě mezi vrstvami: nižší úroveň ustupuje.

### Příště (Sez. 33 — pokračuje na NTBHEJ21)

1. **Verify v OOM** Sez. 32 finální stav: NV Novinský/Malý viadukt + Karlovský tunel I/II
   + Kryštofský + U myslivny. Pokud OK, mosty/tunely uzavřeny. Pokud ne, další iterace.
2. **MVP omezení k vyřešení** (až po verify):
   - Voda × most: ZABAGED data nemusí mít čisté 1-2 průsečíky kvůli noise →
     voda pod mostem se necropuje. Možná detection úhlového rozdílu tangent.
   - Silniční tunel × silnice: passage near_mm=2.0 threshold může vyloučit silnice
     s drobně jinou trasou. Možná zvýšit threshold nebo jiná strategie.
3. **Dlužné audity** (cadence překročena ≥ 3):
   - %AUDIT:DOCS (poslední Sez. 19 → 13/10 = +3)
   - IDEAS/TODO pruning (poslední Sez. 17 → 15/12 = +3)
   - %CALIBRATE (poslední Sez. 17 → 15/15 = due, +0)
4. **Lesní_průsek 508 + bodové prvky** (kříž 530 / mohyla 526 / věž 524) + Zeď 513.
5. *(drobnosti)* Podjezd 519 / form line smyčky / crossability / ISSprOM / vegetace.

---

### Finalizovaný design pro Sez. 33 (po Q&A s uživatelem)

Diskuse v Sez. 32 po rollbacku otevřela klíčový rozdíl: **most a tunel kreslíme JINÝM
módem téhož symbolu 512**, podle toho, jestli je trať VIDITELNÁ (most = ve vzduchu,
nahoře, celá vidět) nebo NEVIDITELNÁ (tunel = pod zemí, jen vstup a výstup).

**Most (ZABAGED `Most` linie = osa mostu = trať/cesta ve vzduchu)** — varianta A
(potvrzena uživatelem A1):
- Trať/cesta nahoře = **plná** přes celý most (509 železnice / 502-506 silnice/cesty
  bez přerušení — viditelná po celé délce)
- **2 paralelní šikmé čáry** podél **CELÉ DÉLKY MOSTU**, vně osy (jako boční stěny mostu)
- Sklon **60° vůči ose** = vně vytočené (otevřené ven od osy mostu)
- Tloušťka 0,18 mm
- Mezera mezi osou a vnitřním okrajem paralelní čáry = dle PDF spec str. 32 (`0.5 (OM)`
  indikátor — interpretace ověřit v Sez. 33)
- **Pokročilý detail (mimo MVP):** prohnutí s obloukem mostu (paralelní čáry kopírují
  zakřivení reálného oblouku železnice). Pro MVP rovné kolmé.
- **Křížení s vrstvou pod mostem** (ZABAGED atribut `premost_p` = silnice/vodoteč):
  spodní vrstva **přerušená** v místě křížení s mostem (= 1 break v silnici v bodě,
  kde se kříží s `Most` linií). Realizace: spárovat `Most` linie s liniemi pod (cesty/
  voda), najít průsečík, tam výstupní vrstvu přerušit.

**Tunel (ZABAGED `Tunel` linie = osa tunelu)** — potvrzeno A2:
- **1 závorka 512** na vstupu (kolmá k ose tunelu, otevřená VEN — pryč od tunelu)
- **1 závorka 512** na výstupu (kolmá, otevřená VEN)
- Závorka = pár šikmých čárek pod 60° symetricky kolem osy (= 1 závorka má 2 šikmé
  čárky, jednu nad osou a jednu pod ní)
- Mezi závorkami uvnitř tunelu: **dashed trať NEBO úplně vynechaná** — vyzkoušíme
  obě v OOM, uživatel rozhodne podle vizuálu (A3 *„Uvidíme jak to bude vypadat"*)

**Důsledek pro mapování ZABAGED→ISOM:**
- `Most` linie → **paralelní 2 čáry mode** (po celé délce, šikmé 60° vně)
- `Tunel` linie → **endpoint závorky mode** (1 závorka na konci, mezi nimi dashed/vynechaná
  původní trať)
- `Lávka (linie/bod)` → **single dash mode** (template 512.2 = 1 kolmá čárka kolmo na vodu)

Velký rozdíl proti Sez. 31: trojvrstevný most/tunel/lávka render dělíme na **3 různé
geometrické módy**, ne jeden univerzální line_symbol.

**Verify lokality Sez. 33:**
- Novina (NV): Novinský viadukt (199 m kamenný most, železnice nahoře, silnice pod) +
  Karlovský tunel I 314 m železniční (= ukázkové oba symboly v jedné lokalitě)
- Lidové sady (LS): velký silniční tunel u nádraží v Liberci (= verify rychlostní silnice
  mode tunelu)

### Censure / Kudos (Sez. 32 souhrn)

- **Censure já (souhrn Sez. 31+32, 4×):** (a) V-křídla na osu, (b) 4 kolmé čárky vně osy,
  (c) rastr-zoom místo OOM-first, (d) nepřečtení ISOM PDF spec str. 32 PŘED kódem.
  Lekce → memory [[bridge-symbol-render-needs-oom-first]] rozšířena na hierarchii
  **PDF spec → OOM template → ruční render v OOM → kód**.
- **Kudos! (uživatel)** — „Velký ROLLBACK" + „Zjisti, jak se mapuje v ISOM. Vypiš!" =
  zlomovou volbu z 4 iterace zacyklení (Sez. 31) na čisté restartování spec-driven.
  Bez tohoto by se Sez. 32 zase ztratila v rastrovém ladění.
- **Kudos! (uživatel)** — postupné rozkrývání **mostového** vs **tunelového** módu
  v Q&A: nejprve „most = závorky vedou po celé délce" (= 2 paralelní čáry, ne pár
  zlomyslné), pak „tunel = závorka na konci + dashed/vynechaná" (= jen endpoint).
  Bez tohoto bych implementaci dělal jako jediný univerzální mode → další iterace.

### Foto verify + implementace (Sez. 32 pokračování)

Uživatel poslal 2 fotky reálných OB map: image (2) = sprintová mapa s tunelem (svislá
silnice pod vodorovnou silnicí), image (1) = lesní mapa s mostem (silnice nad vodou).
Analýza fotek **úplně přesměrovala interpretaci**:

**Klíčové zjištění z fotek:** most i tunel mají SHODNÝ LAYOUT — závorky 512 jen na
začátku a konci linie, NE podél celé délky. Rozdíl je jen ve viditelnosti trati mezi
závorkami:
- **Most**: trať mezi závorkami = **plná, viditelná** (silnice/železnice nahoře nad
  vodou/údolím, vidět po celé délce mostu)
- **Tunel**: trať mezi závorkami = **úplně vynechaná** (terén skrz, ne dashed — image
  (2) ukazuje vynechání, ne dashed)

Slovní popis „závorky vedou po celé délce trati ve vzduchu" jsem vyložil jako *2
paralelní souvislé čáry podél celé osy* (varianta A v Q&A), ALE foto ukázalo, že to
ve skutečnosti znamená *„most je viditelný po celé délce, závorky jen vymezují
začátek a konec"*. Slovní vs fotický zdroj = velký rozdíl interpretace.

**Závorka 512** (oba módy):
- 2 šikmé čárky pod úhlem 60° vůči ose, symetricky NAD i POD osou
- Délka 0,4 mm, pata 0,25 mm od osy
- Tloušťka 0,18 mm
- Vně vytočené (= otevřené ven od osy mostu)

### Implementace (Sez. 32 pokračování)

`connectors/zabaged.py`:
- LAYER_IDS doplněn Most=73, Tunel=74, Lávka (linie)=67, Lávka (bod)=66
- BRIDGE_LAYERS, TUNNEL_LAYERS (oddělené!), FOOTBRIDGE_LINE/POINT_LAYERS
- fetch_bridges, **fetch_tunnels** (oddělená funkce, Sez. 31 měla v balíku), fetch_footbridges
- map_bridge_to_isom (→ 512), map_tunnel_to_isom (→ 512 stejný!), map_footbridge_to_isom (→ 5122)

`sandbox/generator-poc/generator.py`:
- ISOM_BRIDGE = ISOM_TUNNEL = 512, ISOM_FOOTBRIDGE = 5122
- 3 maskové třídy (1=most, 2=tunel, 3=lávka) — UC5 detektor rozliší z kontextu
- Geom konstanty z PDF spec: BRIDGE_BRACKET_LEN_UM/OFFSET_UM/ANGLE_DEG/LINE_WIDTH_UM
- `_draw_bridge_brackets`: 2 šikmé čárky pod 60° symetricky NAD+POD osu na obou koncích linie
- `_draw_bridge`: plná osa + závorky (most = trať mezi viditelná)
- `_draw_tunnel`: JEN závorky (tunel = osa vynechaná)
- `_draw_footbridge`: single dash kolmo (lávka)
- `_generate_real_bridges_tunnels` (4-tice návrat: bridges/tunnels/footbridges/info)
- CLI flag `--bridges {off,real}` default real, validace `bridges=real ⇒ terrain=real`
- Mask `mask_bridges.png` (multi-class 1/2/3)

`sandbox/generator-poc/omap_export.py`:
- USED_CODES += "512", "512.2"; ROTATABLE_CODES += "512.2"
- Parametry `bridge_features`, `tunnel_features` (oddělené!), `footbridge_features`
- Most + tunel emit jako line objekt symbol 512 (= 125); OOM dnes oba kreslí stejně
  (= s plnou osou). Pro skutečné odlišení tunelu = úprava template TODO Sez. 33.
- Lávka emit jako point objekt 512.2 (= 127) s rotací v radiánech

`sandbox/generator-poc/stats.py`:
- SYMBOLS doplněn 512 + 512.2; alias 5122 → "512.2"
- Sekce "bridges" doplněna do agregace

### Verify (ve `.venv`)

- **Proc baseline 65 drží** (--terrain noise; mosty/tunely/lávky = 0).
- **5 lokalit přegenerováno** s mosty:
  - NV (3×5 km, Novina): **26 mostů/tunelů/lávek** = 17 mostů (Most ZABAGED vč. Novinský
    + Malý viadukt) + **4 tunely** (Karlovský I/II + Kryštofský + U myslivny — vše železniční
    trať 086) + 5 bodových lávek. Σ 1388 omap obj.
  - SV (6×4 km, Soví vrch): 24 (17+0+7), 3413 obj.
  - NL (6×4 km, Nová Louka): 18 (13+0+5), 1406 obj.
  - LS (6×4 km, Lidové sady): **78** (65+2 tunely+11 lávek), 13636 obj. (vč. silničního
    tunelu u nádraží Liberec — uživatel ho zmínil jako verify cíl).
  - HS (5×5 km, Hrubá Skála): 13 (11+0+2), 4368 obj.
- STATISTICS regenerován: **24/24 ISOM symbolů aktivních** (512 + 512.2 zpět z 22/22).

### Kudos / Censure

- **Kudos (uživatel) × 3:** „Velký ROLLBACK" + „Zjisti, jak se mapuje v ISOM. Vypiš!"
  + „Foto z mapy seženu" + samotná fotka image (1) ukazující 2 páry závorek (ne souvislé
  paralelní čáry). Bez fotky bych implementoval variantu A (paralelní čáry) a museli
  bychom udělat 5. iteraci.
- **Censure (já souhrn Sez. 31+32):** 4× špatná interpretace slovního popisu + 1× špatná
  interpretace „závorky po celé délce". Foto reálné OB mapy = autoritativní zdroj,
  který předbíhá i PDF spec (ten ukazuje jen prototyp na jediném krátkém mostu, ne
  reálnou OB praxi). Foto verify-against-source patří do hierarchie:
  **fotka reálné mapy → PDF spec → OOM template → kód**.

### Příště (Sez. 33)

1. **Verify v OOM** — otevřít `sandbox/generator-poc/Novina/map.omap` ručně a podívat
   se, jak OOM renderuje symbol 512 (= line_symbol id=125) na Novinský viadukt a
   Karlovský tunel. Pokud render nesedí s fotkou (= závorky asymetrické / jen půlka),
   upravit template (přidat zrcadlovou polovinu start_symbol/end_symbol).
2. **Tunel cropping** — drobnost: ZABAGED `Železniční_trať` nese železnici i pod tunelem,
   takže v rastru/omap se železnice prolíná s tunelem. Najít překryv `Železniční_trať` ×
   `Tunel` → cropnout železnici v úseku tunelu (= vynechané pod zemí).
3. **%AUDIT:DOCS** (12+1/10 = 3 přes, dlužné).
4. **IDEAS/TODO pruning** (14+1/12 = 3 přes, dlužné).
5. Lesní_průsek 508 + bodové prvky (kříž 530 / mohyla 526 / věž 524) + Zeď 513.
6. *(drobnosti)* Podjezd 519 / form line smyčky / crossability / ISSprOM / vegetace.

---

## Sezení 33 — Mosty/tunely DOKONČENY (OOM verify na NTBHEJ21): nožičky 512 + buffer crop pod mostem + tunelové vjezdy + out_dir do adresářů

### %BEGIN
Sync: klon **33 commitů pozadu** (přesně scénář paměti [[stale-clone-fetch-first]]) → ff na
`6d56dc6` (Sez. 24–32 z druhého stroje, přechod mrkla→**NTBHEJ21** = stroj pro odložený OOM
verify). Audit cadence: **%AUDIT:DOCS 14/10 (+4)**, **pruning 16/12 (+4)** — oba ≥2 přes,
dlužné; CALIBRATE 16/15 (+1). Generátor po ff: chyběla `.venv` + výstupy (gitignored) →
postavena venv (numpy/contourpy/pyproj/**Pillow**). Fokus (uživatel): dotáhnout rozdělané mosty.

### Mosty — finální (verify-against-source: Most.omap demo)
1. **Nožičky 512 obráceně** (Sez. 32 7. iter nikdy neověřena v OOM). Diagnóza NE hádáním:
   změřeno v Most.omap demu (2 linie symbol 125) vs kód — **demo má levá strana osy reversed /
   pravá forward, kód to měl zrcadlově** → nožičky dovnitř. Symboly 125 v Most.omap == template
   (identické), takže rozdíl jen v orientaci linií. Fix: offset paralel na **pravou normálu**
   (záporný `BRIDGE_PARALLEL_OFFSET_UM`). Self-check: relace (reversed, L2 vlevo) == demo. ✓
2. **Buffer crop pod mostem** (uživatel: „cutnout všechny linie 0,5 mm přes a za závorkami").
   Měření Most.omap: cut endpointy křížících linií perp ≈ **1250 µm = 0,75 (paralela 512) + 0,5
   („za závorkami")**. Nahrazena křehká crossing strategie (bod-průsečíky, `>2 → ignore` selhával
   na ZABAGED noise, cropla jen ±0,5 mm → linie vykukovala) **buffer pásem ±1,25 mm KOLMO od osy**
   + **úhlovým filtrem** (úsek ∥ s osou < 25° = NESENÁ trať na mostě, nahoře viditelná → necropovat;
   jen příčné pod mostem). Interpolované hraniční body (`_split_by_zones_interp`) → přesná mezera
   i u řídkých linií. Self-check: voda 130→145, cesty 486→499 (dělení), železnice 5 (nesená ∥ ✓).

### Tunely — dvě iterace, obě OOM verify
1. **512 otočené o 90°** (uživatel: tunel ≠ paralely mostu; 512 zobrazují **vjezdy**). Oddělen
   emit: most = 2 paralely podél osy (`_bridge_parallels`); **tunel = 2 krátké 512 KOLMÉ k ose
   na obou koncích** = vstup/výstup (`_tunnel_portals`, délka 1,5 mm). Self-check: 8 portálů přesně
   1500 µm (4 tunely × 2). Verify uživatel (ortofoto): **vjezdy na správných místech.** ✓
2. **Passage crop železnice ustřihával ~4 mm před vjezd** místo 0,5 mm. Příčina: snap na nejbližší
   **vrchol** trati (řídké body). Fix: vjezdy se **projektují přesně na trať** (`_project_to_line`,
   bod na úsečce) + interpolovaný okraj (sdílený split s mostem, DRY). Self-check: konce železnice
   **499–500 µm** od vjezdů (= 0,5 mm). ✓

### Output do adresářů dle lokality (+ DRY názvů)
`--location` teď nastaví `out_dir` = název lokality (pokud není explicitní `--out`). **Nekonzistence
opravena:** názvy složek byly definované 2× (ASCII v `DEV_LOCATIONS`, diakritika v `stats.py`) →
sjednoceno na diakritickou verzi (`Soví Vrch`/`Nová Louka`/`Lidové sady`/`Hrubá Skála`/`Novina`,
SSoT = shoda obou). Orphany `output/` (testy) + `Hruboskalsko/` (starý HS) smazány.

### Úklid (dead code, DRY)
Smazány osiřelé `_segment_intersection_pt`, `_crop_line_at_cutters` (legacy wrapper),
`_apply_cut_zones` (nahrazen interp splitem). Vytaženy `_split_by_zones_interp`, `_emit_512_line`,
`_point_on_line_px`, `_interp_grid_at`, `_project_to_line`.

### Verify
- **proc baseline 65 drží** (regrese OK, cropping/tunel se proc netýká).
- **Všech 5 lokalit přegenerováno** do adresářů + STATISTICS regenerován (24/24): SV 3299 /
  NL 1421 / LS 13726 / HS 4378 / NV 1420. 512: SV 17, NL 13, LS 67, HS 11, NV 21.
- 3× deterministický geometrický self-check proti Most.omap/datům (nožičky, portály, mezera) →
  **0 zbytečných OOM kol** uživatele (na rozdíl od Sez. 31/32 zacyklení v rastru).

### Kudos / Censure
- **Kudos! (uživatel)** — `Most.omap` demo jako pixel-perfect ground truth (orientace nožiček i
  rozestup paralel se z něj daly **změřit**, ne hádat) + jasné zadání tunelů („otočené o 90°,
  vjezdy") + přesná čísla u vady (0,5 vs 4 mm) → cílené fixy bez iterací.
- **Kudos (já)** — deterministický geometrický self-check proti demu/datům PŘED posláním do OOM
  (relace stran, délky portálů, vzdálenost mezery). Rozšiřuje [[bridge-symbol-render-needs-oom-first]]:
  kde je k dispozici demo/měřitelná spec, ověř geometrii v kódu → šetří uživatelova OOM kola.
- **Censure (já, drobné)** — buffer crop = ~120 LOC nového kódu na jeden požadavek (riziko); zachráněno
  tím, že parametr (1,25 mm) i pravidlo byly odvozené z dema, ne odhadnuté.

### Příště (Sez. 34)
1. **Dlužné audity (první bod sezení, oba ≥2 přes práh):** %AUDIT:DOCS (15/10 = +5),
   IDEAS/TODO pruning (17/12 = +5). %CALIBRATE (17/15 = +2) také zralý.
2. **Lesní_průsek 508** + bodové prvky (kříž 530 / mohyla 526 / věž 524) + **Zeď 513**.
3. *(drobnosti)* Podjezd 519 / form line smyčky / crossability / ISSprOM / vegetace.
