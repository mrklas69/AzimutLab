# AzimutLab — deník 2026-06-16

## Sezení 131 — Png2Line krok 1: plný trénink + reálný transfer + conf_thr práh (HAL3000)

Fokus (Příště #1 ze Sez. 130): plný trénink Png2Line krok 1 (watercourse 304/305) → test mIoU +
`eval_real`. Stroj HAL3000 (CUDA + korpus). KPI generátoru beze změny 58,6 % (dnešní práce je na
straně reconstructoru, ne `generate_map`).

### Plný trénink (`model/png2line/train.py --seed 0`)
40 epoch, batch 16, lr 1e-4, cosine LR, ~2 h. Čistá konvergence (loss hladce klesá, žádná divergence):
**test mIoU 0,774** (pozadí 0,99 / **watercourse IoU 0,55**), best val 0,751 @ ep37. Overfit gate Sez. 130
dal watercourse 0,77 na train → plný 0,55 je u ~5px linie slušné (IoU krutě citlivá na 1px posun hranice,
předpověděno Sez. 130). Promotnut na `resources/line_model/unet_best.pt` (`--promote krok1_watercourse_s0`).

### Reálný transfer — nový `model/png2line/eval_real.py`
Postaven izomorfně s png2area/png2point eval_real: X = reálný sken downscale na kanonické 1,33; GT = LINE
objekty (type=2) toků 304/305 z kartografovy `.omap` přes crosswalk 2000→2017, rasterizované NAFOUKLE
(`GT_LINE_WIDTH_PX`) jako trénink. Georef = reuse `paper_to_scan_px` z png2area (rot_sign=−1, Sez. 120);
maska mapového pole `_detect_map_area` (proti striping halucinaci, jako png2point). **Dvojí metrika**
(izomorf strict+soft Sez. 120): (1) přísné pixel IoU watercourse · (2) relaxované completeness/correctness
s tolerančním bufferem (Wiedemann 1998, standard pro liniové prvky — robustní vůči ~1px georef posunu).

**Výsledek (holý argmax) na Velbloud/Blatná/Bedřichovka:**
completeness **0,90–0,96**, correctness 0,42–0,59, strict IoU 0,20–0,29 (mean 0,25), relaxed F1 0,66.

**Verdikt: Png2Line krok 1 PŘENÁŠÍ na realitu** — completeness 0,93: model reálné toky **najde a trasuje**
(žádný kolaps jako Png2Point 210). Třetí reconstructor čte reálné mapy hned napoprvé. **Slabina = precision**
(přestřel: pred 2–4,5× GT px; na overlay červené FP sledují hlavně cestní/pěšinkovou síť → model fíruje na
„tenkou linii" obecně, ne čistě modrou). Strukturně stejný vzor jako 417 přestřel (Sez. 129).

### conf_thr práh (measure-first sweep, `temp/sweep_thr_line.py`)
Inference 1× per sken (GPU), pak softmax práh watercourse sweep na CPU. **Monotónní**: F1 vrcholí na
**τ≈0,95–0,97** (plató 0,773–0,774), nad 0,95 se completeness drolí. **Volba τ = 0,95** (z plochého plató,
ne in-sample špička — izomorf rozhodnutí 417 → 0,45 Sez. 129; drží nejvyšší recall z vysokých prahů).
**Dopad vs argmax (0,5):** strict IoU **0,251 → 0,409** (+63 %), correctness 0,513 → 0,718, completeness
0,934 → 0,847, relaxed F1 0,659 → **0,773**. Bez re-tréninku.

### Rozhodnutí
- Práh patří do registru jako per-class parametr (izomorf `PointClass.peak_thr`, Sez. 129): `LINE_CLASSES`
  povýšen z `tuple` na dataclass **`LineClass(name, codes, conf_thr)`**; `LINE_CONF_THR` = SSoT prahu.
- Práh se aplikuje JEN při inferenci (`eval_real.predict_scan` demote sub-threshold foreground na pozadí),
  NE v tréninku (`train.evaluate` jede čistý argmax). Užitečný i pro budoucí vektorizaci (čistší maska).
- Caveaty (poctivě): (a) in-sample — prahy laděné na týchž 3 skenech, na kterých reportuju → optimistické;
  (b) symptom-fix — neřeší, PROČ model fíruje na cesty; strukturální cure = krok 2 (víc liniových tříd) /
  re-trénink.

### Kód
- `generator/omap_raster.py`: `LINE_CLASSES` → dataclass `LineClass` + `conf_thr` (watercourse 0,95) +
  `LINE_CONF_THR` registr; `import dataclass`. Konzumenti (LINE_CODE_TO_LABEL/LABEL_NAME/N_LINE) přepsáni
  na atributy.
- `model/png2line/eval_real.py`: NOVÝ modul (parse_carto_lines crosswalk, rasterize_line_Y, predict_scan
  s conf_thr, dvojí metrika, overlay). `temp/sweep_thr_line.py` = scratch sweep (měřicí, gitignored).

### Regenerace DEV map (přání uživatele — test v OOM)
Všech 5 DEV lokalit (SV/NL/LS/HS/NV) přegenerováno `generator.py --location` plnými real vrstvami,
**5/5 exit 0**, čerstvé rgb.png. Vizuál Hrubá Skála ověřen (skalní město, vrstevnice, voda, open/pole).
Pozn.: `generate_map` výstup se dnešní reconstructor-prací nemění — regenerace dává čerstvé mapy k testu,
ne odraz dnešních změn.

### Ověření
- `python -m compileall generator model connectors`: OK.
- `python -m unittest discover -s tests`: **8/8 OK**.
- Produkční `eval_real` reprodukuje sweep τ=0,95 (mean IoU 0,409 / F1 0,773) → práh správně zabudován.

### Příště (po Sez. 131)
1. **Vektorizace maska→polyline** (sdílený „.omap assembly": skeletonize + RDP) — segmentace stojí (synt
   0,55 / real IoU 0,41 + completeness 0,85). Měřit dashed/crossing jako known limitations.
2. **Png2Line krok 2** — dashed **508** (top-5 KPI díra) + **516** fence; gap-bridging. Zároveň strukturální
   cure zbylých FP (víc tříd naučí model odlišit tok od cesty/plotu).
3. **417 re-trénink** (carry Sez. 129, příčina přestřelu) — CUDA okno.
4. **A2b geometrická augmentace** (warp X i Y, Y nearest).

## Sezení 132 — Png2Line vektorizace maska→polyline→.omap + Buschdörfl + nález poledníků (HAL3000)

Fokus (Příště #1 ze Sez. 131): vektorizace = sdílený „.omap assembly" krok. Volba uživatele: scikit-image +
plný scope včetně zápisu `.omap`. KPI generátoru beze změny 58,6 % (práce na straně reconstructoru).

### Vektorizace (`model/vectorize.py` — sdílený postprocess, čistá geometrie bez torch)
Pipeline dle IDEAS/Sez. 130 (architektura A): `skeletonize` (skimage, Zhang-Suen/Lee) → graf kostry
(stupeň přes 8-sousedství: uzel = deg≠2, endpoint/junkce) → trasování řetězců mezi uzly → **vlastní RDP**
(iterativní, zásobník — dlouhé toky by rekurzi přetekly). + `rasterize_polylines` (zpět na masku pro měření
ztráty). KISS, žádná torch závislost (sdílitelné area/point/line). +scikit-image do requirements.

### Georef inverze + `.omap` assembly (`model/png2line/vectorize_omap.py`)
- `scan_px_to_paper` — inverze `paper_to_scan_px` (SSoT vedle ní v png2area, DRY): sken px → world (pgw fwd)
  → paper µm (inverze rotace = transpozice + dělení scale). Round-trip ověřen ~1e-8 µm.
- `build_omap` — `.omap` z čistého `template_classic` se **naklonovaným georef** kartografovy mapy (paper µm
  jsou počítané vůči její georef → musí sedět) + liniové objekty kód 304. String-based jako omap_export.
- Měření: vektor rasterizován ZPĚT → strict IoU + relaxed comp/corr/F1 vs GT (izomorf eval_real).

**Ztráta vektorizace (3 resources mapy): ΔIoU −0,039 / ΔF1 −0,028** — skeletonize+RDP drží strukturu,
neutápí ji. (Velbloud 197 / Blatná 75 / Bedřichovka 84 polyline; junkce tříští toky = crude, known limit.)

### Buschdörfl (přání uživatele — 4. mapa, NĚMECKÁ Livelox, `maps/` struktura)
Vstup `source_livelox.png` (čistý Livelox raster, mpp 1,333 = kanonické, f≈1). Georef NENÍ čistý `.pgw` —
sestaven z `projectedBoundingQuadrilateral` (rovnoběžník → afinní `.pgw`, mpp vyšel 1,333 ✓). gen.omap nemá
ref_point → paper↔world přes meta bbox (scratch `temp/run_buschdorfl.py`). **143 polyline →
`vecline_source_livelox.omap`.** GT = gen.omap (GENEROVANÁ → correctness 0,27 neférová). **Čistá metrika:
98 % vektoru do 3 px od modrých pixelů skenu → model NEFÍRUJE na cesty, čte modré toky.** Posiluje generalizaci.

### Nález uživatele: magnetické poledníky → falešné vodstvo
Buschdörfl má modré rovné rovnoběžné **poledníky** (orientace mapy na sever); Png2Line je bere jako
watercourse 304/305 (modré + liniové). Doloženo daty: **ostrý peak 22 polyline na 90° (svislé), 4× nad
pozadím** (organické toky = plochý histogram) + vizuál. **Korekce uživatele:** NELZE geometrickým filtrem na
watercourse výstupu — rovný vodní KANÁL ‖ severu by se smazal. → **vlastní poledníkový detektor** (diskriminátor =
členství v pravidelné rovnoběžné SOUSTAVĚ, ne „rovná ∧ modrá ∧ ‖ sever"; projekce kolmo na sever → periodické
peaky). Zapsáno IDEAS + TODO jako svébytný úkol.

### Vedlejší: oprava zvonku
Zvonil 3× (globální wav hook + projektový console-beep hook + manuální `servant-bell-ring.mp3` z CLAUDE.md).
Sjednoceno na JEDEN: globální `service-bell.wav` na Stop+Notification (volba uživatele). Manuální zvonek
smazán z `CLAUDE.md` + projektový beep hook + mp3 permission. Paměť `bell-via-hook-not-manual`.

### Ověření
- `compileall` model/generator/connectors OK; **`unittest discover`: 14/14 OK** (8 původních + 6 nových `test_vectorize`).
- Round-trip georef ~1e-8 µm; ztráta vektorizace měřena na 3+1 mapách.

### Příště (po Sez. 132)
1. **Poledníkový detektor** (nález Sez. 132) — svébytný modul, soustava ne osamělá linie; ověřit, zda gen render
   poledníky kreslí do X párů.
2. **Png2Line krok 2** — dashed **508** + **516** fence; gap-bridging u junkcí (tříští toky). CUDA okno.
3. **417 re-trénink** (carry Sez. 129).
4. **A2b geometrická augmentace** (warp X i Y, Y nearest).

## Sezení 133 — Png2Line krok 2 (508+516) → měřením ZAVRŽEN, revert na watercourse-only (HAL3000)

Fokus (Příště #2 ze Sez. 132): Png2Line krok 2 = dashed **508** narrow ride + **516** fence. Stroj HAL3000
(CUDA + korpus). KPI generátoru beze změny **58,6 %** (dnešní práce je reconstructor, ne `generate_map`).
Volba scope (uživatel): oba 508+516 najednou (`N_LINE 2→4`). **Výsledek: měření zavřelo krok 2 v této
podobě — revert na krok 1 watercourse-only.** Hodnotný negativní nález (měření-first ušetřilo stavbu na
vadném základu, jako forest-age archiv).

### Measure-first pokrytí (strop tréninku NENÍ problém)
Probe `temp/probe_line_codes.py` přes 207 párů (objekty, ne symbol-definice): **508 = 3815 obj / 198 map,
516 = 2176 obj / 160 map** — oba hojnější než watercourse (304+305 = 5732 / 182). Generátor je kreslí
(508 z `--rides`, 516 jako plot kolem RÚIAN privátních oblastí 520). Dat dost.

### Pipeline (vše izomorfní s krokem 1, Sez. 130–131)
- **Registr** `LINE_CLASSES` (`generator/omap_raster.py`, SSoT) +`narrow_ride` [508] +`fence` [516] → `N_LINE 4`,
  conf_thr 0,5 (sweep až po tréninku); `LINE_LABEL_VIS` doplněn (ride tmavá / fence oranžová pro verify).
- **Re-tile** (`tile.py --allow-missing`, 1 mapa `1192962` bez páru přeskočena): 10093 train dlaždic,
  class% narrow_ride 0,409 / fence 0,389 (~watercourse 0,611, ne extrémně vzácné), median-freq 1,25 / 1,31.
- **`eval_real.py` zobecněn** ze single-class (watercourse) na **per-class loop** přes všechny foreground
  třídy + per-class souhrn (průměr přes mapy s neprázdným GT). **Toto vylepšení revert PŘEŽÍVÁ** (funguje
  pro N_LINE=2 i víc, DRY).
- **Overfit gate PROŠEL** (`--overfit`, 2 mapy s plným pokrytím {1,2,3}, 80 ep): train mIoU 0,857,
  watercourse 0,82 / **narrow_ride 0,83 / fence 0,78** — všechny tři synteticky naučitelné.
- **Plný trénink** (`--seed 0`, 40 ep): best ep 36, val mIoU 0,462, **test mIoU 0,499** (watercourse 0,37 /
  ride 0,33 / fence 0,32). Watercourse synt klesl 0,55→0,37 = třídy si konkurují o kapacitu.

### Reálný transfer (`eval_real`, 3 kartografovy skeny) — DVA negativní nálezy
| třída | reálný IoU | completeness | correctness | F1 |
|---|---:|---:|---:|---:|
| watercourse | 0,230 | 0,96–0,98 | 0,37–0,54 | 0,623 |
| narrow_ride 508 | 0,089 | **0,08–0,20** | 0,32–0,66 | 0,223 |
| fence 516 | 0,045 | 0,00/0,00/0,67 | — | 0,141 |

Per-class pred vizuál (Bedřichovka): watercourse hustá modrá síť (recall + přestřel), 508 jen útržky
(netrasuje), 516 vyplněné bloky kolem zástavby (jen Bedř, jinde pred 0).

### conf_thr sweep (`temp/sweep_line_thr.py`, measure-first PŘED rozhodnutím) — rozhodující
Inference RAW prob 1× per sken, pak prahy levně. Mřížka 0,50–0,99.
- **watercourse**: monotónně roste s prahem, optimum **thr 0,99** → IoU 0,311 / F1 0,701. Vyšší práh pomáhá,
  **ale i tak pod krokem 1** (single-class 0,409 / 0,773). Multi-class watercourse trigger-happy = degradace
  REÁLNÁ, ne jen prahová (−24 % IoU).
- **508 narrow_ride**: optimum **thr 0,50** (nejnižší!), F1 0,223. completeness **strop 0,14** — recall
  prahem nezvedneš (demote jde jen dolů). Model dashed průseky na reálu netrasuje.
- **516 fence**: optimum 0,99, F1 0,189. completeness **strop 0,18–0,22**. Stejný recall problém.

**Závěr:** krok 2 = čistá ztráta. (1) 508/516 mají DOMÉNOVÝ GAP (recall strop 0,14–0,22, prahem
neřešitelný — vzor 210: přerušované/málo výrazné symboly se nepřenášejí). (2) Multi-class ZHORŠIL
watercourse. Hypotéza „víc kontrastních tříd → lepší diskriminace tok vs cesta" se **nepotvrdila**.

### Revert (volba uživatele „revert + zaznamenat nález") — ověřený
- `omap_raster.py` → `N_LINE=2` watercourse-only; negativní nález zapsán do komentáře registru (proč zavržen).
- Kanonický model = krok 1 `runs/krok1_watercourse_s0` (n_line=2, test_miou 0,774) promotnut zpět.
- **`eval_real` verify: watercourse IoU 0,409 / F1 0,773** (= Sez. 131, ŽÁDNÁ degradace, revert čistý).
- `line_tiles` re-tiled zpět na N_LINE=2 (konzistence s registrem, no stale state).

### Nález pro budoucí krok 2 (až bude řešen)
Dashed linie (508/516) potřebují JINÝ přístup než prosté přidání třídy: **(a)** verify gen renderu dashed
vs reálný kartografův sken (render-mismatch?); **(b)** dashed-specifická augmentace (přerušení v X);
**(c)** morfologické přemostění přerušení před segmentací. Souvislá modrá watercourse je distinktivní
(barva) → přenáší; černá přerušovaná 508 splývá s cestami/rastrem (jako 210 vs 204 u bodů).

### Ověření
- **18/18 testů OK** (test_vectorize 6 + checkpoints 2 + cut 5 + north_grid 4 + purple 1).
- Net kódová změna: `generator/omap_raster.py` (registr revert + nález) + `model/png2line/eval_real.py`
  (per-class loop, zůstává). Vše ostatní (line_model/line_tiles) regenerováno do krok-1 stavu.

### Příště (po Sez. 133)
1. **Poledníkový detektor** (nález Sez. 132) — svébytný modul, soustava ne osamělá linie; ověřit, zda gen
   render poledníky kreslí do X párů.
2. **417 re-trénink** (carry Sez. 129) — příčina přestřelu = řidší/distinktivnější injekce 417.
3. **A2b geometrická augmentace** (warp X i Y, Y nearest).
4. **Png2Line krok 2 v2** (až s nálezem) — dashed transfer vyřešit JINÝM přístupem (viz „Nález" výše),
   ne prostým přidáním třídy.

## Sezení 134 — Poledníkový detektor: ověření + data-driven rozestup (HAL3000)

Fokus (Příště #1 ze Sez. 133): poledníkový detektor. **Nález při startu:** detektor už mezi-sezeními
napsal Codex (commit `ac953ab`, dnes 11:37, co-author Codex) — `model/png2line/north_grid.py` (211 ř.) +
integrace do `vectorize_omap.py` + 4 unit testy — ale **nepropsal do řídících docs** (TODO/Příště ho pořád
vedly jako „k udělání"). Klasický `parallel-threads-verify-shared-docs` (+ `.codex/` untracked v repu).
Místo slepé re-implementace → ověřit a dotáhnout (zásada „nález agenta ≠ fakt", verify-against-source).

### Ověření Codexova řešení
- **Koncept v souladu se zadáním:** diskriminátor = členství v pravidelné rovnoběžné SOUSTAVĚ (≥3 čáry,
  konst. rozestup), ne naivní „rovná ∧ modrá ∧ ‖ sever" → osamělý kanál ‖ severu se nemaže (unit test to hlídá).
- **Reálný transfer (Buschdörfl, scratch `temp/probe_buschdorfl_north_lines.py` nad existující
  `vecline_source_livelox.omap` 143 polyline):** filtr najde **5-liniový grid, úhel 77,4°** (= pod grivací,
  ne 90° — zadání předpovědělo „grivace ≠ 0"), **rozestup 29 958 µm / std 75 µm** (extrémně pravidelný),
  odstraní **27 fragmentů** (10 dlouhých seedů + 17 krátkých přerušených). Vizuál (source overlay/crop nad
  reálným skenem): červené = jen rovné diagonály v gridu, klikaté modré toky bez překryvu = **vody zachovány**.

### Otázka gen renderu (Příště #1, druhá půlka) — ZODPOVĚZENO: NE
Generátor poledníky **nekreslí** (`grep generator/*.py` prázdný; vizuál `maps/Buschdörfl/rgb.png` je nemá,
reálný sken `Buschdörfl.jpeg` ano). Grivace je jen **georef metadata** (`.omap` declination, OOM natočí
zobrazení; geometrie i rastr zůstávají v S-JTSK gridu). → **doménový gap**: Png2Line poledníky v tréninku
nikdy nevidí → na reálném skenu je bere jako watercourse. → post-vectorization filtr je legitimní náplast
(alternativa „učit model je ignorovat" by chtěla kreslit poledníky do gen renderu jako zvláštní třídu —
větší a koncepčně spornější práce, neděláme).

### Oprava: rozestup data-driven (volba uživatele)
Codex zafixoval `expected_spacing_um=30 mm ±7` (okno 23–37 mm). Na Buschdörfl vyšlo **shodou** (1:10000 +
poledníky po 300 m = 30 mm), ale 1:15000→20 mm / 1:7500→40 mm = mimo okno → **tiché selhání** (grid se
nenajde, poledníky zůstanou jako voda; porušení `no silent fallback`). Zadání IDEAS navíc chtělo periodu
**z dat** (autokorelace/FFT). Fix: `_spacing_runs` odvodí periodu jako **medián mezer mezi clustery** +
relativní toleranci (`spacing_rel_tol=0,25`, `min_spacing_um` sanity floor); `NorthGridConfig` zbavena fixní
konstanty. **Buschdörfl výsledek beze změny** (produkční modul: 27 odstraněno, 5 linií, 77,37°, 29 958 µm),
nový test `test_grid_with_non_default_spacing_is_detected` (20 mm grid = scénář 1:15000) doloží účel napříč
měřítky — fixní okno by ho minulo.

### Drobnost + ověření
- Odstraněn `from __future__ import annotations` z `north_grid.py` (jediný png2line modul s ním; Codex porušil
  `python314-no-future-annotations`). Import + testy OK (Python 3.14.3).
- **19/19 testů** (test_north_grid 5 + vectorize 6 + cut 5 + purple 1 + checkpoints 2).
- **Generator() netknut** (změny jen `model/png2line/`) → mapy se nepřegenerovávají.
- `.codex/hooks.json` (Codex Stop beep 880 Hz) NENÍ regrese dvojího zvonění — je to config separátního
  nástroje, náš service-bell žije v `~/.claude`; necommitovat (untracked, cizí nástroj).

### Příště (po Sez. 134)
1. **417 re-trénink** (carry Sez. 129/133) — příčina přestřelu = řidší/distinktivnější injekce 417.
2. **A2b geometrická augmentace** (warp X i Y, Y nearest).
3. **Poledníkový detektor — napojit do produkční cesty?** Dnes filtr volá jen `vectorize_omap.main`
   (verify nástroj); zvážit, jestli patří i do `eval_real`/budoucí inference. + ověřit na 2. reálné mapě
   s poledníky (jediný doložený případ je Buschdörfl).
4. **Png2Line krok 2 v2** (až s nálezem) — dashed transfer jiným přístupem.

## Sezení 135 — 417 re-trénink: řidší injekce (příčina přestřelu) → reálný transfer + audit kódu (HAL3000)

Dvě fáze. **(1) %AUDIT:CODE** (commit `a1cc450`, 19:55) — mrtvý kód, konvence, docs-drift, struktura; nálezy
a opravy v commitu. **(2) 417 re-trénink** (tento záznam) = Příště #1 ze Sez. 134, dotažení PŘÍČINY 417 přestřelu
(Sez. 129 řešil jen symptom prahem). KPI generátoru beze změny **58,6 %** (práce na straně reconstructoru).

### Příčina 417 přestřelu = příliš hustá injekce (Sez. 129 symptom → teď cause-fix)
Sez. 129 srazil 417 přestřel per-class prahem (`peak_thr=0,45`) — symptom. Příčina: injekce 417 do tréninku měla
`n_range (20,60)` ⌀40 symbolů/dlaždici, ale **reálná hustota** (měřeno z eval_real GT/plocha) je Velbloud ~20 /
Blatná ~12 / Bedř ~12 → staré ⌀40 = **2–3× nad realitou** → model fíroval 417 na porost (FP). Umělá hustota kvůli
focal balancu (jako 204) je dnes zbytečná: **focal prior bias init** (Sez. 125) tu roli převzal. Fix `inject.py`:
`n_range` 417 **(20,60) → (10,30)** (419 ponecháno — transfer funguje).

### Re-trénink (`train.py --seed 0`, run `s135_417sparse_s0`)
40 epoch, batch 16, lr 1e-4, BF16, resnet34 U-Net, 4 třídy 204/210/417/419, cosine LR. Čistá konvergence, žádný
overfit (val křivka plochá). **best ep 35 val mF1 0,861 / test mF1 0,856** (synt): 204 0,83 / 210 0,77 /
**417 0,90 (R0,98)** / 419 0,93. Obava „řidší injekce srazí recall" se NEPOTVRDILA (417 synt recall 0,98).
Promotnut (`--promote s135_417sparse_s0`); záloha předchozího kanonického = `unet_best_presez135_baseline.bak`.

### Reálný transfer (`eval_real`, 3 kartografovy skeny) — cíl experimentu
| sken | 417 F1 | P | R |
|---|---:|---:|---:|
| Velbloud | 0,549 | 0,664 | 0,468 |
| Blatná | 0,484 | 0,386 | 0,650 |
| Bedřichovka | 0,573 | 0,651 | 0,511 |

**Verdikt: cause-fix POTVRZEN, ale zisk vůči symptom-fixu je neutrální (poctivě).** 417 reálné F1 0,48–0,57
(medián 0,55) = nad syrovým Sez. 128 pásmem (0,40–0,49), ale ~na úrovni Sez. 129 prahového baseline (0,52–0,55).
Precision na **horní hraně** starého rozpětí (0,65 na Velbloud/Bedř), Blatná P drží nízko (0,386). **Hodnota není
v metrickém skoku, ale v principu:** model je teď trénován na REÁLNÉ hustotě (ne 2–3× přefoukané) bez recall
kolapsu — čistší základ než prahová náplast (a obě řešení se sčítají, eval běží s `peak_thr` registrem). Ostatní
třídy dle dokumentace (injekce neměněna): 419 0,63–0,74, 204 0,44–0,71, 210 kolaps (Bedř 0,00) = samostatný problém.

### Ověření
- Log byl celou dobu prázdný = **blokově bufferovaný stdout** (`python … > log` nebufferuje po řádcích, flush až
  na konci/ukončení). Trénink přitom zdravě běžel (GPU 99 %, proces živý). Příště `python -u` / `PYTHONUNBUFFERED=1`
  pro živé epochy.
- Promote ověřen (`unet_best.pt` přepsán z `runs/s135_417sparse_s0/best.pt`); revert = `.bak` (pojistka, nevyužita).

### Příště (po Sez. 135)
1. **A2b geometrická augmentace** (warp X i Y, Y nearest) — carry Sez. 123/132.
2. **Poledníkový detektor — napojit do produkční cesty** (`eval_real`/inference) + ověřit na 2. mapě s poledníky.
3. **Png2Line krok 2 v2** (až s nálezem) — dashed transfer jiným přístupem.
4. **417 multiseed potvrzení** (volitelné) — dnešní cause-fix je single-seed (s0); s1/s2 by zatvrdilo závěr.
