# Diář 2026-05-27

## Sezení 24 — Katalog ZABAGED→ISOM (149 vrstev) + el. vedení (510) + dvě fáze (pseudorealistic)

Dvacáté čtvrté sezení. Přechod **ntbhej → mrkla**. `%BEGIN`: git pull +29 commitů (ntbhej
odpracoval Sez. 16–23), fast-forward, `.venv` na mrkla OK, `connectors/` na místě. Audit cadence
nic due (CODE/DOCS naposled Sez. 19, pruning/CALIBRATE Sez. 17). Fokus z „Příště" Sez. 23 = bod 1
(chybějící vrstvy ZABAGED, start el. vedení).

### Diskuse / co se dělalo

- **Katalog VŠECH 149 vrstev ZABAGED Polohopis → ISOM.** Uživatel: „vytvoř tabulku, jaké objekty
  (všechny!) jdou z mapových služeb získat; u každé jak jsme mapovali na ISOM, resp. proč ne."
  Verify-against-source na obou koncích: vrstvy z `GetCapabilities` (149 typů), geometrie z
  `DescribeFeatureType` (57 bodů / 45 linií / 47 ploch), ISOM kódy z `template_classic.omap` (plná
  knihovna) — nic z paměti. Nový `docs/kb/zabaged-isom-catalog.md` (13 tematických sekcí, každá
  vrstva = ISOM symbol nebo důvod nepoužití; ✓9 / ◐ kandidáti / ○ možné / ✗ ~100 mimo doménu).
- **El. vedení `--powerlines real`** (první kandidát z katalogu). `Elektrické_vedení` (linie) → ISOM
  **510 Power line**. Implementace izomorfní s vodou/budovami napříč `zabaged.py` + `generator.py`
  + `omap_export.py` (fetch, `map_powerline_to_isom`, `mode "powerline"`, `mask_powerlines.png`,
  `.omap` liniový objekt, CLI, meta).
- **Koncepční reframe uživatele — dvě oddělitelné fáze** (po mé chybě s příčkami): real-větev má
  (1) **fázi 1 = projekci** (deterministický převod tvrdých dat, 100% věrnost) a (2) **fázi 2 =
  pseudorealistickou dekoraci** (doplní symboly, co v datech nejsou, ale dělají mapu „orienťácky
  vypadající"). Parametr **`pseudorealistic`** (default `True`; CLI `--only-real` = `False`),
  per-vrstva. Zapadá do reframe projekce/predikce (Sez. 23). `%THINK` přes dosavadní vrstvy:
  všechny jsou čistá projekce (`pseudorealistic` je u nich no-op), budoucí hlavní konzument fáze 2
  = **vegetace** (gate → predikce, UC5). Generalizace L1/L2 budov = fáze 1 (věrná kartografie).

### Verify-against-source nálezy (oprava zděděných předpokladů)

- **El. vedení = ISOM 510, NE 516.** 516 v template = **Fence** (plot). Mylné „516 Power line" se
  táhlo 4 dokumenty (TODO `[!]`, Příště Sez. 23, `data-sources.md`, komentář `zabaged.py`). Chyceno
  dřív, než vznikl řádek kódu. Oprava 516→510 propsána napříč.
- **`Most` = linie, ne bod** (komentář `zabaged.py` tvrdil „most = bodový objekt"). DescribeFeatureType:
  linie → ISOM 512. Opraveno.
- **`NAPETI`/`NAZEV` prázdné** (Soví vrch 7 linií, Č. ráj 2) → nelze rozlišit 510 vs 511 Major power
  line → vše 510 (KISS, jako budovy 521).
- **Sloupy mají polohu** (`Stožár_elektrického_vedení`, bod; 11 na Sovím vrchu, leží na vrcholu vedení).

### Příčky symbolu 510 = sloupy (dvě fáze)

Uživatelův doménový fakt (změnil mou oponenturu): na OB mapě příčky vedení **odpovídají sloupům,
běžci se jimi řídí** — nejsou to libovolná dekorace symbolu. Proto:
- **Fáze 1** (vždy): příčka na poloze REÁLNÉHO sloupu (`fetch_powerline_masts`, `_nearest_seg` +
  `_draw_tick_at`, orientace dle nejbližšího segmentu).
- **Fáze 2** (`pseudorealistic`): linie BEZ jediného evidovaného sloupu → rovnoměrné příčky
  (`_draw_perp_ticks`, dekorace; poloha vymyšlená). Linie se sloupy zůstanou poctivě jen se sloupovými.
- Verify: Soví vrch (11 sloupů kryje všech 7 vedení → fáze 1 = fáze 2, žádná dekorace); simulace
  „0 sloupů" → rovnoměrné příčky všude (fáze 2 ověřena). `--only-real` dá holé linie + sloupy.

### Kód

- `connectors/zabaged.py`: `POWERLINE_LAYERS` + `POWERLINE_MAST_LAYERS`, `fetch_powerlines`,
  `fetch_powerline_masts`, `_geom_to_points`, `map_powerline_to_isom` (→510). Oprava komentářů (516→510, most=linie).
- `sandbox/generator-poc/generator.py`: konstanty 510, `_draw_tick_at` + `_nearest_seg`, `_draw_perp_ticks`
  (DRY přes `_draw_tick_at`), `mode "powerline"` (holá linie), `_draw_powerline`, `_generate_real_powerlines`
  (fáze 1 sloupy + fáze 2 dekorace), `--powerlines` + `--only-real`, `pseudorealistic` v `generate`/`_build_meta`.
- `sandbox/generator-poc/omap_export.py`: kód 510 v `USED_CODES`, `powerline_features` parametr `write_omap`.
- Docs: nový `zabaged-isom-catalog.md`; `data-sources.md` (sekce vedení + odkaz katalog), `generator-procedural.md`
  (§0b dvě fáze + §4.9c vedení), `GLOSSARY.md` (el. vedení 510, pseudorealistic), `README.md`, `TODO`/`DONE`.

### Úklid (na žádost uživatele)

- **`temp/` konvence**: dočasné výstupy (`output_*`, WFS capabilities/schema, diagnostiky, scratch
  rendery) → `temp/` (přidáno do `.gitignore`); kořen i generator-poc vyčištěny.
- **lasertool ponechán** (gitignored, 13 MB; budoucí nástroj pro vegetaci z multi-echo LiDARu — gate
  zavřená, teď ho nepotřebujeme, generátor jede z DMR rastru).
- **generator-poc cleanup**: scratch output adresáře smazány; kanonické `Soví vrch/` (3,3 km², 253 obj
  / 7 vedení) + `Nová louka/` (50.8140386, 15.1579069; 6×4 km, 1079 obj / 3 vedení). Oba gitignored.

### Kudos / Censure

- **Censure! (AI)** — příčky vedení jsem nejdřív vykreslil rovnoměrně (vymyšlená poloha) bez označení,
  že nejde o data. Uživatel: poloha příček = sloupy, nevymýšlet → reframe na dvě fáze. Lekce: co se
  vymýšlí pro vzhled, patří do fáze 2 a musí jít vypnout. → paměť `pseudorealistic-phases`.
- **Kudos! (uživatel — koncept dvou fází)** ostrá hranice projekce (tvrdá data) vs pseudorealistická
  dekorace; obecnější než vedení (řídí budoucí vegetaci).
- **Kudos! (verify-against-source)** template chytil 516→510 a `Most`=linie dřív, než vznikl kód.

### Příště

1. **Další kandidáti z katalogu** (◐, dle vizuální návratnosti): `Most` (512, linie) + `Lávka` (512.2),
   `Osamělý_balvan`/`Skalní_útvary` (204/201 — jádro lesního OB), `Lesní_průsek` (508), bodové orient.
   prvky (kříž 530 / mohyla 526 / bunkr / věž 524), `Zeď` (513).
2. **Pravoúhlost budov (L1 tvar)** — uživatelův fokus odložený od Sez. 22 (orthogonalizace footprintu).
3. **Přejmenování `generate()` → `synthesize_pseudorealistic_map()`** — samostatný refaktor.
4. *(fáze 2, dlouhodobě)* vegetace jako pseudorealistická/predikční vrstva — až bude UC5 model.

---

## Sezení 25 — Refaktor `synthesize_pseudorealistic_map` + dev lokality (SV/NL/LS 6×4) + WFS limit nález

Dvacáté páté sezení, mrkla (bez přechodu). `%BEGIN`: sync 0/0 (origin = HEAD `49fa20d`).
Audit cadence nic due (CODE/DOCS naposled Sez. 19 = 6 sez; pruning/CALIBRATE Sez. 17 = 8 sez;
prahy 8/10/12/15 nepřekročeny). Žádná stale „Příště". Z Příště Sez. 24 zvolen **bod 3 = refaktor**.

### Diskuse / rozhodnutí

- **Volba signatury (oponentura).** Uživatel chtěl `pseudorealistic_map(X, Y, W, H, only-real)`.
  Oponoval jsem: ta signatura není refaktor — tiše zahazuje noise (Option 1) větev (`seed/rug/det`),
  per-vrstva toggly i `out_dir`; navíc `pseudorealistic_map` je podstatné jméno (funkce *koná*) a
  `only_real=True` pseudorealismus vypíná → název by popíral parametr. **Dohoda:** sloveso
  `synthesize_pseudorealistic_map(lat, lon, w_km, h_km, only_real=False, out_dir="output", *, …)`;
  hlavních 6 parametrů vepředu, noise/toggly **zachovány** jako keyword-only ocas („kdyby"). `lat/lon`
  (WGS84) místo navrhovaného `n/e`. Default `terrain` překlopen `noise→real` (jinak by funkce jménem
  „pseudorealistic" defaultně dělala noise a souřadnice ignorovala).
- **`only_real` jen na hranici.** Interní `pseudorealistic` (doménový pojem, GLOSSARY) zůstal — na
  začátku funkce `pseudorealistic = not only_real`. Tím se `_generate_real_powerlines`/`_build_meta`
  neměnily (minimální diff, DRY). `_apply_extent(w_km, h_km)` přesunut z `main()` dovnitř funkce.
- **Dev lokality (A2 uživatele „jediná funkce na všechny tři").** `DEV_LOCATIONS` {SV/NL/LS} +
  `DEV_W_KM/H_KM=6/4` + CLI `--location KÓD` (DRY zdroj souřadnic; dřív ad-hoc v hlavě/diáři). Všechny
  3 lokality **6×4 km** (uživatelův požadavek).
- **Lidové sady = classic ISOM (oponentura sprintu).** `template_sprint.omap` je ISSprOM, ale celý
  generátor stojí na ISOM (omap_export classic, ZABAGED→ISOM 502/521/510). Skutečná sprint mapa =
  nový balík (přepínání template + přemapování ISSprOM + ladění na město). LS teď jako classic ISOM
  (natrénuje i hustou zástavbu, A1 uživatele); **ISSprOM/sprint pipeline → IDEAS** (samostatné sezení).
- **Sázka uživatele:** keyword-only ocas (noise/toggly) jednou zmizí. Pravděpodobně ano (real-prediktor
  = hlavní směr po reframe Sez. 23), ale noise má i pak hodnotu jako **offline regresní baseline** (proc
  65 bez WFS) → spíš degraduje na test fixture. → IDEAS „Zánik noise větve".

### Kód

- `generator.py`: `generate` → `synthesize_pseudorealistic_map`, nová signatura, `_apply_extent`
  dovnitř, `pseudorealistic = not only_real` na hranici. `DEV_LOCATIONS`+`DEV_W_KM/H_KM`. `main()`:
  `--location`, CLI defaulty `terrain/paths/water/buildings/powerlines = real`, `_apply_extent` z main pryč.
- `batch.py`: obě volání na nový název (noise = DEF extent + proc/off → baseline drží; real = paths/water/
  buildings, bez vedení = zachované chování). Import `DEF_*` z generator.
- `diagnose_displacement.py` **nedotčen** — `generate()` nevolá (jede přes `fetch_*` přímo; odhad v
  TODO/Příště o „referenci v diagnose" se nepotvrdil).

### Verify (ve `.venv`)

- **noise baseline 65 drží** přesně (56 vrstevnic + 2 cesty + 7 bodů, grid 170×116 = default extent).
- Real 6×4 km (grid 696×464, `--location`, `layer_errors: None`): **SV** 2689 obj, **NL** 1079 obj,
  **LS** 3701 obj. Vizuály sedí na terén (SV lesní + obce; NL Jizerky + jezírka; LS Liberec městsko-lesní).

### WFS limit — nález (verify-against-source změnil plán)

- **SV i LS 6×4 km = přesně 1000 budov** (NL les 124, pod stropem) → podezření na server-side limit.
- **Probe `temp/wfs_probe.py`:** `count=5000` strop NEzvedl (ArcGIS „max records" = tvrdých 1000);
  `startIndex` paging NESPOLEHLIVÝ (`startIndex=500→0`, `=1000→78` — anomálie). Tj. **NE malá změna**,
  jak jsem nejdřív odhadl. Robustní fix = spatial tiling (dělit bbox + dedup) nebo přechod WFS→ArcGIS
  REST `MapServer/<id>/query` (`resultOffset`+`returnCountOnly`). **Odloženo** → TODO `[!]`: bije hlavně
  hustá města (= sprint doména, odložená), lesní ISOM lokality jsou pod stropem.

### Censure / Kudos

- **Censure! (AI):** WFS fix jsem nejdřív označil za „malou změnu (count/startIndex)" — vyvráceno až
  verify-against-source (ArcGIS WFS paging rozbitý). Měl jsem ověřit chování zdroje, než ho prohlásím
  za snadný. → paměť `arcgis-wfs-1000-limit`.
- **Kudos! (uživatel):** trvání na oponentuře signatury (zachovat noise „kdyby") + ostrá hranice
  classic ISOM vs ISSprOM sprint (foundations — nemíchat dvě spec v jednom sezení).

### `%END` úklid

- **Nové pravidlo → `docs/PROMPTS.md`:** maž jen scratch (`temp/`, `output_*/`); cache reálných dat
  (`.dmr_cache`, `.zabaged_cache`) i `__pycache__` NECH (regenerovatelné, ale zrychlují — mazat = re-fetch).
  Vzniklo z dotazu uživatele na `.dmr_cache` (1,5 MB, gitignored).

### Příště

1. **Pravoúhlost budov (L1 tvar)** — uživatelův fokus 2× odložený (Sez. 22→25, orthogonalizace footprintu).
2. **WFS limit fix** — rozhodnout spatial tiling vs přechod na ArcGIS REST (po průzkumu REST endpointu).
3. **Další kandidáti z katalogu** (◐): `Most` 512, balvany 204/201, `Lesní_průsek` 508, bodové prvky, `Zeď` 513.
4. *(dlouhodobě)* ISSprOM/sprint pipeline (LS); vegetace jako pseudorealistická/predikční vrstva (UC5).

---

## Sezení 26 — Ortofoto podklad + WFS→REST (města kompletní) + asset pattern + reálné řopíky

Dvacáté šesté sezení, mrkla. Dlouhé, silně uživatelem řízené — fokus se vyvíjel
(ne z „Příště" Sez. 25): nejdřív zobrazit 3 mapy → **ortofoto podklad** → **WFS→REST** →
**asset pattern + řopíky**. Audit cadence: CODE/DOCS Sez. 19 (7 sez, CODE LOC práh se blíží),
pruning/CALIBRATE Sez. 17 — nic přes práh+2.

### Co se dělalo (chronologicky)

1. **Tři mapy SV/NL/LS 6×4 km** — na disku byl jen NL 6×4 + SV malý (default extent);
   přegenerováno všech tří přes `--location` (kanonické outputy se mezi stroji nepřenášejí).
2. **Ortofoto podklad (3a, „verify generátoru proti realitě")** — uživatelův návrh: PNG výseku
   georeferencovaně připnout do OOM jako podklad. **`connectors/ortofoto.py`** (ČÚZK ORTOFOTO
   MapServer na **3. instanci `arcgis1`**, S-JTSK 5514, CC BY 4.0, sdílí `build_bbox`; **dlaždicování**
   nad strop 4096 px → libovolné rozlišení). Generátor `--ortho`/`--ortho-mpp` (default 0,5 m/px)
   ukládá `ortofoto.png`; `omap_export` připne **podkladový `<template>`** do `map.omap`
   (paper-space, x=y=0 jako objekty, scale = map-mm/px, opacity 0,5; first_front_template=1 = pod
   mapou). **Formát `<template>` ověřen proti reálnému OOM 0.9.6 výstupu** (uživatel ručně připnul →
   odečten XML; WebFetch dal jen parafrázi → nehádat). Verify: SV 0,5 m/px sedí pixel-přesně, i v rozích.
3. **WFS→REST fix** (řešení nálezu Sez. 25) — `zabaged._fetch_layer` přepsán z WFS GetFeature na
   ArcGIS REST `MapServer/<id>/query` + **sériová paging smyčka** (`resultOffset += 2000`). Verify
   (`temp/probe_rest_paging.py`): REST strop 2000, `resultOffset` SPOLEHLIVÝ (overlap 0), `returnCountOnly`.
   `LAYER_IDS` (typeName→numerické ID, ověřeno katalogem). **Oprava `typuskom_k`** (REST malými, WFS
   velkými — chyceno verify Pěšiny PŘED implementací → zabráněn regres 505→506). Cache prefix `zbg_rest_`.
   **Dopad:** SV budovy 1000→**1078**, **LS 1000→8273 budov + 3951 cest** (88 % Liberce dosud chybělo).
4. **Asset pattern** (uživatelův návrh `.omap`) — vyjasněn způsob práce s assety: dvojice
   **`<jméno>.omap`** (vizuální vzor kreslený v Mapperu, přesná geometrie + ISOM symboly) +
   **`<jméno>.rules.xml`** (pravidla, co `.omap` nepojme). `asset/řopík_10000.omap` (1 budova 521 +
   1 vrstevnice 101, pročištěno z 60+4 duplikátů) + `.rules.xml` (`rotation_rule`, `draw_order`, `source`).
5. **Reálné řopíky na SV** — vrstva ZABAGED **`Bunkr`** (id 37, `typbunkr_k='LO37'` = lehký objekt vz.37),
   **70 bodů** na SV. **Orientace = NORMÁLA na lokální linii řopíků** (PCA okolí; nápad uživatele,
   elegantnější než hledat hranici). Post-proces `temp/place_ropiky_real.py` vložil 70 asset-řopíků
   rotovaných dle normály do SV map.omap.
6. **Měřítko fix** — `omap_export` přepisuje georef template (15000) na `MAP_SCALE` (10000); nesoulad
   (Sez. 26 side-finding) opraven u SV/NL/LS.
7. **Displacement práh** (`MAX_DISPLACE_BUILDINGS=2000`) — budova↔budova je O(n²), na LS (8273) neúnosné
   → nad práh se přeskočí (efekt 0,4 mm zanedbatelný). Odblokovalo LS (doběhla za minuty).

### Rozhodnutí

- **Verify generátoru, ne georef mapy** (volba uživatele) → paper-space template (KISS), ne plná S-JTSK georef.
- **ČÚZK ortofoto, ne Google/Mapy** (licence CC BY 4.0 + kvalita 0,2 m; Google/Mapy ToS zakazuje stahování).
- **WFS→REST varianta (ii)**, ne spatial tiling — REST paging spolehlivý (verify), bez dedup logiky.
- **Orientace řopíků z linie, ne z hranice** (uživatel) — intrinsická, univerzální. Disambiguace strany
  („ven") = směr k nejbližší státní hranici (`Hranice správní jednotky` `vyzn_zsh_k=1`) — UNIVERZÁLNĚ pro
  ČR (sever u SV, JV u Šumavy); zatím zadrátováno `OUTWARD=sever` → TODO oprava.
- **`%BEEP`** = Stop/Notification hook v `settings.local.json` (manuální `beep()` přes tool je antipattern —
  vyžaduje potvrzení). Aktivace = restart session.

### Censure / Kudos

- **Censure! (AI)** — do `rules.xml` jsem napsal vymyšlený fortifikační fakt („řopíky míří střílnami na
  hranici, běžci se řídí"), navíc OBRÁCENĚ: k nepříteli je bunkr ZASYPANÝ (ochrana před dělostřelectvem),
  střílny do vnitrozemí (boční palba, dvě řady). Opraveno. Lekce: nevymýšlet doménový fakt ani přenosem
  z jiného kontextu (běžci↔vedení Sez. 24). → paměť.
- **Censure! (AI)** — `OUTWARD=sever` zadrátováno = jednoúčelové pro SV (porušuje univerzalitu ČR).
- **Kudos! (uživatel)** — asset jako `.omap`; orientace z normály linie; univerzalita (Šumava JV);
  spatial cutoff pro displacement (analogie Pixelodynamics — ignorovat vzdálené páry).

### Příště

1. **Univerzalita orientace řopíků** — `OUTWARD=sever` → směr k nejbližší státní hranici (univerzální ČR).
2. **Displacement spatial-index / cell-list** (cutoff vzdálených párů jako Pixelodynamics) → O(n²)→~O(n),
   pak displacement i pro hustá města. Pozn.: pravoúhlost budov možná vyžádá přepracování (mění tvar,
   displacement teď předpokládá tuhou translaci).
3. **Řopíky → generátorová integrace** (`--ropiky`: Bunkr LO37 + auto-rotace z linie + asset placement),
   zatím jen post-proces skript. Rozšíření TODO „řopíky ≠ stavby".
4. **Q duplicita** — dořešit s přesným paper→S-JTSK přepočtem polohy markeru (nedořešeno, nehádáno).
5. **Pravoúhlost budov (L1 tvar)** — uživatelův 3× odložený fokus; **%AUDIT:CODE** (hodně přibylo).
6. Další katalog vrstvy (Most 512, balvany 204/201, …); ISSprOM sprint; vegetace (UC5).

---

## Sezení 27 — %AUDIT:CODE + pravoúhlost (zavržena) → budovy RAW + koupaliště + řopíky + logging

Dvacáté sedmé sezení, mrkla. Velmi dlouhé, iterativní. `%BEGIN`: sync 0/0. Smazán stray
`template_sprint.omap`. **Audit cadence: %AUDIT:CODE due** (LOC práh — +1240 insertions od Sez. 19,
ne počet sezení) → první bod.

### %AUDIT:CODE — D1–D5 + K1–K4 (kritické 0)
Kód zdravý (DRY helpery, žádné debug printy). Nálezy opraveny:
- **D1 WFS→REST drift** (~25 míst): po migraci Sez. 26 zůstal popisek „ZABAGED WFS" jako popis
  AKTUÁLNÍHO zdroje (vč. CLI help) — REST teď, ne WFS. Opraveno v `generator.py`/`batch.py`/
  `omap_export.py`/`zabaged.py` hlavičce; historické „Sez. 16 = WFS" + kontrast „WFS měl velkými"
  ponechány. **Docs-půlka (KB) odložena → dořešena v tomto %END.**
- **D2** `batch.py` real `ortho=False` (feeder verify-podklad nepotřebuje).
- **D3** asset `řopík_10000.*` → `ropik_10000.*` (české znaky v názvu porušují „file names: English").
- **D4** smazán `from __future__ import annotations` v diagnose (regrese vůči Py 3.14 pravidlu).
- **D5** `map_to_isom` → `map_path_to_isom` (izomorfismus s map_water/building/powerline).
- **K1–K4** z-order docstring +vedení; omap_export scale 15000→10000; smazán shebang; „WFS/sítě"→„REST/sítě".
- Verify: proc baseline 65 drží.

### Pravoúhlost budov — implementována, pak ZAVRŽENA (návrat k RAW)
Fokus 3× odložený (Sez. 22→25→26). `%THINK`: tvůj „první návrh nejlepší" = dominantní osa + snap
hran na 90°, malé = orientovaný obdélník. **Verify reálných LS footprintů:** 96,4 % délky hran do 15°
od pravého úhlu, 214 genuinních výjimek (atypy/kostely). Tvoje námitka (desítky výjimek) → **tolerantní
snap ±15°** (genuinní diagonály ponechány z konstrukce). Implementováno (`_orthogonalize` + dominantní
osa + slučování stejně-orientovaných hran + rekonstrukce rohů průsečíky), syntetické testy perfektní,
přegenerováno SV/NL/LS. **ALE:** uživatel „budovy stále špatné, voda dokonalá" → klíčový postřeh:
**voda je věrná, protože RAW; generalizace budovy KAZÍ.** Verify budovy 1028994: RAW 15 vrcholů →
generalizace 5 zkomolených. → rozhodnutí **„negeneralizuj budovy, kresli jako vodu"**. Smazána CELÁ
L1 generalizace (DP/min-size Sez. 18 + dnešní orthogonalizace) **i L2 displacement** (Sez. 21-22) —
**~430 LOC**, + `diagnose_displacement.py`. Nový `_generate_real_buildings` = zrcadlo `_generate_real_water`
(raw S-JTSK→grid→px→polygon). **Ponaučení → CLAUDE.md:** generalizuj jen s důkazem, raw je default.

### Koupaliště (#1) — Pozemní_nádrž → 301
Verify-against-source: Lesní koupaliště LS chybělo, protože je v ZABAGED `Pozemní nádrž` (id 107,
`podtypob_k='BA'` bazén, ~1934 m²), NE `Vodní_plocha` (id 132). Přidáno `Pozemní_nádrž` do
`WATER_AREA_LAYERS` + `map_water_to_isom` → 301 (všechny, KISS). LS teď 49 ploch + 9 nádrží.

### Řopíky (#2) — asset placement, fáze 1 projekce
`fetch_bunkers` (Bunkr LO37) + `fetch_state_border` (`Hranice správní jednotky`, `vyzn_zsh_k='1'` =
státní, ověřeno). Asset loader (`ropik_10000.omap`, JEN mapové objekty z `<objects>` — **oprava AI
parsovacího bugu**: nejdřív mylně počítáno 245 „objektů" = grafika symbolů v `<symbols>`; reálně 2 ISOM
objekty 521+101, diár Sez. 26 měl pravdu). Orientace = PCA-normála lokální linie řopíků, „ven" = směr
k nejbližší státní hranici (univerzální ČR; ruší zděděné `OUTWARD=sever`). `--ropiky off|real`, postprod
fáze v `synthesize_pseudorealistic_map`. SV = 70 řopíků (70/70 míří na sever k německé hranici, ověřeno).

### Logging (nápad uživatele z mého ad-hoc výpisu)
`logging` modul (ne print) v `synthesize_pseudorealistic_map`: INFO průběh po vrstvách + finální souhrn
(`budovy N · řopíky N · voda N …`). CLI (`main`) zapne `basicConfig(INFO)`; `batch.py` tichý (nekonfiguruje).
DEBUG připraven. `_try_layer` stderr print → `_log.warning`.

### Censure / Kudos
- **Censure! (AI)** — prohlásil jsem asset za rozbitý (245 objektů) kvůli parsovacímu bugu (počítal grafiku
  symbolů); zbytečně poslal uživatele re-exportovat. Asset byl čistý. Lekce: ověř strukturu .omap správně.
- **Censure! (AI)** — postavil jsem generalizační vrstvu na budovy, místo abych vztáhl „voda je raw a dokonalá"
  hned. Generalizace = víc škody (komolení, lichoběžníky) → revert. → CLAUDE.md + paměť.
- **Kudos! (uživatel)** — „voda dokonalá, budovy ne" = přesná diagnóza (raw vs generalizace); tolerantní snap
  jako řešení výjimek; nápad na logging; trvání na věrnosti raw dat.

### Příště
1. **Železnice** (ZABAGED → ISOM 509, real-půlka; další katalog vrstva).
2. Další katalog: Most 512, balvany 204/201, Lesní_průsek 508, bodové prvky, Zeď 513.
3. *(dlouhodobě)* ISSprOM/sprint pipeline (LS); vegetace jako predikční vrstva (UC5).
