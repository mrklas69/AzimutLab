# Sezení 181 (2026-07-06) — %BEGIN cleanup + Stella bootstrap + lokální testovací mapy (Stella)

Stroj: `Stella` (HP Z2 Mini G1a / Strix Halo), zdroj hardware inventáře `D:\=STELLA\STELLA.md`.
Sezení bylo primárně provozní: dostat nový stroj do stavu, kde AzimutLab umí běžet generátor a má
lokální mapové výstupy pro testování.

### %BEGIN konzistenční cleanup

Starší auditové sirotky v `TODO.md` byly už fakticky vyřešené ve stromu:
`resources/livelox/_curation.json`, `_split.json`, `_cz_filter.json` jsou tracked a runtime/trénink
závislosti jsou rozdělené do `requirements.txt` + `requirements-train.txt`. TODO očištěn, záznam přidán
do `DONE.md`. Zároveň opraven stale řádek v `README.md`: UC5 status už uvádí canonical KPI **68,7 %**
(HAL3000, Sez. 176), ne starých 67,5 %.

### Stella setup

Doinstalováno do obvyklých cest:
- `uv` přes `winget` (nový shell může být potřeba kvůli PATH refreshi),
- OpenOrienteering Mapper `0.9.5` do `C:\Program Files\OpenOrienteering Mapper 0.9.5`,
- QGIS LTR `3.44.11` do `C:\Program Files\QGIS 3.44.11`,
- projektové `.venv` v `D:\=PROJECT\AzimutLab\.venv`,
- runtime závislosti z `requirements.txt`: `numpy`, `Pillow`, `contourpy`, `pyproj`, `certifi`, `scipy`, `pygeomag`.

`pip check` v `.venv` prošel. Tréninkový stack z `requirements-train.txt` nebyl instalován: Stella je AMD
iGPU bez NVIDIA CUDA a AzimutLab má zatím tréninkovou autoritu `mrkla`/HAL3000. Případný HIP/ROCm stack je
samostatné rozhodnutí, ne automatický runtime setup.

Procesní oprava: projektový zvonek se na Stelle přepnul z neexistujícího
`C:\Users\mrkla\.codex\resources\servant-bell-ring.mp3` na
`C:\Users\stell\.codex\resources\needs_input.mp3` a byl reálně přehrán před čekáním na schválení.

### Mapy pro testování

Reálný běh:
- `.\.venv\Scripts\python.exe generator\generator.py --location SV`
- výstup: `maps\Soví vrch\` (`rgb.png`, `Soví vrch.omap`, `bg_dmr.png`, `bg_ortho.png`, masky, `meta.json`)
- `.omap` měl ~15,6k objektů; vizuální kontrola `rgb.png` ukázala neprázdný, čitelný OB render.

Varování při reálném běhu byla hlasitá a akceptovatelná: velký 6×4 km výřez se pro rock detection
downsamploval a dvě DMR rock dlaždice byly mimo ČR/no-data.

Další DEV lokality nebyly dokončeny: `NL` dvakrát spadla při DMR fetchi na
`ConnectionRefusedError [WinError 10061]` (ČÚZK DMR služba aktivně odmítla spojení). To není validace
generátoru ani Stella dependency chyba. Neprohlašováno za úspěšnou plnou regeneraci.

Lokální fallback pro testovací corpus:
- `.\.venv\Scripts\python.exe generator\batch.py --terrain noise --n 8 --out maps\dataset_noise`
- výstup: 8 map `000`–`007`, každá s `.omap`, `rgb.png`, maskami, `meta.json`; `montage.png` zkontrolována vizuálně.

`maps/` je gitignored, takže výstupy zůstávají lokální testovací stav Stelly, ne změna repa.

### Verify

- `.venv\Scripts\python.exe -m pip check` OK.
- Reálný render `maps\Soví vrch\rgb.png` vizuálně zkontrolován.
- Batch mozaika `maps\dataset_noise\montage.png` vizuálně zkontrolována.
- `git check-ignore` potvrdil, že `maps/` výstupy jsou ignorované.
- Kód generátoru/konektorů/exportu se neměnil, proto se `tests\smoke.py` nespouštěl.

### Příště

1. Na Stelle lze hned používat runtime/generator/docs/audity; pro Livelox `pairs.py`/scan-auto chybí gitignored
   korpus, je potřeba privátní sync.
2. Reálné DEV mapy kromě `SV` přegenerovat znovu, až ČÚZK DMR služba nebude odmítat spojení.
3. Trénink UC5 dál patří na `mrkla`/HAL3000, dokud se výslovně nerozhodne AMD HIP/ROCm cesta pro Stellu.
