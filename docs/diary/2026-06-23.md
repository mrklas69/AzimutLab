# 2026-06-23

## Sezení 158 — Png2Point scope +531 (černý man-made X), reálný transfer potvrzen (HAL3000)

Sezení začalo 2026-06-22, hlavní výpočet (3 retrainy + eval) doběhl 2026-06-23.
Autonomní noční běh (uživatel: „jdu spát, posouvej se dál sám, ráno na to kouknu").

### Diskuse
- `%BEGIN`: lokál in-sync `origin/main` (`e0a505a`). Necommitnutá práce paralelního vlákna
  „260621-Buschdörfl" (scan-mining `isom_scan/*`, M `docs/TODO.md`) ve stromu → necháno vláknu,
  commit selektivní (paměť `parallel-threads-verify-shared-docs`).
- Cadence: %AUDIT:CODE (15 sez) + IDEAS/TODO pruning (19 sez) překročené, ale kolidují s živým
  paralelním vláknem (isom_scan kód roste / TODO editován) + HAL3000 = CUDA okno → úklid doporučen
  handoff na ntbhej, ne tady.
- Audit 260621 stav: **A1 (kritická) ≈ vyřešena** (retrain 156 + docs propsány 0,577/0,774), A3 sirotek
  66,2 % trvá (jen v M TODO paralelního vlákna, nikde jinde nepodložené).
- **Volba fokusu (uživatel): CUDA tah na branku → (c) Png2Point expansion, start 531, po jednom.**
  Měření zdůvodnilo směr: přenáší distinktivní body (419 zelený X silný 0,67–0,76), NEpřenáší vzácné
  plochy (404/407/409 halucinace Sez. 156) ani dashed linie (508 doménový gap Sez. 133/156). 531 = černý
  X (mirror 419) → nejvyšší šance reálného transferu (cíl = eval_real, ne KPI; audit A3).
- Self-check ROADMAP: 531 = rozšíření POKRYTÍ (nový TYP bodu) = vytěžení Etapy 1 `Generator()`, ne
  leštění/degradace ✓.

### Provedení / Kód
1. **Měřitelnost PŘED tréninkem** (anti-A1): `temp/probe_point_gt.py` crosswalk-aware histogram bodových
   GT — **531 Velbloud 20 / Blatná 3 / Bedř 3** → měřitelné (primárně Velbloud). Past A1 se nepotvrdila.
2. **Verify-against-source** (`generator/template_classic.omap` id 156): 531 = 2 černé diagonály (color 2,
   `line_width 240`, ±516 µm), **žádný white element** — na rozdíl od 419 (id 104, 4 elementy se 2 bílými
   color 22 = svatozář). → `inject.PointClass` dostal flag **`halo`** (zelené 417/419 ANO, černý 531 NE);
   `_stamp_cross`/`_stamp_tree` ho respektují.
3. **`inject.py`**: `PointClass(531, radius_mm=0.516, sigma_px=3.5, kind="cross", color=černá, halo=False)`
   → **N_POINT 4→5**. Smoke (`temp/probe_inject531.py`): 5 kanálů, render ověřen vizuálně (černý X bez
   svatozáře čitelný na lese, size jitter). Pipeline beze změny tiles (531 injektuje on-the-fly do
   `point_base`); focal bias init pokrývá 5. kanál automaticky.
4. **Retrain 3 seedy** (`s158_531_s{0,1,2}`, 40 ep, batch 16, train 448 / val 96 dlaždic):
   synt test mF1 **seed 0 0,811 / seed 1 0,843 / seed 2 0,759** (medián 0,811).
5. **Sweep `peak_thr` 531** (`temp/sweep_531_thr.py`, izomorf Sez. 129): seed-0 měl recall vysoký /
   precision nízkou (FP) jako 417 před prahem. Velbloud (20 GT) plochá část křivky 0,55–0,70 → **robustní
   0,60** (F1 0,356→0,737). Nastaveno v registru (nemění model, bez re-tréninku).
6. **eval_real 3 seedy** (`POINT_CKPT` env — nově, multiseed bez promote-tance):
   medián 531 Velbloud F1 **0,708**, Blatná 0,400, Bedř 0,133.
7. **Promote seed 0** (medián-reprezentativní) + ruční `.bak` 4-class pojistka.
8. **Capability registr**: 531 `SCAN_POC` → `SCAN_LIVE_POINT` (mám metriku, izomorf 417/419; audit A2).
9. **`eval_real.py`**: `POINT_CKPT` env + 5. overlay barva (531 oranžová).

### Výsledky
- **531 reálný transfer (eval_real medián 3 seedů): Velbloud F1 0,708 = na úrovni 419 (0,74)** —
  distinktivní ČERNÝ man-made X přenáší stejně jako zelený veg X. Blatná 0,400 / Bedř 0,133 (3 GT = šum,
  Bedř navíc víc černých FP). Sázka A3(c) potvrzena.
- **4 staré třídy bez regrese** napříč 3 seedy (eval_real medián): 204 0,68 / 210 0,20 (kolabuje vždy) /
  417 0,48 / 419 0,74 — vše v historickém rozsahu Sez. 129/135. **Bodový scope snese 5. třídu** (na rozdíl
  od liniového 5-class watercourse, který Sez. 156 regredoval).
- Synt test mF1 medián **0,811** (5 tříd) vs starý 0,827 (4 třídy) = mírné ředění vzácnou třídou, ne regrese.
- KPI 65,8 % netknuto (capability je informativní sloupec, scan kandidáti se do KPI počtů nepřičítají).

### Censure / nálezy
- `find -maxdepth 2` minul `gen_pointbase/rgb.png` (hloubka 3) → krátce mylně „0 point_base renderů";
  ověřeno přímým `ls` (rendery existují, 40 map). Lekce: ověř hloubku `find` než usoudíš „chybí".
- seed 0 synt 531 F1 0,62 byl měřen ještě při `peak_thr=0,30` (registr před sweepem); seedy 1,2 při 0,60.
  Trénink (focal) na prahu nezávisí → modely srovnatelné, arbitr je eval_real (vše při 0,60). Nezaměňovat.
- seed 2 produkoval víc 531 FP mimo mapové pole (−283/−300 filtrováno maskou) = mírná seed-citlivost
  okrajových halucinací; po `_map_area_mask` filtru transfer OK (Velbloud 13/20 TP).

### %AUDIT:CODE 260623 (druhá fáze sezení — volba uživatele „3" = úklid)
Cadence %AUDIT:CODE překročen (od Sez. 143 = 15 sez). Handoff na ntbhej PŘEHODNOCEN: jsem na nejsilnějším
modelu (Opus 4.8 = co PROMPTS žádá pro audit), CUDA okno nevyužívám, audit je read-only report → nekoliduje.
**Metoda (vzor Sez. 143):** 4 paralelní agenti (generator/ · model/ · connectors+isom/ · cross-cutting) →
ruční verify klíčových nálezů proti zdroji. `isom_scan/` vynechán (živě mění paralelní vlákno).
**Výsledek: 0 bug-level kritických** mimo split.py no-silent. Plný report `docs/AUDIT_CODE_260623.md`.
- **Opraveno hned** (vlastní čerstvé + jednoznačné docs): 531 stale docstringy (inject/eval_real, commit
  `bb1ed51`) + `AGENTS.md` faktické chyby (`clip_quad.py`→`cut.py` neexistoval, `forest.py` archiv→smazán,
  png2line doplněn, čísla/scope).
- **Tři páky:** **C1** `split.py` tichý drop map, **D1** `capabilities` 306/309/508 `SCAN_LIVE_LINE` lže o scope,
  **D4/D5** dozrálý DRY balíček. **Falešné poplachy zachycené verify:** PX_PER_MM 2× ne 3×; ISOM_REF 2 konzumenti; 531 OK.

#### 3. fáze — audit follow-up + pruning (volba uživatele „2+3")
**C1 + D1 OPRAVENO** (commit `ed5d7cb`, nejvyšší + bezpečné páky):
- **C1** `split.py` no-silent: chybějící mapa v `_cz_filter.json` teď HLASITĚ varuje (stderr); selekce ZACHOVÁNA
  (mIoU split se nerozejde) = bezpečná půlka. Lazy re-probe (změna chování) NEuděláno.
- **D1** `capabilities` 306/309/508 `SCAN_LIVE_LINE`→`SCAN_CANDIDATE` (sladění s revertem Sez. 156, anti-Goodhart);
  test sladěn + 304 assertce. 48 testů OK.
- **D4 (část) HOTOVO** — bezpečné nejvyšší-páková DRY: ImageNet MEAN/STD → `model/norm.py` (commit `c9341b7`,
  train/serve skew fix) + peak helpery → `model/peaks.py` (commit `ffd999c`, train↔eval konzistence). Behavior-preserving
  ověřeno eval_real Velbloud (identická čísla 0,570/0,737). **eval_common (D4 tiled inference) + read_pgw (D5) NEUDĚLÁNO**
  → ranní revize: eval_common = nejrizikovější (3 různé hlavy, riziko rozbít 3 promotnuté eval_real v noci); read_pgw =
  nízká páka + umístění/import-path rozhodnutí (`mpp.read_src_mpp` nelze reuse — čte meta.json ne .pgw).
- **309 revize VYŘEŠENA:** verify — 309 generátor NEkreslí (fantom, na rozdíl od 306/508 ano). Rozhodnutí: ponechat
  v registru (smazání = KPI Goodhart A4 — odebrání nepokryté díry uměle zvedá KPI); `note` upřesněn + opraven nepřesný
  `omap_raster:258` komentář („306/309/508"→„306/508", 309 NEkreslí).
- **Pruning IDEAS:** Png2Line položka zeštíhlena (hotový changelog → nezralé větve) + aktualizace o Sez. 156 (dashed
  2× zavržen). **TODO pruning ODLOŽEN** — `docs/TODO.md` drží paralelní vlákno (M), pruning by kolidoval.

### Příště
0. **Audit follow-up** (`docs/AUDIT_CODE_260623.md`): C1 split.py no-silent + D1 capabilities line scope (souvisí
   s revertem Sez. 156, sladit registr+test) jsou nejvyšší páka; pak DRY balíček D4/D5. Nálezy → TODO až
   paralelní vlákno uvolní `docs/TODO.md`.
1. **Další distinktivní man-made body do Png2Point** (po jednom, izomorf 531): **525 Small tower ⊤**
   (měřitelné Bedř 24 GT) / 527 Fodder rack Λ (8 GT, vzácný). Generátor je už kreslí (Sez. 141), inject
   potřebuje nové `kind` (⊤/Λ). Měřitelnost ověřit probe PŘED tréninkem (anti-A1).
2. **Hnědé terénní body 109/111/112** (Bedř 103/133/69 GT = dobře měřitelné) — jiný kind (kopeček/jamka/
   oblouk), hnědá barva; ověřit, zda hnědá kresba přenáší jako černá/zelená.
3. **Úklid (handoff ntbhej):** %AUDIT:CODE (`isom_scan/` 5159 LOC nezauditováno) + IDEAS/TODO pruning —
   oba překročené, ale kolidují s živým paralelním vláknem; na ntbhej bez kolize a bez plýtvání CUDA oknem.
4. **A3 sirotek 66,2 %** (audit) — paralelní vlákno tvrdí v M TODO, nikde nepodloženo; po commitu jejich
   větve doložit měřením na `KPI_3MAP_CANONICAL` nebo smazat (hlavní vlákno).

## Sezení 159 — Sjednocení repa (mýtus „paralelního vlákna") + Png2Point scope +525/527 (HAL3000)

### Diskuse / sjednocení repa
- `%BEGIN`: lokál in-sync `origin/main` (`c550c1a`). Ve stromu untracked `isom_scan/*` (mark_isoms,
  water_points_poc, calibration_manifest, markers/) + M `docs/TODO.md`/`isom_scan/{README,score.py}` —
  4 sezení (155→158) je „dědila" jako domnělé **paralelní vlákno** a nechávala být.
- **Námitka uživatele „Jaké paralelní vlákno? Sjednoť repo." → ověřeno fakticky: žádné neexistuje.**
  Všech 20 posledních commitů = `Jan Mrklas`, žádný Codex co-author, `.codex/` jen starý `hooks.json`.
  Byla to rozdělaná scan-mining práce hlavního vlákna ze Sez. 154-157, kterou nikdo nezacommitoval
  (manifest `recall null`/`IN_PROGRESS` → vypadalo „nehotově"). Diáře si „vlákno" vymyslely.
- **Sjednoceno** (2 commity + push): `feat(isom_scan)` harness (mark_isoms/water_points_poc/calibration_manifest
  + Hungarian `score.py` greedy→`linear_sum_assignment` + 4 testy/18 case) + `docs(todo)` ledger zaveden;
  **revert nepodloženého KPI sirotka 66,2→65,8** (audit A3/C5 — `separate.py` 407/409 gate ovlivňuje scan
  separaci párů, ne `.omap` KPI počty). Copyright OK (vše textový harness, izomorf `gt/ground_truth.json`).

### Fokus (volba uživatele): CUDA tah → (c) Png2Point expansion 525/527, oba najednou
Self-check ROADMAP: 525/527 = nové TYPY bodů = vytěžení Etapy 1 `Generator()` ✓ (ne leštění). Izomorf 531
(Sez. 158). Volba uživatele: oba najednou (N_POINT 7), strategie „Sez. 158 doložila bodový scope snese 5. třídu".

1. **Měřitelnost PŘED tréninkem** (anti-A1, `temp/probe_525_527_gt.py` crosswalk-aware přes `compare_isom`):
   525 Bedř **24** / Slovanka 8 / TOTAL 45 · 527 Slovanka 7 / Bedř 6 / TOTAL 18 · 531 kontrola Velbloud 20
   (= přesně Sez. 158 → probe správný). Obě měřitelné → **GO**.
2. **Verify-against-source** (`template_classic.omap` id 150/152, color 2, w 240 µm; osa y dolů):
   525 = ⊤ (příčka -750..750 v y=-462 + noha 0,-462..0,903), 527 = Λ+noha (stříška -615,-55→0,-411→615,-55
   + noha 0,-411..0,819). Oba černé, halo=False. → `inject` dostal **dva nové kind** `tower`/`fodder`
   (`_stamp_tower`/`_stamp_fodder` podle template proporcí). Smoke vizuál ukázán (527 = „šipka"-like = ISOM
   podoba, věrné template).
3. **`inject.py`**: PointClass 525 (radius_mm 0,75) + 527 (0,615), N_POINT **5→7**. Smoke: 7 kanálů, oba
   peak 1,0. Tiles beze změny (on-the-fly do `point_base`); focal bias init pokrývá 7. kanál.
4. **Retrain 3 seedy** (`s159_525527_s{0,1,2}`, 40 ep, batch 16): synt test mF1 **0,821 / 0,778 / 0,844**
   (medián seed 0). Nové synt: 525 medián **0,88**, 527 **0,87** (seed1 outlier 0,77). 5 starých tříd bez
   regrese. Synt 0,821 (7 tříd) ≥ Sez. 158 0,811 (5 tříd) → žádné ředění.
5. **Sweep `peak_thr`** (`temp/sweep_525_527_thr.py` seed 0, inference 1× + levný thr-sweep): černá man-made
   kresba → hodně FP → VYSOKÝ práh. **525→0,70** (Bedř plató F1 0,77), **527→0,65** (mean max 0,71). Registr.
6. **eval_real 3 seedy** (`POINT_CKPT` env): **525 SILNÝ** Bedř medián **0,766** (na úrovni 419, líp než 531),
   **527 STŘEDNÍ-DOBRÝ** seed0 Bedř 0,833 / Slovanka 0,667 (medián 3 seedů 0,50 — seed-citlivý). 5 starých
   tříd bez regrese (medián 531 0,70 = Sez. 158, 419 0,68, 417 0,49, 204 0,66, 210 kolabuje).
7. **Promote seed 0** (synt medián + nejsilnější nové) + `.bak_5class_s158` pojistka. Ověřeno: kanonický
   7-class, Bedř 525 0,766 / 527 0,833 = seed 0.
8. **Capability** 525/527 `SCAN_POC`→`SCAN_LIVE_POINT` (mají eval_real metriku, izomorf 531; test sladěn);
   `eval_real` overlay +2 barvy (525 zlatá / 527 magenta). 48 testů OK.

### Výsledky
- **525 ⊤ Small tower: SILNÝ reálný transfer 0,766** (Bedř 24 GT, na úrovni 419) — distinktivní černý ⊤ přenáší.
- **527 Λ Fodder rack: STŘEDNÍ-DOBRÝ 0,50–0,83** (seed-citlivý; seed0 Bedř 0,833 / Slovanka 0,667).
- **Bodový scope snese 7. třídu** bez regrese 5 starých (na rozdíl od liniového, Sez. 156). Sázka A3(c) potvrzena
  potřetí (531→525→527: distinktivní man-made body přenáší).
- KPI 65,8 % netknuto (capability informativní; scan kandidáti se do KPI počtů nepřičítají).

### Příště
1. **Hnědé terénní body 109/111/112 do Png2Point** (Bedř 103/133/69 GT) — jiný kind (kopeček/jamka/oblouk),
   hnědá barva; ověřit, zda hnědá kresba přenáší jako černá/zelená (otevřená otázka). Probe PŘED tréninkem (anti-A1).
2. **Úklid (handoff ntbhej):** %AUDIT:CODE (`isom_scan/` 5159 LOC) + IDEAS/TODO pruning (≥12 práh, od Sez. 139
   =19) — překročené, bez kolize teď (repo sjednoceno), na ntbhej bez plýtvání CUDA oknem.
3. **D4/D5 audit follow-up** (`AUDIT_CODE_260623`): eval_common (tiled inference 3 hlavy) = nejrizikovější,
   read_pgw nízká páka — ranní revize.

## Sezení 160 — Konsolidace po výpadku + plná regenerace maps/ (13/14) + KPI nález 66,2 (HAL3000)

### Diskuse
- `%BEGIN`: lokál in-sync `origin/main` (`75c0b3c`), working tree clean. Uživatel: „poslední sezení
  došlo k výpadku, konsoliduj, oprav pozůstatky" → poslední nedoběhlá instrukce: „před %END přegeneruj
  všechny mapy + dmr/sken/orto".
- **Konsolidace = čistá:** git clean, Sez. 158/159 konzistentní napříč vrstvami (DIARY index ✓ DONE ✓
  diář ✓ capabilities ✓). Checkpointy lícují s kódem — ověřeno tvarem segmentační hlavy: area `out=21`,
  line `out=2`, point `out=7` (= `N_AREA`/`N_LINE`/`N_POINT`). Výpadek nezničil žádná trackovaná data.
- **Krok 7 PROMPTS odhalil přehlédnutí:** nejnovější supervisor audit byl `260621` (ne `260619` z TODO).
  A1 (KRITICKÁ) ale byla vyřešena Sez. 156/157 (area 21 retrain promote, line 5→2 revert), jen bez
  formální TODO sekce → falešný dojem „nedotčená". Verify-against-source to vyvrátil.

### Provedení / Kód
1. **Recept regenerace (4 typy, z diářů 06-19/06-20):** DEV (5) `generator.py --location <kód>` (ortho+DMR)
   → `gen_backgrounds.py scan-auto` (Livelox sken) · Livelox (5) `pairs.py map <classId> <název>` (vše 3) ·
   KPI (3) `measure_dod.py` (`_gen_sep` ortho=False + bg_scan) → `gen_backgrounds dmr`+`ortho` · Buschdörfl
   special E2E (mobile foto / vecline GT) — generický `pairs.py map` by ho zničil (diár 06-19 varuje).
2. **Pilot Soví vrch** (DEV) ověřil cestu (~4-5 min/mapa) + že `--location` připne jen DMR+ortho, sken
   chce `scan-auto` zvlášť. Orchestrátor `temp/regen_batch.py` (subprocess list-args = UTF-8 safe diakritika).
3. **Batch DEV(4)+Livelox(5)** → OK 9 / FAIL 0. **KPI(3)** přes `measure_dod` (regen + bg_scan + grivace
   + clip na quad) + doplnění dmr/ortho. Buschdörfl **zachována** (volba uživatele — render nezměněn,
   3 podklady platné).
4. **KPI 65,8 % → 66,2 % propsáno** do README (3×), architecture, GLOSSARY, TODO (live current-state;
   historické záznamy netknuté).

### Výsledky
- **13/14 map přegenerováno + 1 zachována; všech 14 má 3 podklady** (orto+dmr+scan ověřeno v `.omap`).
  Vizuál (Soví vrch DEV + Bedřichovka KPI) bez rozbití kresby; Bedř natočená grivací 10,88° + ořez na quad.
- **KPI = 66,2 %** (Bedř 58,5 / Blatná 65,6 / Velbloud 74,3; plocha 75,0 / linie 66,4 / bod 67,9).
  **Doložen sirotek 66,2 (audit A3/A4):** regenerace s committed `separate.py` 407/409 gate (`4a8712e`,
  Sez. 156) dala 66,2. **Sez. 159 „65,8 netknuto" platilo jen na stale `.omap` z 06-20** — gate OVLIVŇUJE
  `.omap` KPI počty přes `predict_areas_sjtsk` separaci → `generate_map`. Sez. 159 revert byl procesně
  správný (tehdy nepodloženo), teď je 66,2 doloženo na `KPI_3MAP_CANONICAL` (= audit A3 doporučení splněno).
- Žebříček děr se posunul: **403/416/409/306/202/109/501/308/108/208** (409 vyskočil — `217 orig / 0 gen`;
  gate 407/409 sráží gen 409 na 0 v KPI měřicích mapách → kandidát na revizi, ne pro teď).

### Censure / nálezy
- **Chybný počet map: tvrdil jsem 15, je 14** (špatně napočteno z `ls` — Buschdörfl připočten dvakrát).
- **`regen_batch.py` v1 spadl na `print('→')`** (Windows cp1250) po 1. mapě — past Sez. 74. Fix:
  `sys.stdout.reconfigure(utf-8)` + ASCII šipky + `PYTHONIOENCODING=utf-8`. Render Nové Louky přitom uspěl
  (chyba byla v orchestrátoru, ne generátoru) → lekce: odliš pád wrapperu od pádu úlohy.
- maps/ je gitignored → regenerace se v gitu neprojeví; commit = jen docs.

### Příště
1. **Hnědé terénní body 109/111/112 do Png2Point** (Bedř 103/133/69 GT) — carry ze Sez. 159; jiný kind
   (kopeček/jamka/oblouk), hnědá barva; ověřit, zda hnědá přenáší jako černá/zelená. Probe PŘED tréninkem.
2. **409 KPI díra** (`217 orig / 0 gen`) — gate `4a8712e` sráží gen 409 na 0 v KPI mapách; zvážit, zda gate
   není moc přísný (měř `--table` před/po na stejné sadě, audit A4 Goodhart-safe).
3. **Úklid (handoff ntbhej):** %AUDIT:CODE (`isom_scan/` ~5159 LOC) + IDEAS/TODO pruning — překročené prahy,
   bez plýtvání CUDA oknem.
