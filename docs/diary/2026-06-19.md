# 2026-06-19

## Sezení 147 — Předání z Claude: supervisor audit, scan-color utilita, oprava `.venv` (ntbhej)

### Diskuse
- `%BEGIN`: sync OK (`main` = `origin/main`), ROADMAP etapa `Generator()`, stroj NTBHEJ21. `resources/livelox/` je lokálně dostupný, ale `Velbloud.pgw` stále chybí → kanonický 3-map KPI trend na ntbhej blokovaný.
- Uživatel předal kontext po Claude a odkázal na `docs/AUDIT_FABLE5_PROMPT.md`. Cíl: podrobně se seznámit s projektem přes supervisor audit.
- Navázali jsme na průlom Sez. 146 / ChatGPT 5.5: pro vytěžování skenu je potřeba separovat barvy mapy do samostatných PNG vrstev. Uživatel správně doplnil, že ISOM paleta z předpisu není dostatečná, protože tisk/sken může být vybledlý nebo barevně posunutý → separace musí vycházet z lokální palety skenu.
- Opraveno místní prostředí: `.venv` ukazovala na smazaný `C:\Users\hejna\AppData\Local\Programs\Python\Python312`. Přes `uv` byla venv znovu vytvořena, doinstalováno `requirements.txt`, `pip check` bez chyb, import smoke OK, `unittest discover -s tests` = 19/19 OK.
- Zvonek: původní cesta z overlaye `C:\Users\mrkla\.codex\resources\...` na ntbhej neplatí. Uživatel upřesnil správnou cestu `C:\Users\hejna\.codex\resources\servant-bell-ring.mp3`; ověřeno přehrání.

### Rozhodnutí
- Scan separace reálných map nemá používat fixní ISOM paletu jako jediný zdroj pravdy. Default má být lokální detektor barev ze skenu; ISOM paleta/SVG katalog jsou reference pro interpretaci a symbolové šablony, ne dogma pro tiskový raster.
- ISOM-scan/classic-CV směr patří pod `Generator() / scan mining`, pokud výstupem jsou barvy, masky, symbolové kandidáty nebo KOMPAS kalibrace. Nesmí být omylem zmražen jako zakázaná `Rekonstruktor()`/degradation práce.
- Auditní nálezy se nebudou potichu přepisovat do `TODO.md`/`IDEAS.md`; čekají na explicitní potvrzení.

### Kód
- Přidán `tools/separate_scan_colors.ps1`: PowerShell/.NET utilita pro rozklad PNG skenu na separované PNG vrstvy podle detekovaných barev.
  - Parametry: `-InputPath`, `-OutDir`, `-MaxColors`, `-MinCount`, `-QuantizeStep`.
  - `QuantizeStep=1` = exact RGB histogram; default `8` shlukuje blízké barvy a je vhodnější pro reálný tisk/sken.
  - Výstup: per-barva PNG vrstvy + `manifest.json` s mean RGB, quantized RGB, počty a coverage.
- Ověřeno na `isom_scan/task_isom_scan.png`:
  - exact varianta: 48 barev, 1 130 403 px / 36,5644 %.
  - quantized `q8`: 48 barev, 1 131 102 px / 36,5870 %.
  - Branžež je velmi paletizovaná, proto exact a q8 dávají skoro stejný výsledek; pro vybledlé skeny je q8 důležitější.
- Lokální PoC `isom_scan/black_brown_poc.ps1` zůstává v gitignored `isom_scan/`; nový obecný nástroj je přesunutý do `tools/`.

### Dokumentace
- Vytvořen supervisor audit `docs/AUDIT_FABLE5_260619.md` podle `docs/AUDIT_FABLE5_PROMPT.md`.
- Hlavní nálezy auditu:
  - **A1 KRITICKÁ:** ISOM-scan/classic-CV průlom není zakotvený v řídicím modelu práce; má být `Generator() / scan mining`.
  - **A2 VYSOKÁ:** `Velbloud.pgw` blokuje srovnatelný 3-map KPI trend.
  - **A3 VYSOKÁ:** `isom_scan/` benchmark má špatně rozdělenou durable hranici mezi copyright rasterem a bezpečným harness kódem.
  - **A4 VYSOKÁ:** KPI Goodhart trvá u pseudo hustot 527/531.
  - **A6 STŘEDNÍ:** testy přibyly, ale generátorový smoke/invariant balík pořád chybí.

### Verify
- `.venv\Scripts\python.exe --version` → Python 3.12.3.
- `.venv\Scripts\python.exe -m pip check` → No broken requirements found.
- Import smoke: `numpy`, `PIL`, `contourpy`, `pyproj`, `scipy`, `skimage`, `pygeomag`, `matplotlib`, `torch`, `torchvision`, `segmentation_models_pytorch` OK.
- Torch stav: `torch 2.12.1+cpu`, `cuda False` — pro ntbhej smoke/eval OK, ne pro mrkla CUDA trénink.
- `.venv\Scripts\python.exe -m unittest discover -s tests` → 19 testů OK.
- KPI nepřeměřeno: generátorové pokrytí se neměnilo; navíc `Velbloud.pgw` na ntbhej stále chybí, takže headline 60,7 % zůstává poslední srovnatelná 3-map hodnota.

### Kudos/Censure
- **Kudos! uživateli** — upozornění na vybledlé/tiskem posunuté barvy zabránilo špatnému fixnímu-paletovému návrhu. Lokální detektor barev je správný základ.
- **Kudos! ChatGPT 5.5 (carry ze Sez. 146)** — classic-CV nápady z trace se ukázaly jako prakticky přenositelné do nástrojů.
- **Censure! sobě** — při prvním pokusu o zvonek jsem použil mrkla cestu z overlaye a neověřil strojovou variantu; opraveno po upozornění uživatele.

### Příště
1. Rozhodnout, zda `tools/separate_scan_colors.ps1` commitnout jako první obecnou `scan mining` utilitu.
2. Po potvrzení propsat největší nálezy z `AUDIT_FABLE5_260619.md` do `TODO.md`/`IDEAS.md` (hlavně A1 scan mining, A2 `Velbloud.pgw`, A3 durable `isom_scan` harness).
3. Rozdělit `isom_scan/` hranici: ignorované rastery/runs vs verzované skripty/schema/README.
4. Doplnit `resources/isom/` manifest/provenanci/licenci pro 113 SVG symbolů.
5. Doplnit `Velbloud.pgw` na ntbhej před další kalibrací pseudo hustot a KPI trendu.

## Sezení 148 — Auditní nálezy 260619: scan mining, durable `isom_scan`, 3-map KPI a první zvednutí KPI (ntbhej)

### Diskuse
- Navázali jsme na dotaz uživatele, jestli pořád platí základní narativ projektu: ROADMAP, KOMPAS, KPI. Odpověď: ano. Upřesnění: `scan mining` není zakázaná práce na `Rekonstruktor()`, pokud krmí `Generator()` a KOMPAS (barvy, masky, symbol candidates, GT/kalibrační signály).
- Uživatel rozhodl začít kritickými nálezy posledního auditu. Vzali jsme primárně `AUDIT_FABLE5_260619`: A1 scan-mining framing, A2 srovnatelný KPI trend po `Velbloud.pgw`, A3 durable hranice `isom_scan/`.
- Potom uživatel zadal přegenerovat všechny mapy a změněné podklady a začít zvyšovat KPI generátoru.

### Rozhodnutí
- `isom_scan/` už nemá být celé `gitignored`: copyright rastery/PDF/runs/overlaye zůstávají lokální, ale textový harness, scoring, prompt, GT manifest a run template jsou verzovatelné.
- První KPI zásah má jít po největší měřené díře, ne podle dojmu. KOMPAS baseline ukázal největší headline díru `403 Rough open land` (orig 1295 / gen 262), čistě separační vrstvu vhodnou pro `Generator() / scan mining`.
- `508 Narrow ride` zůstává další velká díra, ale je tvrdá ZABAGED vrstva; zvyšovat ji pseudo metodou bez silného zdroje by byl Goodhart rizikovější krok než separační 403.

### Kód
- Vytvořen lokální `resources/Velbloud.pgw` (gitignored) z `resources/Velbloud.omap` georeference + `print_area`:
  - `mpp=0,3175`, rotace `-11,3°`, `det=-0,100806`.
  - Soubor:
    ```text
    0.3113451541
    -0.06221290080
    -0.06221290080
    -0.3113451541
    -681365.1355
    -969480.9139
    ```
- `generator/separate.py`: přidán per-class `min_area_px` pro vektorizaci ploch. Jen `403` používá `min_area_px=60`; globální `MIN_AREA_PX=120` zůstává pro 406/408/410.
  - Důvod: experiment nad existujícími `maps/*/sep` ukázal optimum `403` kolem 60 px. Nižší prahy už KPI zhoršují přestřelem/ředěním.
  - Odhad před editací: `403 min_area=120 → KPI 57,55`; `80 → 59,10`; `60 → 59,52`; `40 → 58,68`; `25 → 57,44`.
- `.gitignore`: `isom_scan/` rozděleno na verzovatelný textový harness a ignorované bináry:
  - verzovat: `README.md`, `task_isom_scan.md`, `*.py`, `*.ps1`, `results.csv`, `gt/ground_truth.json`, `gt/task_crop_box.json`, `runs/_run_template.json`;
  - ignorovat: PNG, PDF, syrové `runs/*.json`, overlaye/masky.
- Nově verzovatelný `isom_scan` harness připraven k commitu: `README.md`, `score.py`, `build_gt.py`, `overlay.py`, `black_brown_poc.py/.ps1`, `task_isom_scan.md`, `results.csv`, `gt/*.json`, `runs/_run_template.json`.

### Dokumentace
- `docs/ROADMAP.md`: zakotveno, že `scan mining` patří do etapy `Generator()`, pokud výstup krmí pokrytí/KOMPAS.
- `README.md`: status doplněn o `scan mining` jako součást `Generator()` při vazbě na KOMPAS/coverage.
- `docs/GLOSSARY.md`: nová definice `Scan mining`.
- `docs/TODO.md`: nová auditní sekce `Audit Fable 5 (2026-06-19) — námitky → úkoly`.
  - `260619-A1` rozpracováno: scan mining zakotven, první KOMPAS zásah hotov.
  - `260619-A2` hotovo: `Velbloud.pgw` + 3-map KPI trend obnoven.
  - `260619-A3` hotovo: durable `isom_scan` hranice.
  - `260619-A5`: geometrická augmentace explicitně zmražená phase-2 práce.
- Přidán `docs/HANDOFF_260619_MRKLA_HAL3000.md`: předání pro mrkla/HAL3000.

### Regenerace
- Opravena `.venv`: původní prostředí sice hlásilo `Python 3.12.3`, ale při spuštění skriptu hledalo neexistující `C:\Users\hejna\AppData\Local\Programs\Python\Python312\python.exe`.
- Přes `uv --cache-dir .uv-cache venv --clear` vytvořena nová `.venv` s Pythonem `3.14.5`; `requirements.txt` nainstalován přes `uv pip`.
- Přegenerována KPI sada:
  - `maps/Bedřichovka/` včetně `sep/`, `rgb.*`, `meta.json`, `bg_scan.png`, `.omap`.
  - `maps/Blatná/` včetně `sep/`, `rgb.*`, `meta.json`, `bg_scan.png`, `.omap`.
  - `maps/Velbloud/` včetně `sep/`, `rgb.*`, `meta.json`, `bg_scan.png`, `.omap`.
- Přegenerováno 5 standardních DEV map z `DEV_LOCATIONS`: `Soví vrch`, `Nová Louka`, `Lidové sady`, `Hrubá Skála`, `Novina` včetně `bg_dmr.png`.
- Přegenerováno 5 starších ad-hoc map z jejich `meta.json`: `Borecké skály`, `Borný`, `Doksy`, `Hamr na Jezeře`, `Rovné skály` včetně `bg_dmr.png`.
- Všechny mapové výstupy zůstávají v `maps/` (gitignored).

### KPI / KOMPAS
- Baseline po doplnění `Velbloud.pgw` a plné regeneraci:
  ```text
  Bedřichovka    49,4 %
  Blatná         61,2 %
  Velbloud       62,1 %
  PRŮMĚR KPI     57,6 %
  ```
- Po zásahu `403 min_area_px=60`:
  ```text
  Bedřichovka    51,6 %
  Blatná         64,6 %
  Velbloud       62,1 %
  PRŮMĚR KPI     59,5 %
  plocha         66,5 % → 70,7 %
  linie          59,0 % → 59,5 %
  bod            60,1 % → 60,1 %
  ```
- KOMPAS po změně:
  - `403 Rough open land`: `orig 1295 / gen 488`, provedení `ok` (předtím `podstřel`, gen 262).
  - `416 Distinct vegetation boundary`: mírné zlepšení `257 → 279`, pořád `ok`.
  - Největší zbývající díry: `508`, `204`, `416`, `306`, `409`, `109`, `202`, `404`, `410`, `501`.

### Verify
- `.venv\Scripts\python.exe --version` → Python 3.14.5.
- Import smoke po instalaci závislostí OK (`numpy`, `PIL`, `contourpy`, `pyproj`, `scipy`, `skimage`).
- `measure_dod.py` default:
  - před změnou 403: KPI 57,6 %;
  - po změně 403: KPI 59,5 %.
- `measure_dod.py --table`: 403 potvrzeno `ok`, bez nového přestřelu.
- `.venv\Scripts\python.exe -m unittest discover -s tests` → 19 testů OK.
- Závěrečná kontrola: žádný běžící Python proces; `git status` ukazuje jen očekávané verzované změny. `maps/`, `resources/Velbloud.pgw` a binární `isom_scan` artefakty jsou ignorované.

### Kudos/Censure
- **Kudos! uživateli** — správné nasměrování na kritické auditní nálezy zabránilo tomu, aby se hned skočilo na další kód bez vyjasnění fáze a governance.
- **Kudos! měření/KOMPASu** — výběr 403 podle největší měřené díry dal čistý zisk `+1,9 pb` bez nového přestřelu.
- **Censure! sobě** — při prvním pokusu o „všechny mapy" jsem vzal jen `DEV_LOCATIONS`; až dodatečně jsem dohledal starší ad-hoc mapy z `meta.json`. Opraveno v témže sezení, ale příště nejdřív inventář `maps/`.

### Příště
1. Na mrkla/HAL3000 otevřít/zkontrolovat vizuálně regenerované mapy s podklady, hlavně `403` drobnější paseky po snížení prahu.
2. Další KPI tah dělat jen přes KOMPAS na stejné 3-map sadě. Kandidáti: `508` (ZABAGED ride podstřel), `204`/`210` pseudo skalní body, `306` minor water channel, `409/404` pattern třídy vyžadující scan mining/modelový receptive field.
3. Nehnat pseudo hustoty 527/531 bez nového crosswalk-aware měření na stejné sadě; 527 je pořád přestřel.
4. Doplnit generátorový smoke/invariant balík `260619-A6`.
5. Commitnout a pushnout dvojici: kód+scan-mining docs, potom session docs/handoff.

## Sezení 149 — ISOM SVG index + capability registry SSoT (ntbhej)

### Diskuse
- Uživatel potvrdil směr: SVG symboly vytěžené z omapwiki chceme používat jako shape reference pro scanner,
  ale ne jako zdroj barev skenu. Barvy reálného skenu se musí detekovat lokálně.
- Důležitá korekce: po průlomu v detekci bodových ISOM už některé symboly nemusíme „předstírat".
  Neznamená to ale přepsat fallback generátoru na `real`. Správný model je dvoukanálový:
  fallback generátoru (`real`/`mixed`/`pseudo`) a nad ním mapper-scan signál.
- Konfliktní pravidlo bylo zakotveno explicitně: **mapper scan > external geodata > pseudo**.
  Když sken mapy skutečného mapaře nesouhlasí se ZABAGED nebo pseudo vrstvou, pro scan-mining rozhodování
  vyhrává mapař.

### Rozhodnutí
- `resources/isom/` zůstává lokální katalog SVG: raw SVG jsou ignorované, protože licence/provenance ještě není
  vyjasněná. Verzovaný je jen bezpečný kontrakt (`README.md`, `index.schema.json`).
- Inline SVG do CSV nechceme. Stabilní metadata patří do `resources/isom/index.json`, strojové deskriptory do
  `resources/isom/descriptors/`.
- `isom.capabilities` je jediný živý registr původu symbolů pro generator/KOMPAS. SVG index popisuje tvar symbolu,
  capability registry popisuje původ dat a preferenci při konfliktu.

### Kód
- Přidán nový balík `isom/`:
  - `symbol_index.py`: `build_symbol_index()`, `write_symbol_index()`, `SymbolRecord`, draft discovery ze SVG.
  - `capabilities.py`: `CAPABILITIES`, `generator_codes()`, `capability_by_code()`, `preferred_kind_by_code()`.
- Přidány CLI nástroje:
  - `tools/build_symbol_index.py` pro validaci/generování lokálního indexu.
  - `tools/list_isom_capabilities.py` pro auditní výpis „generator vs preferred vs scanner".
- `generator/omap_export.py`: `USED_CODES` se odvozuje z `isom.capabilities.generator_codes()`.
- `generator/measure_dod.py`: odstraněna vlastní kopie KOMPAS source registru; `--table` ukazuje
  `zdroj · gen · scan · provedení` z capability registru.
- `generator/compare_real_vs_gen.py`: starý název `GEN_CAPABILITIES` přejmenován na `STAT1_GROUPS`, protože jde
  jen o kalibrovaný STAT1 řez pro Soví vrch, ne obecný SSoT.
- `generator/generator.py`: přidán UTF-8 guard pro Windows stdout, aby `--help` nepadal na znacích typu `→`.

### Dokumentace
- `README.md`: doplněn top-level `isom/`.
- `docs/GLOSSARY.md`, `docs/TODO.md`, `docs/architecture.md`, `docs/kb/generator-procedural.md`: srovnáno na nový
  KOMPAS kontrakt `zdroj · gen · scan · provedení` a `isom.capabilities` jako SSoT.
- `resources/isom/README.md` a `index.schema.json`: kontrakt lokálního ISOM symbolového katalogu.
- `TODO.md`: přidán další krok kurace `resources/isom/index.json` pro živé bodové třídy 204/210/417/419 a kandidáty
  418/525/527/531.

### Verify
- `.venv\Scripts\python.exe tools\build_symbol_index.py --resources resources\isom`
  → `symbols=113 (unknown=113), unknown_license=113`.
- `.venv\Scripts\python.exe tools\list_isom_capabilities.py --kind pseudo --format markdown`
  → 210.1 a 419 mají `generator=pseudo`, ale `preferred=mapper_scan`; 418/525/527/531 zůstávají `preferred=pseudo`
  jako scanner candidates.
- `.venv\Scripts\python.exe generator\generator.py --help` → OK.
- `.venv\Scripts\python.exe -m compileall -q isom tools generator tests` → OK.
- `.venv\Scripts\python.exe -m unittest discover -s tests` → 28 testů OK.

### Kudos/Censure
- **Kudos! uživateli** — upozornění „body už umíme detekovat ze skenu" zabránilo špatnému přepsání na čisté pseudo
  a vedlo k přesnější vrstvě `preferred_kind`.
- **Censure! sobě** — první kontrola `generator.py --help` odhalila Windows stdout pád na Unicode; importní změnu
  jsem ověřil až přes CLI, ne jen přes unit testy. Opraveno v témže sezení.

### Příště
1. Kurátorovat `resources/isom/index.json`: začít 204/210/417/419, doplnit licence/provenance, geometrii,
   footprinty a OOM/OCAD aliasy.
2. Navázat `find_isom()` scanner PoC na `build_symbol_index()` a `isom.capabilities`, ne na novou lokální tabulku.
3. Další KOMPAS/KPI tah držet na stejné 3-map sadě a neměnit pseudo hustoty bez crosswalk-aware měření.

## Sezení 150 — KPI 62,0→63,3: 527 kalibrace + 508 lineární stopy z neudržovaných cest (HAL3000)

### Diskuse
- `%BEGIN` na mrkla/HAL3000: `main` byl čistý a srovnaný s `origin/main`; ROADMAP drží fázi `Generator()`.
- Uživatel upozornil, že rozdíl KPI mezi mrkla a hejna může být způsobený jinou lokální sadou map. Proto jsme měřili jen aktuální kanonickou 3-map sadu `Bedřichovka / Blatná / Velbloud` s lokálními `resources/*.pgw`.
- Fresh baseline po Sez. 149 vyšel **62,0 %**. První KOMPAS tlak byl 527 přestřel; potom 508 podstřel, ale s Goodhart kontrolou proti starému závěru Sez. 102 („neštěpit průseky jen kvůli počtu objektů").

### Rozhodnutí
- 527 je čistě pseudo symbol, takže kalibrace hustoty je legitimní jen po crosswalk-aware KOMPAS měření na stejné sadě.
- 508 se nebude zvyšovat umělým štěpením linií. Nový obhajitelný zdroj je pouze ZABAGED `Cesta typcesty_k=025`: ČÚZK ji vrací jako cestu, ale atribut říká „cesta neudržovaná" a ISOM 508 lokální SVG ji popisuje šířeji jako `Narrow ride or linear trace through the terrain`.
- `Cesta typcesty_k=025` nepatří do path kanálu 502-506. Správný tvar je vyjmout ji z `fetch_paths()` a přidat ji do `fetch_forest_rides()` jako 508, aby masky i OMAP zůstaly konceptuálně čisté.

### Kód
- `generator/generator.py`: `PSEUDO_FODDER_PER_KM2` sníženo z `(3.0, 13.0)` na `(0.08, 0.35)`.
  - KOMPAS před: 527 `orig 8 / gen 103`, přestřel.
  - KOMPAS po: 527 `orig 8 / gen 3`, `ok`.
- `connectors/zabaged.py`:
  - nový predikát `is_unmaintained_cesta_trace(layer, props)`;
  - `fetch_paths()` filtruje `Cesta typcesty_k=025` pryč z path sítě;
  - `fetch_forest_rides()` přidává tyto neudržované cesty k `Lesní průsek`;
  - `map_path_to_isom()` na takový prvek selže nahlas, aby se potichu nevrátil do 504.
- `isom/capabilities.py`: zdroj 508 upřesněn na `ZABAGED forest rides + unmaintained Cesta traces`.
- `tests/test_zabaged_mapping.py`: regrese pro novou hranici 508 vs 503/504.
- Zamítnutý pokus: snížení `PSEUDO_STONY_FIELD_PER_KM2` pro 210 z `12.0` na `6.5` KPI zhoršilo na 61,9 %, takže bylo revertováno.

### Dokumentace
- `README.md`, `docs/architecture.md`, `docs/GLOSSARY.md`, `docs/TODO.md`, `docs/kb/generator-procedural.md`,
  `docs/kb/zabaged-isom-catalog.md`, `generator/README.md`: headline KPI a mapování 508/504 aktualizováno.
- Aktivní KOMPAS žebříček po změně: `204 / 416 / 409 / 306 / 202`; 508 už není další prioritní díra.

### Regenerace a podklady
- Přegenerována KPI sada: `Bedřichovka`, `Blatná`, `Velbloud`.
- Přegenerováno 5 DEV map přes `generator.py --location`: `Soví vrch`, `Nová Louka`, `Lidové sady`, `Hrubá Skála`, `Novina`.
- Přegenerováno 5 Livelox prohlížecích map přes `pairs.py map`: `Borecké skály` (`1024666`), `Borný` (`1102878`),
  `Doksy` (`1140600`), `Hamr na Jezeře` (`1116300`, Livelox název „Uprchlík 260510"), `Rovné skály` (`1024718`).
- `Buschdörfl` zůstal jako speciální E2E mapa: generická cesta `pairs.py map 1201511` by nebyla ekvivalentní
  k mobilnímu/GT workflow ze Sez. 122, proto se nepřepisovala horší obyčejnou variantou. Ověřeno, že má scan/ortho/DMR/GT
  i existující `scan_colors`.
- Podklady po closeoutu:
  - všech 14 map má `rgb.*`, `.omap` a DMR hillshade;
  - DEV a Livelox mapy mají `ortofoto.png` v `.omap`;
  - KPI mapy mají `bg_ortho.png` v `.omap`;
  - 9 map se skenem má `bg_scan.png`.
- Separované barvy vyrobeny pro všech 9 map se skenem do `maps/<map>/scan_colors/`: `manifest.csv/json`,
  `summary.json` a 48 masek + 48 overlayů. PowerShell utilita `tools/separate_scan_colors.ps1` je pro celou dávku
  příliš pomalá, proto byla dávka provedena rychlejším inline Pillow/numpy skriptem se stejným výstupním formátem.

### KPI / KOMPAS
- Po 527 kalibraci:
  ```text
  PRŮMĚR KPI 62,5 %
  Bedřichovka 53,5 / Blatná 64,8 / Velbloud 69,2
  plocha 72,7 / linie 59,5 / bod 62,7
  ```
- Po přesunu `Cesta typcesty_k=025` do 508:
  ```text
  PRŮMĚR KPI 63,3 %
  Bedřichovka 57,2 / Blatná 63,1 / Velbloud 69,6
  plocha 72,7 / linie 65,7 / bod 59,5
  ```
- KOMPAS po změně:
  - `508 Narrow ride`: `orig 518 / gen 160`, provedení `ok`.
  - `504 Vehicle track`: `orig 31 / gen 4`, už není přestřel.
  - `527 Fodder rack`: `orig 8 / gen 3`, `ok`.

### Verify
- `.venv\Scripts\python.exe generator\measure_dod.py` → KPI **63,3 %**.
- `.venv\Scripts\python.exe generator\measure_dod.py --table` → 508 `ok`, 504 bez přestřelu, 527 `ok`.
- `.venv\Scripts\python.exe -m compileall -q connectors generator isom tools tests` → OK.
- `.venv\Scripts\python.exe -m unittest discover -s tests` → 30 testů OK.
- Vizuální kontrola `maps/Velbloud/rgb.png` po renderu: čitelné, bez zjevného rozbití kresby.
- Vizuální kontrola po full regen: `maps/Borný/rgb.png` OK; `maps/Borný/scan_colors/color_001_9FD08A_overlay.png`
  potvrzuje, že separace zvýrazňuje barvu ze skenu, ne generovaný rastr.
- `generator\measure_dod.py --table` po regeneraci: 508 `orig 518 / gen 160 = ok`, 504 bez přestřelu,
  527 `orig 8 / gen 3 = ok`.

### Kudos/Censure
- **Kudos! uživateli** — námitka k rozdílným mapám mezi stroji správně držela měření na stejné 3-map sadě.
- **Censure! sobě** — první 508 dry-run byl neplatný: naivní `map_path_to_isom → 508` shodil path renderer, protože 508 patří do ride kanálu. Zachyceno před patchem a opraveno přes fetchery.
- **Censure! tooling** — sandbox odmítal přepis existujících `maps/` výstupů; mimo sandbox stejný copy/generační
  krok prošel. Příště u velkých mapových regenerací rovnou počítat s explicitním mimo-sandbox schválením.

### Příště
1. Další KPI tah začít z KOMPAS žebříčku po Sez. 150: `204` boulder podstřel a `416` boundary podstřel jsou největší, ale 204/210 vyžadují opatrnou hustotní kalibraci proti přestřelu.
2. Nevracet se k 508 přes Png2Line ani štěpení linií; 508 je teď v KOMPASu `ok`.
3. U pseudo hustot dál držet pravidlo: žádná změna bez `measure_dod.py --table` před/po na stejné sadě.
