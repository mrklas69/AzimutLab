# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Výstupy / generovaná data (deštníková fáze: zatím nic, ale připraveno)
# Vzor output_* chytá i pojmenované scratch rendery (output_baseline, output_v2, …)
output/
output_*/
# Dočasné průzkumné výstupy (Sez. 24): WFS capabilities/schema, diagnostiky, scratch
# rendery. Nově sem (ne do kořene/generator-poc): konvence „dočasné výstupy → temp/".
temp/
sandbox/**/output/
sandbox/**/output_*/
sandbox/**/data/
# Kanonické outputy reálných map (regenerovatelné): Soví vrch (Lužické hory, Sez. 17),
# Nová louka (Jizerské hory, Sez. 23), Lidové sady (Liberec, Sez. 28), Hrubá Skála (Sez. 30).
Soví vrch/
sandbox/**/Soví vrch/
Nová louka/
sandbox/**/Nová louka/
Lidové sady/
sandbox/**/Lidové sady/
Hrubá Skála/
sandbox/**/Hrubá Skála/

# Velká binární data (mapy, rastry, LIDAR) — do gitu nepatří, odkaz v KB
*.tif
*.tiff
*.laz
*.las

# Cizí nástroje stažené lokálně (GPL binárky ~12 MB) — odkaz v tools-models.md
lasertool/

# Reálné mapy (páry picture+OMAP) — copyright třetích stran + velká binární data.
# Lokální hold-out / reference, NE do veřejného gitu. Metadata (původ, licence) v KB.
resources/

# Cache stažených reálných ČÚZK dat (nepatří do gitu): DMR 5G dlaždice (výškopis),
# ZABAGED komunikace (cesty/pěšiny, GeoJSON z WFS)
.dmr_cache/
sandbox/**/.dmr_cache/
.zabaged_cache/
sandbox/**/.zabaged_cache/
.ortofoto_cache/
sandbox/**/.ortofoto_cache/

# Claude / IDE lokální
.claude/settings.local.json
.claude/scheduled_tasks.lock
.idea/
.vscode/

# OS
Thumbs.db
.DS_Store
