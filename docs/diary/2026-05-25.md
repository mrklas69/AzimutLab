# 2026-05-25

## Sezení 8 — Vektorizace vrstevnic na ISOM + DRY paleta + ČSOS KB

Osmé sezení projektu, první toho dne. `%BEGIN` proběhl: git čistý/synchronní.
**Audit cadence: LOC práh %AUDIT:CODE poprvé padl** (585 > 500 na začátku sezení,
po dnešku ~750 LOC / 5 modulů) — audit vědomě odložen (sezení produktivní na features),
ale je teď aktivní → první kandidát na Příště. Fokus zvolen uživatelem přes nabídku:
**DRY paleta** (foundations před dalším růstem kódu). Sezení se pak ad-hoc rozvětvilo
(ČSOS KB → vektorizace vrstevnic → `.omap`).

### Diskuse / co se dělalo

- **DRY paleta — oponováno směru z TODO.** TODO říkalo „paleta → jediný zdroj
  `isom-issprom.md`". Oponováno: runtime konzument je Python, parsovat Markdown za běhu
  je proti KISS. SSoT patří do kódu (`palette.py`), Markdown (spec §5, KB) na něj odkazuje.
  Uživatel souhlasil. Spec §5 ponechán jako jazykově nezávislá metodika + odkaz.
- **Mapový portál ČSOS → KB (ad-hoc požadavek).** Zdroj reálných OB map (cesta B).
  Licenci uživatel neznal → ověřena přímo ze stránky „O projektu": gate ZAVŘENA dvojitě
  (copyright klubů + jen náhledy 96 dpi s vodoznakem). Verify-against-source dotáhl
  „nevím" na jednoznačné NE.
- **Vektorizace vrstevnic — rozpleteny dvě vrstvy DAGu.** Uživatel narazil na „pixelové
  vrstevnice" + AutoTrace. Oponováno: pro náš generátor je AutoTrace ztrátová oklika —
  vrstevnice už JSOU polylinie z contourpy, jen je rasterizujeme do PNG (proto pixelové
  + PIL bez antialiasingu). Vektorizace rastru patří k reálným skenům (UC4-III/UC3).
  `lasertool` (lokální binárka) zhodnocen: LIDAR point cloud→rastr (Karttapullautin rodina),
  naráží na stejnou vegetace gate (multi-echo mračno). Multi-echo lze koupit → odloženo.
- **`.omap` — neslepě bez verify.** OOM nejdřív „nenalezen" (hledal jsem špatnou cestu),
  pak zjištěno, že 0.9.6 má jen `windows` platform plugin → headless konverze nejde.
  Rozděleny role: já píšu + self-verify (XML/struktura/rozsah), uživatel otevře v OOM.

### Rozhodnutí / nálezy

- **`palette.py` jako SSoT:** `PALETTE` slovník (slug→`Swatch(rgb, meaning)`) + odvozené
  `C_*` konstanty (žádná druhá kopie hodnot). Hex↔RGB byly konzistentní, ale na 2-3 místech
  (kód + spec §5 + §4.2-4.5) → reálné DRY riziko, ne hypotetické.
- **Vektor z přesného zdroje, ne z pixelů:** contourpy dává polylinie přímo → GeoJSON
  s ISOM symbolem 101/102. Georef: real = S-JTSK (EPSG:5514, přes zveřejněný
  `dmr.build_bbox`), noise = lokální metry. CRS member jen pro real (GeoJSON-2008,
  OOM/QGIS ho čtou). 103 Form line generátor nedělá.
- **`.omap` template-based (jako Pic2Omap `db2omap`), ale jiný vstup:** db2omap jde
  z rastru (pixel→coord přes .pgw/cv2), my z přesných polylinií → sdílíme jen FORMÁT,
  ne kód (CLAUDE.md: neduplikovat sourozence). Local CRS, paper-space (1 m→100 µm při
  1:10000, vycentrováno, bez Y-flip — gy roste dolů jako paper y). Symbol id z template.
- **ISOM symboly ověřeny** (O-Map Wiki): 101 Contour / 102 Index contour / 103 Form line.
  Malé uzavřené izolinie (lok. extrémy) → kartograficky bodové symboly 112/113/115/116
  (pozorováno uživatelem v .omap; zapsáno jako odložený bod do TODO).

### Kód / artefakty

- `sandbox/generator-poc/palette.py` (nový): paleta jako SSoT.
- `sandbox/generator-poc/omap_export.py` (nový): template-based `.omap` writer.
- `generator.py`: import palety, sběr vrstevnic + symbol, `_write_contours_geojson`,
  `--omap-template`, `TILE_M`, `ISOM_*` konstanty, meta `contours_vector`/`omap`.
- `dmr.py`: `_build_bbox` → veřejná `build_bbox` (georef vektoru i při cache-hit).
- Verify (ne odhad): GeoJSON real 67 / noise 68 linií, rozsah přesně 1465×1000 m;
  `.omap` self-verify (XML well-formed, 67 objektů, symbol 0×53/1×14, paper 146,6×100 mm),
  **finální verify uživatelem v OOM — vrstevnice sedí.**
- SLAP propsání: spec §5/§9, `isom-issprom.md`, `data-sources.md` (ČSOS + multi-echo),
  `tools-models.md` (lasertool/AutoTrace), `RESEARCH.md`, sandbox `README.md`, `.gitignore`
  (`lasertool/`).

### Kudos / Censure

- **Kudos! (verify-against-source):** ČSOS licence dotažena z „nevím" na jednoznačné NE
  ze zdroje; GeoJSON rozsah i `.omap` struktura ověřeny čísly, ne vírou.
- **Kudos! (oponování):** tři dobré odklony — paleta do kódu (ne MD), AutoTrace zbytečný
  pro generátor (vektory už máme), `.omap` ne naslepo bez verify.
- **Censure! (AI, sobě):** test OOM CLI spustil GUI aplikaci, která uživateli **vyhodila
  dialogová okna na obrazovku**. Měl jsem předvídat, že OB-mapový GUI nástroj nemusí mít
  headless režim, a netestovat ho spuštěním naslepo (outward-facing akce). Poučení:
  cizí GUI aplikaci nespouštět v testu bez očekávání okna.

### Příště

1. **%AUDIT:CODE** — LOC práh padl (~750 LOC, 5 modulů: generator/dmr/batch/palette/omap_export).
   Audit terminologie/architektury/duplicit před dalším růstem (foundations před záclonou).
2. **Generalizace malých izolinií → bodové symboly** (112/113/115/116) — pozorováno dnes
   v `.omap`; detekce knoll vs depression (výška uvnitř smyčky) + plocha pod prahem. Bonus GT.
3. **Cesty (§4.9)** — Catmull-Rom splajn; odkládáno od Sez. 6 (vědomě, slabší feeder).
4. *(zrající)* **Spouštěč B→A** — generátor teď produkuje `.omap`; `omap_export.py` je
   kandidát na sdílení s Pic2Omap (oba píšou OMAP). Rozhodnout, až bude druhý konzument.

## Sezení 9 — %AUDIT:CODE + %AUDIT:DOCS (foundations úklid)

Deváté sezení projektu, druhé toho dne. `%BEGIN` proběhl: git čistý/synchronní.
**Audit cadence: %AUDIT:CODE práh padl výrazně** (nikdy neproběhl · 8 sezení · ~750 LOC /
5 modulů) → byl to první bod programu, ne volba. Uživatel zvolil oba audity v řadě
(CODE → DOCS), pak opravit vše. Čistě foundations-before-curtains sezení: žádná nová
feature, jen úklid před dalším růstem.

### Diskuse / co se dělalo

- **%AUDIT:CODE nad `sandbox/generator-poc/`** (fokus: obsolete / zbytný kód). Hlavní
  zjištění **proti očekávání**: mrtvého kódu skoro není — žádný zakomentovaný kód, debug
  print, nepoužitý import ani parametr. `%END` cleanup po každém sezení zjevně funguje.
- **%AUDIT:DOCS nad 19 `.md`.** KB udržovaná dobře (SLAP propsání vidět); ověřeno i DRY
  napětí paleta §5 ↔ `palette.py` — všech 9 hex↔RGB párů sedí. Reálné vady: dvě faktické
  nepravdy o stavu (sandbox „prázdný", architecture „kód zatím žádný") + spec-vs-kód drift.
- **GLOSSARY rozhodnut: založit, ne škrtnout** referenci v PROMPTS. Projekt má bohatou
  doménovou terminologii (ČÚZK sady, S-JTSK, UC DAG, gate, feeder, SLAP) → slovník je
  foundations, ne záclona.

### Rozhodnutí / nálezy

- **%AUDIT:CODE — opraveno R1/K1/K2/K3, R2 vědomě ponecháno:**
  - **R1 (DRY):** `C_WHITE` byl definovaný v paletě, ale obcházený hardcoded `255` v
    `np.full(...)` → bílá žila na 2 místech. Zapojen `C_WHITE` (numpy broadcast trojice).
  - **K1:** `from __future__ import annotations` redundantní na Pythonu 3.14 (ověřeno:
    3.14.3; PEP 649/749 dělá anotace líné nativně) → smazán ze všech 5 modulů.
  - **K2:** duplicita `TILE_M * (GW/GH)` → modulová konstanta `WORLD_W_M`.
  - **K3:** jazyková drobnost v komentáři importu palety.
  - **R2 ponecháno:** `C_PURPLE` / `Swatch.meaning` mrtvé, ale vědomé izomorfní API palety
    (purpurová trať §4.13 připravená).
- **%AUDIT:DOCS — opraveno všech D1-D7:**
  - **D1 (kritické):** `sandbox/README.md` tvrdil „zatím prázdný" (5 sezení nepravda) →
    výčet experimentů + konvence `<NN>-` uvolněna na doporučenou (přejmenovat `generator-poc`
    by rozbilo ~20 odkazů; KISS = opravit pravidlo, ne odkazy).
  - **D2 (kritické):** `architecture.md` vnitřní rozpor „první reálný kód" vs „kód zatím žádný".
  - **D3:** spec §4.5 tloušťky vrstevnic 0,7/1,3 px → realita 1/3 px (Sez. 6 drift).
  - **D4:** `tools-models.md` stack doplněn o `pyproj`. **D5:** spec §8.1 `mask_symbols`
    označena jako neimplementovaná. **D7:** `data-sources.md` URL `.cz` → `.gov.cz`.
  - **D6:** založen `GLOSSARY.md` (root) + propsán do README (layout + Docs).

### Kód / artefakty

- `generator.py` / `dmr.py` / `batch.py` / `palette.py` / `omap_export.py` — úklid
  (R1/K1/K2/K3). Žádná změna chování.
- `GLOSSARY.md` (nový, root): doménový slovník (OB/ISOM, ČÚZK data, projekt DAG, nástroje).
- Docs: `architecture.md`, `generator-procedural.md` (§4.5, §8.1), `tools-models.md`,
  `data-sources.md`, `sandbox/README.md`, `README.md`.
- **Verify (ne odhad):** noise + real (z cache) běh prošly bez chyby → odstranění
  `__future__` nezpůsobilo forward-ref problém. Roh pixelu `(255,255,255)`, 8 barev =
  přesně paleta. Real: `EPSG:5514`, **67 linií = bitově shodné s baseline Sez. 8** →
  změny behavior-preserving.

### Kudos / Censure

- **Kudos! (verify-against-source):** `C_WHITE` broadcast i odstranění `__future__`
  ověřeny spuštěním + porovnáním s baseline (67 linií), ne vírou. Python 3.14 PEP 649/749
  ověřen verzí venv, ne odhadem.
- **Kudos! (poctivý audit):** hlavní závěr CODE auditu („mrtvého kódu skoro není") jde
  proti tomu, co uživatel čekal — nehledalo se smetí na sílu.
- **Censure! (AI, dluh):** dvě faktické nepravdy v docs (sandbox „prázdný", architecture
  „kód zatím žádný") přežily 5 sezení, přestože SLAP propsání po každém sezení probíhalo.
  Aktualizace stavových vět zaostala za přidáváním kódu — `%END` SLAP má hlídat i úvodní
  status odstavce, ne jen měněné sekce.

### Příště

1. **Generalizace malých izolinií → bodové symboly** (112/113/115/116) — knoll vs depression
   detekce (výška uvnitř smyčky + plocha pod prahem). Bonus GT pro UC5. Z „Příště" Sez. 8.
2. **Cesty (§4.9)** — Catmull-Rom splajn; odkládáno od Sez. 6 (**4. sezení v Příště —
   hlídat stale práh 5**, příště rozhodnout DO/DROP, pokud zase nevybráno).
3. *(zrající)* **Spouštěč B→A** — `omap_export.py` kandidát na sdílení s Pic2Omap.

## Sezení 10 — Bodové symboly lokálních extrémů

Desáté sezení projektu, třetí toho dne. `%BEGIN` proběhl: git čistý/synchronní.
**Audit cadence: vše čerstvé** (CODE i DOCS proběhly Sez. 9) → zpátky k features.
**Stale práh padl: Cesty (§4.9)** byly 4. sezení v Příště → eskalováno DO/DROP;
uživatel zvolil **DO brzy** (→ `[!]` v TODO). Fokus zvolen uživatelem: **bod 1
z Příště — malé izolinie → bodové symboly** (čistý enabler-feeder, ne záclona).

### Diskuse / co se dělalo

- **Rozsah ISOM symbolů — oponováno TODO.** TODO chtělo 112/113/115/**116**. Oponováno:
  116 Pit je jiná feature class (umělá/erozní díra), z výškového pole **neodlišitelný**
  od 115 (oba jen lokální min) → cpát ho tam uměle = „náhodný soubor symbolů" (CLAUDE.md).
  Uživatel souhlasil: **112 knoll + 113 elongated + 115 depression**, 116 vynechán.
- **Výstup — raster + maska + meta, vektor odložen.** `.omap` point objekt by vyžadoval,
  aby template měl symboly 112/115 (forest template nejspíš nemá → fallback risk). GeoJSON
  body easy, ale i ty odloženy — raster + `mask_symbols.png` + `point_symbols` je samonosný
  UC5 přínos teď. (Rozhodnuto přes AskUserQuestion.)
- **Metoda detekce = přesně dle TODO.** Uzavřená smyčka (contourpy vrací vnitřní smyčky
  s prvním bodem == poslední) + plocha shoelace pod prahem + výška centroidu vs úroveň
  vrstevnice. Reklasifikovaná smyčka vypadne z vrstevnic, masky vrstevnic i vektoru —
  místo ní bodová značka (ISOM kartografická generalizace). Okolní větší vrstevnice zůstávají.

### Rozhodnutí / nálezy

- **Generalizace patří k §4.5 (vrstevnice), bodový výstup k §4.10.** Spec ukotven přes
  §4.10 (sekce „Bodové značky") + cross-odkaz z §4.5 → kódové komentáře (§4.10) zůstaly
  konzistentní bez churnu. Terénní knoll/depression jsou výjimka z det-řízených značek.
- **116 Pit / skalní 204/206 vědomě vynechány** — neodvoditelné z výškopisu (jiná feature
  class). Realistické ≠ náhodný soubor symbolů.
- **§8.1 `mask_symbols` konečně implementováno** — Sez. 9 audit (D5) ho značil jako chybějící.
  Multi-class (1=112 / 2=113 / 3=115). Bonus GT pro UC5 klasifikaci bodových symbolů.
- **Práh `KNOLL_MAX_AREA_M2` = 600 m²** (⌀ ≈ 28 m) dává ~10 % smyček jako symboly (7 z 63–67)
  na obou terénech — vizuálně vyvážené. Laditelná konstanta.

### Kód / artefakty

- `generator.py`: konstanty `ISOM_SMALL_KNOLL/ELONGATED_KNOLL/SMALL_DEPRESSION`,
  `KNOLL_MAX_AREA_M2`, `KNOLL_ELONGATED_RATIO`, `SYMBOL_R`, `SYM_CLASS`/`SYM_NAME`;
  helpery `_polygon_area` (shoelace), `_classify_loop`, `_draw_point_symbol`; sběr
  `point_symbols` ve smyčce vrstevnic + reklasifikace (`continue`); zápis `mask_symbols.png`
  + `point_symbols`/`symbol_classes` do meta. Jediný dotčený modul.
- **Verify (ne odhad):** zákon zachování `linie + symboly` ověřen monkeypatchem prahu na 0
  (baseline) — noise 63=56+7, real 67=60+7. **Real 67 = bitově shodné s baseline Sez. 8/9.**
  Maska: 7/7 symbolů má nenulovou třídu u středu; třídy 1/2/3 (noise), 1/3 (real, bez
  elongated). Zvětšené výřezy potvrdily tvary tečka/elipsa/oblouk + spojitost vrstevnic.
- SLAP propsání: spec §4.5/§4.10/§8.1, `GLOSSARY.md` (kopeček/prohlubeň/generalizace),
  `README.md` (GT výčet), TODO/DONE.

### Kudos / Censure

- **Kudos! (oponování):** 116 Pit odmítnut se zdůvodněním (jiná feature class, neodvoditelný)
  — bránilo to „náhodnému souboru symbolů". Stejně tak vektor vědomě odložen (template risk),
  ne implementován naslepo.
- **Kudos! (verify-against-source):** zákon zachování ověřen baseline runem (monkeypatch prahu),
  ne tvrzením; bitová shoda s real baseline Sez. 9 potvrzena číslem.
- **Censure! (AI):** kód nejdřív použil §4.10 ve všech komentářích, ač detekce je §4.5
  (generalizace vrstevnic) — vyřešeno ukotvením spec přes §4.10 + cross-odkaz, ale příště
  rozmyslet sekci dřív, než ji rozšiju po celém modulu.

### Příště

1. **Cesty (§4.9)** — Catmull-Rom splajn, `[!]` priorita (rozhodnuto DO brzy). Waypointy
   od okraje k okraji s jitterem; volitelně terénně vázané (Dijkstra, §9). Slabší feeder,
   ale odbyde dlouho odkládaný dluh.
2. **Body do vektoru** (GeoJSON Point 112/115, příp. `.omap` při vhodném template) —
   dokončení dnešní práce směrem k vektorovému výstupu.
3. *(zrající)* **Spouštěč B→A** — `omap_export.py` kandidát na sdílení s Pic2Omap.

## Sezení 11 — Přestavba generátoru: řez na vrstevnice + cesty (§4.9)

Jedenácté sezení, čtvrté toho dne. `%BEGIN` proběhl: git čistý/synchronní, audit
cadence čerstvá (CODE+DOCS Sez. 9). Fokus z „Příště" Sez. 10: cesty (§4.9, `[!]`).
Sezení se zlomilo: po volbě metody cest přišel od uživatele požadavek **zahodit
plošné vrstvy a generátor přestavět „znovu a lépe"** (vrstevnice → cesty → …).
**Pozn. k přechodu strojů:** sezení končí přechodem **mrkla → ntbhej**; git je SSoT,
na druhém stroji stačí `%BEGIN` (pull + tento záznam), kód bude synchronní.

### Diskuse / co se dělalo

- **„Cesty ze ZM5" — oponováno dvěma pastmi + křižovatkou.** Uživatel navrhl převzít cesty
  502-507 ze ZM5. Oponováno: (1) **ZM5 je zrušený rastr** (1.7.2023 → ZTM5); vektor komunikací
  je v **ZABAGED Polohopis** (WFS, CC BY 4.0) — z rastru by se cesty musely zase segmentovat
  (ztrátový UC4-III problém). (2) **„Není těžší než contours" neplatí:** vrstevnice počítáme
  z gridu, co držíme (DMR 5G); ZABAGED je nový datový kanál (WFS, parsování, mapování na ISOM,
  rasterizace). (3) **Křižovatka procedurální vs data-driven:** §4.9 je procedurální (UC4-I),
  ZABAGED je UC2→UC4-II a funguje jen pro real terén (noise nemá souřadnice). Nabídnuty 3 cesty
  (A procedurální / B ZABAGED real / C hybrid). **Uživatel zvolil A** — ZABAGED → IDEAS jako
  samostatný enabler.
- **Požadavek „zahodit a přestavět lépe".** Uživatel: zahodit balvany, vegetaci, bažiny
  („divné plusové okraje"), stavět znovu — vrstevnice + cesty. Oponováno proti plošnému mazání
  (GT ztráta) → vyjasněno přes Q1-Q3: **A1 = vizuální věrnost** (vrstvy vypadaly uměle, ne
  problém přístupu), **A2** vrstevnice + body 112/113/115 + vektor/`.omap` zůstávají, **A3**
  cíl sezení = vrstevnice + cesty (→ řez, ne architektonický přepis). Princip přijat: špatně
  vypadající vrstva kazí domain gap feederu → zahodit ji je oprava, ne destrukce.

### Rozhodnutí / nálezy

- **Řez (`generator.py`):** pryč §4.2 vegetace, §4.3 paseky, §4.4 bažiny, §4.11 balvany +
  mrtvá pole, co na nich visela (`slope/eb/gradient`, `_to_pixels`, `box_blur`, `_draw_dotted`)
  + masky `mask_veg/water/rock` + meta `veg_classes`. Import palety zúžen na 3 reálně použité
  barvy (bílá/hnědá/černá). `generate()` se zeštíhlil, moloch zmizel sám (řez, ne přepis — A3).
- **Cesty (§4.9):** `n = 1+round(det*1.6)`, waypointy okraj→okraj (H/V) + kolmý jitter,
  Catmull-Rom. Hlavní plná černá (ISOM **503**, 2 px) / vedlejší čárkovaná (ISOM **505**).
  Nové helpery `_catmull_rom` (reusable — až přijdou rýhy/toky §4.6/4.8, použijí ho) a
  `_draw_dashed` (čárkování po délce oblouku, kreslí mapu i masku). Nový `--det`,
  `mask_paths.png` (multi-class 1/2). Z-order: po vrstevnicích, před body.
- **RNG otázka odpadla:** opatrnost kvůli posunu balvanového streamu byla na stole, ale
  mazáním balvanů zmizel jediný stream k ochraně → cesty losují přímo z `rng`, nejjednodušší.
- **`batch.py`** srovnán na `generate(seed, rug, det, …)` (noise losuje rug+det, real jen det).
- **Spec NEsmazána, označen stav:** §4.9 realizace, §8.1 masky, §4 status box (řez Sez. 11,
  metodika zahozených vrstev zůstává platná pro přestavbu). Spec je dvojí (zadání + prompt) →
  metodika vegetace/bažin je dál platná, jen implementace ji teď nedělá.

### Kód / artefakty

- `generator.py`: přepsán (řez + cesty). Jediný dotčený modul logiky + `batch.py` (signatura).
- **Verify (čísly, ne odhadem):** real **60 vrstevnic + 7 bodů = bitově shodné s baseline
  Sez. 8-10** (řez se vrstevnic/bodů nedotkl — ověřeno `n_lines` v meta + počet `point_symbols`).
  Noise: 56 vrstevnic, 2 cesty (det 0,8), 7 bodů. Staré masky pryč (jen contours/paths/symbols),
  `mask_paths` nenulová. Vizuál obou renderů čistý a „orienťácký".
- SLAP propsání: spec §4/§4.9/§8.1, `README.md` (status + layout), sandbox `README.md`
  (výčet vrstev / parametry / masky), `GLOSSARY.md` (cesta, Catmull-Rom), `IDEAS.md`
  (ZABAGED cesty), TODO/DONE.

### Kudos / Censure

- **Kudos! (oponování):** „cesty ze ZM5" rozebráno na tři problémy (zrušený rastr vs ZABAGED
  vektor, nový kanál ≠ contours, procedurální vs data-driven) — uživatel zvolil A informovaně,
  ZABAGED odložen jako čistý enabler, ne tichý scope creep.
- **Kudos! (verify-against-source):** zachování vrstevnic/bodů ověřeno čísly proti baseline
  (real 60+7), ne tvrzením „řez se jich netýká".
- **Kudos! (princip nad sunk-cost):** zahození 4 vrstev = uznání, že špatně vypadající feeder
  kazí UC5 víc, než pomáhá. Foundations „znovu a lépe" před dalším růstem.
- **Censure! (AI, sobě):** úvodní plán cest jsem stavěl na předpokladu, že balvany zůstanou
  (řešil RNG stream pro zpětnou kompatibilitu noise datasetu) — uživatel vzápětí balvany
  zahodil a otázka byla zbytečná. Měl jsem dřív zvednout „je vůbec ten dataset hoden
  zachování?", než ladit jeho reprodukovatelnost.

### Příště

1. **Terénně vázané cesty (§9)** — Dijkstra least-cost (cena ~sklon) místo přímého splajnu;
   cesty pak vedou údolím / traverzují svahy, ne přes vrcholy. Nejbližší krok k vyšší věrnosti
   cest (řeší dnešní „cesty kříží kopce"). Vstup: `slope` pole (vrátit — bylo zahozeno s balvany).
2. **Přestavba další vrstvy „lépe"** — vegetace / bažiny / vodní toky věrnějším vzhledem než
   zahozená verze (záměr „vrstevnice → cesty → …"). Rozmyslet, co dělalo starou bažinu „plusovou".
3. *(zrající)* **ZABAGED reálné cesty** (IDEAS) — real-půlka cest; až vznikne WFS konektor (UC2).
4. *(zrající)* **Spouštěč B→A** — `omap_export.py` kandidát na sdílení s Pic2Omap.
