# Diář 2026-05-31

## Sezení 50 — %AUDIT:CODE + 2 refaktory (zabaged DRY, A1 meta) + Stale 501/513 + pruning

Stroj: **ntbhej21**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (clean), žádný handoff
pozadu. **Úklidové sezení** — tři prahy auditů zralé naráz: %AUDIT:CODE (+8 práh / +1175 net LOC
od Sez. 41), IDEAS/TODO pruning (+14/12, ≥2 nad), %CALIBRATE (+15, hraničně). Volba uživatele:
**CODE + pruning + Stale** (CALIBRATE odložen na Sez. 51).

### %AUDIT:CODE — metodika + nálezy
Přečten celý `generator.py` (3511 ř.) sám; na konektory (`zabaged/arcgis/ruian/dmr/ortofoto`) a export
(`omap_export/stats/batch/palette/compare`) puštěni 2 paralelní agenti. **Agentí nálezy kriticky ověřeny
proti zdroji** (lekce Sez. 46: agentí audit halucinuje). **0 kritických** — kód výjimečně zdravý:
izomorfismus důsledný (všechny `_draw_*`/`_generate_real_*`/meta-sekce jsou mirror), DRY transport
`arcgis.py` čistý, dead-file scan prázdný. Ověřeno, že **B1 (batch noise větev) neregreduje** — obě
větve `batch.py` vypínají všech 14 dev-map vrstev, validační smyčka nepadne.

Nálezy: 🔴 0 · 🟡 3 strukturální (A1 monolit, DRY napříč moduly BRIDGE_*, zabaged fetch_* duplikace)
· 🟢 ~9 kosmetických (dead code + drift komentářů/katalogu). Dominanta = drift komentářů (typické po
rychlé feature vlně Sez. 41–49).

### Bezpečný balík (behavior-preserving)
Dead code: `_draw_landmark` jednoprvková smyčka `for col, mc in ((C_BLACK, cls),)` (`mc` unused);
nepoužitá konstanta `ISOM_TUNNEL` (kód bere code_t z `map_tunnel_to_isom`); mrtvá větev `_normalize_code`
`3011→301.1` (voda items nesou symbol 301 int, 3011 se nikdy neuloží). Drift: z-order docstring (chyběly
landmarks/linefeatures), `zabaged.py` modul docstring (zaostalý ~9 sez), `batch.py` výčet off vrstev,
`stats.py` „10 sekcí"→14 + docstring; katalog (312 Spring „ústí dolů"→**nahoru ∪**, oprava driftu vs kód
+ GLOSSARY ze Sez. 44; Most/Tunel/Lávka stav **◐→✓** DOKONČENO Sez. 33).

### ⚠ Stale 501/513 (visel Sez. 44→49 = 6×) — DO/DROP rozhodnut verify proti template
- **501 obrys → DO (hnědý→ČERNÝ).** Verify `template_classic.omap`: „Paved area, **with bounding line**"
  + „thin **black** line". Kód kreslil `outline=C_BROWN` → **nebyl px-tuning, byla chybná barva** (521/206
  mají obrys černý). Sladěno na `C_BLACK`.
- **513 Wall → DROP (doloženo px-tuning).** Template `line_symbol color="2"` (černá) + mid_symbol tečka
  á 3 mm; rastr kreslí plnou černou = legitimní px-tuning (tečka á 3 mm v rastru zaniká), `.omap` nese
  věrný 513 → OOM vykreslí tečky. Konzistentní s render-px-tuned-vs-omap-věrný (505/508). Stale zrušen.

### Refaktor 1 — `zabaged.py` DRY (−157 ř, 1194→1037)
14 `fetch_*` funkcí mělo **identické tělo** lišící se jen `*_LAYERS` / parserem (`_geom_to_lines` vs
`_geom_to_polygons` vs `_geom_to_points`) / klíčem (`lines`/`rings`) — izomorfismus dotažený do copy-paste.
→ 2 helpery: `_collect_features(layers, …, parser, key)` (linie/plochy) + `_collect_points(layers, …,
predicate=None)` (body, volitelný filtr — řopíky `typbunkr_k`). Speciální (`fetch_water`/`fetch_footbridges`
kombinované → 2 volání; `fetch_state_border` filtr+coords, `fetch_landmarks` centroid) ponechány vlastní.

### Refaktor 2 — A1 jádro, meta-konstrukce sjednocena (−123 ř, 3511→3388)
Jádro A1 smellu nebyl počet souborů, ale **ohromná duplikace meta-konstrukce** (tentýž ~10řádkový blok
sekce zkopírovaný **14×** — 9 v `_build_meta`, 5 injektovaných zvlášť za ním = **asymetrie / porušení
izomorfismu**). → `_layer_meta_section(mask, info, name_map, class_map)` helper (jediná pravda struktury
sekce) + **tabulkový `real_sections` registr** v `generate_map` (12 vrstev jedním cyklem + bridges vlastní
kvůli hardcoded symbols). `_build_meta` **26→18 parametrů**; všech 13 reálných vrstev teď jednou cestou
(asymetrie zrušena). **Fyzický split souboru na moduly vědomě NEproveden** (oponentura zadání): kreslicí
helpery závisí na module-level globálech `GW/GH/W/H` (mutované `_apply_extent`); split = přepsat globály
na předávaný stav = velký refaktor proti fázi B (sys.path skripty, ne balík; KISS). Spouštěč splitu =
přechod na balík (fáze A). TODO A1 → `[~]` (jádro hotovo, fyzický split podmíněný).

### Verify
- proc baseline **65 drží** po každém kroku (noise → prázdný `real_sections`, `_build_meta` nespadl).
- zabaged refaktor: SV meta **deep-diff identické** (14 sekcí + 6031 omap objektů).
- A1 refaktor: SV meta **deep-diff identické** (29 klíčů vč. symbols/classes/items).
- `py_compile` všech modulů OK; `stats.py` čte nové meta OK.
- **5 lokalit přegenerováno s ortofotem** (počty drží na historii: SV 6031 / NL 1830 / LS 35623 /
  HS 7549 / NV 2300 omap objektů), STATISTICS regen. Vizuál SV ukázán (render konzistentní).

### Pruning (práh +14/12)
IDEAS: zkrácen blok ISOM 2000↔2017 verzní gap (zavřen Sez. 40 → pointer + DONE/GLOSSARY) + jméno
(vyřazené alternativy smazány). TODO: obří katalog-changelog řádek `[!]` zkrácen (hotové dávky Sez. 24–49
→ DONE/katalog, ponechán akční „zbývá zábrana 519 / komín 524"); nález Stale 501/513 odebrán (→ DONE).

### Audit cadence
%AUDIT:CODE **reset Sez. 50** (+0). DOCS Sez. 46 (+4). pruning **reset Sez. 50**. %CALIBRATE +15 (odložen,
DO Sez. 51 — hraničně zralý). Stale Příště: 501/513 **vyřešen** (už nebude viset).

### Kudos / Censure
- **Kudos uživatel:** jasná volba rozsahu (všechny 3 refaktory) + důvěra v oponenturu fyzického splitu.
- **Proces dobře:** audit přes vlastní čtení jádra + agenti na okraj, nálezy ověřeny proti zdroji (0 halucinací
  prošlo); každý refaktor verifikován deep-diff meta PŘED dalším krokem (behavior-preserving prokázán, ne tvrzen);
  Stale rozhodnut verify-against-source template (501 barva), ne od oka (paměti `isom-colors-from-palette-not-eye`).
- **Oponentura zadání (90 %):** „A1 split" interpretován jako řešení smellu (duplikace + asymetrie + param-count),
  ne mechanické dělení souborů — fyzický split by u sys.path-skript architektury s globály udělal víc škody než užitku.

### Příště (Sez. 51)
1. *(zralý, odložen ze Sez. 50)* **%CALIBRATE** (+15 práh) — meta-audit spolupráce, settings, role-discipline docs.
2. *(feature, z katalogu)* zábrana (54)→519 / komín (31)→524 — poslední kandidáti ZABAGED (verify spec před kódem).
3. *(carry-over)* oplocené volné terény (RÚIAN {5,13} ~80 % limit); 115 `Ostatní plocha v sídlech` → 501.

---

## Sezení 51 — dokončení neuzavřeného %END Sez. 50 + %CALIBRATE (zralý +16)

Stroj: **ntbhej21**. `%BEGIN` krok 0 odhalil **neuzavřený `%END` Sez. 50**: `HEAD == origin/main`
(0/0, „clean"), ALE working tree nesl rozsáhlé necommitnuté změny + nový `diary/2026-05-31.md`
s kompletním záznamem Sez. 50 (Verify, Kudos, „Příště"). Přesně případ paměti `stale-clone-fetch-first`
(„clean ≠ up-to-date; hlídej i necommitnutou hotovou práci"). Propagace Sez. 50 ověřena čistá
(DIARY index ✓, DONE plný záznam ✓, refaktory ve working tree ✓).

### Dokončení %END Sez. 50
Dva commity dle pravidel (kód měněn → feat/fix + docs): **(1)** `refactor(generator,zabaged):` —
`zabaged.py`/`generator.py`/`batch.py`/`stats.py`/`STATISTICS.md` (DRY `_collect_*` −157 ř, A1 meta
−123 ř, audit drift, 501 obrys černý); **(2)** `docs(session): …[50]` — `docs/*` + nový diář. Push OK.
**Drobná vada propagace opravena:** `DIARY.md` index měl pořadí `…48, 50, 49` (řádek 50 vložen špatně)
→ srovnáno číselně `…48, 49, 50`.

### %CALIBRATE (fokus, zralý +16 od Sez. 35) — 0 kritických
Přečteno: oba `CLAUDE.md`, `PROMPTS.md`, poslední diáře, `TODO`/`IDEAS`, `settings.local.json`,
git historie velikosti `CLAUDE.md`.
- **A) Context hygiene.** Role discipline **čistá** — projektový `CLAUDE.md` 42→44 ř / 384→414 slov od
  Sez. 35 = **+8 % slov, hluboko pod 50 % sub-prahem**; obsah čistý AI overlay, žádný project-info leak.
  **🟡 D1 — `settings.local.json` allow-list zaneřáděný (50 patternů, ~½ mrtvá/jednorázová):** mrtvé
  `sandbox/` reference (3×, zrušeno Sez. 39), jednorázovky (`2+2`/`ping test`/`recovered`/`exit 1`/echo `$?`
  varianty), překlep `DONE.md4`, konkrétní awk/grep s čísly řádků, mrtvé `temp/probe` skripty, redundantní
  konkrétní python příkazy (pokryté wildcardy).
- **B) Process review.** `%BEGIN` fetch-first prokázal hodnotu (zachytil dnešní neuzavřený %END). **🟡 D2 —
  `DIARY.md` index bobtná:** řádky Sez. 41–50 jsou plné záznamy (150–250 slov), ne hooky (kontrast: Sez. 48 =
  1 věta). Index se čte celý každý `%BEGIN` → tokenová zátěž. Cadence vyhodnocena na datech: %AUDIT:CODE/DOCS
  vrací 0 kritických (checklist Sez. 34 + zdravý kód drží stav) → prahy lehce přísné, **ponechat, sledovat**.
- **C) Collaboration.** Trend zlepšení — historické Censure clustery (verify-against-source, orientace symbolů,
  slepé iterace, paralelní rendery, mis-diagnostika) vyřešené pamětmi; Sez. 50 Censure 0.

### Opraveno (D1 + D2, volba uživatele)
- **D1:** `settings.local.json` allow-list **50 → 22 patternů** (JSON validní). Gitignored (stroj-specifický)
  → **zůstává lokální na ntbhej21**; na `mrkla` se nepropisuje (inherentní vlastnost `.local.json`).
- **D2:** `PROMPTS.md` `%END` — přidáno pravidlo „indexový řádek `DIARY.md` = stručný hook (1–2 věty +
  ISOM kódy), ne kopie záznamu" (dogfooding hned: tento řádek Sez. 51 je stručný).

### Audit cadence
%CALIBRATE **reset Sez. 51**. %AUDIT:CODE Sez. 50 (+1). %AUDIT:DOCS Sez. 46 (+5). pruning Sez. 50 (+1).
Stale Příště: **čisté**.

### Kudos / Censure
- **Kudos uživatel:** jasná volba (dokončit %END Sez. 50 → %CALIBRATE → D1+D2 → %END), bez zdržování.
- **Proces dobře:** `%BEGIN` fetch-first + paměť `stale-clone-fetch-first` zachytily neuzavřený %END dřív,
  než začala nová práce (žádný drift na zastaralém stavu); %CALIBRATE nálezy ověřeny proti reálným datům
  (velikost `CLAUDE.md` z git historie, ne odhad).
- **Censure: 0.** (Retro Sez. 50: závěrečný commit %END nedoběhl — ale safety-net Sez. 51 ho podchytil.)

### Příště (Sez. 52)
1. *(feature, z katalogu — zbylo z fokusu Sez. 51)* zábrana (54)→519 / komín (31)→524 — poslední
   kandidáti ZABAGED (verify-against-source REST jméno + ISOM spec PŘED kódem).
2. *(carry-over)* oplocené volné terény (RÚIAN {5,13} ~80 % limit); 115 `Ostatní plocha v sídlech` → 501.

---

## Sezení 52 — komín → 524 + zábrana → 519 na zdi (poslední kandidáti ZABAGED, katalog vyčerpán)

Stroj: **ntbhej21**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (0/0, clean), žádný
neuzavřený %END. Audit cadence: všechny prahy daleko (CODE/pruning reset Sez. 50, CALIBRATE Sez. 51,
DOCS +6). Fokus z „Příště" Sez. 51 — poslední dva kandidáti ZABAGED katalogu.

### Foundations před kódem (verify-against-source)
Probe `temp/probe_barrier_chimney.py` (MapServer `?f=json` + reálné prvky na LS):
- **Komín** (id 31, `Tovární komín`, bod): atributy `jmeno`(None)/`vyska_obj`(výška, část None),
  žádný kategoriální typ → KISS. Přímočaré → **524 High tower** (mirror věží/sila).
- **Zábrana** (id 54, `Zábrana`, bod): jediný typ `typ_k=Z` „Závora, brána" — nelze rozlišit závoru od brány.
- **ISOM spec z template** (id 134/149): **519 Crossing point** = *„a way through or over a wall, fence…
  including a gate or stile"* (2 rovnoběžné čárky, rotatable) = PRŮCHOD plotem; **524 High tower** = neorientovaný.

### Rozhodnutí (kartografova doména, 2× AskUserQuestion)
- **Sémantická past 519:** ZABAGED „Zábrana" mísí závoru (na cestě, v OB irelevantní) a bránu (v plotě).
  519 = průchod přes bariéru, ne závora. Uživatel zvolil **„519 jen na nosné linii 513"** (zeď/hradba).
- **Krok 0 — změřit PŘED kódem** (paměť `geometric-selfcheck-before-oom`): z 66 zábran na LS leží na zdi 513
  jen **2** (≤5 m; medián vzdálenosti **183 m**) → zábrany jsou převážně závory na cestách. Data předložena
  uživateli (volba podmíněná předpokladem, který se skoro nesplní); uživatel: **„přesto 519 jen ty na zdi"**
  (úplnost i za nízký výtěžek — princip „nic užitečného nevypadne").

### Kód
- **Komín:** `Tovární komín` → `LANDMARK_POINT_LAYERS_524` + LAYER_IDS (3 řádky). Žádné render změny.
- **Zábrana → nový `--barriers`:** `zabaged.fetch_barriers` (resolve bodů na zeď 513 + tangenta), `map_barrier_to_isom`→519;
  `_draw_crossing_point` (2 čárky kolmé na zeď, orientace = tangenta S-JTSK→px transformací 2 bodů); omap rotatable bodový
  objekt (mirror lávky 512.2); `mask_barriers.png` single-class; validační smyčka + argparse + batch off **obě** větve (B1)
  + stats `SYMBOLS` += 519.
- **Přerušení zdi 513 pod brankou** (uživatel: „přes branku přerušena ohradníková linie" = ISOM „line broken at the
  crossing point"): brány spočteny **1× v pre-fetch** (sdíleno s break i symboly), zeď se v místě branky přeruší mezerou
  1,2 mm přes `_split_by_zones_interp` (mechanismus passage cropu tunelů); sráz 104 se neřeže; count zdí v meta beze změny.

### Verify
- proc baseline **65 drží** (2×, i po break); B1 noise barriers=0 (validace nepadla).
- 5 lokalit regen s ortofotem: prostupy 519 **jen LS 2** (ostatní 0, řídké), komín zvedl 524 (SV 6→7, LS +12);
  STATISTICS **41/41 symbolů aktivních**, 513 Wall počty beze změny (break ≠ nový prvek).
- Rastr overlay (zeď 513 vs prostup 519): branka kolmá na zeď, zeď přerušená. **OOM Test OK** (uživatel ověřil
  orientaci + break přes 704 pointery dočasně vložené do LS.omap; rastr ≠ omap bariéra dodržena, paměť bridge sága).

### Kudos / Censure
- **Kudos uživatel:** kartografický catch „přes branku přeruš ohradníkovou linii" (ISOM-korektní, doplnilo break);
  jasné volby přes AskUserQuestion (sémantika 519 + úplnost za nízký výtěžek).
- **Proces dobře:** verify-against-source PŘED kódem (probe atributů + ISOM spec); krok 0 změřil výtěžek (2/66) a
  předložil uživateli místo slepého plnění (paměť `stale-todo-verify-rationale`/`verify-data-not-assume`); OOM verify
  orientace přes 704 pointery (paměť bridge sága: rastr ≠ omap).
- **Censure: 0.**

### ZABAGED katalog vyčerpán
Zábrana + komín byli poslední kandidáti (`zabaged-isom-catalog.md`). Zbytek vrstev = ✗ (administrativa / topologie /
0-prvkové). Drobné dořešitelné zbývají (115 → 501, oplocené terény) — viz TODO.

### Audit cadence
%AUDIT:CODE Sez. 50 (+2; net LOC malé — feature). DOCS Sez. 46 (+6). pruning Sez. 50 (+2). %CALIBRATE Sez. 51 (+1).
Stale Příště: čisté.

### Příště (Sez. 53)
1. *(carry-over)* **115 `Ostatní plocha v sídlech` → 501** (uživatel: v LS skoro vše parkoviště) — drobná projekce.
2. *(carry-over, %THINK)* oplocené volné terény (RÚIAN {5,13} ~80 % limit — open-land enklávy obklopené 520).
3. *(volné)* věrnost/doladění existujících vrstev, nebo posun k UC5 (palette separation = první střípek).

---

## Sezení 53 — udržovaná zeleň → 402 / 402.1 + 501.1 odloženo na holes

Stroj: **ntbhej21**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (clean), žádný neuzavřený %END.
Audit cadence: všechny prahy daleko (CODE Sez. 50 +3, DOCS Sez. 46 +7, pruning Sez. 50 +3, CALIBRATE
Sez. 51 +2). Fokus z carry-over: drobné land-cover dořešení. Uživatel zadal **tři mapování**: park a okrasná
zahrada → 402, ostatní udržovaná zeleň → 402.1, ostatní plocha v sídlech → 501.1.

### Foundations před kódem (verify-against-source)
- **Template (ISOM 2017-2):** 402 = Open land **with scattered trees** (žlutá screen + pattern velkých BÍLÝCH
  teček, color 30); 402.1 = …with scattered **bushes** (ZELENÉ tečky, color 27 „Green 60%"); 501 = Paved area
  **with bounding line**, 501.1 = Paved area **bez** obrysu. Geometrie 402/402.1: tečka r 0,3 mm, grid 1,05 mm, 45°.
- **Probe ZABAGED (`temp/probe_greens_paved.py`):** `Udržovaná zeleň` (134) **nese atribut `typ_pudy_k`** ∈
  {`PO` „park, okrasná zahrada", `UZ` „ostatní udržovaná zeleň"} (LS 3 PO / 14 UZ) → štěpení = **čistá projekce
  přes atribut**, ne hádání. `Ostatní plocha v sídlech` = id 115, bez kategoriálních atributů.
- **Barvy z palety, ne od oka** (paměť `isom-colors-from-palette-not-eye`): color 30 = bílá (C_WHITE),
  color 27 = Green 60% ≈ **C_GREEN2** (paleta má green1/2/3 = 30/60/100 % screen).

### Kód 402 / 402.1 (hotovo)
- `zabaged.map_open_land_to_isom`: `Udržovaná zeleň` → `402 if typ_pudy_k=='PO' else 402.1` (necelý kód 402.1 =
  float, render přes `str(code)`, precedent 203.2/412.1). Komentář OPEN_LAND + docstring `fetch_open_land`.
- `generator.py`: `ISOM_SCATTERED_TREES=402` / `ISOM_SCATTERED_BUSHES=402.1`; rozšířeny `SURFACE_NAME`/`_CLASS`
  (4/5)/`_FILL`/`_DOT`/`_MIN_AREA_PX2`. **`SURFACE_DOT` zobecněn na (barva, poloměr, ROZESTUP)** per-symbol →
  `_draw_dotted_surface_area` bere `sp` z tuple (412 zachová chování, smazána globální `SURFACE_DOT_SPACING_PX`).
  Min. plocha < 9 mm² → fallback 401 (izomorf 412, volba uživatele). Import `C_GREEN2`.
- `omap_export.py`: `USED_CODES`+`AREA_CODES` += „402"/„402.1" (samostatné combined area symboly v template →
  emit `else (code,)` beze změny, NErozbaluje se jako 412=401+412.1). `stats.py`: SYMBOLS +402/402.1.
- **Vegetace gate:** 402.1 = první „scattered bushes" zeleň z dat — **neporušuje** (tvrdý ZABAGED objekt nesoucí
  kategorii, mirror stromořadí 406 Sez. 45; ne LiDAR predikce průchodnosti).

### Verify (402/402.1)
- proc baseline **65 drží** (noise → prázdný `real_sections`). `py_compile` + smoke test mapperů OK.
- LS render (surfaces+paved real): 402:13 / 402.1:203 ringů (per-ring; MultiPolygon rozbalen), masky tříd 4/5,
  `.omap` 13+203 objektů (id 75/76). **Vizuál:** žlutá s bílými tečkami (park) + žlutá se zelenými (zeleň) — OK.

### 501.1 — ZKOUŠENO, ODLOŽENO (klíčový nález sezení)
- Uživatel 2× trval na „velké plochy 501.1 jsou OK (parkoviště/náměstí)". **Oponentura s daty (90 %):** můj první
  verify byl **zavádějící** — testoval jen surfaces+paved izolovaně, nic lososovou nepřekrylo. Plný render LS:
  501.1 zalila **41,5 % mapy** (celé zastavěné území Liberce lososově). Vizuál předložen.
- Uživatel: „a jakého typu jsou ty díry? jsou v ZABAGEDu?" → stáhl raw GeoJSON 115: **ZABAGED díry NESE** —
  obří polygony (2371/1734/494 ha) mají **stovky vnitřních prstenů** (571/692/578) pro budovy/zeleň/cesty. Vrstva
  = administrativní výplň zastavěného území. **Náš `_geom_to_polygons` bere jen vnější obrys, díry zahazuje** →
  zalije celé sídlo. Mapování → 501.1 je tedy správné, ale **chybí podpora děr (holes)**.
- Rozhodnutí uživatele: **odložit 501.1 + hole support do samostatného sezení** (zásah do sdíleného
  `_draw_area_symbol` → riziko regrese u všech plošných vrstev; foundations, ne narychlo). Dnešní 501.1 kód
  kompletně **vrácen** (grep 0 reziduí; preexisting zmínka 501.1 v generator.py:3305 = historický komentář, OK).
  Nový TODO „podpora děr" (parser + rastr maska + omap hole flag). 115 → `[~]` blokováno.

### Propagace
katalog (Udržovaná_zeleň 401→402/402.1, Ostatní_plocha ◐ s důvodem holes), spec §4.8, GLOSSARY, README root +
generator, stats 41→43, **5 lokalit regen + STATISTICS**. batch off obě větve (surfaces kanál, B1 OK — žádná
nová toggle vrstva). architecture nezměněna (`--surfaces` flag pokrývá).

### Kudos / Censure
- **Kudos uživatel:** otázka „jsou díry v ZABAGEDu?" trefila jádro — odhalila, že 501.1 není špatné mapování,
  ale chybějící infrastruktura (holes); rozhodnutí odložit místo narychlo splácat.
- **Censure já:** první 501.1 verify izolovaný (jen surfaces+paved) → lososová „zalila" vše bez překrytí, vypadalo
  to hůř než realita; měl jsem rovnou renderovat plně. Naopak dobře: krok 0 / verify-against-source předložil
  data (probe atributů 402, raw geometrie 115) a oponoval předpokladu „skoro vše parkoviště" místo slepého plnění.

### Audit cadence
%AUDIT:CODE Sez. 50 (+3). DOCS Sez. 46 (+7). pruning Sez. 50 (+3). %CALIBRATE Sez. 51 (+2). Stale Příště: čisté.

### Příště (Sez. 54)
1. *(enabler, nález Sez. 53)* **Podpora děr (holes)** v plošných vrstvách → odemkne 501.1 (115) + věrnější velké
   plochy. Parser + `_draw_area_symbol` maska + `.omap` hole flag; verify regrese u všech plošných vrstev.
2. *(carry-over, %THINK)* oplocené volné terény (RÚIAN {5,13} ~80 % limit).
3. *(volné)* věrnost/doladění, nebo posun k UC5 (palette separation).

---

## Sezení 54 — podpora děr (holes) + `Ostatní plocha v sídlech` → 501.1 + color-table průlom

Stroj: **ntbhej21**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (clean), žádný neuzavřený %END.
Audit cadence: všechny prahy daleko (CODE Sez. 50 +4, DOCS Sez. 46 +8, pruning Sez. 50 +4, CALIBRATE Sez. 51 +3).
Fokus z „Příště" Sez. 53: **podpora děr (holes)** jako enabler → pak odemčení 501.1. Uživatel: „jdi do toho".

### Foundations: verify-against-source PŘED kódem
Otázka uživatele „jsou díry v ZABAGEDu mapovány?" → probe `temp/probe_holes.py` na vrstvě 115 (LS):
**ano, jako vnitřní prsteny GeoJSON** (RFC 7946 `coords[1:]`). Tvrdá data: 9 prvků, 3 obří polygony (493/2371/1734 ha)
mají 92/578/692 děr = **1363 vnitřních prstenů**, plocha děr 68–78 % vnějšího. Náš `geom_to_polygons` bral jen `[0]`.

### Hole support (enabler) — 3 vrstvy, izomorfní
- **parser** `arcgis.geom_to_polygons` → `[[vnější, díra1, …], …]` (list polygonů, polygon = list prstenů).
- **rastr** `_draw_area_symbol` + scanline (`_draw_dotted_surface_area`/`_draw_marsh_area`): bez děr RYCHLÁ PIL
  cesta (0 regrese), s děrami **even-odd scanline** `_fill_rings_scanline` vyřízne výřezy (hrany per-prsten).
- **`.omap`** `area_object` zřetězí prsteny, hole-flag 18 (16+2) na hranicích, **poslední prsten close-only (2)** —
  konvence ověřena verify-against-source proti REÁLNÝM mapám (`resources/SampleMap.omap`/`Blatná.omap`: hole-bit jen
  na vnitřních hranicích, ne na úplném konci). DRY helper `_poly_to_grid_px`; 6 call-sitů + treerow/ropík obaleny.
- Přehlédnutý call-site `zabaged.fetch_landmarks` (centroid) opraven (crash při prvním renderu → hned chyceno).

### Verify holes
- proc baseline **65 drží**; LS reálný **35639 = čistý HEAD 35639** (git stash test) → **behavior-preserving co do
  počtu objektů** (díra = další prsten v TÉMŽE objektu, ne nový). Per-vrstva počty sedí.
- rastr: 13002 px (0,26 %) vykrojeno PŘESNĚ v zastavěné oblasti (RÚIAN 185 děr na LS), nikde v lese — geograficky
  koherentní. `.omap` 1822 objektů s 2017 děrami, 0 chybných flagů. Σ STATISTICS +90 = **preexisting drift** (čistý
  HEAD dává taky 35639), ne má regrese.

### 115 → 501.1 (aplikace, odemčena děrami)
- `map_paved_to_isom` rozliší 501.1 (float); `PAVED_OUTLINE` (501 obrys / 501.1 bez), `PAVED_CLASS` třída 2;
  LAYER_IDS 115; USED_CODES+AREA_CODES 501.1 (template symbol id 106). LS 501.1 = 9 objektů, **10 % výseku** (ne 41 %
  záplava Sez. 53 — díry vykrojí budovy/zeleň/cesty).
- **Z-order dvouprůchod** `_generate_real_paved(urban_base=…)`: 501.1 base ÚPLNĚ VESPOD (před surfaces). Verify daty:
  původně 501.1 NAD surfaces → olivová 520 RÚIAN parcel −12,4 % (překryto); po fixu **520 px 1071020=1071020 identicky**
  (nedotčené). 520 „nepřístupné" (věrnější RÚIAN katastr) má přednost před „zpevněná".

### Barva „Dolní hnědá 50%" — PRŮLOM color-table (uživatel: „zaznamenáníhodný")
- **Test (OOM) neprošel:** silnice na 501.1 mizely. Diagnóza (verify-against-source template): 501.1 (id 106) sdílel
  color **„Upper brown 50%" (11) se silnicemi 502**, a v color-table prioritě překrýval jejich černé okraje
  („Black below lower brown 50%", 14, pod 11). Rastr OK (kreslí pořadím), problém jen `.omap` (OOM kreslí PRIORITOU).
- **501.1 je první velkoplošná base výplň pod mnoha symboly → default ISOM paleta Mapperu NESTAČILA.** Fix dle
  uživatele: rastr nová `C_PAVED=(240,205,175)` (paleta „paved", světlejší než silnice `C_ROAD`); `.omap` —
  `template_classic.omap` color-table **rozšířena o vlastní color „Dolní hnědá 50%" priority 35 (úplně dole, pod
  silničními okraji i pěšinami)**, symbol 501.1 přepojen 11→35. XML edit s PŘESNÝMI kroky od uživatele (jeho volba
  „udělej v XML"). **OOM verify uživatelem: „Super! Tohle je ono!"**
- Zaznamenáno: paměť `omap-colortable-base-fill-priority`, poznámka v `omap_export.py` hlavička + `generator/README.md`
  (template = foundation artefakt, k výrobě nestačí prázdná OOM mapa, needitovat naslepo).

### Propagace + verify
- 5 lokalit regen s ortofotem (501.1: SV 4/NL 1/LS 9/HS 6/NV 2); **ortofoto připojení ověřeno všech 5** (`<templates
  count="1">` def+ref, `ortofoto.png` existuje) — color-table edit `<templates>` bloky nerozbil. STATISTICS regen.
- katalog (115 ◐→✓), TODO (holes+115 → DONE), DONE, GLOSSARY (501.1/holes/Horní-Dolní hnědá), spec §4.9e′/e″,
  README root+generator. batch beze změny (501.1 pod existujícím `--paved`, B1 OK).

### Audit cadence
%AUDIT:CODE Sez. 50 (+4; net LOC větší — hole support enabler). DOCS Sez. 46 (+8). pruning Sez. 50 (+4).
%CALIBRATE Sez. 51 (+3). Stale Příště: čisté.

### Kudos / Censure
- **Kudos uživatel:** kartografický catch „silnice zmizely → color-table" (rozlišil rastr vs OOM prioritu);
  návrh „přidat Dolní hnědou 50% pod Horní" = přesně ISOM color-table řešení; OOM verify autorita (paměť bridge sága).
- **Proces dobře:** verify-against-source PŘED kódem (probe děr 115) i v diagnóze (OOM flag konvence z reálných map,
  color symbolů z template); behavior-preserving prokázán git-stash diff (35639=35639), ne tvrzen; z-order překryv
  520 změřen daty (1071020), ne od oka.
- **Censure já (drobné):** první diagnóza color 501.1 spárovala špatně (regex chytil 501 místo 501.1 → „color 13"),
  opraveno přímým dotazem (501.1 = color 11). Lekce: u duplicitních area_symbol stringů cílit přesně přes id.

### Příště (Sez. 55)
1. *(carry-over, %THINK)* oplocené volné terény (RÚIAN {5,13} ~80 % limit).
2. *(idea)* věrnější velké plochy obecně teď s děrami (voda s ostrovy, areály) — ověřit.
3. *(volné)* věrnost/doladění, nebo posun k UC5 (palette separation = první střípek).
