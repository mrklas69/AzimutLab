# Deník — 2026-07-01

## Sezení 178 — KOMPAS data-gate strop: rozlišení ověřené příčiny od akční díry (HAL3000)

Stroj **HAL3000** (`mrkla`, plná výbava — CUDA + Livelox korpus + `resources/*.pgw`). `%BEGIN`:
git sync čisté/aktuální, ROADMAP etapa `Generator()`. Audit cadence pod prahy (poslední
%AUDIT:CODE/DOCS Sez. 175, jen 3 sezení / +22 LOC zpět); **%CALIBRATE přesně na prahu** (15 sezení
od Sez. 163) — nabídnut jako zralý kandidát na handoff silnějšímu modelu (sezení běželo na
Sonnet 5), NEvynucen. **⚠ Stale Příště eskalován**: „naklikat Velbloud + ostatní skenové mapy"
(mapfield) viselo beze změny 5 sezení (173→177) — nabídnuta volba DO/DROP. Uživatel na obojí
(handoff %CALIBRATE, mapfield DO/DROP) zatím **NEODPOVĚDĚL** — zvolil rovnou fokus na bod 2 z
Příště Sez. 177 („Go!").

### Diskuse / rozhodnutí
- Fokus = Sez. 177 Příště #2: **zafixovat data-gate strop v KOMPASu**, aby `gen=0` kódy s OVĚŘENOU
  příčinou (ZABAGED danou oblast/typ nenese, nebo design rozhodnutí) nevypadaly v tabulce stejně
  jako skutečně nedotčená akční díra. Bez CUDA, uzavírá vlastní nález ze včerejška.
- **Design (ROADMAP self-check OK — čistě diagnostická vrstva, nesahá na generátor ani KPI):**
  NEpřidávat sporné kódy do `isom.capabilities.CAPABILITIES` — porušilo by test-vynucený invariant
  `CAPABILITIES == USED_CODES` (paměť [[capability-registry-equals-used-codes]]); 201/202/108
  generátor navíc vůbec nekreslí, přidání by udělalo falešnou KPI díru jako u 112 Pit. Místo toho
  samostatný `DATA_GATE_CEILING` dict lokálně v `measure_dod.py`, konzumovaný jen `_provedeni`/
  `run_table`/`run_kpi` — čistě KOMPAS-diagnostická anotace, KPI ani `USED_CODES` netknuté.
- **Honest scope:** do registru jen kódy s OVĚŘENOU příčinou ze Sez. 177 (107/201/202/205/208/308).
  **108 Small erosion gully záměrně VYNECHÁN** — Sez. 177 zaznamenal jen počet (155/0), ne
  mechanismus; radši undercount stropů než falešný strop (verify-against-source, ne guess).

### Kód
- `generator/measure_dod.py`: `DATA_GATE_CEILING` (6 kódů + zdůvodnění + odkaz na sezení nálezu),
  `_print_ceiling_footnotes` helper, `_provedeni(code, …)` nová větev (`g==0 and code in ceiling`
  → `"strop"`, dřív vždy jen `chybí`), `run_table`/`run_kpi` obě volají footnotes + inline marker
  `[strop]` u žebříčku děr.
- `tests/test_measure_dod.py` (nový soubor): 6 testů pro `_provedeni`/`DATA_GATE_CEILING`.
- `docs/GLOSSARY.md`: nový termín „Data-gate strop" + `provedení` výčet doplněn o `strop`.

### Postmortem (bug odhalený verify-against-source, ne testem)
První verze porovnávala **string klíč** (`"308"`) proti kódu, který `run_table`/`run_kpi` reálně
předávají jako **`int`** (crosswalk cíl — stejná konvence jako sousední `_kompas_source(code: int)`,
který dělá `str(code)` před dotazem do `isom.capabilities`). Unit test s ideálním string vstupem
prošel, i když živý `--table` pořád hlásil `chybí` pro všech 6 kódů. Odhaleno teprve živým
spuštěním na reálných datech — přesně proč tenhle projekt trvá na verify-against-source, ne jen na
zelených testech. Oprava `str(code)`, testy přepsané na reálný `int` vstup + přidán regresní test
na přesně tenhle typový mismatch (`test_all_ceiling_codes_survive_int_roundtrip`).

### Verify
- `py_compile` OK · `unittest discover` **80 OK + 1 skip** (74→79→80, +6 nových testů) ·
  `tests/smoke.py` OK.
- Živý `--table` i default KPI běh (HAL3000, reálná data): 202/201/107/205/208/308 hlásí `strop`
  s vypsaným důvodem; **108 čestně zůstává `chybí`**; **KPI beze změny 68,7 %** (plocha 79,4 /
  linie 68,7 / bod 65,0) — přesně sedí na Sez. 176/177 zápis, žádná regrese diagnostické vrstvy do KPI.

### Kudos / Censure
- **Censure sobě (mírné):** první verze kódu prošla vlastním testem, ale test používal string
  literály místo reálného int vstupu → falešná jistota, že je hotovo. Živé ověření na reálných
  datech (ne jen unit testy) bug odhalilo hned při první kontrole.

### Příště
1. **⚠ STÁLE OTEVŘENO (6. sezení v kuse, 173→178): mapfield Velbloud + ostatní skenové mapy.**
   DO (ruční klikání `tools/mark_mapfield.py`) vyžaduje uživatele — DROP je taky platná volba.
   Eskalace z `%BEGIN` zůstala nezodpovězená, uživatel zvolil jiný fokus. Rozhodnout příště jako
   PRVNÍ bod, ať nenaroste na 7.
2. **%CALIBRATE zralý kandidát** (15 sezení od Sez. 163) — nabídnut handoff silnějšímu modelu,
   nezodpovězeno. Práh není překročen o ≥2 (zatím nevynucen), ale blíží se.
3. **202 Passable rock face — %THINK extraktor** (Sez. 177 Příště #1, největší data-derivovatelná
   díra, teď formálně `strop` v KOMPASu, ale řešitelná designem).
4. Scan-mining GT (ruční, uživatel) — 311/313 na jiné mapě, 417/418 review.
5. Carry: code-audit D6/D9 (nezměněno).
