# 2026-06-26

## Sezení 168 — Podklady SSoT layout (pořadí/opacity/bg_ prefix) + GT factory bodová infrastruktura (ntbhej)

**Stroj:** ntbhej (NTBHEJ21, plná sada — korpus 265, 6× `.pgw`, isom_scan GT, `.dmr_cache`; bez CUDA tréninku).
**Etapa (ROADMAP):** `Generator()`. Self-check ✓ u obou tahů (podklady = verify pomůcka; GT factory = anti-Goodhart
validace KOMPASu na reálném skenu = legitimní scan mining, audit supervisor pravidlo 3). Sezení překlenulo půlnoc
(start %BEGIN 2026-06-25 večer).

### %BEGIN — stav
- Git čistý, `HEAD == origin/main` (b65568b), nejsem pozadu.
- **KPI:** ntbhej KPI_3MAP **67,1 %** (Sez. 166, po 210 kalibraci); canonical HAL3000 67,5 % je STALE (před 210, ~69 po
  přeměření = carry). Generátor se KPI-relevantně neměnil od Sez. 166 → nepřeměřuji. Díry: 403/416/306/408/109/202/501/308/108/208.
- **Cadence:** %AUDIT:CODE sezení-count zralý (10 od Sez. 158), ale **LOC-trigger ≥500 NESPLNĚN** (jádro net +88,
  isom_scan čerstvě auditován Sez. 165) → nevynucený. Ostatní prahy ne. Stale Příště: nic ≥5.
- **Audit supervisor 260621 sekce C** držena (scope bez tréninku ne, kalibrace = recall, KPI feeder ne cíl, jedna metrika).

### Side-task (požadavek uživatele) — podklady SSoT layout
Uživatel poslal `<templates>` blok z OOM (Branžež) + „podklady u map v tomto pořadí a s touto průhledností,
shodně prefix bg_". **Klíčové zjištění:** Branžež na disku už byl v cílovém stavu (jeho ruční OOM úpravy:
pořadí `bg_dmr/ortofoto/bg_scan`, opacity `1,0/0,5/0,25`) — reálný požadavek = aby to **generátor produkoval sám**
pro všechny mapy + sjednotil dvojí název ortofota.
- **Q1 jméno → `bg_ortho.png`** (sjednotí s existujícím helperem `attach_ortho`, zruší dvojí název
  `ortofoto.png`/`bg_ortho.png`). **Q2 rozsah → regen všech maps/.** Navrhl jsem levnější cestu než full-regen
  (re-fetch/separace): **re-aplikace layoutu na existující `.omap`** (post-process, nemění kresbu) — splní cíl bezpečně.
- **Kód (DRY, SSoT):** `gen_backgrounds.BG_LAYOUT = [(bg_dmr,1.0),(bg_ortho,0.5),(bg_scan,0.25)]` (pořadí = z-order,
  index 0 = back). Nová mechanika `omap_export.apply_image_template_layout(doc, layout)` (re-order def bloků +
  přemapování view `<ref>` indexů + opacity dle jména; izomorf `remove_image_templates`). `_write_omap_with_layout`
  ji aplikuje na **každé** připnutí podkladu (attach/add cesty) → jeden bod, kde se layout sjednotí, nezávisle na
  pořadí připnutí. Ortofoto výstup sjednocen na `bg_ortho.png` (`generator.py` + `omap_export.py` default + help).
  CLI `gen_backgrounds.py layout <.omap>` pro ruční re-aplikaci.
- **Migrace 14 map** (scratchpad, reuse `BG_LAYOUT` SSoT): legacy rename reference `ortofoto.png`→`bg_ortho.png`
  (vč. absolutního path) + re-order + opacity + rename souboru. Idempotentní (2. běh `.omap —`). Bez full-regenu.
- **Verify:** nový `tests/test_omap_export.py` (6 testů: reorder/opacity/idempotence/unknown-kept/no-op/geometrie) →
  **54 unittestů OK**, smoke OK, compileall OK. Branžež OOM stav potvrzen (view dmr 1,0 / ortho 0,5 / scan 0,25).
  Proaktivní náhled vrstvení (PIL blend) ukázán — reliéf vespod plný, ortofoto 0,5, sken 0,25 navrchu. **Uživatel
  OOM verify = carry** (jeho oko = finální slovo). `_existing_ortho_templates` docstring sladěn (slap-rewrite-comments).

### Fokus (volba uživatele) — GT factory bodový benchmark (infrastruktura)
Prolomit zeď headline `point_F1 = 0,0`: generátorová GT (`build_gt only_real`) má jen **2 skórovatelné body
526/530 na ~1 lokaci** (11 px od sebe = artefakt; nález Sez. 165). Cesta = **ruční GT všech bodů přes GT factory**.
- **Průzkum nástrojů (Explore agent + verify):** `mark_isoms.py` = klikání na CELÝ sken → `markers/*.json`
  v **absolutních px** (= formát GT) — přímá cesta. `gt_ui.py` = per-dlaždice (lokální px, vyžaduje detektor). Chyběl
  konverzní skript markers→GT.
- **Scope (diskuse, volba uživatele):** „**všechny ISOM, začneme bodovými**" + **Branžež MVP** (1 sken, NCC zarovnání
  Sez. 146 hotové). Doménový nález ze skenu: Branžež je **pískovcová/skalnatá** — dominuje skalní kresba, izolované
  body nejsou dominantní; jasné = hnědé kupky 109 + man-made; **balvany 204 splývají se skalní kresbou** (= proč
  ChatGPT trefil 429 px vedle — informační mezera, ne slabina metody).
- **Infrastruktura (kód):** (1) `mark_isoms.CODE_LABELS` rozšířen z 12 na **21 bodových kódů** (doplněno 204/526/530/
  110/419/207/524/519/203.2; anglické `name` sladěné s capability SSoT). (2) Nový `gt_from_markers.py`: markery →
  **hybrid GT** (ruční body nahradí gen body, gen linie/plochy zůstanou) + **sanity-check** blízkých bodů různých kódů
  (526/530 red flag) + záloha v1. (3) `score.py` čte `_benchmark_version` z GT → hybrid reportuje **2.0**, generátorová
  v1 fallback 1.0 (build_gt README pravidlo: nemíchat tiše). Verify: compileall isom_scan OK, import řetězec OK,
  `test_mark_isoms` 11/11 OK.
- **Klikání = uživatelova carry** (interaktivní, dlouhé). Příkaz připraven (`mark_isoms` na `task_isom_scan.png`
  s 21 bodovými kódy). Po naklikání: `gt_from_markers.py` → `score.py` → první reálný `point_F1`.

### KPI / KOMPAS
**Beze změny — ntbhej 67,1 % / canonical 67,5 %.** Podklady = post-process nad `.omap` (nemění ISOM kresbu);
`generator.py` změna = jen jméno podkladu `bg_ortho.png` (ne kresba); GT factory = benchmark, ne generátor.
Žebříček děr beze změny.

### Censure / Kudos
- **Kudos sobě:** (1) podklady — re-aplikace layoutu místo full-regenu (KISS, bezpečné, splní cíl levně), SSoT
  `BG_LAYOUT` + jeden `_write_omap_with_layout` bod místo per-cesta záplaty; (2) GT factory — scope diskuse PŘED
  kódem odhalila, že Branžež je skalnatá (limit bodového benchmarku) = doménový vhled formoval infrastrukturu.
- **Censure mírné sobě:** podklady jsem zpočátku vedl k full-regenu (uživatelova volba), než jsem nabídl levnější
  post-process; měl jsem to navrhnout rovnou v otázce rozsahu.
- **Kudos uživateli:** (1) přesný `<templates>` blok z OOM = jednoznačné zadání; (2) „všechny ISOM, začneme
  bodovými" = správné fázování GT factory (univerzální cíl, bodový MVP).

### Příště
1. **GT factory klikání (uživatel) → první reálný `point_F1`.** Naklikat bodovou GT Branžež (`mark_isoms`, 21 kódů),
   `gt_from_markers.py` → `score.py`. Ověřit hybrid GT konverzi na reálných markerech (formát). Pak iter3 re-run
   benchmarku (Opus/ChatGPT5.5) na v2 GT = fér leaderboard (carry Sez. 165).
2. Carry Sez. 166/167: **521 budovy granularita** (%THINK, +1,5 pb sim) · RNG nezávislé streamy · baseline sirotek
   (Bedř ntbhej↔HAL3000) · HAL3000 přeměření KPI po 210.
3. Carry: isom_scan opt-iny (circularity+skeleton filtr), GT factory rozšíření na linie/plochy (po bodech).

---

## Sezení 169 — GT factory: ruční bodová GT Branžež (155 bodů / 14 kódů) + detektor tuning na uživatelových diskriminátorech (ntbhej)

**Stroj:** ntbhej. **Etapa (ROADMAP):** `Generator()`. Self-check ✓ — GT factory = scan mining (anti-Goodhart
doklad recallu pro KOMPAS); detektor tuning = uživatelova vize „nálezy → tuning detektoru pro generátor", ne
rekonstruktor/degradace. Navázání na Příště #1 Sez. 168 (prolomit zeď `point_F1`). User-directed celé sezení.

### Fokus — GT factory ruční bodová GT Branžež
**Cíl:** zeď headline `point_F1 = 0,0` (generátorová GT měla jen 2 body 526/530 na ~1 lokaci, artefakt). Cesta =
ruční GT všech bodů přes `mark_isoms`. **Ověření řetězce PŘED klikáním** (anti-ztráta uživatelovy práce): dummy
markery → `gt_from_markers` → `score` end-to-end OK (hybrid GT v2.0, sanity-check blízkých párů, záloha v1 `.bak`,
`benchmark_version` 2.0 do CSV). Coordinate: markery na `task_isom_scan.png` 1655×1868 (lícuje s GT, TOL_PX 20).

**5 setů klikání (uživatel) → spojeno do master `bransez_gt.json`** (workflow: per-set čistá plocha, markery
client-side → **Save nutný** než přepnu paletu, spojení dedup do master):
- 1. terénní hnědé (101.1 spádovka / 102.1 kóta / 103.1 spádovka-form / 104.1 sráz-min / 109 kupka) + klasifikace
  105/106 → **100**; 2. terénní tvary (110/111/112/113.1/115) → **18**; 3. skalní srázy (201.1/202.1; 202.3 = 0,
  mapař nahradil 206) → **2**; 4. balvany (203.1/204/204.5/205) → **31**; 5. vodní (jen 308.1 ×6, Branžež suchá).
- **Finální GT: 155 bodů / 14 kódů** (z 2 šumivých). `point_F1`: **ChatGPT 5.5 0,121 (z 0,0!)**, Opus 0,0.

### Detektor 109 — kalibrace na ruční GT + 3 uživatelovy diskriminátory
Naivní component detektor recall **2 %** (kupky slité s vrstevnicemi → velké komponenty). Opening → **96 %**,
precision 13 % (fragmenty vrstevnic). Uživatelovy doménové insighty postupně vyladily:
- **kupka+černá skála** (jako řopík): hypotéza potvrzena daty (71 % kupek má černou), ale wildcard maska jako CV
  signál ZHORŠILA (skalní textura přebije) — doménově správné, S/N špatné.
- **108 erozní rýha = menší kruhy v řadě**: velikost (medián 7,8 vs 4,8) + linearita (0 vs 2 sousedi) → precision
  13 → **58 %**, návrhy 150 → 28 (klíčový zvrat).
- **110 elipsa (ecc)**: ecc<0,77 odfiltruje elipsy; 865,721 ecc 0,80 ✓. **105 earth wall (průchozí val)**: angular
  měřič nezachytil (4 ramena, 0 protilehlých) = limit classic-CV → oko/ML.
- **ZÁSAH:** detektor našel 1 přehlédnutou 109 (383,4/960,3).

### Detektor balvanů (černá maska) — velikost RELATIVNÍ
Use-case: lokalizace snadná (černé kruhy), velikost subjektivní (204/204.5/205). Mediány ⌀ stoupají (7,2/8,6/9,2),
ale absolutní práh shoda jen **39 %** — **204.5 je definované RELATIVNĚ** (spec „larger than neighboring 204") →
detektor navrhne ⌀ jako vodítko, kategorii určí kartograf. **ZÁSAH:** 3 přehlédnuté balvany; FP = budovy
(pravoúhelník) / skály 206 (plošné).

### Strop classic-CV (doložen — potvrzuje uživatelovu vizi o ML rekonstruktoru)
Výrazné tvary (109 disk, 110 elipsa, 115 △, balvan) detektor lokalizuje. Jemné (111 oblouk 1/10 disk2 → 9/10
disk1; 112 ▽ 1/3) pod rozlišovací schopností na husté Branžeži. Kontext (105 průchozí val, 204.5 relativní
velikost) classic-CV neumí = hranice, kde ML přebírá. Fragmenty vrstevnic (FP): protáhlejší (elong 3,6 vs 2,0) +
tenčí → diskriminátor pro budoucno.

### Kód
`mark_isoms.CODE_LABELS` 12 → **39 kódů** (vrstevnicové 101.1-104.1 / val-rýha 105/106/108 / terénní 110-115 /
skalní-balvany 201.1-205 / vodní 303-313; name verify-against-source z `template_classic.omap`, 311/313 drift fix).
Detektory zůstaly **scratchpad** (opening + size + linearita + ecc + shape_f1 + circularity) — zabudování do
`terrain_points_poc` = carry. KPI/KOMPAS **67,1 % netknuto** (GT factory = benchmark, ne generátor).

### Censure / Kudos
- **Kudos uživateli:** 6 doménových diskriminátorů (108 velikost+linearita = zvrat precision 13→58; 110 elipsa;
  105 průchozí val; budova=pravoúhelník; 206=plošný; fragment vrstevnice=protáhlost) + důkladné klikání (5 setů,
  155 bodů). Meta-feedback rámec (classic-CV dočasná, ML rekonstruktor překoná) potvrzen měřením.
- **Kudos sobě:** ověření řetězce PŘED klikáním (anti-ztráta práce); measure-first u každého diskriminátoru.
- **Censure mírné sobě:** dedup omylem spojil 2 různé 105 body (22,5 vs 972,6) — uživatel opravil; naivní wildcard
  maska (black∩dilate) moc hrubá (215607 px slitá komponenta) než zúžena.

### Příště
1. **Zelené body (417/418/419)** — pokračovat GT factory (volba uživatele).
2. Zabudovat 109 diskriminátory (velikost+linearita+ecc) do `terrain_points_poc` (uživatelova vize „tuning detektoru
   pro generátor").
3. Carry Sez. 166/167: 521 budovy granularita · RNG streamy · baseline sirotek · HAL3000 přeměření KPI.

---

## Sezení 170 — Zabudování terrain diskriminátorů do detektoru + měřená kalibrace 109/111/112/115 proti ruční GT (ntbhej)

**Stroj:** ntbhej (NTBHEJ21, plná sada — korpus 265, 6× `.pgw`, isom_scan GT, `.dmr_cache`; bez CUDA). **Etapa
(ROADMAP):** `Generator()`. Self-check ✓ — scan mining (detektor krmí KOMPAS + budoucí scan-transfer; kalibrace =
měřený recall = anti-Goodhart governance, audit C3). Příště #2 Sez. 169 (uživatelova vize „tuning detektoru pro
generátor"). User-directed pokračování GT factory série.

### %BEGIN — stav
- Git čistý, `HEAD == origin/main` (`2cd53e2`), nejsem pozadu.
- **KPI:** ntbhej KPI_3MAP **67,1 %** (Sez. 166); generátor KPI-relevantně netknut od Sez. 166 → nepřeměřuji.
  Díry: 403/416/306/408/109/202/501/308/108/208.
- **Cadence:** žádný vynucený. %AUDIT:CODE jádro net +256 LOC od Sez. 158 (pod ≥500); isom_scan +1834 od Sez. 165
  (nad práh, ale čerstvě auditováno + aktivní GT factory → zralý kandidát, odložen). Ostatní pod prahem. Stale Příště: nic ≥5.
- **Audit supervisor 260621 sekce C** držena (zvlášť C2 kalibrace=měřený recall, C3 scan mining krmí KOMPAS/kurátorovaný artefakt, C4 KPI feeder ne cíl).
- Ruční GT (155 bodů, Sez. 169) = tracked (`markers/bransez_gt.json`) → žádné riziko ztráty (C7/A6 ✓).

### Fokus — diskriminátory + měřená kalibrace
**Volby uživatele:** Q1 = **celá terrain rodina 109/111/112/115** (měřit všechny, křehkost u 111/112/115 hlásit poctivě),
Q2 = **detektor + kalibrace + manifest** (bez generátor exportu).

**Výchozí stav (nález):** Sez. 169 diskriminátory zůstaly scratchpad → nepřežily, rekonstruováno z diáře. `109`
entry v `calibration_manifest.json` měl **`marker_recall: null`** (zdroj Hamr) = přesně ten **A2/C3 PoC-limbo** stav.
Ruční GT Branžež má **51 bodů 109** (+ 110:2 / 111:10 / 112:3 / 115:3) → poprvé lze doplnit **měřený** recall.

**Measure-first (proti ruční GT, tol 20 px, reuse `score.match_points`):**
- **Baseline 109 recall 0,020 (2 %)** — přesně reprodukuje Sez. 169 (kupky slité s vrstevnicemi).
- **Diagnóza (`probe_109`):** `open_px=1` je optimum (50/51 kupek má komponentu, všechny projdou size/area); recall
  se ale ztrácel dál — **`max_fill=0.75` odmítal plné disky 109** (kruh fill ≈ 0,79). 109 chce vlastní params
  (`max_fill 0.95`, `min_area 25`, `max_size 54`) jako manifest-Hamr — **per-symbol, ne společný set**.
- **109 FINAL:** open1 + max_fill 0.95 + min_area 25 + **ecc<0,77** → recall **0,745** / precision **0,576** / F1 **0,650**
  (38 TP / 28 FP z 51). Opening dal recall 2→88 %, ecc precision 15→58 % (reprodukuje Sez. 169 „precision 58 %").
  Ecc sweep monotónní (0,74 = F1 0,692), držím **0,77** (Sez. 169 doménová, recall-favoring, anti-overfit na 51 bodů).
- **reject_collinear NA BRANŽEŽ NEPOMÁHÁ** (poctivý nález): precision se hne jen 0,150→0,161, recall klesá. FP nejsou
  čisté kolineární řady — Branžež nemá 108 erozní rýhy, jen rozházené fragmenty vrstevnic. Sez. 169 měl linearitu na
  Hamr (108). Ponechán jako otestovaný opt-in (default off, dostupný pro mapy s 108), neaktivován pro 109.
- **111/112/115 = strop classic-CV doložen** (vysoký recall, mizerná precision, 3–10 GT): 111 R 0,80/P 0,14 ·
  112 R 0,67/P 0,007 (FP záplava) · 115 R 1,0/P 0,125 (ecc<0,85 zvedl P 0,03→0,13 bez ztráty recallu). **Neladěno
  na šumu** (3 body = {0;0,33;0,67;1}, anti-Goodhart A4) — jemné tvary husté Branžeže pod rozlišovací schopností →
  potvrzená vize ML rekonstruktoru.

### Kód
- **`points_common.py`** (sdílené, DRY): `eccentricity()` (z druhých centrálních momentů, jen numpy, bez skimage) +
  `component_candidates(open_px=0, max_eccentricity=None)` — oba **volitelné default off** → water/vegetation/manmade
  zůstávají byte-identické (audit Sez. 165 D2/D3: změna chování jen jako měřený opt-in) + `reject_collinear_runs()`
  per-code post-filtr (linearita) + `run_simple_detector(post_filter=None)` hook.
- **`terrain_points_poc.py`:** CLI `--open-px / --max-eccentricity / --reject-collinear / --collinear-{radius,min-neighbors,tol-deg}`,
  wiring do component_kwargs + post_filter; **109 přidán do `DEFAULT_CODES`**.
- **`calibration_manifest.json`:** 109/111/112/115 přepsány na **měřenou** kalibraci proti Branžež ruční GT
  (`marker_recall` + `false_positive_count` + `precision` + per-code parametry + reprodukovatelný `command`);
  109 status `candidate_calibrated`, 111/112/115 `classic_cv_ceiling`. **A2/C3 PoC-limbo pro 4 kódy uzavřeno**
  (validátor: zmizely z `marker_recall null` warnings).

### Verify
**65 unittestů OK** (54 → 65; nový `tests/test_points_common.py` 11 testů: ecc kruh/úsečka, opening rozdělí slité,
ecc filtr, collinear per-code + izolovaný bod) · manifest validátor OK (109/111/112/115 mimo warnings) · vegetation
detektor regrese OK (sdílený points_common nerozbit) · compileall isom_scan OK · vizuál overlay 109 (66 detekcí na
terénu, ne záplava). Generátor netknut → smoke dle %END pravidla nepotřebný.

### KPI / KOMPAS
**Beze změny — ntbhej 67,1 % / canonical 67,5 %.** Detektor = kalibrace/benchmark, ne generátor (žebříček děr netknut).

### Censure / Kudos
- **Kudos sobě:** measure-first u KAŽDÉHO diskriminátoru zvlášť rozlišil reálný přínos (ecc) od neúčinného (collinear);
  poctivé zaznamenání, že collinear na Branžeži nepomáhá + strop 111/112/115 (neladěno na šumu); A2/C3 PoC-limbo
  uzavřeno měřeným recallem.
- **Censure mírné sobě:** rekonstruoval jsem „linearitu" jako plný collinear filtr z diáře, než jsem ověřil, že FP na
  Branžeži jsou vůbec kolineární (nejsou — Sez. 169 ji měl na Hamr/108). Measure-first to chytil před aktivací, ale
  diagnóza FP-tvaru měla přijít dřív.
- **Kudos uživateli:** ruční GT 155 bodů (51× 109) proměnila PoC-limbo v měřenou kalibraci; volba „celá rodina"
  poctivě odhalila strop classic-CV (doklad vize ML rekonstruktoru), ne nažhavená čísla.

### Příště
1. **Zelené body (417/418/419)** — pokračovat GT factory (Sez. 169 #1, uživatelova volba; interaktivní klikání).
2. **Scan-transfer 109 → generátor** (plní KOMPAS díru 109) = navazující krok kalibrace (Q2 odložený export; rozhodnout
   pseudo hustota vs přímé body + přeměřit KPI).
3. Carry Sez. 166/167: 521 budovy granularita · RNG streamy · baseline sirotek · HAL3000 přeměření KPI po 210.

---

## Sezení 171 — GT factory dokončení bodových ISOM (zelené + man-made, master 155→187, zeď point_F1 prolomena) (ntbhej)

**Stroj:** ntbhej (NTBHEJ21, plná sada — korpus 265, 6× `.pgw`, isom_scan GT, `.dmr_cache`; bez CUDA). **Etapa
(ROADMAP):** `Generator()`. Self-check ✓ — celé sezení scan mining (ruční GT = recall doklad pro KOMPAS,
anti-Goodhart, audit C3); GT factory = benchmark, ne generátor/rekonstruktor. User-directed (Příště #1 Sez. 170,
uživatelova série GT factory). Sezení překlenulo večer.

### %BEGIN — stav
- Git čistý, `HEAD == origin/main` (`90759ff`), nejsem pozadu.
- **KPI:** ntbhej KPI_3MAP **67,1 %** (Sez. 166); ověřeno diffem, že generátor KPI-relevantně netknut od Sez. 166
  (Sez. 167–170 = podklady SSoT layout `gen_backgrounds`/`omap_export` + GT factory `isom_scan`, žádná ISOM kresba)
  → nepřeměřuji. Díry: 403/416/306/408/109/202/501/308/108/208.
- **Cadence:** žádný vynucený. %AUDIT:CODE sezení-count zralý (12 od Sez. 158) ale LOC pod prahem (jádro ~+256 < 500);
  ostatní pod. **Stale Příště nález:** carry blok `521/RNG/sirotek/HAL3000` visí ≥5 sez jako vedlejší bod (eskalováno
  v otázce fokusu, uživatel zvolil GT factory sérii).
- **Audit supervisor 260621 sekce C** držena (C2 kalibrace=měřený recall, C3 scan mining krmí KOMPAS, C4 KPI feeder ne cíl).

### Fokus — GT factory dokončení bodových ISOM (volba uživatele)
Navázání na Sez. 168/169 (infrastruktura + 155 bodů terénní/skalní/vodní). Cíl: doklikat **zelené + man-made** body
→ kompletní ruční bodová GT Branžež → skórovatelný `point_F1`. **Tři sety přes `mark_isoms` HTTP server** (per-set
čistá plocha, paměť `mark-isoms-gt-factory-workflow`):
- **zelené 417/418/419** (27: **419×24** dominuje, 417/418 po 1 + 1 unknown jáma) — Branžež skalnatá, výrazná
  vegetace převažuje nad solitérními stromy/keři;
- **man-made 512.1/512.2/519/521.1/523.1/524** (1: jen **523.1 zřícenina** — „jediný výskyt", skalnatá oblast);
- **man-made2 525/526/527/530/531/603** (4: 527×2, 526, 531).

**Klíčové bezpečnostní zjištění (anti-ztráta ruční práce):** `mark_isoms.save()` = **full replace** souboru +
`_load_existing_markers` filtruje dle palety `codes` → spustit server s `--markers <master> --codes <filtr>` by
**přepsalo master 155 bodů jen novými** (past). Proto **per-set čistý soubor** (`bransez_green/manmade/manmade2.json`),
master chráněný, merge až nakonec.

### Nálezy ze sběru
- **Přehlédnutá jáma 112** @(130,610): uživatel při zelených narazil na ne-zelený bod, označil `unknown`, ptal se
  „máme v GT?". Změřeno proti masteru: nejbližší 112 = **737 px**, nejbližší cokoliv = 61 px (val rozpadlý 106) →
  **NE, přehlédnuto** (Sez. 169 klikání 155 ji minulo, jako tehdejší „přehlédnutá kupka 109"). Přemapováno `unknown→112`.
- **4 z 6 man-made kódů chyběly v paletě** (512.1/512.2/521.1/523.1) → doplněno do `CODE_LABELS`
  **verify-against-source** z `template_classic.omap` (všech 6 = `type="1"` body; uživatel vybral konzistentně
  `.1` minimum-size varianty, žádný liniový problém). **603 = „Spot height, dot"** (výšková kóta tečkou, ne 102.1
  Contour value).

### Kód
- **`isom_scan/merge_marker_set.py`** (NOVÝ, durable): set→master merge (Sez. 169 dělal ad-hoc `add_markers.py`,
  nepřežil). Dedup (stejný kód ≤3 px), **sanity red-flag** blízkých RŮZNÝCH kódů <20 px (izomorf `gt_from_markers`
  /`score`), přepočet `codes`+`summary`, záloha `.pre_merge.bak`, **dry-run default** + `--apply`, no-silent-fallback.
  **Ověřen na dummy PŘED reálným mergem** (anti-ztráta, Sez. 169 lekce): kopie master 155→159, 109 zachováno, sanity
  chytá schválně kolizní bod.
- **`isom_scan/mark_isoms.py`** `CODE_LABELS` +5 (512.1 most/tunel min / 512.2 lávka / 521.1 budova min / 523.1
  zřícenina min / 603 spot height), name verify-against-source z template.

### Merge + výsledek
Záloha master (`bransez_gt.session170.bak`, 155) → 3× `merge_marker_set --apply` → **master 155→187 bodů / 21 kódů**
(žádné dedup, žádná 526/531 kolize; 1 sanity flag 419~109 18,9 px = vegetace u kupky, legitimní). `gt_from_markers`
→ **hybrid GT v2.0** (187 bodů + 16 ne-bodových gen, score_codes 35). **`score` proti v2 GT (staré Sez.146 běhy):
ChatGPT 5.5 point_F1 0,269 (z 0,0!) / class_recall 0,69**; Opus 0,0 / GPT-5.5-thinking 0,0 (slabé Sez.146 běhy).
**Zeď `point_F1=0` prolomena.** Caveat: staré běhy na 2-bodový úkol → orientační dolní odhad, fér leaderboard chce
re-run modelů na v2 GT (carry). `gt_from_markers` sanity vypsal 12 blízkých párů — drtivá většina legitimní
(spádovky 101.1/103.1 = směrové značky U kupek 109, ne „jeden objekt 2 kódy"); k druhému pohledu 101.1~204.5 7,7 px.

### Verify
**65 unittestů OK** (paleta edit nerozbil). Overlay GT (187 bodů) na sken vykreslen + ukázán (always-show-visual-output;
body realisticky rozeseté — hustě v terénu/zeleni). Merge řetězec ověřen na dummy. Generátor netknut → smoke dle
%END pravidla nepotřebný.

### KPI / KOMPAS
**Beze změny — ntbhej 67,1 % / canonical 67,5 %.** GT factory = benchmark, ne generátor (žebříček děr netknut).

### Censure / Kudos
- **Kudos sobě:** ověření merge řetězce na dummy PŘED klikáním (anti-ztráta, Sez. 169 lekce); odhalení `save()`
  full-replace pasti PŘED spuštěním serveru (jinak by master 155 zmizel); verify-against-source paleta z template.
- **Kudos uživateli:** konzistentní výběr `.1` bodových variant man-made (žádný liniový problém); přehlédnutá jáma =
  recall nález tvým okem; doklikání 3 setů (+32 bodů) → prolomení zdi `point_F1`.
- **Censure 0.**

### Příště
1. **Re-run modelů na v2 GT** (fér leaderboard, carry Sez. 165/168): uživatel pustí Opus 4.8 / ChatGPT 5.5 na
   `task_isom_scan` aktuálním promptem → naskórovat proti v2 187-bodovému GT.
2. **GT factory rozšíření na linie/plochy** (po bodech — „začneme bodovými" dokončeno) NEBO další mapa (víc než
   1 sken pro robustní benchmark; chce nové NCC zarovnání jako Branžež Sez. 146).
3. **Scan-transfer 109 → generátor** (plní KOMPAS díru 109, Sez. 170 #2): pseudo hustota vs přímé body + přeměřit KPI.
4. Carry Sez. 166/167: 521 budovy granularita · RNG streamy · baseline sirotek · HAL3000 přeměření KPI po 210.

---

## Sezení 172 — Scan-transfer 109/111 z polovičních vrstevnic (reframe: podstřel, ne díra) → KPI bod +2,3 pb (ntbhej)

**Stroj:** ntbhej (NTBHEJ21, plná sada — korpus 265, 6× `.pgw`, isom_scan GT, `.dmr_cache`; bez CUDA). **Etapa
(ROADMAP):** `Generator()`. Self-check ✓ — čisté vytěžení (rozšíření pokrytí 109/111 z reálných DMR vrstevnic, ne
pseudo/degradace/rekonstruktor). Navázání na Příště #3 Sez. 171 (Scan-transfer 109 → generátor).

### %BEGIN — stav
- Git čistý, `HEAD == origin/main` (`2b98f69`), nejsem pozadu. 5. sezení dne (po 168–171).
- **KPI:** ntbhej KPI_3MAP **67,1 %** (Sez. 166); generátor KPI-relevantně netknut od Sez. 166 → výchozí.
  Díry: 403/416/306/408/109/202/501/308/108/208.
- **Cadence:** žádný vynucený. %AUDIT:CODE jádro LOC pod prahem (~+256 < 500 od Sez. 158); isom_scan čerstvě
  auditován Sez. 165. Stale Příště: carry blok 521/RNG/sirotek/HAL3000 visí ≥5 sez (vedlejší bod, eskalováno do fokusu Q2).
- **Audit supervisor 260621 sekce C** držena (C2 kalibrace=měřený recall, C3 scan mining krmí KOMPAS, C4 KPI feeder ne cíl).

### Fokus — 109 scan-transfer (volba uživatele, Příště #3)
**Reframe (verify-against-source, zachránil před zbytečnou prací):** 109 NENÍ díra „negeneruje se" — generátor ho
KRESLÍ z uzavřených vrstevnicových smyček (`_classify_loop`, §4.10: lokální max < `KNOLL_MAX_AREA_M2=600 m²` → hnědý
disk). Measure-first přes `measure_dod --table` (crosswalk-aware, ne ad-hoc probe): **109 orig 251 / gen 18 (7 %) =
největší terénní PODSTŘEL**, 111 orig 173 / gen 31. **Příčina (kód-verified):** 109/111 se detekují JEN z plných 5 m
vrstevnic (`CONTOUR_STEP=5`); kupka 2,5–5 m udělá uzavřenou smyčku až na poloviční hladině (`level+2,5 m`), ta se ale
zpracuje jen jako čárkovaná formline (4058+) — `_classify_loop` se na ni nevolá → 109 se nevykreslí. Kupky < 2,5 m
DMR 5G nezachytí jako smyčku vůbec.

**Volba uživatele Q1 = A) poloviční vrstevnicové smyčky** (kartograficky správné §4.10, reuse mašinérie, fyzikálně
z DMR, KISS; B) DMR-maxima = riziko šum + jiný přístup, C) pseudo = zavrženo doménově — terénní bod ≠ náhodné rozmístění).

### Kód (3 edity `generator.py`, +23/−8)
- `_classify_loop` vrací i `"elev"` (výška uvnitř smyčky) ve všech 3 větvích.
- Half-hladina (`level+2,5 m`) volá `_classify_loop` PŘED formline cestou; **guard `|elev − hlevel| < half (2,5 m)`**
  ⇒ extrém leží CELÝ mezi dvěma plnými vrstevnicemi → nezdvojí se s bodem/kroužkem z plné cesty a vyloučí vrcholy
  velkých kopců (ty dosáhnou plné). Jen `terrain=="real"` → noise byte-identický (smoke checksum).
- Log „pomocné vrstevnice (103): … · body extrémů (half): N".

### Verify (ntbhej, measure-first PŘED i PO)
- **`measure_dod`: 109 18→32 (+78 %)**, **111 31→52 → „ok"** (bonus, izomorf — jedna změna řeší obě). **KPI
  67,1→67,3 % (+0,2 pb)**, bod 67,9→**70,2 (+2,3 pb)**, **žádná regrese** (past [[kpi-fill-undershoot-dilutes]] se
  nenaplnila — velká mezera 109 překonala dilution; ověřeno agregátní sim PŘED kódem +3,5 pb směr). 109 vypadlo z top-5 děr.
- Smoke OK (97 s). Žádný unit test se `_classify_loop`/`point_symbols`/knoll nedotýká.
- **Vizuál Soví vrch** (uživatelova GPS lokalita): plné body 76 + half **77** (zdvojnásobeno na členitém terénu);
  crop+zoom verify = hnědé tečky uvnitř uzavřených vrstevnic = kupky na lokálních maximech, **bez šumu, bez zdvojení**
  (guard funguje). Na KPI mapách přírůstek menší (méně členitý terén = realita dat, ne bug).
- **DMR strop (uznáno):** zbytek 109 mezery = drobné kupky pod rozlišením 5 m vrstevnic; víc hladin = nad DMR
  rozlišení (šum) + ISOM zakazuje >1 pomocnou mezi vrstevnicemi → vrstevnicová cesta vytěžena. Volba Q2 = přijmout & commit.
- **Sirotek (audit A3, nemíchat):** 67,3 je **NTBHEJ**; canonical HAL3000 67,5 (Sez. 164) NEpřeměřeno (změna kódu se
  tam projeví +~0,2) → carry HAL3000 přeměření.

### Kudos / Censure
- **Censure 0.** Reframe verify-against-source (109 už se kreslí) zabránil falešnému „přidat 109" pseudo tahu.
- **Kudos!** uživateli za volbu A (nejčistší data-driven cesta, drží ROADMAP „terén musí dávat smysl").

### Příště
1. **HAL3000 přeměření KPI po 109/111** (canonical 67,5 → ~67,7?) — změna kódu přenositelná, sjednotit headline (audit A3).
2. Větší KPI díry: **403 Rough open land (3,5pb) / 416 veg boundary (2,9pb) / 306 Minor water channel (1,7pb)**.
3. Re-run modelů na v2 GT (carry Sez. 165/168) · GT factory linie/plochy (carry Sez. 171).
4. Carry: 521 budovy granularita · RNG nezávislé streamy · baseline sirotek (Bedř ntbhej↔HAL3000).
