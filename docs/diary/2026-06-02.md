# Diář 2026-06-02

## Sezení 65 — rock-relief hi-res DMR fetch padal na HTTP 500 (server práh ~7 Mpx) → Sez. 63 regen byl nekompletní

Stroj: **mrkla**. %BEGIN: sync OK (lokál == origin/main `1fb1b31`, clean). Cadence: **IDEAS/TODO pruning +14
od Sez. 50 → práh 12 překročen o 2** (zralý, ale fokus jinam — carry); %AUDIT:CODE +5 od Sez. 60 (LOC narostl
forest+rock_relief, sleduji); %AUDIT:DOCS +8, %CALIBRATE +13 (nezralé). Stale Příště: čisto. Z Příště Sez. 64
uživatel zvolil **OOM verify forest-age + rock-relief** (carry „tvoje").

Pozn. z %BEGIN: commit `1fb1b31` nese „Sez. 64", ale šlo o čistý IDEAS `%THINK` (měření vegetace přes ortofoto)
bez `## Sezení 64` sekce → nemá řádek v DIARY indexu. Formálně OK (pravidlo platí pro `## Sezení N` sekce),
stopa po sezení 64 je jen v IDEAS.

### Nález: NL/LS/SV `.omap` jsou starší než rock-relief fáze (uživatelův postřeh)
Při přípravě OOM verify uživatel: *„Novina OMAP má včerejší datum, nechceš přegenerovat?"* → mtime check
odhalil: Novina/HS `.omap` z **23:28/23:19** (pozdní rock-relief fáze Sez. 63), ale **Nová Louka 22:22 /
Lidové sady 22:24 / Soví vrch 22:37** — o hodinu starší, z **rané forest-age fáze**, ještě před dokončením
rock-relief. `mask_rocks.png` i `mask_forest_age.png` u těch tří taky 22:xx. Diár Sez. 63 přitom tvrdí „8 map
regen na 1,5 m". Verify-against-source: nehádat, přegenerovat.

### Diagnóza: ImageServer vrací HTTP 500 nad ~7 Mpx
Regen NL/LS/SV (`--location NL/LS/SV` — pozor, CLI bere **kódy**, ne plné názvy) → všechny padly na
`urllib.error.HTTPError: HTTP Error 500` v `rock_relief.detect_rock_areas` → `dmr.fetch_elevation_grid`
(samostatný hi-res DMR fetch). Klíč: `fetch_elevation_grid` **cachuje** → 500 = cache miss = rock-relief
hi-res na NL/LS/SV v Sez. 63 **nikdy neuspěl** (jinak by byl v cache).

Empirický test (4× deterministicky, ne náhodný výpadek — terrain DMR menšího gridu přitom prochází, server
není dole):

| dotaz | Mpx | výsledek |
|---|---|---|
| 3000×2000 | 6,0 | OK |
| 3200×2133 | 6,8 | OK |
| 3500×2333 | 8,2 | **500** |
| 4000×2667 (NL @ 1,5 m) | 10,7 | **500** |

Práh ~7 Mpx na jeden `exportImage` (F32 tiff). Cache odhalila, proč Sez. 63 vypadal funkční — **míchané
rozlišení**: HS cache `2501×2501` = generováno s **TARGET_PX_M=2,0** (6,3 Mpx, pod prahem); NV cache
`2000×3334` = TARGET_PX_M=1,5 ale portrait 3×5 → jen 6,67 Mpx (pod prahem). Jen **6×4 landscape @ 1,5 m =
4000×2667 = 10,7 Mpx → 500**. `TARGET_PX_M` bylo během Sez. 63 zvednuté 2,0→1,5 kvůli detailu, čímž velké
výseky překročily práh; `MAX_PX=4000` clampuje **stranu** (4000), ne **plochu**, takže neochránil. Docstring
`rock_relief.py` navíc pořád tvrdil „≈ 2 m" (drift po té změně).

### Fix: plošný cap (volba uživatele — KISS)
`generator/rock_relief.py`: nová konstanta **`MAX_AREA_PX = 6_500_000`** (pod prahem; HS 6,3 Mpx prochází
opakovaně = doloženo) → clamp `gw_hi·gh_hi` odmocninou (škáluje obě strany rovnoměrně, poměr drží), stranový
`MAX_PX` ponechán jako sekundární pojistka pro extrémní poměry. Opraven docstring drift („≈ 2 m" → 1,5 m).
Výsledné rozměry: NL/LS/SV → 3122×2081 (1,92 m/px), HS → 2549×2549 (1,96 m), NV → 1974×3291 (1,52 m, portrait
se skoro vejde na cíl). Live fetch NL nových rozměrů **OK**. Tiling pro plné 1,5 m všude = budoucí (TODO).

Zvažovány a zamítnuty: **tiling** (víc kódu, Sez. 63 ho vědomě odložil) a **retry-degradace** (drží 1,5 m kde
server dovolí, ale rozlišení nekonzistentní mezi mapami). Plošný cap = nejmenší změna, sjednotí rozlišení.

### Regen všech 5 DEV (konzistentní stav)
Plošný cap → reprodukovatelnost jedním parametrem (kořen problému byl nekonzistentní stav po Sez. 63).
Skály 206 z DMR (počty se liší od Sez. 63, protože rozlišení se sjednotilo): **NL 197 / LS 219 / SV 239 /
HS 936 / NV 49**. NL/LS/SV teď mají **skutečnou DMR rock-relief**, ne starý ZABAGED blob. SV skály řídké/drobné
(Lužické hory ≠ skalní město jako Hrubá Skála 936). Vše exit 0.

### Forest-age: SV bílý les je doložená mezera, ne bug
Uživatel: *„Soví vrch: les je bílý, ani jeden hustník."* → ověřeno AOPK probem: **SV vrací 0 porostních skupin**
(Sez. 63 hlásilo 4 „slívky", dnes 0 — Lužické hory v AOPK „Les_Mapy" datasetu nejsou). Forest-age na SV nikdy
nebude, dokud AOPK to území nepokryje. Dnešní změna se forest-age nedotkla (jen rock fetch).

Histogram `BARVA` jako reference pro OOM verify (rozpad na ISOM zelené):

| lokalita | skupin | 410 fight | 408 walk | 406 slow | bílá |
|---|---|---|---|---|---|
| Nová Louka | 213 | 4,2 % | 13,6 % | 26,8 % | 55,4 % |
| Lidové sady | 12 | 0 | 8,3 % | 0 | 91,7 % |
| Novina | 67 | 0 | 31,3 % | 6,0 % | 62,7 % |

Závěry: **NL = nejlepší forest-age verify** (všechny tři zelené, 410 fight vzácných 4,2 % = nejtmavší má být
skvrnka, ne plocha). **LS slabý** (jen 12 skupin, 1 zelená — městsko-lesní pod Ještědem, řídká data; nečekat
tam zeleň). Kalibrační knoflík: **`BARVA 11` balancuje na hraně** — NL má 55× BARVA 11 (26 %), dnes → bílá
(`BARVA_SLOW_MAX=11` exkluzivně); řez 12 by je překlopil na 406 → skokově víc zeleně. Ostatní řezy zdravé.

### Verify + propagace
Změnil se jen `generator/rock_relief.py` (maps/ gitignored). proc baseline neměřen explicitně tady, ale
rock_relief je jen v `--rocks real` (noise ho nevolá) → baseline 65 nedotčen. Propagace: spec §4.9f (rozlišení
+ plošný cap + nález o nekompletním Sez. 63 regenu). Architecture/GLOSSARY/katalog netřeba — není nová vrstva,
jen oprava existující rock-relief.

### Kudos / Censure
- **Kudos uživatel:** postřeh „OMAP má včerejší datum" = spustil odhalení nekompletního Sez. 63 regenu
  (verify-against-source nad mtime); „les je bílý" = legitimní kontrola, byť doložená mezera.
- **Proces dobře:** nehádat (mtime → empirický práh serveru → fix), volba KISS plošného capu, live fetch
  test před plným regenem, histogram jako číselná OOM reference.
- **Censure 0** v tomto sezení; nález ale ukazuje, že **Sez. 63 „8 map regen" nebyl ověřen** (counts v diáři
  byly z compare běhu, ne z uložených map) — propagovat poučení: po regenu ověřit mtime/maska, ne jen log.

### Příště (Sez. 66)
1. *(carry, tvoje)* **OOM `.omap` verify** — forest-age 406/408/410 na **Nová Louka** (nejlepší, 213 skupin) +
   Novina; rock-relief 206 na **Hrubá Skála** (936, skalní město) + Soví vrch (239 řídké). Sleduj `BARVA 11`
   hranici → případně `BARVA_SLOW_MAX` 11→12 + regen.
2. *(cadence, zralé)* **IDEAS/TODO pruning** (+14 od Sez. 50, práh 12 překročen o 2). %AUDIT:CODE +5 od Sez. 60.
3. *(carry)* compare hloubka vektor-na-vektor / Slovanka UTM33 / zeleň gen vs reálná zmapovaná.

---

## Sezení 66 — strategická diskuse (zelená = jen forest-age; ortofoto predikce = UC5, ne deterministická vrstva) + příprava OOM verify

Stroj: **mrkla**. `%BEGIN`: sync OK (lokál == origin/main `678caee`, clean), žádný neuzavřený %END.
Cadence: **IDEAS/TODO pruning +15 od Sez. 50 → práh 12 překročen o 3** (zralý, carry — fokus zůstal na
foundations); %AUDIT:CODE +6 od Sez. 60, %AUDIT:DOCS +9 od Sez. 57, %CALIBRATE +14 od Sez. 51 (práh 15, +1
od zralého). Stale Příště: čisto. **Bez kódu** — sezení diskusní + příprava verify.

### Otázka 1: „zelenou děláme zatím jen z forest-age?" — ANO, s upřesněním
Rozlišení, ať „zelená" nesplývá do jedné věci:
- **Plošná runnability zeleň lesa (406/408/410 jako průchodnost podrostu) = jediný zdroj `--forest-age`**
  (AOPK věk porostu, **PROXY**). Důsledek vegetace gate (Sez. 59): open ČÚZK data neumí věrný podrost →
  věk je hrubý proxy, jediná predikční cesta, co máme.
- **Ale ne každý zelený symbol jde z forest-age** — z TVRDÝCH dat (ne predikce, gate neporušují): **406**
  i ze **stromořadí** (`--treerows`, `Liniová vegetace` → buffer „lineární les", alej ne plošný hustník);
  **402 / 402.1** udržovaná zeleň (`--surfaces`, `typ_pudy_k` park `PO`→402 / ostatní zeleň `UZ`→402.1).

### Otázka 2: „proč jsou HS a SV bílé?" — doložená datová mezera, ne bug
**HS a SV nejsou v AOPK „Les_Mapy" datasetu** (Sez. 65 probe: SV 0 porostních skupin; Lužické hory / Český
ráj mimo) → forest-age nemá z čeho kreslit → les bílý vždy, dokud AOPK to území nepokryje. Důležité odlišení:
- **Bílý les na HS/SV = datová mezera** (mimo dataset).
- **Bílý les na NL/NV = záměrná predikce** (`BARVA` nad `BARVA_SLOW_MAX` = starý/průchodný les = bílá).

### Otázka 3: „co dál — ortofoto predikce?" — oponováno z VLASTNÍHO měření (Sez. 63/64)
Ortofoto cesta byla 2× measure-first probnuta, oba výsledky negativní pro deterministickou cestu. Rozlišení
dvou významů „ortofoto predikce":
- **(a) Single-epoch greenness → ISOM zelená třída = ZMĚŘENO, NEJDE** (Sez. 64 drátěný model, Velbloud:
  separabilita ~50 % = náhoda). Strukturální důvod: NDVI odlišuje vegetaci od ne-vegetace, ale **všechny ISOM
  zelené třídy JSOU vegetace** (open=tráva, les/hustník=koruna → spektrálně totéž shora); podrost je shora
  neviditelný = vegetace gate.
- **(b) Multi-temporal časosběr → věk → krmí UC5 model = koncepčně nejsilnější, ale UC5** (Sez. 63: naivní
  2-epoch NDVI diff = 12,8 % „holin" z velké části šum; CIR je display-PNG ne reflektance; pokrytí řídké).
  Závěr IDEAS: „skutečný CV projekt, NE samostatná deterministická vrstva — svou povahou vstup pro PREDIKCI".

**Pointa:** „ortofoto predikce" = **UC5 model**, ne další deterministická `--vrstva` jako forest-age. Je to
gate-breaker, ale velká investice / rozhodnutí o směru projektu. Foundations-before-curtains tlačí: forest-age
+ rock-relief ještě **nejsou OOM-ověřené** → stavět UC5 ortofoto nad neověřenými vrstvami = záclona před základy.

### Příprava OOM verify (mtime check — lekce Sez. 65)
Sez. 65 ukázal past „starého `.omap`" (o hodinu pozadu za rock fází). Před předáním verify checklistu ověřeno
mtime 4 verify lokalit: **NL/NV/HS/SV `.omap` z 2026-06-02 09:13–09:20**, v každé lokalitě `.omap` = `mask_rocks`
= `mask_forest_age` shodný mtime → **konzistentní, opak pasti Sez. 65**. Mapy připravené k otevření.

Verify checklist předán (reference = histogram `BARVA` Sez. 65): **NL** = nejlepší forest-age (213 skupin,
všechny 3 zelené; 410 fight 4,2 % má být skvrnka ne plocha); **NV** = kontrola (jen 408+406, 0× 410);
kalibrační knoflík **`BARVA 11`** (NL 55× = 26 %, dnes → bílá; řez 12 by překlopil na 406 = skokově víc zeleně).
**HS** = hlavní rock-relief verify (936, skalní město); **SV** = řídký konec (239; bílý les = mezera, ne bug).

**OOM verify zůstává carry** — uživatel zavolal `%END` před otevřením OOM (ruční práce, `omap-verify-oom`).

### Verify + propagace
Žádný kód, žádný produkční výstup → proc baseline 65 triviálně drží (nedotčen). Propagační checklist netřeba
(žádná nová vrstva). Jen DIARY index + DONE + tento diár.

### Kudos / Censure
- **Proces dobře:** oponentura ortofoto opřena o vlastní měření (Sez. 63/64), ne o pocit — `verify-data-not-assume`
  platí i na směrová rozhodnutí; mtime check před předáním verify (lekce Sez. 65 aplikována).
- **Censure 0.**

### Příště (Sez. 67)
1. *(carry, tvoje)* **OOM `.omap` verify** — NL/NV forest-age + HS/SV rock-relief; dle nálezu `BARVA_SLOW_MAX`
   11→12 + regen.
2. *(cadence, zralé +15)* **IDEAS/TODO pruning** (práh 12 překročen o 3).
3. *(směr projektu)* rozhodnout, jestli **UC5 ortofoto model** je další velký skok (age proxy z multi-temporal
   struktury = první měřitelný střípek), nebo compare hloubka / Slovanka UTM33 dřív.

---

## Sezení 67 — OOM verify Test OK + IDEAS/TODO pruning + %THINK směr → UC5 runnability korpus (Livelox)

Stroj: **mrkla**. `%BEGIN`: sync OK (lokál == origin/main `678caee`, clean), žádný neuzavřený %END. Cadence:
**IDEAS/TODO pruning +15 od Sez. 50 → práh 12 překročen o 3** (zralé, dle projektového pravidla „o ≥2 → první
bod"; carry-ováno 3× — dnes provedeno). %AUDIT:CODE +6 od Sez. 60, %AUDIT:DOCS +9 od Sez. 57, %CALIBRATE +14 od
Sez. 51 (nezralé). Stale Příště: bod 1 (OOM verify) = carry „tvoje" 3×, ale blokovaný na ruční práci uživatele.

### Bod 1: OOM verify Test OK (hlavní carry uzavřen)
Uživatel ověřil v OpenOrienteering Mapperu: **forest-age 406/408/410 (NL/NV) + rock-relief 206 (HS/SV) = Test OK.**
Carry táhnoucí se Sez. 62→66 uzavřen; foundations forest-age + rock-relief hotové → odblokováno uvažování o dalším
UC5 kroku. (`BARVA_SLOW_MAX` 11→12 doladění zůstalo nevyužito — verify prošel bez něj.)

### Bod 2: IDEAS/TODO pruning (cadence reset Sez. 67)
Předloženy 3 kategorie (A bezpečné / B zhuštění / C sporné DROP); uživatel odsouhlasil A+B a všechny 3 sporné DROP.
- **TODO hotové `[x]` pryč** (detail v DONE): holes support + 501.1 (Sez. 54), **forest-age** (carry OOM verify dnes
  uzavřen → celé hotové), balvany-linie 208 → zhuštěno na čistý `[ ]` zbytek `Sesuv_půdy_suť` 210.
- **TODO DROP:** oplocené volné terény (DROP Sez. 57, ploty v datech nejsou), crossability vody (vyvráceno měřením
  Sez. 58), `map.omap` area close-flag verify (vyřešeno desítkami pozdějších OOM verify).
- **IDEAS zhuštěno 5 dozrálých bloků** → odkaz do DONE (prediktor mapy Sez. 23/25, reálné vrstvy ZABAGED 16-57,
  synteticky renderované Sez. 4, ISOM 2000↔2017-2, INSPIRE). Vše dozrálé žije v DONE → bez ztráty informace.

### Bod 3: %THINK směr projektu → volba C (korpus/GT nejdřív)
Po vytěžení UC2 fáze I (~60 %) a OOM-ověření forest-age/rock-relief: 4 směry — A (UC5 ortofoto model) / B (compare
hloubka) / C (korpus/GT nejdřív) / D (UC3 de-purple). Foundations-first oponentura: **UC5 runnability model je
supervised → potřebuje GT z reálných map; bez korpusu nestavitelný (záclona před základy).** Sez. 63/64 už změřily,
že deterministická cesta nejde. **Volba uživatele = C.**

**Conceptual-integrity nález:** teze „trénink = jen syntetika, licence reálných map bezpředmětná" (reframe Sez. 4,
KB `data-sources.md` ř. 257-258) vznikla PŘED doložením vegetace gate (Sez. 59). Platí jen pro **strukturu**; pro
**runnability** je syntetika cirkulární (generátor runnability neumí věrně) → **reálný GT POTŘEBUJE.** KB opravena.

**Pragmatická cesta (volba uživatele):** projekt z ~99 % privátní nekomerční experiment (repo private) → korpus
~100 map **bez předchozí licence**, legalizace (ČSOS) AŽ pokud model funguje. Právní krytí = **TDM výjimka** (AutZ
ČR po novele 2023 / EU DSM 2019/790 — automatizovaná analýza pro vědecký výzkum, nekomerční bez opt-out; přesné
znění k ověření).

### Deep research „zdroje reálných OB map" (103 agentů, ~2,8M tok., 21 zdrojů, 21/25 claims confirmed)
- **Livelox = nejlepší dostupný zdroj** — stažitelný přes interní endpointy `/Data/ClassInfo`+`/Data/ClassBlob`
  (POST classId → URL mapy + 4 rohy quad); 2 nezávislé aktivní OSS nástroje (`yoav28/livelox-map-downloader-extension`
  MIT, `routechoiceslivegps/map-downloader` live web). **Jen RASTR** (PNG; vektor 3. strana nestáhne) → GT runnability
  = barevná segmentace. Georef = **4 WGS84 rohy** (žádný EPSG) → reprojekce S-JTSK 5514.
- Obchází Livelox podmínky („maps not accessible through API for copyright reasons"); práva kartograf/pořadatel.
- **Routegadget** slabší (JPG/GIF 150-200 dpi, strop 1700 px). **MapAnt FI/MapantES VYLOUČIT z GT** (strojové
  z LiDAR Karttapullautin → trénink cirkulární). Žádný hotový ML korpus OB map (jen metodické papery).
- **Petrovič 2018 (peer-reviewed ICA) VALIDUJE směr:** paradigma „GT = co kartograf nakreslil"; derivace zelené
  z LiDAR hlučná — i po tuningu ~47 % overlap, zelené třídy nejhorší ~30-31 % → ML nad deterministickým prahem má smysl.

### %THINK georef pipeline (gen jako reference = inverze compare)
Nápad uživatele: použít náš generátor (tvrdá geometrie ZABAGED v S-JTSK) jako georeferenční referenci pro fit
staženého reálného rastru. Pipeline: ORIS lookup (název→lokalita, jen metadata/fallback — nepřidává nad quad) →
Livelox download (rastr + quad) → gen projekce téže lokality jako kotva → feature-fit (podobnostní transformace
x/y/scale/**rotace = grivace**; cesty/voda univerzálnější než olivové areály; multi-layer) → georef rastr →
segmentace zelená/žlutá = GT. Lokální deformace → reziduál OK (plošná runnability chce ~metr, ne pixel).

**Dvě gates measure-first (dřív než korpus 100 map):** gate 1 rozlišení (Livelox full-res, nebo down-scaled náhled?)
+ gate 2 přesnost quadu (sedne reprojikovaný quad rovnou na ortofoto, nebo nutný fit? — pokud přesný, ORIS i fitter
overkill). Probe lokalita = závod uživatele (`classId=1116300`), olivový areál **50.6906797N, 14.8303997E**.
Nástroje: `connectors/livelox.py` + GT segmentace (jisté), `oris.py` / georef fitter (contingency — „stav až s důkazem").

**Formát-rozhodnutí (uživatel):** stáhnout OBA — vektor = GT (preferovaný, z Livelox nejde → od kartografů),
rastr = picture (UC3/UC4-III + fallback); párovat přes společný georef.

### Verify + propagace
Bez produkčního kódu (proc baseline 65 triviálně drží, nedotčen). Propagace: IDEAS (nový blok „UC5 runnability
korpus" + 5 zhuštěných), TODO (pruning + measure-first probe položka), KB `data-sources.md` (Livelox sekce + oprava
rozporu ř. 257-258), GLOSSARY (Livelox + runnability), DIARY index, DONE, tento diár.

### Kudos / Censure
- **Kudos uživatel:** nápad „gen jako georef reference" (inverze compare = DRY); pragmatický reframe „99 %
  experiment" odblokoval korpus; konkrétní kotva (areál 50.69N/14.83E) pro gate 2.
- **Proces dobře:** foundations-first oponentura (UC5 model bez korpusu = záclona), deep research opřel směr o cizí
  měření (Petrovič), measure-first gates dřív než stavba, conceptual-integrity nález (KB rozpor reframe Sez. 4 × gate Sez. 59).
- **Censure 0.**

### Příště (Sez. 68)
1. *(carry, UC5 korpus)* **Measure-first probe Livelox** — gate 1 (rozlišení) + gate 2 (přesnost quadu) na závodě
   uživatele (`classId=1116300`, areál 50.6906797N 14.8303997E). Dle výsledku → `connectors/livelox.py` + GT segmentace.
2. *(cadence)* %AUDIT:CODE +6 od Sez. 60 (LOC sleduji), %CALIBRATE +14 od Sez. 51 (práh 15, příště zralý).
3. *(carry)* compare hloubka vektor-na-vektor / Slovanka UTM33 — vedlejší, až korpus cesta poběží.

---

## Sezení 68 — Livelox probe gate 1+2 PROŠLY → `connectors/livelox.py` + `map_gt.py` GT + georef blend

Stroj: **mrkla**. `%BEGIN`: sync OK (lokál == origin/main `cb1b944`, clean), žádný neuzavřený %END.
Cadence: pruning reset Sez. 67 (+1); %AUDIT:CODE +7 od Sez. 60 (LOC roste o livelox+map_gt, sleduji);
%AUDIT:DOCS +10 od Sez. 57 (na prahu, ne +2); %CALIBRATE +15 od Sez. 51 (na prahu, ne +2). Žádný práh
překročen o ≥2 → audit nevynucen. Stale Příště: čisto. Fokus z Příště Sez. 67 = Livelox probe; volba
uživatele (2× AskUserQuestion): **port endpointů do Pythonu**, žádný audit napřed.

### Foundations PŘED kódem — request tvar ze zdroje (verify-against-source)
Request tvar ověřen ze zdrojového kódu OSS nástroje (`yoav28/livelox-map-downloader-extension`, MIT,
`src/popup.js` přes `gh api`), ne hádán:
- `POST https://www.livelox.com/Data/ClassInfo`, body `{eventId:null, courseIds:[], relayLegs:[],
  relayLegGroupIds:[], classIds:[<int>]}`, hlavička `X-Requested-With: XMLHttpRequest` (jinak 400) → `general.classBlobUrl`
- `GET <classBlobUrl>` (Azure blob) → `map.images[]` + `map.projectedBoundingQuadrilateral` (georef).

### Probe na 4 mapách (závod + 3 dodané uživatelem)
Závod 1116300 (areál 50.69N/14.83E) + **uživatel dodal 3 rozmanité**: Mimoň 1116255, Peklicko 1144077,
Slezsko 1192962 (různé oblasti/měřítka/kartografové). Parametrizovaný probe `temp/livelox_probe/run.py`.

### GATE 1 — rozlišení: PROŠEL s výhradou
- Stažitelné maximum = `map.images[0]` (největší ne-thumbnail) = **1236×1784 px → 1,33 m/px**. `map.tiles`
  (4–12 dlaždic) pokrývají PŘESNĚ rozměr `images[0]` → jen rozřezaný tentýž obraz, **NE vyšší rozlišení**.
  `images[1..6]` = thumbnaily (pyramida 22×32…709×1024, `isThumbnail=True`).
- Deklarovaná nativní `map.resolution = 0,75 m/px` (= 2× větší) je **server-side, přes API nedostupná**.
- **1,33 m/px konstantní napříč velikostmi i měřítky** (1:10000 i 1:15000 Slezsko — rozlišení vázané na
  terén, ne mapScale). Barvy stažených PNG = reálné OB mapy (zelená 5–14 %, žlutá 4–12 %), ne placeholder.
- **Pro PLOŠNOU runnability GT** (zelená/žlutá, min. ISOM plocha ~5×5 m ≈ 4 px) **stačí**; jemné symboly ne.

### GATE 2 — přesnost quadu: PROŠEL, fit NENÍ potřeba
- Livelox dává **`projectedBoundingQuadrilateral`** (4 rohy v CRS mapy) + `boundingQuadrilateral` (WGS84).
  Reprojekce CRS→S-JTSK 5514 (pyproj), afinní warp mapy do axis-aligned S-JTSK gridu, blend s ortofotem
  ČÚZK (`ortofoto._export_tile`) → **quad sedne BEZ feature-fitu na všech 4 mapách** (vizuál verify: lesní
  okraje, cesty, vodní plochy, mýtiny sedí; žádné zdvojení = systematický posun by se projevil).
- → **`oris.py` lookup i georef fitter jsou OVERKILL** — princip „nestav fitter, dokud quad neselže" (IDEAS)
  potvrzen měřením. Plocha quadu (rovnoběžník) přesně sedí na `map.area` = vnitřní konzistence.

### 🔴 KLÍČOVÝ NÁLEZ — CRS číst z dat, nikdy hardcode
CRS se mezi mapami LIŠÍ (viděno **S-JTSK 5514 i UTM33 32633**) a **NEZÁVISÍ na geografické poloze**:
Slezsko 18,8°E (geograficky UTM zóna 34) bylo nahráno v **S-JTSK 5514**, ne UTM34. CRS = co kartograf
nastavil v OCAD. → connector MUSÍ brát `projectionEpsgCode` z blobu, pyproj reprojikuje univerzálně.

### Kód (2 nové moduly, UC2/UC5 konektor — mimo generátor, proc baseline nedotčen)
- **`connectors/livelox.py`** — `download_map(classId)` → `resources/livelox/<classId>/`: `map.png`
  (největší ne-thumbnail) + `meta.json` (quad WGS84 + projikovaný, **epsg z dat**, name, mapScale, m/px,
  provenance/licence) + `blend.png` (`make_blend=True`, warp přes ortofoto = georef důkaz; `build_georef_blend`
  + afinní helpery `_fit_affine`/`_warp_to_grid`, lazy importy numpy/pyproj/ortofoto). Idempotentní
  (existující map.png přeskočí). Request tvar = zdroj extension; sourozenec dmr/zabaged/ortofoto (urllib).
- **`connectors/map_gt.py`** — `segment_gt(map.png)` → `gt_labels.png` (index 0=průchodný/1=406 slow/2=408
  walk/3=410 fight/4=open, trénink) + `gt_vis.png` (barevná, verify). Nearest-color na ISOM refs +
  majority(7px) filtr (potlačí tenké linie uvnitř ploch — vrstevnice/cesty/symboly). **ISOM_REF = KOPIE
  z `compare_real_vs_gen` (Sez. 64)** — connectors nezávisí na generator; DRY dluh do TODO (extrakce až 3. konzument).

### Volba umístění korpusu (AskUserQuestion) — `resources/livelox/<classId>/`
Odděluje auto-stahovaný korpus od ruční compare sbírky (6 map v `resources/` kořeni), per-mapa struktura
(map.png + meta.json + gt_labels/gt_vis + blend). Gitignored jako celé `resources/` (kryje TDM/privátní režim).

### GT probe (verify before invest, `temp/livelox_probe/gt_probe.py`)
Nearest-color + majority(7px) → **použitelná plošná runnability GT** (vizuál: zelené plochy 3 úrovní +
žluté souvislé, sedí na originál). **Omezení doložené:** olivová 520 (oplocené areály) NENÍ v refs →
klasifikuje se jako brown; runnability GT nevadí (520 = out-of-bounds, ne běhatelnost). Rozpady 4 map:
zelená 7–18 %, open 6–23 % (Slezsko nejbohatší).

### Škálování na ~200 map (vstup uživatele) → příští fokus
Uživatel: Livelox `?tab=allEvents&country=CZE` = seznam ČR eventů; ORIS detail má souřadnice shromaždiště.
**Oponentura (ušetří půlku práce): ORIS souřadnice NEPOTŘEBUJEME** — gate 2 dokázal, že Livelox blob nese
vlastní přesný georef → `download_map(classId)` dá mapu I georef z jednoho zdroje. Pipeline = **allEvents →
seznam classId → batch download+segment** (žádné ORIS/párování názvů). Zbývá probnout: jak z allEvents
vytáhnout classId (reverse endpoint jako ClassInfo) + rate-limit. Carry-ováno.

### Verify
- 4 mapy stažené + GT + blend; **vizuál sedí** (uživatel viděl blendy přes ortofoto). Produkční blend
  identický s probe (přesun do livelox.py bez bugu).
- **proc baseline 65 nedotčen** — nový kód je UC2/UC5 konektor mimo `generator/` (generator.py netknut).
- **gitignore ověřen**: `git check-ignore resources/livelox/.../map.png` ✓; `git status` ukazuje jen
  `connectors/livelox.py` + `map_gt.py` (nový kód) untracked, korpus se necommituje.
- `epsg` čten z dat ověřen na 4 mapách (2× 5514, 2× 32633); idempotence ověřena.

### Propagace
architecture (UC5 zdroj Livelox), connectors README (livelox + map_gt řádky), root README (status +
repository layout), KB data-sources (Livelox sekce + gate výsledky + konektor hotov), GLOSSARY (GT
runnability / gt_labels / blend), TODO (probe `[x]` → allEvents škálování + DRY dluh ISOM_REF), DONE, DIARY index.

### Audit cadence
pruning reset Sez. 67 (+1). %AUDIT:CODE Sez. 60 (+8; LOC narostl o livelox+map_gt → zralý kandidát).
**%AUDIT:DOCS Sez. 57 (+11 → zralý)**, **%CALIBRATE Sez. 51 (+16 → zralý)**. Stale: čisto.

### Kudos / Censure
- **Kudos uživatel:** dodal 3 rozmanité classId (různé epsg odhalily hardcode riziko — Slezsko 5514 ne
  UTM34); nález allEvents+ORIS pro škálování; nápad blend do korpusu (parádní georef důkaz); pragmatický
  reframe „99 % experiment"; konkrétní kotva areálu pro gate 2.
- **Proces dobře:** request tvar ze ZDROJE (gh api extension), ne hádán; measure-first 2 gates dřív než
  konektor; gate 2 ověřen na 4 mapách ne jediné (odhalil CRS variabilitu); GT probe (verify) před produkčním
  modulem; oponentura „ORIS netřeba" opřena o gate 2 měření (ušetří půlku škálovací práce).
- **Censure 0.**

### Příště (Sez. 69)
1. *(carry, UC5 korpus)* **allEvents → batch ~200 map** — reverzovat endpoint pro seznam classId (jako dnes
   ClassInfo ze zdroje), batch `download_map`+`segment_gt`, rate-limit ohled. ORIS netřeba (blob georef stačí).
2. *(cadence, zralé)* **%CALIBRATE** (+16/15) / **%AUDIT:DOCS** (+11/10) — oba přes práh, úklid před dalším kódem.
   %AUDIT:CODE LOC narostl (livelox+map_gt).
3. *(carry, vedlejší)* compare hloubka vektor-na-vektor / Slovanka UTM33.

---

## Sezení 69 — %CALIBRATE + %AUDIT:DOCS (úklidové sezení, oba audity zralé)

Stroj: **mrkla**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (`9e6d890`, clean), žádný neuzavřený %END.
Cadence: **%AUDIT:DOCS +11/10** a **%CALIBRATE +16/15** oba přes práh (zralé). %AUDIT:CODE +8 od Sez. 60
(LOC roste o livelox+map_gt, sledováno). pruning +2 (reset Sez. 67).

### Volba fokusu (AskUserQuestion) — „2 pak 1"
Z Příště Sez. 68: bod 1 = škálování korpusu (`[!]`), bod 2 = audity. Doporučil jsem **audity napřed**
(foundations-before-curtains: dva moduly ze Sez. 68 — `livelox.py`/`map_gt.py` — ještě neprošly úklidem
a škálovací kód na ně naváže). Uživatel: **„Spusť 2 a pak uděláme 1"** → audity teď, škálování příští sezení.

### Stale Příště check — compare/Slovanka 9× → DROP
Bod „compare hloubka vektor-na-vektor / Slovanka UTM33" visel jako **vedlejší carry od Příště Sez. 59 → 9×**,
daleko přes práh 5. Eskalováno (volba uživatele = **DROP** — zůstává v TODO jako nález, přestane se navrhovat
v Příště; mirror DROP oplocených terénů Sez. 57). Root cause → C2 (Stale check nesledoval vedlejší body).

### Metoda — dva audity najednou (precedent Sez. 34/57), jedno čtení docs
%CALIBRATE jsem dělal sám (meta-proces vyžaduje porozumění průběhu). %AUDIT:DOCS přes **3 paralelní agenty**
(rozděleno: status&architektura / KB / sub-READMEs&tracking), nálezy **kriticky profiltrované proti zdroji**
(lekce Sez. 46 — agenti halucinují). Z ~15 agentích nálezů potvrzeno 5 tvrdých + 3 měkké; zbytek zamítnut
jako preference/šum (Livelox do README status-bloku, wikilink kotvy, interpunkce, dlouhý connectors výčet).

### %AUDIT:DOCS — 5 tvrdých nálezů (ověřeno proti zdroji) + 3 měkké, vše opraveno
- **T1** `README.md:81` `DEV_LOCATIONS` zastaralé: „3 lokality, all 6×4 km" → kód (`generator.py:593`) má **5**
  (HS 5×5, NV 3×5 km) + „Nová louka" → „Nová **L**ouka". Opraveno.
- **T2** rokle → **107 Erosion gully** chyběla ve výčtu `--linefeatures` ve **3 souborech** (Sez. 58 nepropsána):
  `README.md:54`, `architecture.md:64`, `generator/README.md:64`. Doplněno.
- **T3** `generator-procedural.md` drží staré **301.1** (voda) na 3 místech (ř. 220/683/685) — kód Sez. 58
  změnil na **301 combined** (`generator.py:3550`, `omap_export.py:52`). Opraveno 301.1 → 301 (3×).
- **T4** `generator/README.md:54` „Parkoviště → **501**" — Sez. 57 opravila na **501.1** (bez obrysu, průchozí).
  Opraveno.
- **T5** `zabaged-isom-catalog.md:228` překlep `Maloplošné_zvlástě_` (sesterská ř. 229 má správně „zvláště").
  Opraveno (asymetrie = chyba psaní, ne verbatim ČÚZK typeName).
- **měkké:** `tools-models.md:14` Karttapullautin prázdný odkaz `—` → URL z `RESEARCH.md:97` (DRY, ne hádáno);
  `GLOSSARY` doplněno vlastní heslo **rock-relief 206** (velká změna Sez. 63, jen se mihla v 208);
  `generator-procedural.md` §4 stav-blok („generátor dělá 3 věci", Sez. 11) doplněn o real-půlku §4.9 (Sez. 16-68).

### %CALIBRATE — 5 nálezů, vše opraveno
- **C1 (tvrdý, opakující se 3×):** `settings.local.json` allow-list **~190 → 15**. Z ~190 bylo ~40 odkazů na
  **zrušený `sandbox/generator-poc/`** (mrtvé od Sez. 39) + desítky jednorázových scratch s absolutními cestami
  (přesný match, nikdy se znovu netrefí). Ponecháno 15 generalizovatelných (`git *`, venv `python *`, py_compile
  přes wildcard, klíčové WebFetch domény cuzk/uhul/livelox). Hooks (beep) zachovány. (Předchozí pruning: Sez. 17
  36→17, Sez. 51 50→22 → bobtná zpět, protože každý approve scratch zůstává; lokální gitignored soubor.)
- **C2 (proces):** PROMPTS %BEGIN Stale check **nezachytil** compare/Slovanka 9×, protože sledoval jen hlavní
  fokus (bod 1). Doplněno: **počítat VŠECHNY Příště body, i vedlejší/carry**.
- **C3 (drobnost):** cadence pravidlo „překročen o ≥2 → první bod" vs praxe „zralý při +1" sladěno: **práh
  dosažen (≥) = zralý kandidát** (nabídni), **o ≥2 = vynucený první bod** (úklid má přednost).
- **C4 (hygiena):** projektový `CLAUDE.md` „Klíčové soubory" popis `connectors/` z éry Sez. 16 → doplněny
  `ruian.py` (Sez. 42), `forest.py` (Sez. 62), `ortofoto.py`, `livelox.py`+`map_gt.py` (Sez. 68) + `arcgis.py`
  sdílený transport (místo neúplného „sdílí `dmr.build_bbox`"); pozn. že `rock_relief.py` je v `generator/`.
- **C5 (token efektivita):** `DIARY.md` index ~28k tokenů (čte se celý každý %BEGIN, překračoval read cap →
  stránkování). Jednorázově **zkráceno 6 nejdelších hooků** (Sez. 26/32/33/42/43/50) na 1-2 věty + ISOM kódy
  (vědomé porušení „staré nepřepisovat" — volba uživatele; úspora opakovaná každý %BEGIN).

### Cleanup
`temp/rockcore/` (README + HANDOFF_FOR_AI) smazán — obsolete scratch po dokončeném handoffu do
`generator/rock_relief.py` (Sez. 63). Gitignored, nebyl v repu.

### Verify
- **Žádný kód generátoru nedotčen** — `git status` ukazuje jen 10 tracked docs/config souborů (README, CLAUDE,
  4× docs, 3× kb, 2× sub-README), 0× `.py`. proc baseline 65 triviálně drží (behavior-preserving).
- T1-T5 ověřeny proti zdroji PŘED opravou (kód `generator.py`/`omap_export.py`, sesterské řádky katalogu).
- `settings.local.json` je gitignored → necommituje se, neovlivní druhý stroj.

### Propagace
Vše opraveno v dotčených souborech přímo (to BYL účel auditu). %END: DONE záznam, DIARY index, TODO (DROP
compare/Slovanka). Cadence reset: **%AUDIT:DOCS a %CALIBRATE oba Sez. 69**.

### Kudos / Censure
- **Kudos uživatel:** jasná volba „2 pak 1" (úklid před škálováním = foundations); DROP compare/Slovanka
  (uvolnil 9× viselou položku).
- **Proces dobře:** agentí nálezy profiltrovány proti zdroji (5/15 tvrdých, zbytek zamítnut jako šum —
  kontrast halucinacím Sez. 46); Karttapullautin URL z existujícího doc (DRY, ne hádáno); C1 odhalil systémový
  vzorec (allow-list bobtná 3×) → root cause pojmenován.
- **Censure 0** (verify-against-source dodržen u všech 5 tvrdých DOCS nálezů i C1 mrtvých cest).

### Příště (Sez. 70)
1. *(carry, `[!]` UC5 korpus)* **allEvents → batch ~200 map** — reverzovat Livelox `allEvents` endpoint
   (seznam classId), batch `download_map`+`segment_gt`, rate-limit ohled. ORIS netřeba (blob georef stačí, gate 2).
2. *(DRY dluh)* `ISOM_REF` triplikát (`compare_real_vs_gen` + `map_gt.py`) — extrakce až 3. konzument.
3. *(cadence)* %AUDIT:CODE +9 od Sez. 60 (LOC narostl o livelox+map_gt+forest+rock_relief) — zralý kandidát.

## Sezení 70 (2026-06-02) — UC5 korpus škálování: `allEvents` reverz → batch 268 reálných OB map

Fokus z Příště Sez. 69 bod 1 (`[!]` UC5 korpus). Rozhodnutí uživatele 2× AskUserQuestion: **batch rovnou**
(ne probe-skript jako produkt), **1 class/event** (~840 map ne 8267 tříd), **měřit na vzorku pak full**;
geografie **severní Čechy + historie téhož prostoru** (cenná pro UC5 časová řada); navíc **DE příhraničí
u bydliště** (Žitavsko-Šluknovsko, série SAXBO).

**Reverz `allEvents` (verify-against-source proti home.js):**
- `?tab=allEvents` je jen URL Knockout SPA; skutečný endpoint = **`POST /Home/SearchEvents`** (JSON).
- Tělo (z `home.js` bundle): `geoRectangle` = **GeoBox {south,north,west,east}** (ne corners — můj první omyl),
  `timePeriod` enum ('allTime'/'customTimePeriod'/…), `from`/`to` server čte **JEN při customTimePeriod**,
  `orderBy` ∈ {time,relevance,distance,participantCount}, `maxNumberOfResults` strop **500**.
- Event nese `classes:[{id,name,participantCount}]` → `id` = classId pro `download_map`. **Žádný ORIS/párování**
  (potvrzuje gate 2 Sez. 68).
- **Klíč: 8267 tříd ≠ 8267 map** — třídy 1 eventu (H21/D21/…) sdílí JEDNU mapu (různé tratě) → pro runnability
  GT identické. 1 class/event (max participantCount) dá ~840 unikátních map; historická řada vzniká mezi EVENTY
  (Slovanka 9×, Bedřichov 2×), ne třídami → zachována.

**Měření severních Čech** (geobox 50.4–50.95N / 14.3–15.5E, po ročních oknech): **842 CZ eventů** celá historie,
nárůst od 2020 (žádný rok nenarazil na strop 500 = kompletní). DE Žitavsko +25 → **865 eventů**.

**Batch pipeline** (`livelox.py`): `search_events` / `list_events_by_year` (roční okna obchází strop 500) /
`pick_class_id` / `download_corpus` (idempotentní, error-souhrn, progress+ETA) + CLI `list`/`batch`. Konstanty
`NORTH_BOHEMIA_BOX` + `LUSATIA_BORDER_BOX`. Batch jede **od nejnovějších** (cenná data první, staré faily na konci).

**Bug fix (nález uživatele „adresáře 2* prázdné"):** `download_map` volal `mkdir` PŘED `fetch_class_blob` →
faily nechávaly prázdné adresáře. Přesunut mkdir až ZA získání dat (blob+image+meta+PNG do paměti, pak teprve
adresář+zápis). 30 starých prázdných uklizeno.

**ORIS návrh uživatele ZAVRŽEN s daty (synergie):** „pro staré bez blobu ztotožnit přes ORIS souřadnice závodu".
Měření 50 starých (2020–22): **typ A 56 %** (`classBlobUrl: None`, jen tratě+GPS routy, mapa fyzicky není) +
**404 40 %** (blob smazán) + **typ B 4 %**. → 96 % starých **nemá rastr** → ORIS dá souřadnice ne kresbu → nepomůže.
ALE měření odhalilo **typ B** (rastr+georef JSOU, jen pod `boundingQuadrilateral` WGS84 místo
`projectedBoundingQuadrilateral`; `projectionEpsgCode: None`) → **WGS84 fallback zachrání napříč lety, ne ORIS**.

**Krok 2 (uživatel „1 potom 2"):** (a) **WGS84 fallback** v `_build_meta` (vyčleněn `_resolve_georef`: epsg=4326,
quad z WGS84 rohů {x:lon,y:lat}, mpp přes metrickou aproximaci stupňů; `georefFallbackWgs84` flag) — georef
ověřen blendem (gate 2 sedí i pro fallback); (b) **`_open_with_retry`** exponenciální backoff 2→4→8s (404/403/410
= trvalé, neretryovat; timeout/5xx/conn = transient) — řeší `WinError 10060` (transient, ne náš rate-limit, žádné
429) I prevence banu (sebeškrtící); sjednoceno se `_post_search_events` (DRY); (c) `sleep_s` 0,5→1,0.

**Výsledek:** běh 1 = 205 map; re-run krok 2 = **268 map** (+63: 43 typ B + ~20 transient), všech 268 se segmentací
GT, 0 prázdných. Výtěžnost 268/865 ≈ 31 % (zbytek doloženě mrtvý — Livelox staré mapy nemá). **UC5 korpus z 4 → 268.**

**Nálezy (mimo fokus, do TODO):** olivová 520 chybí v `ISOM_REF` → městské mapy (Turnov) mají falešnou zelenou
runnability na zástavbě/zahradách (vizuální verify, oko=source); legenda v layoutu mapy → šum v GT rohu.

**Censure 0.** Kudos uživatel ×2 (postřeh prázdných adresářů = bug; ORIS „blbá otázka" → synergie WGS84 fallback).

### Příště (Sez. 71)
1. *(nález Sez. 70, čistota GT)* **olivová 520 → `ISOM_REF` jako label 0** — městské mapy mají falešnou zeleň na
   zástavbě; přidat olivovou referenci (out-of-bounds, ne runnability). Levné, zvedne kvalitu GT.
2. *(cadence, vynuceno)* **%AUDIT:CODE** — +10 sez od Sez. 60, LOC práh ≥500 překročen výrazně (livelox batch +
   forest + rock_relief + map_gt). Zralý, blíží se vynucení.
3. *(rozšíření korpusu, volitelné)* víc map/event u etapových závodů (dnes 1/event) nebo dedup map podle georef
   overlap; legenda-crop u layout map. Až bude UC5 model konzumovat — zatím 268 stačí.
4. *(DRY dluh, carry)* `ISOM_REF` triplikát (`compare` + `map_gt`) — extrakce až 3. konzument.

---

## Sezení 71 — olivová 520 GT fix + %AUDIT:CODE (vynucený) + kurace korpusu (taxonomie + manifest)

Stroj: **mrkla**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (`4cbc70e`, clean), žádný neuzavřený
%END. Cadence: **%AUDIT:CODE +10 od Sez. 60, LOC práh ≥500 výrazně překročen → vynucený první bod**
(úklid má přednost). %AUDIT:DOCS/%CALIBRATE +2 (čerstvé Sez. 69), pruning +5. Stale Příště: čisto.
Volba uživatele: **B (olivová) pak A (audit)**, nakonec kurace + %END.

### B — olivová 520 → label 0 (čistota runnability GT)
`map_gt._classify` neměla olivovou referenci → městské mapy klasifikovaly out-of-bounds (520) jako
falešnou zelenou runnability (Turnov gt_vis = bloky zahrad světle zelené). Verify-against-source:
olivovou jsem NEvzal z generátorové aproximace `C_OLIVE`, ale **odečetl z reálných rastrů** — histogram
dominantních barev → **dva odstíny** (`olive_a` 152/184/24 sprint, `olive_b` 168/168/56 lesní OCAD profil),
oba padaly na `green_m`. Přidány obě reference → label 0. Math + verify: nekradou legitimní zeleň
(`green_m` u obou >10000 vzdálená, vlastní ref 0). **Verify:** Máchovka olivová vesnice → bílá; lesní mapy
olivová 1–7 %. Korpus 268 map přesegmentován (`segment_corpus`).

**Nález při opravě (Turnov sprint):** olivová byla jen TŘETINA falešné zeleně — dominovala **růžová
canopy** (248/200/184 → green_l) + **šedé budovy** (120/120/120 → green_m). Na LESNÍCH mapách obě <1,5 %
→ sprint artefakt, ne cross-cutting bug. Volba uživatele **„jen olivová + kurace"**: růžovou/šedou NEřešit
v segmentaci (ladit segmentaci na mapách, co stejně nepatří do tréninku = špatně) → patří do kurace.

### A — %AUDIT:CODE (+1121 LOC od Sez. 60, 0 kritických)
Čten celý nový kód: `livelox.py` (483), `rock_relief.py` (247), `forest.py` (121), `map_gt.py` (127) +
diff `generator.py` (+151 forest-age/rock integrace). Zdravý; B1 lekce dodržena (batch.py `forest_age="off"`
obě větve), CRS-z-dat i WGS84 fallback správné. Nálezy, **vše opraveno** (uživatel „opravit vše"):
- **D1 (mrtvý kód po Sez. 63):** plocha 206 přešla ZABAGED→DMR, ale ZABAGED půlka zůstala viset —
  `zabaged.fetch_rock_areas` + `map_rock_area_to_isom` + `ROCK_AREA_LAYERS` smazány (nikdo nevolal);
  drift 2 komentářů (zabaged:340, generator:3361) srovnán. Docs (katalog/architecture) **už správné**
  (Sez. 63 propsal) → jen kód lingeroval.
- **K1** `livelox` DRY `_ensure_connectors_on_path()` (sys.path boilerplate 2×), **K2** CLI `classCount`
  přes `.get`, **K3** `forest` komentář řezů (chytá i BARVA 0/1).
- **Behavior-preserving** doloženo: proc objektů 81=81 před/po (git stash, seed 42). py_compile OK,
  reziduum odebraných jmen 0 (jen vysvětlující komentář).

### Kurace korpusu (foundations UC5 GT — měřením podložená)
Otázka uživatele „učit model na nekvalitní/chybné mapě?" → GT je strop supervised modelu. Rozlišeno:
**špatná extrakce** (olivová — řeší segmentace) vs **špatný zdroj** (řeší kurace). Měřením (268 `meta.json`
+ GT rozpad):
- **Překvapení:** green% sprint vs les NEodlišuje (olivová už label 0 → zbylá zeleň reálná). Rozdíl je
  v sémantice label 0 (les = běhatelný bílý les = signál; sprint = ulice/budovy). → hlavní osa = **měřítko**.
- **Recency NEměřitelná** — `meta.json` neukládá datum eventu (gap; korpus stahován od nejnovějších = proxy).

**(b) `connectors/curate.py`** — merge-aware taxonomie + manifest `resources/livelox/_curation.json`
(gitignored). Discipline {classic/sprint/mtbo/overview} + quality tagy (auto: variant_contour/variant_black/
base_layer/basemap/training/foreign_crs; vizuál: legend/logo/damage). `keep = keep_override JINAK (classic
AND bez disqualify tagu)`. Merge zachová ruční vizuál tagy + override přes re-run (ověřeno idempotencí).
Reader `load_curation`/`kept_dirs('classic')` = kontrakt pro UC5 loader.

**(a) vizuál průchod** (kontaktní archy náhledů + index pozice→classId):
- **Refinement:** variant/base tagy gatovány na **green <8** (degenerace = ~bez vegetace; jméno jen hint) —
  opravilo **false-drop `1177415`** „Bohemia podkladovka" (g33 = plná mapa, ne odstrojený podklad).
- **False-keepy chycené okem/jménem:** `1117080` „OSM Bright" (ne-OB OSM podklad = tvoje „zabaged" kategorie
  → nový auto-tag **`basemap`**); `655492`/`656122`/`732266` = **fotky vytištěných map** (papír/sklon/loga/
  control-description tabulka → špinavé GT) → `damage` (disqualifying). `970619` legenda+logo, ale mapa OK → keep.
- Gestalt keep set potvrzen (genuine OB lesní mapy); ne každý high-green je špatný (`1144271` g54 = čistá
  saská jezerní mapa, keep).

**Výsledek: 268 → 216 keep classic / 52 dropped.** Tréninkové jádro = 216 foot-O map s čistou olivovou GT.

### Verify + propagace
- proc baseline 81=81 (git stash); kurace/olivová jsou UC2/UC5 konektory mimo `generator/` proc cestu.
- map_gt olivová: korpus přesegmentován 268/268. curate.py idempotentní (3× build = 216).
- Propagace: DONE, DIARY index, TODO (olivová [x], kurace, audit, follow-upy), GLOSSARY (kurace/discipline/
  keep/basemap), connectors README (curate.py), architecture (UC5 kurace), tento diár.

### Kudos / Censure
- **Kudos uživatel:** otázka „učit na nekvalitní mapě" = otevřela kuraci ve správný moment; postřeh s
  bohatší TAG taxonomií (trénink/zabaged/popisky/logo/poškození + MTBO) místo binárního sprint/les.
- **Proces dobře:** olivová odečtena z reálných dat (ne aproximace); olivová-první jako PŘEDPOKLAD kurace
  (jinak sprint mapy vypadají zeleně); measure-first kurace; visual pass chytil false-drop I false-keep,
  co metadata/green minuly (OSM g46, podkladovka g33); merge-aware manifest (ruční práce přežije re-run).
- **Censure 0.** Drobnost: montáž-náhledy 240px nestačí na spolehlivé čtení classId → řešeno indexem
  pozice→classId + přímým náhledem suspektů.

### Příště (Sez. 72)
1. *(kurace follow-up)* **recency osa** — uložit datum eventu do `meta.json` při stahování (časový nesoulad
   vstup×GT); volitelně doplnit re-dotazem na existující korpus. Pak řez „posledních N let".
2. *(GT kvalita, strukturální)* **legenda/okraj/přetisk crop** — místo per-mapa tagování legend (nespolehlivé
   v náhledu) strukturální GT krok: detekce+mask okrajových legend/control-description tabulek + fialového
   přetisku tratí. Manifest `legend` tagy + green_pct to podpoří.
3. *(UC5 model)* s 216 čistými keep mapami + GT = první runnability model (supervised) — vstupy ortofoto/DMR/
   forest-age → predikce zelená/žlutá. Foundations (korpus+GT) stojí.
4. *(DRY dluh, carry)* `ISOM_REF` triplikát (`compare` + `map_gt`) — teď se rozešel (olivová jen v map_gt);
   extrakce až 3. konzument.

## Sezení 72 — fialový přetisk tratě → label 255 ignore (GT kvalita, část A z Příště Sez. 72 bodu 2)

Stroj: **mrkla**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (`242d953`, clean), žádný neuzavřený
%END. Cadence: %AUDIT:CODE reset Sez. 71 (+0), DOCS +2, pruning +4, %CALIBRATE +2 — žádný zralý. Stale
Příště: čisto. Fokus z Příště Sez. 72 = **bod 2 (GT kvalita)**. Volba uživatele přes AskUserQuestion:
**dnes část A (purpurový přetisk), B (legendy) follow-up; detekce přes referenční barvy** (izomorf olivové
Sez. 71).

### Měření (measure-first) — přetisk je systematický
Hrubá purpurová maska napříč 216 keep classic: **68 map (31 %) nese přetisk** >0,1 %, 41 nad 0,5 %.
→ strukturální krok oprávněný (ne per-mapa tag — ten byl Sez. 71 uznán za nespolehlivý v 240px náhledu;
jen 1 keep mapa měla ruční `legend` tag). Rozliš tenký přetisk vs plošná OOB šrafa (downscale + connected
components): **72 map = jen tenký přetisk** (trať: kroužky/spojnice/čísla/text), **2 mapy = velká OOB
šrafa plocha** (ISOM 709, 262617/990759).

### Implementace (`connectors/map_gt.py`)
Purpurová odečtena **z reálných rastrů** (ne hádaná — verify-against-source, jako olivová): histogram
purpurových pixelů na 7 mapách s tratí → **2 dominantní odstíny** `purple_a` (178,24,148) magenta tratě +
`purple_b` (176,8,230) fialovější. Přidány do `ISOM_REF` → nový **label `IGNORE=255`** (`_LABEL`).
Verify izolovanosti: `purple_a` d=138 od brown, `purple_b` d=199 od blue (obě > olivová ~100); žádná
legitimní ISOM barva (brown/blue/black) na purpurovou nepadne → nekrade.
Pořadí v `segment_gt`: ignore maska oddělena **PŘED** median (255 v okně by zkreslil medián sousedů →
ignore dočasně na 0), median na runnability 0-4, pak maska **dilatovaná** (`_OVERPRINT_DILATE=2` px ≈ 2,7 m,
pokryje antialiasovaný okraj čar) přepsána zpět na 255. `gt_vis` ignore = magenta (255,0,255), breakdown +ignore.

### Rozhodnutí: OOB šrafa (709) → taky ignore, bez výjimky
Velká purpurová OOB plocha (709, dočasně zakázaná oblast) padá do stejné barvy → po dilataci velká magenta
plocha (1027569). Měření: vzácné (2-3 mapy). **Volba uživatele: ignore jednotně, „lepší menší vzorek než
zavádějící výjimky".** Zdůvodnění: (1) „generalizuj jen s důkazem" — 2-3 mapy nejsou důkaz pro extra logiku;
(2) ignore konzervativně poctivé — šrafa zakrývá podklad, bez inpaintingu GT neznáme; (3) KISS/konzistence
jedna barva→jeden mechanismus; (4) pro UC5 model ignore > label 0 (učit „les=průchodný bílý" by bylo špatně —
runnability závisí na podrostu). Kdyby velká OOB vadila u konkrétní mapy: `keep_override=false` (mechanismus už je).

### Verify
- 262617 (nejvíc přetisku): purpurová trať+titulek+tiráž → magenta ignore (8,4 %), zelené/žluté plochy
  netknuté, kroužky nepřefouknuté. **vizuál OK.**
- 970619 (bez tratě, čistý export): ignore **0,0 %** — žádné false-ignore, mapa beze změny.
- 1027569 (lesní s tratí 2,69 %): ignore 4,7 % (+ OOB šrafa → magenta plocha, dle rozhodnutí).
- Korpus přesegmentován (`segment_corpus`, 268/268).

### Propagace
connectors/README (map_gt řádek +255 ignore), architecture (UC5 GT), README (layout +session 72), GLOSSARY
(Ground-truth +ignore label), DONE, DIARY index, TODO (bod 2 část A `[x]`, B follow-up), tento diár.

### Kudos / Censure
- **Proces dobře:** purpurová odečtena z dat (ne aproximace, izomorf olivové); measure-first rozlišil tenký
  přetisk (72) vs OOB plochu (2) → rozhodnutí podložené; verify-against-source izolovanost referencí; OOB
  šrafa zachycena vizuálně (gt_vis 1027569) — neprošla tiše.
- **Censure 0.**

### Příště (Sez. 73)
1. *(GT kvalita, část B z bodu 2)* **legenda/okraj/control-description tabulka crop** — strukturální detekce
   mapového území (největší souvislá komponenta obsahu po morfologickém uzavření) → oddělené bloky (legenda,
   tabulka, tiráž) label 255 ignore. Rizikovější (false-crop připojené legendy) → měřit + vizuál verify.
   Pozn. Sez. 72: bílé buňky control-description tabulky teď zůstávají label 0 (falešná průchodná plocha).
2. *(kurace follow-up)* **recency osa** — datum eventu do `meta.json` při stahování (časový nesoulad vstup×GT).
3. *(UC5 model)* s 216 keep + čistou GT (olivová Sez. 71 + přetisk Sez. 72) = první runnability model (supervised).
4. *(DRY dluh, carry)* `ISOM_REF` triplikát — extrakce až 3. konzument.

---

## Sezení 73 — layout mimo mapové území → label 255 ignore (GT kvalita, část B)

Stroj: **mrkla**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (`63ad41b`, clean), žádný neuzavřený
%END. Cadence: %AUDIT:CODE reset Sez. 71 (+2), DOCS Sez. 69 (+4), pruning Sez. 67 (+6), %CALIBRATE
Sez. 69 (+4) — žádný zralý práh. Stale Příště: čisto. Fokus z Příště Sez. 73 = **bod 1 (GT kvalita, část B)**,
volba uživatele přes AskUserQuestion (před modelem dočistit enabler — foundations before curtains).

### Measure-first vyvrátil geometrii (probe PŘED implementací)
Návrh Sez. 72 zněl „největší souvislá komponenta obsahu po morf. uzavření". Probe na 4 reálných mapách
(732266 tabulka vpravo / 262617 tabulka+titulek / 970619 čistá / 1017132 composite):
- **Naivní největší-komponenta (ne-bílé pixely):** kernel je jeden globální parametr, který musí současně
  překlenout vnitřní mezery mapy A neprostoupit mezeru mapa↔tabulka → nejde naladit. 732266 tabulku SPOJIL
  s mapou, 262617 uřízl kus mapy. **Selhalo.**
- **XY-cut (projekční profil bílých koridorů):** 732266 separátor nenašel vůbec. **Taky křehké.**
→ geometrie (souvislost / bílé koridory) je nespolehlivá, protože tabulka bývá přilepená a mapa má řídké oblasti.

### Hybridní BAREVNÝ detektor (volba uživatele, AskUserQuestion)
Klíčový insight z probu: mapa má sytou ISOM paletu (zeleň/modř/hněď/žluť), mimo-mapové bloky jsou černobílé
(mřížka/text) nebo bílý papír. Barevnost = stabilnější diskriminátor než geometrie. Prototyp v3 (`temp/`):
- `_MAP_COLOR_KEYS` = mapově-barevné klíče `ISOM_REF` (white/black/purple NE).
- Dlaždicová mřížka (~1/120 strany) → dlaždice „mapová" když >4 % mapově-barevných pixelů (`_MAP_TILE_FRAC`).
- **Dilatace scelí řídké okraje** mapy → největší souvislá komponenta + `fill_holes` = mapa (vnitřní bílý
  les se vyplní) → eroze zpět. Zbytek → `IGNORE=255`. (THR=0.04/div=120/dil=3 laděno na 12 mapách.)
- Iterace: vysoký práh rozpadal řídké oblasti (false-crop), flood-od-okraje ukrajoval mapu přes bílý les →
  až „největší komponenta na barevných dlaždicích po dilataci" obojí vyřešilo.

### Kód (`connectors/map_gt.py`)
- **`_classify` refaktor:** vrací index nejbližší ISOM barvy (ne rovnou label) → **jeden argmin** pro
  runnability label (`_LABEL`) i mapovou plochu (`_MAP_COLOR_KEYS`).
- **`_detect_map_area(mappix)`** → bool maska mapového území (dlaždice → maptile → dilatace → největší
  komponenta + fill_holes → nearest resize zpět na pixely). Konstanty `_TILE_DIV/_MAP_TILE_FRAC/_MAP_SCELE_DIL`.
- **`segment_gt`:** layout-ignore aplikován AŽ NAKONEC (`labels[~map_area]=IGNORE`), vedle přetisk-ignore Sez. 72.
- `LABEL_NAME[IGNORE]` → „ignore (přetisk+layout)"; docstring modulu + komentáře.

### Verify (17 map vizuálně, ne cherry-pick)
- 12 prototypových (4 s tabulkou/titulkem + 8 organických) + 5 nových napříč korpusem (rovnoměrný řez keep).
- **Titulky / tiráž / loga (černobílé) / papírový okraj** zachyceny → ignore. **Mapa BEZ false-cropu** na
  žádné z 17. Vysoké mimo-% (medián ~36 %) = legitimní papír kolem organické mapy, ne vada (jinak falešný label 0).
- Korpus přesegmentován **268/268** (`segment_corpus`). 970619 layout-ignore 40,6 % = přesná shoda s prototypem.

### Known limitation (přijato, AskUserQuestion „částečné B")
**Control-description tabulka s barevnými ISOM symboly blízko mapy proklouzne** (262617): symboly = mapová
barva → dlaždice projdou, dilatace tabulku spojí s mapou. Byl to hlavní deklarovaný cíl B (Sez. 72) → upřímně
eskalováno. Uživatel zvolil **přijmout částečné B** (titulky/tiráž/papír/loga vyřešeny = většina kontaminace;
tabulka odložena na cílený **detektor mřížky** — nový TODO). Konzistentní s „barevný titulek nechat konzervativně".

### Propagace
DONE, DIARY index, TODO (část B `[x]` částečně + nový bod „detektor mřížky tabulek"), GLOSSARY (Ground-truth
+layout ignore), architecture (UC5 GT), connectors/README (map_gt +layout), README root (UC5 status + layout EN),
tento diár.

### Audit cadence
%AUDIT:CODE Sez. 71 (+2; net malý, ~+50 LOC map_gt). DOCS Sez. 69 (+4). pruning Sez. 67 (+6). %CALIBRATE
Sez. 69 (+4). Žádný zralý. Stale: čisto.

### Kudos / Censure
- **Proces dobře:** measure-first vyvrátil návrh ze Sez. 72 PŘED kódem (geometrie křehká) — ušetřil stavbu
  nespolehlivého cropu; barevný diskriminátor objeven z dat; verify na 17 mapách napříč korpusem (ne cherry-pick);
  known limitation eskalována upřímně, ne zameteno. Konzervativní asymetrie (radši kontaminace než false-crop).
- **Censure 0.**

### Příště (Sez. 74)
1. *(UC5 model — hlavní tah)* s 216 keep + čistou GT (olivová Sez. 71 + přetisk Sez. 72 + layout Sez. 73) =
   **první runnability model (supervised)**. GT enabler dočištěn (až na tabulky = malá kontaminace).
2. *(GT follow-up, volitelné před modelem)* **detektor mřížky control-desc tabulek** (known limitation Sez. 73) /
   **recency osa** (datum eventu do `meta.json`).
3. *(DRY dluh, carry)* `ISOM_REF` triplikát — extrakce až 3. konzument.

---

## Sezení 74 — HW dokumentace + %THINK UC5 model + smoke test (krok 0)

Stroj: **mrkla**. `%BEGIN` krok 0: `git fetch`, `HEAD == origin/main` (`d7bb904`, clean), žádný neuzavřený
%END. Cadence (žádný zralý práh): %AUDIT:CODE Sez. 71 (+3 = 72/73/74), DOCS Sez. 69 (+5), pruning Sez. 67
(+7), %CALIBRATE Sez. 69 (+5). Stale Příště: čisto. Fokus z Příště Sez. 74 = **bod 1 (první runnability
model)**. Uživatel si nejdřív vyžádal uvedení do obrazu (proč korpus → vegetace gate → supervised model),
pak dodal HW.

### HW dokumentace (vyžádáno uživatelem)
Uživatel: „mrkla je výkonný herní stroj, možná stojí za zadokumentování" + CPU-Z report
`StressTest/HAL3000.txt` (5673 ř.) + parametry `ntbhej` si zjistit z webu (HP EliteBook 855 G8).
- **`mrkla` = HAL3000:** NVIDIA **RTX 5070** (12 GB GDDR7, Blackwell GB205, 6144 CUDA + 192 Tensor +
  48 RT, driver 12/2025) + AMD Ryzen 7 7700 (8C/16T Zen 4, AVX-512 BF16) + 32 GB DDR5-6000. Trénovací stroj.
- **`ntbhej` = HP EliteBook 855 G8** (WebSearch): Ryzen 5000U (Zen 3, 15 W) + **integrovaná Radeon Vega
  (BEZ CUDA)** → NENÍ trénovací; jen editace/git/CPU inference. Přesná konfigurace kusu TBD.
- Nový KB list `docs/kb/hardware.md` (sourozenec `tools-models.md`) + odkaz v README layout + doplněna
  paměť `two-machines-git-sync` (stroje pro UC5 nerovnocenné, trénink jen mrkla).
- **Zvýrazněna past:** RTX 5070 = Blackwell **sm_120** → `pip install torch` z PyPI stáhne build bez
  Blackwell kernelů → runtime `no kernel image available`. Nutný **cu128** build.

### %THINK UC5 runnability model
Před spekulací o architektuře jsem ČETL DATA (verify-against-source, sparse-GT past): `map_gt.py` (GT
labely 0-4 + 255), struktura `resources/livelox/<id>/` (map.png + gt_labels + meta.json), `livelox.py`
(`build_georef_blend`), `ortofoto.py` (ČÚZK jen ČR). **Klíčový nález:** v adresáři mapy je GT i georef,
ale **ortofoto se neukládá** — `build_georef_blend` produkuje jen `blend.png` (verify), ne čistý
zarovnaný pár (X,Y). → **vstup modelu vůbec neexistuje vyrobený.**

**Teze (oponentura „skoč rovnou na model"):** největší riziko NENÍ architektura (segmentačních sítí je
pět a všechny fungují; U-Net/ResNet34 = sourozenec Pic2Omapu, rozhodnuto), ale **kvalita párů (X,Y)** —
model je tak dobrý jako zarovnání vstupu s cílem. GT je hustá/čistá (3 sezení práce), ale k ničemu,
dokud nesedí pixel-na-pixel na ortofoto. Foundations: pár = základ, model = záclona → **gaty PŘED model.**

Osy A-H (detail IDEAS „UC5 runnability model"): A úloha 5 tříd, eval per-class IoU (průměr maskuje vzácný
fight); B vstup; C zarovnání (GATE 1); D ČR/DE; E split (leak); F dlaždice; G architektura; H imbalance.

**Rozhodnutí uživatele (Q1-Q3):** Q1 vstup **jen ortofoto RGB** (DMR/věk až měřená ablace — „raw default,
generalizuj s důkazem"; navíc ISOM zeleň = vegetace ne sklon); Q2 **všech 5 tříd** (open pomáhá kontextu,
eval zelená); Q3 **smoke test první** (nejlevnější gate; když Blackwell nepojede, vše předčasné).

### Krok 0 — smoke test (`temp/smoke_test_gpu.py`)
- **Dvojitá past** (Blackwell sm_120 + Python 3.14 cp314): dry-run PŘED stažením 2,5 GB ověřil
  `torch-2.11.0+cu128-cp314` existuje → obojí dostupné.
- Instalace torch+torchvision z **cu128 indexu** (ne PyPI — to by byl CPU build, padl by); pak `smp 0.5.0`
  z PyPI AŽ POTOM (jinak by stáhl CPU torch a přepsal cu128). torch zůstal cu128 (ověřeno).
- Smoke test záměrně testuje **REÁLNÝ výpočet, ne jen `is_available()`** — past „no kernel image" se
  projeví až při operaci na GPU. Výsledek: torch 2.11.0+cu128 / CUDA 12.8 / **sm_120 v `arch_list`** /
  matmul fp32+bf16 OK / **U-Net fwd (1,3,512,512)→(1,5,512,512)** = přesně cílová úloha / 11,4 GB volné VRAM.
- Drobná chyba: Windows konzole cp1250 spadla na `→` v printu (výpočet sám OK) → fix `sys.stdout.reconfigure
  (utf-8)`. Re-run plně OK.
- `requirements.txt` doplněn (cu128 instrukce + varování PyPI; segmentation-models-pytorch).

### Propagace
DONE (Sez. 74), DIARY index, TODO (krok 0 `[x]`, kroky 1-4 přidány; krok 1 `[!]`), IDEAS (blok architektury),
architecture.md (UC5 model start), README root (UC5 status + layout `hardware.md`), `requirements.txt`,
paměť `two-machines-git-sync`, tento diár.

### Audit cadence
Žádný zralý práh (viz výše). Net kód malý (requirements.txt + gitignored temp skript) — substantivní výstup
= docs + setup prostředí. Stale: čisto.

### Kudos / Censure
- **Proces dobře:** uvedení do obrazu před prací (uživatel ztratil kontext); HW jako reálná závislost
  zdokumentován; %THINK četl DATA před architekturou a našel díru (vstup nevyrobený) PŘED stavbou modelu;
  dvojitá past ověřena dry-runem před 2,5 GB stažením; smoke test testuje reálný výpočet ne jen detekci.
- **Censure 0.**

### Příště (Sez. 75)
1. *(UC5 model — hlavní tah, GATE 1)* **krok 1: zarovnané páry (ortofoto+GT grid) + měření georef offsetu** —
   rozšířit `build_georef_blend` (uložit čistý ortofoto rastr + GT přewarpovaný do téhož S-JTSK gridu, dnes
   jen `blend.png`); změřit posun ortofoto↔mapa na hrstce map (>~3-5 m = model se učí šum). Bez čistých párů
   nemá model vstup. `[!]` v TODO.
2. *(krok 2, navazuje)* měření datasetu: ČR/DE filtr (DE = prázdné ČÚZK ortofoto) + rozložení tříd v GT.
3. *(DRY dluh, carry)* `ISOM_REF` triplikát — extrakce až 3. konzument.
