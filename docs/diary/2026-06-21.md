# 2026-06-21

## Sezení 155 — Supervizní audit 260621 a přejmenování konvence FABLE5 na SUPERVISOR (mrkla)

### Diskuse
- Uživatel zadal supervizní audit dle `docs/AUDIT_SUPERVISOR_PROMPT.md` (tehdy ještě `AUDIT_FABLE5_PROMPT.md`).
  Proveden jako **8-agent workflow** verifikace tvrzení proti zdroji (kód, ne docs) + moje syntéza řídících docs.
- Po auditu uživatel rozhodl **odpojit název supervize od konkrétního modelu** — audit dělaly už tři modely
  (Fable 5 → 260612, Codex GPT-5 → 260619, Opus 4.8 → 260621), takže `FABLE5` byl zavádějící.
- **Souběžně běželo paralelní vlákno „260621-Buschdörfl"** (scan-mining kalibrace) — pracovní strom během
  sezení nesl jeho necommitnuté změny (TODO 66,2 %, `calibration_manifest.{json,py}`, `mark_isoms.py`,
  `separate.py`, `score.py`, `.gitignore`).

### Supervizní audit (`docs/AUDIT_SUPERVISOR_260621.md`)
- 8 paralelních agentů (generator / model / connectors / scan-mining / tests / docs-ssot / dry / prior-audits)
  ověřilo proti zdroji; `unittest discover` = 37 OK.
- Stav 260619: většina VYŘEŠENA (Velbloud.pgw, scan-mining framing, isom_scan governance, smoke, zmražená geometrie).
- **4 hlavní námitky:**
  - **A1 KRITICKÁ:** scope rozšířen Sez. 152 (`N_AREA` 18→21, `N_LINE` 2→5) v kódu+docs, ale kanonické
    checkpointy area/line zůstaly staré (změřeno z tvaru hlav: area=18, line=2) → `eval_real` spadne →
    nové typy 404/407/409 + 306/309/508* **nikdy neměřeny na realitě**; README 0,683/0,774 = starý scope.
    Inverze foundations-before-curtains uvnitř `Generator()`.
  - **A2 VYSOKÁ:** scan mining běží před governance — `calibration_manifest.json` má všude `marker_recall=null`
    (`IN_PROGRESS`), untracked, ale TODO ho tvrdí „zavedený"; `capabilities.py` ↔ manifest se rozcházejí
    (417 `SCAN_LIVE_POINT` vs „candidate"; 112/115 v registru chybí → `KeyError`).
  - **A3 VYSOKÁ:** KPI feeder (65,8 %) a cíl (čte reconstructor realitu?) se rozešly; generátor „pokrývá"
    typy bez natrénovaného reconstructoru. Sirotek **66,2 %** v TODO (jediný výskyt v repu, nepropsaný).
  - **A4 VYSOKÁ:** bus factor 1 trvá přes tři audity (`_curation.json` neregenerovatelná ruční práce /
    `_split.json` nezamrzlý / `resources/` gitignored) + nově untracked scan-mining kalibrace.
- Připomínky B1-B8 (CLAUDE.md/README vynechávají `isom_scan/`+`png2line`; `detect_version` tichý default;
  DRY `_IMAGENET` 7×/tiling/parse_symbol_ids; arcgis paging; `compare_isom` stale DoD; DIARY hook bloat;
  smoke mimo discover) + C imperativy pro kolegy + D co funguje. Plný detail v souboru.

### Přejmenování FABLE5 → SUPERVISOR (model-agnostic)
- `git mv` 4 souborů `AUDIT_FABLE5_{PROMPT,260612,260619}.md` → `AUDIT_SUPERVISOR_*` + nový `260621`.
- token `AUDIT_FABLE5` → `AUDIT_SUPERVISOR` všude (i historické logy — odkazy nepadají).
- živé docs: TODO sekce „Audit supervisor", GLOSSARY term `AUDIT_SUPERVISOR`, PROMPTS cadence + `%BEGIN`
  krok 7 „Audit supervisor check", README layout.
- atribuce nálezů v kódu+docs „Fable5 audit A1" → „supervisor audit A1"
  (`model/png2{area,point}/eval_real.py`, `model/purple.py`, README, TODO).
- genericizace template: „(Fable/Mythos třída)" → „nejsilnější dostupný model".
- **Volba uživatele (A2):** historická atribuce modelu PONECHÁNA — `AUDIT_SUPERVISOR_260612.md`
  „Auditor: Claude Fable 5", `260619` „od minulého Fable auditu", diáře „Meta-audit Fable 5"/„Fable5 vlákno",
  „ChatGPT 5.5" = fakt, kdo co dělal, ne závislost názvu.

### Commit (selektivní — paralelní vlákno)
- **`4c8d2b8`** rename + audit. `TODO.md` smíchané (parallel WIP 66,2/calibration + můj rename) →
  commitnut **jen MŮJ rename** přes `git show HEAD:TODO > TODO; sed rename; git add; commit; cp backup zpět`
  → parallel WIP zachován v working tree. Čistě parallel soubory (.gitignore/separate.py/score.py/
  isom_scan README + untracked) NEcommitnuty (paměť [[parallel-threads-verify-shared-docs]]).
- Ověřeno: **0× `AUDIT_FABLE5`** v commitnutém stromu; HEAD `TODO` = jen rename (bez 66,2).

### Verify
- `.venv\Scripts\python.exe -m unittest discover -s tests` = **37 OK** (mrkla, py 3.14.3, torch cu128).
- **smoke nerelevantní** — měnily se jen docs + komentáře v `model/*.py`; generator/konektory/`.omap` export netknut.
- renames detekované gitem 88–98 % podobnost; žádné rozbité odkazy.
- KPI nepřeměřeno (generátor jsem neměnil; committed headline zůstává 65,8 % `KPI_3MAP_CANONICAL`,
  paralelní vlákno tvrdí 66,2 % — viz audit A3, čeká na doložení).

### Kudos/Censure
- Censure 0.
- Proces: concurrent paralelní vlákno → selektivní commit přes HEAD-restore + reapply techniku
  (paměť [[parallel-threads-verify-shared-docs]] rozšířena o konkrétní postup pro hunk-level smíchané soubory).

### Příště
1. **Rozhodnout zápis audit nálezů A1–A4 do TODO** (jako `260621-Axx` položky s kódem) — nabídnuto,
   čeká na souhlas uživatele (audit prompt: „nezapisuj bez souhlasu"). Zatím žijí jen v souboru.
2. **A1 (audit):** retrénovat `png2area` (`N_AREA=21`) + `png2line` (`N_LINE=5`), `eval_real` na realitě,
   `--promote`; do té doby v README/architecture označit 0,683/0,774 jako stale scope.
3. **A2 (audit, hlavně paralelní vlákno):** doměřit 602-marker recall + commitnout `calibration_manifest`;
   smířit `capabilities.py` ↔ manifest (doplnit 112/115).
4. Paralelní vlákno „260621-Buschdörfl" si vezme **Sez. 156** (155 patří tomuto auditu).
