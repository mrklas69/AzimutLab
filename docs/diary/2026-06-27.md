# Deník — 2026-06-27

## Sezení 173 — budovy 521 granularita + ruční crop polygon (mapfield) + scan-vytěžení bodů 419/111/112/115 (ntbhej)

Stroj **ntbhej** (bez CUDA/korpusu, ale Livelox korpus + resources `.pgw` lokálně dostupné).
Reakční série vedená uživatelem: budovy → mapfield → podklady → scan-vytěžení bodů.
Git sync OK, žádný audit práh dosažen. KPI baseline (Sez.172 ntbhej) 67,3 %.

### Diskuse / rozhodnutí
- **Fokus 1 (521 budovy granularita):** doložený KPI přestřel (gen 498 / orig 136, 3-map).
  Doménové rozhodnutí uživatele (OB kartograf): **vyřadit vrstvu „Kůlna, skleník, fóliovník,
  přístřešek"** (kartograf zahrádkářské drobné stavby na 1:10000 negeneralizuje) + ISOM min-size
  0,4×0,5 mm (≈20 m²). Poctivě sděleno: plný match na orig 136 NEdosažitelný legitimně (Goodhart);
  cíl = defensible generalizace + měřit reálný zisk.
- **Nález uživatele „zase zelená z log":** separace Bedřichovky vyrábí falešný les z barevného
  layoutu (zelený banner/hlavička + R-loga). Doloženo overlay — log bloky MAJÍ mapovou barvu
  (zeleň) a jsou spojené s lesem → auto detekce (`_detect_map_area` connected-component) i KPI
  convex hull (98,3 %) selhávají; **KPI counts KONTAMINOVANÉ**, ne jen rgb. Volba uživatele:
  **ruční crop polygon per sken** (nejrobustnější, nahrazuje OBA slabé auto-mechanismy).
- **Přesměrování uživatele na scan-vytěžení bodů** (ROADMAP Etapa 1, „ať si ISOM nemusíme
  vymýšlet"). Baseline proti ruční GT Branžež (Sez.171, 187/21) odhalil: kalibrovaný jen 109,
  9 bohatě-GT kódů bez detektoru. Volby: 419 → rekalibrace 111/112/115.

### Kód (4 feat commity)
1. **`fc6afa0` budovy 521** (`zabaged.py` + `generator.py`): vyřazení vrstvy Kůlna z
   `BUILDING_AREA_LAYERS` + `BUILDING_MIN_AREA_PX2` (0,2 mm² ISOM min, jen 521 ne 523 Ruin,
   izomorf SURFACE/TREEROW/BOULDER filtrů). gen 521 **498→370**. Čisté A/B (git stash):
   **plocha 74,0→75,9 (+1,9 pb)**, Bedř +4,9 / Blatná 0 (kontrola, 0 budov) / Velbloud +0,6.
   Headline 67,3→67,2 neutrální = bodový měřicí šum na Velbloudu (−4,5 pb, separace běh-citlivá,
   body nedotčeny). **Metodický nález:** agregátní HI simulace (+3,23 pb) PŘESTŘELILA — `measure_dod`
   normalizuje per-mapa. Příště simulovat per-mapa.
2. **`39fd0f6` ruční crop polygon (mapfield)** (`map_gt.py` + `measure_dod.py` + `tools/mark_mapfield.py`
   + `.gitignore`): formát `resources/<name>_mapfield.json` {scan_size, polygon_px}; klikací nástroj
   (matplotlib); `segment_gt(map_field_mask=)` + `rasterize_map_field` util; `_load_map_field`
   konzumace v separaci i KPI ořezu; **no-silent fallback** (chybí → auto + warn). `.gitignore`
   výjimka `!resources/*_mapfield.json` (textový, bez copyright, jako `.pgw`). Provizorní Bedř polygon
   (odečet z mřížky, DOLADIT nástrojem): **Bedř KPI 62,3→66,1** (plocha 69,2→78,5 +9,3 / linie
   64,6→69,1), **headline 67,3→68,4 (+1,2)**. gt_vis čistý (banner/loga magenta). Integritní fix.
3. **`3049f46` scan-detekce 419 (zelený X)** (`vegetation_points_poc.py` + `calibration_manifest.json`):
   +419 X-template (`_cross_template`, mirror gen render). Kalibrace vs GT (24 bodů, tol 12px):
   green-min 70 / green-red-diff 20 / green-blue-diff 5 → **recall 0,38 prec 0,90 F1 0,53** (z 0
   detektoru). Strop classic-CV pro tenké X (vysoká precision, konzervativní recall = argument pro ML).
4. **`ccc5a7b` rekalibrace 111/112/115** (`calibration_manifest.json`): sdílené params
   `--min-area 30 --score-threshold 0.7` (přísnější shape-match vyřadí FP z vrstevnic; ecc
   KONTRAPRODUKTIVNÍ — U/▽ mají vysoký ecc). **111 F1 0,18→0,58** (10 GT, candidate_calibrated),
   **112 0,05→0,44** (4 GT), **115 0,05→0,80** (3 GT, malý vzorek). Terénní rodina 109/111/112/115
   nyní kalibrovaná proti ruční GT.

### Mimo commity
- **Regen 3 KPI map se 3 podklady** (ortho+DMR+scan) pro OOM verify uživatele (maps/ gitignored).
- **SSL fix:** ČÚZK přešel na nový Let's Encrypt cert, uv-cpython default trust store ho neuznává
  („certificate has expired", ač server cert platný). Workaround `SSL_CERT_FILE` → `certifi` bundle.
  Trvalý fix (connectory použít certifi přímo) = carry.

### Kudos / Censure
- **Kudos! uživateli** — 3× správné nasměrování: doménové rozhodnutí budov (kartograf nekreslí kůlny),
  nález „zelená z log" (integritní bug v KPI), přesměrování na scan-vytěžení bodů (ROADMAP priorita,
  kterou jsem v reakční sérii ztratil ze zřetele).
- **Censure! sobě (mírné)** — agregátní HI simulace budov přestřelila (ignorovala per-mapa normalizaci);
  scan-vytěžení bodů jsem měl navrhnout sám (je to ROADMAP priorita + pokrok Sez.169-172), ne čekat na
  výtku uživatele.

### Příště
1. **Scan-vytěžení dalších bodů** (roadmapa = GT bez detektoru): 103.1(26) / 101.1(14) / 204.5(10) /
   204(9) / 205(7) / 106(7) / 308.1(6) / 203.1(5). Skály 204/205 = nová rodina, KOMPAS velké.
2. **418 přestřel** — vegetation default masivně přestřeluje (overlay stovky FP) → kalibrovat.
3. **Mapfield doladit** Bedř (provizorní → přesný nástrojem) + naklikat Velbloud/ostatní skenové mapy.
4. **SSL certifi do connectorů** (ortofoto/dmr/arcgis SSL context) — jinak každý ČÚZK fetch na ntbhej
   potřebuje `SSL_CERT_FILE`. Soví vrch regen (DEV cesta, .omap neaktuální vůči budovám).
5. Carry: HAL3000 přeměření KPI po budovách+mapfield (canonical sirotek 67,5 vs ntbhej 68,4) ·
   RNG nezávislé streamy · re-run modelů na v2 GT.
