# Diář 2026-06-11

## Sezení 110 — Korpus na ntbhej (57→264) + deep research + rock_relief v2 + ChatGPT %AUDIT:CODE (kritický 301/301.1)

Stroj **NTBHEJ21** (hejna = ntbhej; oprava aliasů — stroje jsou DVA: mrkla=HAL3000, hejna=ntbhej). Sync `main` =
`origin/main` (8e7f297), clean. Cadence čistá (Sez. 108 vynuloval 4 audity → 2/15, 2/10, 2/12, 2/8, žádný vynucený).
KPI 59,1 % (Sez. 107, generátor netknut na začátku → platí). Dlouhé sezení, 4 paralelní směry + externí audit.

### 0. %BEGIN orientace (první BEGIN na projektu pro tento model) + oprava strojů
Plné zorientování (README/architecture/struktura). Uživatel opravil: **stroje jsou jen dva** — mrkla=HAL3000
(CUDA RTX 5070 + korpus, trénink), hejna=ntbhej=NTBHEJ21 (docs/měření/ČÚZK). Špatně jsem z aliasů v diářích
vyrobil třetí stroj (`docs/kb/hardware.md` to vede správně).

### 1. Livelox korpus na ntbhej: 57 → 264 map (+207), GT ~258
`livelox.py batch` (870 eventů CZ S.Čechy + DE příhraničí; idempotentní, GT inline). ok=204/skip=38/fail=628
(faily = staré smazané bloby na Livelox straně, očekávané). **22 map bez GT** diagnostikováno: batch-time selhání
**tranzientní** (paměť pod zátěží), standalone segmentace projde → `temp/fill_missing_gt.py` doplnil 15.
**Nový nález — paměťový strop `segment_gt` na ntbhej je ~30 Mpx, ne 100** (Sez. 90): alokace (N_px, 13, 3) int32
= 5 GiB @ 35 Mpx → 6 obřích map (Branžež/Bezděz/Bramberk/Bohemia 76-97 Mpx + 2× 35 Mpx) bez GT = **carry downscale**.

### 2. Deep research (deep-research skill, 103 agentů) — částečný (session limit 12:20)
Verifikace uťata limitem → **7 tvrzení potvrzeno (3-0), zbytek NEverifikován (abstain, NE vyvráceno)**. → `RESEARCH.md`.
**Klíčový negativní nález:** žádný otevřený plošný archiv ZDROJOVÝCH vektorů (.omap/.ocd) → vektorová GT zůstává
prakticky jen z našeho `generator()` (potvrzuje směr). **ČSOS Archiv** (potvrzeno): ~12 000 CZ map, ale veřejně jen
96 dpi watermark náhledy + JSON API → metadatový index, NE zdroj dat (Livelox dává lepší 1,33 m/px). Leady k doověření:
arxiv 2405.04634 (ML nad OB mapami), Copernicus SWF + AOPK CC-BY (vegetace/mokřady), OO Mapper sample data.

### 3. rock_relief v2 (z jiného threadu — handoff + zlatý vzorek) → integrováno
Uživatel přinesl vylepšený `rockcore` v2 (`docs/kb/rock-detection-v2/`: handoff + golden Šulcák + rockcore reference).
Measure-first probe `temp/probe_rock_v2.py` + `temp/probe_rock_despeckle.py` doložily **dvě vady** našeho `rock_relief.py`:
- **(1) rozlišení:** `TARGET_PX_M=1,5` poddetekoval o **43 %** (40/1,29 ha vs 0,5 m/px) — práh sklonu je svázaný s
  rozlišením (handoff §4a). → **pevný `ANALYSIS_RES=0,5 m/px`** + **tiling fetch** `_fetch_assembled_grid` (dlaždice
  ≤ `MAX_FETCH_PX`, seamless assembled grid → morfologie JEDNOU; čistší než handoff §7 per-dlaždice merge; `dmr.py`
  netknut — reuse `fetch_elevation_grid` per sub-tile přes inverzní transformer 5514→4326).
- **(2) despeckle:** `OPEN_M=1,0` = disk r=2px @ 0,5m maže tenké stěny (−27 %, 1,85 vs golden 2,53 ha) → **`OPEN_M=0,5`**
  = r=1px (rockcore golden). Měřeno: r=1px → 2,57 ha = golden.
**Verify (vše ntbhej, čistě DMR):** golden Šulcák **48/2,56 ha = match** (golden 48/2,53, tol ±2/±5 %); seamless tiling
(vynucené 4 dlaždice = identické); reálný Hrubá Skála 2×2 km 159 bloků (živý server); **E2E `generate_map` 1×1 km HS =
49× 206 v .omap + 411× 204 pseudo** (downstream skalnatost maska OK). `MAX_TOTAL_PX=50 Mpx` strop → velký výsek zhrubne
s HLASITÝM varováním (no silent fallback). Kontrakt funkce identický → generátorová cesta beze změny.
**KPI = CARRY HAL3000/Velbloud:** kompas Sez. 110 ukázal Bedř/Blatná **nejsou skalnaté** (206 orig=0/gen=1, 208
orig=31/gen=0), skalnatá Velbloud nemá `.pgw` → KPI dopad rock v2 **na ntbhej neměřitelný** (víc skal na ne-skalnatých
mapách KPI jen zhorší). Měřit až na skalnaté mapě.

### 4. ChatGPT 5.5 %AUDIT:CODE — verify-against-source → kritický bug + 4 další
Uživatel přinesl externí audit. **Ověřeno proti zdroji (ne slepě přijato), všechny nálezy sedí:**
- **① KRITICKÝ — voda 301/301.1, Png2Area Y zahazoval vodu.** Generátor (`generator.py:3968`) zapisuje vodu jako
  `301` (combined, od 2026-06-01), `omap_raster.AREA_ZORDER` měl `301.1` (od vzniku 2026-06-04, type 4 ≠ type 16).
  Změřeno: Lidové sady 58 objektů `301` → omap_raster zachytil **0**, Nová Louka 11→0, Bedř 8→0. **Git timeline
  PRŮKAZNÝ:** voda 301 (06-01) ≪ omap_raster vznik 301.1 (06-04) ≪ trénink Sez. 103 (06-09) → **omap_raster NIKDY
  vodu nezachytil; oba Png2Area tréninky (mIoU 0,640 i 0,568) se vodu nenaučily, reportovaná mIoU vodu nepočítala.**
  Komentář „měření Sez. 87: voda=301.1" byl chybný už při zápisu. **Oprava:** `301.1`→`301` + **root-cause SSoT**
  (`omap_export.AREA_CODES` = `frozenset(omap_raster.AREA_ZORDER)`, import bez cyklu — raster je deklarovaný SSoT,
  nikdo neimportuje export). Behavior-preserving membership (17 kódů identických). Voda po fixu 58→58/11→11/8→8.
- **② stale `_tiles.json`** (n_area 16 vs N_AREA 18) → `class_weights()` guard (selže nahlas, ověřeno).
- **③ no-silent-fallback** `build_tiles`/`load_split` → fail na prázdný split / chybějící páry (`--allow-missing`).
- **⑤** `zabaged.map_path_to_isom` fallback 503 → `ValueError` na neznámou vrstvu.
- **⑥** stale komentáře `dataset.py` (0..15 → N_AREA/18). py_compile čistý (6 souborů), všech 8 konzumentů importuje.
- **Implikace:** Png2Area re-trénink povýšen na `[!]` PRECONDITION (voda 301 poprvé v Y).

### Kód (commity: fix + feat + docs)
`fix(model)`: voda 301/301.1 drift + SSoT AREA_CODES + no-silent-fallback guardy (omap_raster/omap_export/dataset/
tile/zabaged). `feat(generator)`: rock_relief v2 (ANALYSIS_RES 0,5 + tiling + despeckle r=1px). `docs(session)`:
diár + DONE + TODO + RESEARCH + DIARY index + docs/kb/rock-detection-v2/ (handoff + golden).

### Kudos / Censure
- **Kudos uživateli:** oprava aliasů strojů (verify-against-source); přinesení rock v2 + ChatGPT auditu = dva
  vysoce hodnotné externí vstupy. ChatGPT audit byl přesný (kritický bug s reprodukovatelnými řádky).
- **Kudos (measure-first × 3):** despeckle hypotéza potvrzena přesně (r=1px → golden); voda bug potvrzen měřením
  + git timeline; KPI rock dopad NEMĚŘITELNÝ na ntbhej (kompas to odhalil → fabrikaci KPI na ne-skalnatých mapách
  zabráněno). Audit ověřen proti zdroji, ne slepě přijat (verify-against-source).
- **Censure 0** — externí audit, ne moje chyba; bug 301/301.1 byl zděděný drift (06-04).

### Příště (Sezení 111)
- **[!] Png2Area re-trénink** (mrkla/HAL3000) — PRECONDITION splněna (voda 301 fix); regen párů + dlaždic
  (guard teď selže na stale). Voda poprvé v Y → mIoU bude poprvé vodu počítat.
- **rock_relief v2 KPI před/po** — na skalnaté mapě (Velbloud `.pgw` doplnit / Livelox korpus na HAL3000).
- **Korpus ntbhej dokončit:** rozhodnout kurace+split (kopie z HAL3000 vs auto-regen — `_curation.json`/`_split.json`
  nejsou, gitignored, ruční vizuální tagy Sez. 71 žijí na HAL3000) + 6 obřích map downscale-before-segment.
- *(volitelné)* re-spustit deep research po resetu limitu (doověřit leady: arxiv 2405.04634, Copernicus SWF, AOPK).
- *(carry HAL3000)* veg body 417/419/418 Png2Point + reálný transfer 204/210 na Livelox skenu.

## Sezení 111 — Korpus GT 264/264 (chunked classify) + ostrý bg podklad + Velbloud .pgw dead-end

Stroj **NTBHEJ21** (hejna = ntbhej). Sync `main` = `origin/main` (5706ad5), clean (jen untracked `AGENTS.md`).
Cadence čistá (od Sez. 108 velký úklid → 3/15, 3/10, 3/12, 3/8, žádný vynucený). Dostupnost ověřena `ls`
(paměť `gitignored-availability-verify-not-assume`): **Livelox korpus 264 map JE lokálně**, ale `_curation.json`/
`_split.json` **nejsou** (žijí na HAL3000); `resources/` 5 map s `.pgw`, **Velbloud bez `.pgw`**. KPI 59,1 %
(HAL3000) / 54,9 % (ntbhej, bez Velbloud) — generátor od Sez. 110 měněn jen v rock cestě (neměřitelná na ntbhej).
Oba hlavní `[!]` tahy (Png2Area re-trénink, rock KPI) mrkla-blokované → fokus na ntbhej-doable enabler (volba uživatele).

### 1. Korpus GT dokončen 258 → 264 (chunked `_classify`, byte-identický)
Měřením doloženo, že RAM blowup `segment_gt` je **JEN v `_classify`** (`N×13×3 int32` ≈ 5,5 GiB @ 35 Mpx; strop
ntbhej ~30 Mpx, Sez. 110) — median_filter/`_detect_map_area` na uint8 jsou levné. Probe `temp/probe_segment_mem.py`:
chunked classify (řez po řádcích) prošel 97 Mpx mapou (classify 41s + median 15s, peak 3,4 GB) a je **byte-identický**
(per-pixel argmin nezávislý). **Oprava `connectors/map_gt.py`:** `_classify` přepsán na řez po pixelovém rozpočtu
(`_CLASSIFY_CHUNK_PX = 4 Mpx/chunk`, bound nezávislý na šířce mapy), výstup `int64→uint8` (13 < 256 referencí).
**Volba chunk vs downscale výstupu:** downscale gt by rozbil tvrdý invariant `gt.shape == map.png.shape`
(`livelox.py:404-406` RuntimeError) + affine v `separate.py`/`pairs.py` → zásah do 5+ konzumentů; chunk je
byte-identický, plné rozlišení, nulový zásah. **Verify:** regrese na hotové mapě byte-identická; 6 obřích map
(35–97 Mpx) segmentováno (29–71 s), všech 6 vizuálně OK (`temp/verify_*.png` — nepravidelné bloby správně
izolované `_detect_map_area`, vysoký ignore 50–75 % = bílý okraj papíru, ne přeřezání; 868899 málo zeleně =
doložená open contour mapa). py_compile 7 konzumentů OK. **Korpus na ntbhej 264/264 GT.**

### 2. Ostrý bg podklad v OOM (dotaz uživatele „proč je bg_scan rozostřený")
Příčina `gen_backgrounds.py`: podklad se warpuje **do gen pixelového gridu**, který je HRUBÝ (Bedřichovka gen
rgb.png jen 1182×1498 @ 2,18 m/px), a klauzule `min(1.0, _BG_MAX_PX/max)` **zakazovala supersampling** nad gen
rozlišení → 135 Mpx sken (0,26 m/px) zmáčknut na ~1,72 m/px. Cap 1500 px tu ani nebyl binding — limit byl sám gen
grid. **Oprava (3 edity):** odstraněna `min(1.0,…)` klauzule (povolen supersampling — jemné kroky vzorkují detail ze
zdroje místo zmáčknutí na gen rastr) + `_BG_MAX_PX` 1500 → **6000** + DRY helper `_bg_out_size` (byly dvě identické
kopie v `add_backgrounds` i `add_resources_scan_background`). Georef nedotčen (`sx = pw/out_w` škáluje úměrně).
Bedřichovka bg_scan **1182×1498 → 4734×6000** (0,43 m/px). **Verify:** před/po výřez (`temp/bg_sharp_cmp.png`,
ostré vrstevnice/cesty), overlay gen 50 % přes bg (`temp/overlay_*.png`, lícování OK), py_compile. **Přegenerovány
Bedřichovka + Blatná** (jediné s bg_scan) přes `_gen_sep`; ostatní auto při dalším `measure_dod` (úprava kódu
invalidovala `_gen_sep` cache přes `_code_mtime`).

### 3. Velbloud `.pgw` rekonstrukce → doložený dead-end (measure-first)
Cíl: syntetizovat chybějící `Velbloud.pgw` z georef → odblokovat rock KPI před/po na skalnaté mapě TADY. **Negativní
výsledek** (`temp/probe_pgw_recon.py`): (1) `.omap` nese jen minimální georef (`scale`+`grivation`+round-number
`ref_point`) + ortofoto template `LIBE25.jpg` (neexistující cesta) — **žádný pixel→S-JTSK transform**; (2) rekonstrukce
z object-bbox **selhala při validaci** na mapě se známou pravdou (Soví vrch: implikované m/px W→0,194 vs pravda 0,095 =
**2× mimo**, aspekt W≠H nesedí) — scan je rotovaný **grivací** (magnetic-north) + okraje/legenda, grid-aligned bbox ≠
pixelový obdélník skenu. Potvrzený nález: `.pgw` rotace = grivation (Bedř −10,88° = griv 10,88°), ale origin a m/px
z `.omap` neodvoditelné. **Závěr: rock KPI zůstává HAL3000 carry** — fabrikovat špatný `.pgw` by vyrobilo nesmyslné
KPI (no silent fallback). Měření vyloučilo cestu za pár minut místo stavění georef mašinérie naslepo.

### AGENTS.md
Untracked `AGENTS.md` = Codex protějšek projektového `CLAUDE.md` (přinesl jiný thread, Sez. 110 ChatGPT/Codex
spolupráce). Commitnut jako verzovaný (mirror `CLAUDE.md`, konvence Codex).

### Kód (commity: feat ×2 + docs)
`feat(connectors)`: map_gt chunked `_classify` (odblok GT obřích map, byte-identický). `feat(generator)`:
gen_backgrounds supersampling + `_BG_MAX_PX` 6000 + `_bg_out_size` DRY (ostrý bg podklad). `docs(session)`:
diár + DONE + TODO + DIARY index + AGENTS.md.

### Kudos / Censure
- **Kudos (measure-first × 3):** chunked classify hypotéza potvrzena (byte-identita + 97 Mpx průchod) PŘED přepisem;
  bg příčina lokalizována na gen grid (ne jen cap) → fix mířil správně; Velbloud `.pgw` dead-end vyloučen validací
  na známé pravdě (Soví vrch 2× mimo) místo fabrikace.
- **Kudos uživateli:** dotaz na rozostřený podklad odhalil reálnou vadu vizuálního verify nástroje.
- **Censure 0.**

### Příště (Sezení 112)
- **[!] Png2Area re-trénink** (mrkla/HAL3000) — PRECONDITION splněna (voda 301 fix Sez. 110); regen párů + dlaždic.
  Korpus na ntbhej teď **264/264 GT** (chunked classify) — pokud se přenese na HAL3000, rozšiřuje tréninkový set.
- **rock_relief v2 KPI před/po** (HAL3000) — Velbloud `.pgw` na ntbhej neodvoditelný (Sez. 111) → měřit na HAL3000
  (kartografův georef workflow / korpus rocky mapa).
- **Korpus kurace+split** — kopie `_curation.json`/`_split.json` z HAL3000 (zachová split) vs auto-regen.
- *(volitelné)* deep research re-run (leady arxiv 2405.04634, Copernicus SWF, AOPK).

## Sezení 112 — Regen všech 7 map + grivace (3 vrstvy: parametr / ze skenu / konektor) + clip_quad matplotlib fix

Stroj **NTBHEJ21** (ntbhej). Sync `main` = `origin/main` (992626e), clean. Cadence čistá (od Sez. 108
velký úklid → 4/15, 4/10, 4/12, 4/8, žádný vynucený). Dostupnost ověřena: korpus 264 map + `resources/`
5 map s `.pgw` (Velbloud bez). KPI 59,1 % (HAL3000) / 54,9 % (ntbhej) — generátor dnes měněn jen v georef
metadatech (grivace) + clip_quad point-in-poly (oba KPI-neutrální). **Zadání uživatele:** přegenerovat
všech 7 map se všemi podklady → odhalilo bug + spustilo grivační feature (dotaz „má Bedřichovka aktuální
grivaci? odchylka od skenu je velká").

### 1. Regen všech 7 map se všemi podklady (dvě cesty)
**5 DEV lokalit** (SV/NL/LS/HS/NV): `generate_map` plný režim (vše `real` default + ortofoto). **2 resources**
(Bedřichovka/Blatná): separační cesta `_gen_sep` (separace ze skenu → `predict_areas_sjtsk` → clip na quad →
bg_scan podklad). Vizuál všech 7 OK (rock v2 výrazný na HS; separace věrná na Bedř/Blatná).

### 2. Bug: clip_quad táhl matplotlib do produkční ntbhej cesty (vrstvení)
Bedř/Blatná napoprvé **SELHALY**: `clip_quad.py` importoval `matplotlib.path.Path` (point-in-polygon na
quad), ale `requirements.txt` řadí matplotlib jako **trénink-only (mrkla, křivky učení)** — produkční
separační cesta (ntbhej) na něm záviset neměla. **Fix:** nahrazen lehkým numpy crossing-number `_point_in_quad`
(žádná nová závislost, čisté vrstvení). Bedř clip 2110 v poli / −1067 přesah (Stráž n. Nisou), numpy ray-cast
dal věrohodné počty.

### 3. Grivace — %THINK → tři vrstvy (volby uživatele přes AskUserQuestion)
**Tvrdá fakta ověřena:** generátor je grid-north (S-JTSK, vědomě Sez. 37); meta slot `grivation_deg` prázdný.
Tři severy: **konvergence** S-JTSK (grid→true) **≈ −7,4°** (pyproj, VELKÁ!), **deklinace** (true→mag) ≈ +5°
(WMM), **grivace** (grid→mag) ≈ +12° (sken Bedř 10,88° sedí). **Pointa dotazu uživatele: odchylka je velká
hlavně kvůli konvergenci S-JTSK ~7,4°, ne jen deklinací.** Volby: **poloha** = jen `.omap` georef metadata
(KISS, OOM zobrazí natočené, geometrie/rastr zůstává grid; rotace rastru = curtains odložena) · **default**
(grivace nezadána) = grid-north (žádný posun dnešních map) · **konektor zdroj** = lokální model (NOAA WMM API
vyžaduje registraci+key → blocker → pyIGRF/pygeomag offline).
- **(A) Ruční parametr** `--grivation <deg>` → `omap_export` zapíše `declination=grivation=G` do
  `<georeferencing>` (Local georef → konvergence=0 → grivation=declination, izomorf s historickým skenem;
  OOM vztah `grivation=declination+grid_convergence`). `generate_map` +kwarg, `_georef_meta` `north="magnetic"`
  + `grivation_deg`. E2E ověřeno (Nová Louka výsek → `.omap` + meta 10,88°).
- **(B) Ze skenu** (řeší původní dotaz): `_gen_sep` čte grivaci z `resources/<name>.omap` (`_scan_grivation`)
  → gen.omap orientačně lícuje s bg_scan podkladem. Bedř 10,88° / Blatná 11,90° dosedly. **No-silent**: chybí
  atribut → None (grid-north).
- **(C) Konektor** `connectors/magnetic.py` (UC2, sourozenec dmr/zabaged): `grivation(lat,lon,when)` = deklinace
  (**pygeomag WMM**, offline vestavěné koef.) + konvergence (pyproj). **Znaménko OVĚŘENO empiricky proti skenům**
  (verify-against-source): `grivace = decl − pyproj_conv` (OOM `grid_convergence = −pyproj`); decl+conv=−2° nesmysl.
  Dnešní ~12,7° vs skeny ~11° (starší, deklinace roste ~0,13°/rok → trend potvrzuje znaménko). CLI
  `--grivation-auto` / `--grivation-date` (WMM platnost 2024–2029, mimo VARUJE). pyIGRF zavržen (vadná
  instalace bez coeff souboru) → **pygeomag** (čistý python WMM, vestavěné koef.).

### 4. Regen všech 7 s grivací auto (volba uživatele) — s oponenturou u resources
DEV 5 map → `--grivation-auto` (dnešní WMM 12,61–12,91°). **Resources oponovány a PONECHÁNY ze skenu**
(10,88/11,90°): auto by obnovilo odchylku od historického skenu, na kterou se uživatel původně ptal
(bg_scan je historický, dnešní pole o ~1,8° jinde) → lícování > aktuálnost. Všech 7 `.omap` nese grivaci.

### Kód (commity: feat ×2 + docs)
`feat(connectors)`: `magnetic.py` grivace konektor (pygeomag WMM + pyproj, znaménko z verify). `feat(generator)`:
`--grivation`/`--grivation-auto`/`--grivation-date` + georef zápis (omap_export/generator/measure_dod `_gen_sep`
ze skenu) + clip_quad matplotlib→numpy. requirements +pygeomag. `docs(session)`.

### Kudos / Censure
- **Kudos uživateli:** dotaz na grivaci Bedř odhalil reálnou vadu vizuálního lícování (gen grid-north vs sken
  magnetic-north ~11°) + dobrá volba „resources ze skenu" implicitně potvrzena oponenturou.
- **Kudos (verify-against-source ×2):** znaménko konvergence vyřešeno empiricky proti skenům (ne hádáno —
  decl−conv vs decl+conv); NOAA key blocker odhalen testem PŘED stavbou konektoru (→ pivot na offline pygeomag).
- **Kudos (oponentura):** resources auto vyvráceno (obnovilo by opravenou odchylku) místo slepého „všech 7 auto".
- **Censure 0.** (matplotlib bug byl zděděný drift, ne dnešní chyba — odhalen regenem.)

## Sezení 113 — Test výstupů: 3 bug fixy (balvany/plot na vodě, plot druh 13, GAP) + %THINK sjednocení ořezu (cut.py)

Stroj **mrkla**. Zadání = test posledních výstupů generátoru → 4 nálezy. **Pořadí řešení 4→3→2→1.**

### 1. Balvany 204/210 + plot 516 na vodní hladině (bod 4, Nová Louka) — FIX
**Root cause:** `_generate_pseudo_boulders` nedostával vodní masku → pseudo balvany padaly na 301; plot se generuje
PŘED vodou (pokryv z-order vespod) → vodní masku v ten moment neznal. **Fix:** `_rasterize_water_grid` (outer=voda,
holes=ostrov→souš) odečte vodu ze seed masky + per-tečka check u 210 polí (elipsa přesahuje břeh); `_clip_fences_off_water`
(post-water) vyřízne fence úseky nad hladinou z `.omap` (rastr OK — voda kreslí nad plotem). **Verify měřením:** balvany
na vodě 0 px po erozi břehu 3 px (max zbylý shluk 16 px = okraj symbolu na břehu, fyzikálně OK); plot 0 bodů >3 px od
břehu. Vizuál nádrže čistý (ostrov má balvany správně = souš). NL regen, LS regen plot ořez 215→220 úseků.

### 2. Plot 516 „uvězňuje" panelák (bod 3, Lidové sady Hokejka) — FIX
**Root cause:** fence kolem RÚIAN parcel druhu {5 zahrada, **13 zastavěná plocha**}; panelák JE parcela druhu 13 →
plot obkresloval otisk budovy. **Volba uživatele (AskUserQuestion): vyřadit druh 13.** Fix: fence seed `olive_ruian_img`
jen `str(druhpozemkukod)=="5"` (zahrada); druh 13 zůstává v 520 olivové. LS 215 plotů (jen zahrady). Komentáře/glosář
propsány (paměť `slap-symbol-rewrite-comments`).

### 3. Čistý les GAP (bod 2) — ROZHODNUTÍ + glosář
Lokalita bez skenu/páru = vše mimo ZABAGED zůstává bílé (ISOM 402–414.1 GAP). **Volba uživatele: necháme bílý, predikci
přenecháme UC5 `Png2Area`** (Novina = benchmark, NEdá tréninkové tiles). Odhad poměrů z ortofota = nová generalizační
vrstva → „generalizuj jen s důkazem", odloženo. Termín **„Čistý les GAP" zapsán do `GLOSSARY.md`** (u Runnability).

### 4. Ořez `.omap` bez Livelox páru (bod 1) — %THINK + skeleton, implementace zbývá
**Důkaz vady ověřen:** Novina papír ~300×500 mm, ale objekty sahají ±~20 km („Nisa do Vesce") — ArcGIS vrací CELÉ
features protínající bbox, `rgb.png` ořeže canvas, `.omap` vektor NE; `clip_omap_to_quad` běží jen s párem. **%THINK
(Q1=A, jedna cesta):** centroid clip nestačí (řeka se středem uvnitř → celá). Hint uživatele „použij ořez linií pod
mostem" → `_split_by_zones_interp` (interpolovaný řez). **Schválená DRY architektura:** primitiva `cut_line` (reuse
`_split_by_zones_interp`) / `cut_area` (Sutherland-Hodgman) / `cut_point` (`_point_in_quad`) → JEDEN orchestrátor
`clip_omap(.omap, clip_poly)` (přepíše `<coords>`) → wrappery `cut_box` (rohy papíru) + `clip_omap_to_quad` (quad,
povýšen z centroidu). Box = degenerovaný quad. **`clip_quad.py` → `generator/cut.py`** (`git mv`, importy
measure_dod/pairs aktualizovány). Implementace primitiv = příští blok (riziko: přepis coords s flagy → mini-verify).

### Kód (commity: fix + docs)
`fix(generator)`: voda filtr pseudo balvanů (`_rasterize_water_grid` + 210 per-tečka) + `_clip_fences_off_water`
(plot na hladině) + plot 516 jen druh 5 (ne 13). `generator/clip_quad.py` → `cut.py` (rename, plán sjednocení
v hlavičce). `docs(session)`: GLOSSARY (Čistý les GAP, Plot 516 druh 5 + voda), TODO (body 3/4 [x], bod 1 architektura).

### Kudos / Censure
- **Kudos uživateli:** test reálných výstupů odhalil 3 konkrétní vady (verify-against-source = oko uživatele); hint
  na reuse most/tunel ořezu (`_split_by_zones_interp`) = přesně správný DRY směr pro bod 1.
- **Kudos (verify měřením):** balvany/plot na vodě ověřeny erozí břehu + per-objekt kontrolou proti masce (ne „vypadá OK").
- **Censure 0.**

### Příště
- **Bod 1 — implementace ořezu (`cut.py`):** primitiva `cut_line`/`cut_point` → mini-verify na jednoduchém objektu →
  `cut_area` (Sutherland-Hodgman) → orchestrátor `clip_omap` (přepis `<coords>` + flagy) → wrappery. Sjednotit s
  pairs E2E (TODO ř. 95-98) a ořezem linií na bbox (ř. 253). Pak regen Novina = verify (přesah pryč).
- Bod 3 doverify: potvrdit v OOM, že panelák Hokejka už není oplocený (přesný výřez / poloha od uživatele).

### Příště (Sezení 113)
- **OOM verify grivace** — otevřít gen `.omap` v OOM, ověřit natočení na magnetic-north + lícování Bedř/Blatná
  s bg_scan (oko = uživatel; rastr `rgb.png` se nemění, grivace je georef metadata).
- *(curtains, odloženo)* **rotace rastru `rgb.png` o grivaci** — až rastrový konzument (reconstructor) potřebuje
  magnetic-north sken; patří na úroveň augmentace/dlaždice (transformuje X i Y), ne do `generate_map`.
- **[!] Png2Area re-trénink** (mrkla, PRECONDITION voda 301 Sez. 110) — stále mrkla-blokované.
- *(volitelné)* DEV grivaci historizovat (`--grivation-date`) kdyby tréninkové páry chtěly konkrétní epochu.

## Sezení 114 — Implementace ořezu cut.py (primitiva + clip_omap + cut_box) + regen DEV map (ntbhej)

Stroj **ntbhej (NTBHEJ21)**. Fokus z Příště Sez. 113 bodu 1 (volba uživatele přes AskUserQuestion): implementace
ořezu `cut.py` — běží na ntbhej (čistá geometrie + string `.omap`), enabler čistoty produktu, schválená DRY
architektura Sez. 113. KPI 59,1 % (Sez. 107) drží (generátor jen bug fixy, dotčené mapy bez `.pgw`). Audit cadence:
nic zralé.

### 1. Geometrická primitiva (mini-verify 10/10)
`cut_point` (nad `_point_in_quad`) · `cut_line` · `cut_area` (Sutherland-Hodgman). **Nález mini-verify (proto byl
předepsán):** schválený reuse `_split_by_zones_interp` pro `cut_line` NESTAČÍ — je stavěn na vyřezávání děr UVNITŘ
linie (mosty/tunely) a KONCOVÉ body vždy ponechá → neumí oříznout KONEC linie (in→out), což ořez na hranici quadu
typicky vyžaduje (vidět: `in→out→in` prošlo, `in→out` propadlo). → **přímá konstrukce úseků**; DRY reuse zůstal na
`_interp_grid_at` (interpolovaný bod na hraně = týž jako mosty). Odchylka od schválené architektury Sez. 113, doložena
měřením, ne naslepo.

### 2. Orchestrátor clip_omap (mini-verify 9/9)
`clip_omap(.omap, clip_poly)` čistě v paper µm — routing BOD(type=0,`cut_point`)/LINIE(type=1 bez close,`cut_line`,
1 obj→N kusů)/PLOCHA(type=1 close flag &2 → prsteny→`cut_area`), přepis `<coords>` se zachováním flagů (close 18 /
hole 16). Scoped JEN na `<objects>` blok (knihovna `<symbols>` s curve-flag definicemi netknuta). Objekty CELÉ uvnitř
se NEpřepisují (0 drift). String/regex, ne ET (paměť omap-edit-string-not-et). Verify proti zdroji (Bedř `.omap`):
type 0/1, flag 1=curve start jen v defs, area flag 18, `<objects count>` na jednom řádku.

### 3. Wrappery + integrace
`clip_omap_to_quad` povýšen z centroidu na **geometrický** (quad px → paper µm invertem `to_px`; zpřesní pairs/
measure_dod ořez = řez dlouhých linií na hraně quadu) + nový `cut_box` (papírový obdélník = degenerovaný quad) + DRY
helper `_paper_extent` (pw/ph zpětně z lineární `to_px`, bez duplikace meta). Napojení `cut_box` do CLI main (gated
real terrain) → řeší Novinu. Log VŽDY (ne jen `removed>0` — nález: přesah jsou většinou KŘÍŽÍCÍ linie = oříznuty,
`removed=0` → tichá operace proti no-silent).

### 4. Verify regen DEV map — ořez 1.00× všude
Existující Novina `.omap` přesah **9,6× půlpapíru → 1,00×** (coords do 1,44 m papíru = ~14 km svět = „Nisa do Vesce" pryč).
Regen všech 5 DEV: NL/NV (rocks on), LS/HS (rock OOM → fallback `--rocks off`), SV (border noData → `--rocks off`) —
**všech 5 přesah 1,00× = ořez funguje napříč tvary/velikostmi**. Uživatel potvrdil v OOM: „ořezy perfektní". **TODO
bod 1 (ořez) DOKONČEN.**

### 5. Rock_relief blokátor (zmizelé skály — NEŘEŠENO, → Příště)
Plný regen SE SKALAMI spadl u 3/5 DEV: **OOM** (LS/HS — `_contour_rings` `np.pad(mask.astype(float))` ~382 MiB @ >50 Mpx
float64 i po zhrubnutí; ntbhej OOM) + **border noData** (SV — rock tiling `_fetch_assembled_grid` sahá za hranici ČR →
`dmr.py` guard raise na podezřelé výšky; TODO ř. 86 řešil jen hlavní grid, ne rock dlaždice). LS/HS/SV teď bez skal 206
(HS = skalní ukázková lokalita → degradace). Uživatel: „zmizely kontury skal" → fix rock_relief = příští fokus.

### Kód (commity: feat + docs)
`feat(generator)`: `cut.py` — primitiva `cut_point`/`cut_line`/`cut_area` + helpery `_seg_seg_t`/`_signed_area`/
`_line_line_pt`/`_emit_line`/`_emit_area`/`_parse_coords` + orchestrátor `clip_omap` + wrappery `cut_box`/geometrický
`clip_omap_to_quad` + `_paper_extent` (+292/−36 ř.); `generator.py` main hook `cut_box` (real terrain). `docs(session)`.

### Globální / paměť
- **Feedback uživatele → globální `AGENTS.md`** (SSoT, sync Codex; Claude/Gemini importují živě): doporučenou odpověď
  NEdoručovat textem do chatu, ale přes `AskUserQuestion` (predikce/volba, potvrzení `<tab>`/`<enter>`). Projektová
  Claude memory smazána (redundantní s AGENTS.md).
- **Paměť `lidove-sady-no-ortofoto`** (sdělil uživatel): LS nemá ortofoto podklad (rozpor s generickým ČÚZK fetch →
  externí fakt, ne konfig).

### Kudos / Censure
- **Kudos (mini-verify):** předepsaný mini-verify odhalil, že schválený `_split_by_zones_interp` reuse neumí oříznout
  konce linie → měřením vynucená oprava PŘED napojením (ne naslepo). Risk z %THINK Sez. 113 (přepis coords s flagy) =
  zachycen mini-verify, 0 regrese.
- **Censure 0.**

### Příště
- **[!] Rock_relief OOM + border (zmizelé skály)** — (a) paměť: `_contour_rings` float64→float32 / efektivnější `np.pad`
  / nižší `MAX_TOTAL_PX` bez ztráty 0,5 m/px detekce; (b) border: `_fetch_assembled_grid`/`dmr` noData → clamp/maska
  místo raise (SV za hranicí). Cíl: plný regen DEV map SE SKALAMI projde na ntbhej → regen LS/HS/SV se skalami 206.
- **build_pair E2E ořez** (HAL3000/mrkla) — `clip_omap_to_quad` teď geometrický, ověřit v párovací cestě (carry,
  syrový korpus na ntbhej).
- **[!] Png2Area re-trénink** (mrkla, PRECONDITION voda 301 Sez. 110) — stále mrkla-blokované.
- *(carry)* OOM verify grivace (Sez. 112/113).
