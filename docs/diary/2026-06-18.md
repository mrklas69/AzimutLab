# Deník 2026-06-18

## Sezení 141 — pseudo man-made body 527/525/531 + nález dvojího ISOM číslování (ntbhej)

Vytěžovací sezení (Příště #3 ze Sez. 140: „Body 523.1/525/527/531"). Self-check ROADMAP prošel —
nové ISOM typy v generátoru = pokrytí KOMPASu, čistá etapa `Generator()`. Sezení začalo 2026-06-17
(%BEGIN), práce přešla přes půlnoc. Stroj NTBHEJ21 (bez CUDA/korpusu; ČÚZK + docs + render jdou).

### Diskuse / measure-first
- **ISOM spec PŘED renderem** (paměť `isom-spec-before-render`) z `template_classic.omap` (verify-against-source):
  523.1 Ruin min size (bod, malý čtverec) · 525 Small tower (posed/plošina) · 527 Fodder rack (krmelec) ·
  531 Prom. man-made feature – x. Všechny bodové (type=1) → izomorfní s `_draw_landmark` (524/526/530).
- **Measure-first hustota** (paměť `measure-coverage-source-on-dod-first`): probe výskytu v kartografových
  `.omap` z `resources/` → nejdřív NAIVNĚ (jen `code="527"`) vyšlo 527=39 (1 mapa), 525/531=0 → mylný závěr
  „525/531 skip".

### KLÍČOVÝ NÁLEZ — dvojí ISOM číslování (postřeh uživatele: „Bedřichovka má posed/krmelec jako 536/538!")
- **Tytéž ISOM 2017-2 symboly mají DVĚ číslovací sady** podle programu/šablony: **OOM set** (náš
  `template_classic.omap`, generátor) 524/525/526/527/530/531 vs **OCAD set** 535/536/537/538/539/540.
  Doloženo z dat: Bedřichovka.omap `id="OCD"`, krmelec `538.0`, posed `536.0`; Soví vrch (OOM) krmelec `527`.
  Crosswalk = `docs/kb/ISOM2000-ISOM2017-2.crt`. → paměť `isom-dual-numbering-oom-ocad`.
- **Můj naivní probe Bedřichovku (krmelec=538) minul → 0.** Censure! sobě — věděl jsem o `.crt` (četl na
  začátku sezení) a stejně grepoval bez crosswalku. **Kudos! uživateli** — postřeh chybu odhalil.
- **measure_dod to UMÍ** (`_load_crosswalk`+`_resolve_targets`, integer prefix `538.0→538`, ověřeno
  deterministicky: `resolve(538,"2000")={527}`, `536→525`) → headline KPI Bedřichovku počítá věrně.
- **Latentní křehkost (TODO):** `detect_version` je BINÁRNÍ (2000/2017-2), realita TROJÍ (ISOM2000 /
  OOM-2017 / OCAD-2017). Vrací „2000" i pro Soví vrch (OOM-2017) → `resolve(527,"2000")={520}` = nesmysl.
  Soví vrch NENÍ v default KPI sadě → headline nezkresluje, ale past.
- **Opravené crosswalk-aware měření** (527↔538, 525↔536, 531↔540, 523.1↔530.2) přes 5 ČR `.omap`:

  | ISOM | medián /km² | výskyt | dřív naivně |
  |---|---|---|---|
  | 527 krmelec | 7,69 | **5/5** | 1 mapa |
  | 531 X-feat | 1,34 | **5/5** | 0 ❌ |
  | 525 posed | 1,10 | **5/5** | 0 ❌ |
  | 523.1 ruina | 0 (Velbl 1) | 1/5 | 1 |

  → původní scope „523.1/525/527/531" byl SPRÁVNÝ; moje zúžení padlo (artefakt měření bez crosswalku).
  Všechny mají orig>0 → kreslit je NEsnižuje KPI (paměť `kpi-fill-undershoot-dilutes`).

### Kód — 3 pseudo man-made body (izomorfní rozšíření 418/419)
- **Registr** (`generator.py`): `ISOM_FODDER=527`/`ISOM_SMALL_TOWER=525`/`ISOM_PROM_X=531`, `LANDMARK_NAME`/
  `LANDMARK_CLASS` (10/11/12), render PX konstanty z template µm, hustoty `PSEUDO_{FODDER,SMALL_TOWER,PROM_X}_PER_KM2`.
- **Render** `_draw_landmark`: 527 = černá „Λ" stříška + svislá noha; 525 = vodorovná příčka + noha „⊤";
  531 = ČERNÝ X (mirror 419 zelený X). color 2, px-tuned; `.omap` věrný ze symbolu.
- **Pseudo injekce:** `_generate_pseudo_veg_points` → zobecněno na **`_generate_pseudo_points`** (zelené veg
  417/418/419 + černé man-made 527/525/531, SDÍLENÁ umísťovací logika mimo voda/skály/budovy/cesty + rejection
  sampling; DRY). Call-site jméno + log; `omap_export.USED_CODES += 527/525/531`; `measure_dod.KOMPAS_SOURCE`
  527/525/531 → (pseudo, pseudo).
- **Verify NV Novina** (seed 0): render 527:70 / 525:18 / 531:38 (15 km² → 4,7 / 1,2 / 2,5 /km², sedí na
  mediány); vizuál — symboly mimo vodu/mokřady/forbid, černý X 531 odlišný od zeleného 419; `.omap` point
  symboly id 152/150/156 v počtech sedí. POZN.: krmelec (mělká „Λ", apex 411 µm) vs posed (rovná příčka)
  v rastru při této velikosti těžko rozlišitelné — věrné template, `.omap` je rozlišuje (různé sym id).

### Odloženo (carry, s důvodem)
- **523.1 Ruin min size** — měřením marginální (1/5 map, 1 objekt) + invazivní (buildings vrací area,
  523.1 je point → cross-pipeline) → odloženo (volba uživatele). Reálná páka ≈ 0 (kartografové ho nekreslí).
- **KPI přeměření tří bodů** — `measure_dod` na ntbhej padá na RAM (`map_gt.segment_gt` separace skenu
  Bedřichovky 3,4 Mpx × 13 barev = `ArrayMemoryError`; known >~100 Mpx limit) → carry na HAL3000/mrkla.
  Velbloud.pgw tu navíc chybí. KPI zůstává 60,7 % (Sez. 138); 527/525/531 jsou nově v generátoru
  (pokrytí KOMPASu ↑, přesné číslo = HAL3000).
- **detect_version trojí-realita fix** — latentní křehkost (viz výše), neakutní.

### KPI/KOMPAS rekapitulace
KPI **60,7 %** (Sez. 138, beze změny — přeměření blokováno RAM limitem ntbhej). KOMPAS: 3 nové man-made
typy 527/525/531 nově generované (`KOMPAS_SOURCE` pseudo); na měřicích mapách mají orig>0 (crosswalk) →
očekávané zaplnění děr, kvantifikace = carry HAL3000.

### Kudos / Censure
- **Kudos! uživateli** — postřeh „Bedřichovka má 536/538" odhalil dvojí číslování a chybu mého měření.
- **Censure! sobě** — naivní crosswalk-slepé měření, ačkoli jsem `.crt` (524↔535…) četl na začátku sezení
  (paměti `verify-data-not-assume` + `isom-dual-numbering-oom-ocad`).

### Příště
1. **Neatline roh/multi-řez = fokus** (fill+border split; OOM verify) — carry ze Sez. 140.
2. **KPI přeměření 527/525/531** + 3 pseudo body dopad — na HAL3000/mrkla (ntbhej RAM limit).
3. **Přestřel hustoty** (balvany 204/210) — measure-first na `resources/`, kalibrovat dolů.
4. **Oplocenky 516–518** (pseudo Etapa 1) — carry.
5. (Backlog: 523.1 marginální; `detect_version` trojí-realita; KOMPAS díry 508/403/409/202.)

## Sezení 142 — Neatline fill/border split vody 301 (roh/multi-řez) + Q hotový model (ntbhej)

Fokus z Příště #1 Sez. 141: neatline roh/multi-řez. Self-check ROADMAP: generátorová kvalita ořezu
(kvalita párů X/Y), ne degradace/rekonstruktor — etapa `Generator()` OK. Stroj NTBHEJ21. Sezení začalo
incidentem **disk 100 % plný** (uživatel uvolnil ~5 GB stažením modelu do LM Studia).

### Q — hotový model místo trénování Area/Line/Point? (mezi-diskuse uživatele → IDEAS)
Pravděpodobnost plug-and-play „sken → ISOM" ~5 %: problém není „umět detekovat vzory", ale „znát NAŠI
symbolovou sadu" (ISOM kódy = doménový slovník, který pretrained modely nemají). SAM/SAM2 = masky bez
sémantiky + out-of-domain; open-vocab (GroundingDINO/CLIP) nefunguje na abstraktní značky. Cokoli stáhneš
= stejně doučit na párech (X,Y) → proto existuje `generator()`. **Reálná páka = silnější pretrained BACKBONE**
(dnes všechny tři `train.py` UŽ `encoder_weights="imagenet"` resnet34 — transfer learning těžíme; změna
`ENCODER` = levný experiment). Etapa 2 (za závorou) → IDEAS, ne teď.

### Měření / verify-against-source PŘED kódem (rozsah)
Explore agent + ruční verify template `template_classic.omap`: **jediná emitovaná combined ohraničená
plocha s integrovaným borderem = voda 301** (combined id 49: fill 301.1 id 50 + břeh 301.4 id 53, černá 270).
**520 (id 135) i 521 (id 139) jsou `area_symbol` BEZ `<borders>` = čistě fill** → border problém NEMAJÍ;
TODO seznam „521/520/lom" byl falešný poplach (obrys privátní oblasti = samostatný plot 516, řeže `cut_line`).
Reálná instance fokusu (roh Borný = Máchovo jezero) i {X} Novina = voda 301. Rozhodnuto s uživatelem:
**rozsah = jen 301**; 501 paved (taky combined) ODLOŽENO — jeho fill 501.1 koliduje s base-fill třídou
v `omap_raster` (label 1, „administrativní výplň sídel") → chtělo by vlastní template symbol, navíc border
501 = tenká hnědá (color 12, 210 µm) = marginální vizuál vs černý břeh 301.

### Kód — fill/border split (nahradil flag-16 trik pro ohraničené plochy)
- **`cut.py`:** `_BORDERED_AREA = {"301": ("301.1","301.4")}` (kód→fill+border); id resolve z `<symbols>`
  daného `.omap` (`_SYMBOLS_DEF_RE`, ne hardcode). `_border_runs(ring, clip_poly)` = maximální běhy
  segmentů MIMO clip-hranu (= reálný břeh); **klasifikace segmentu podle jeho STŘEDU** (`_on_clip_edge(mid)`),
  ne krajních bodů — rohový vrchol leží na DVOU hranách současně a per-bod klasifikace by sousední
  clip-segment v rohu mylně označila za reálný (dostal by border). `_emit_bordered_area` → fill-only
  objekt (symbol 301.1, normální close flagy) + obrysové linie (301.1→301.4) jen na reálných úsecích.
  Routing v `clip_omap` (sym_id ∈ bordered_ids → split). Řeší roh I libovolný multi-run automaticky.
- **`omap_raster.py`:** alias `CODE_TO_LABEL["301.1"] = CODE_TO_LABEL["301"]` (label 8) — klipnutá voda
  dostane fill 301.1, jinak by ji rasterizace tiše zahodila z tréninkového Y (přesně drift Sez. 110,
  Lidové sady 58/0). `N_AREA` beze změny (NEpřidáno do `AREA_ZORDER`); 301.4 = linie, mimo area raster.

### Verify (unit probe `temp/probe_border_split.py` — vše OK)
1. jednoduchý řez (pravá hrana): border `[(10,8),(5,8),(5,2),(10,2)]` vynechává x=10. 2. **ROH (přesně Borný):**
border `[(5,10),(5,5),(10,5)]` vynechává pravou I horní řeznou hranu. 3. plný `clip_omap`: combined 301 →
1× fill 301.1 + 1× linie 301.4, 0 zbylých combined, `kept=2`. 4. omap_raster: 301.1→8, N_AREA=18, 301.4 ∉ area.
Starý `maps/Borný/Borný.omap` potvrzen = 1× combined 301 (= „před" stav).

### Odloženo (carry, ntbhej dvojnásobně blokován)
- **OOM vizuál reálného Borný = HAL3000/mrkla.** Přegenerování `pairs map 1102878 Borný_v142` selhalo:
  RAM (`separate.py _is_pale_yellow` 1,38 GiB flat array na skenu 30 Mpx — sourozenec `segment_gt`
  blokátoru Sez. 141) + disk 92 M. classId 1102878 dohledán matchem georef quadu (5 Borný kandidátů).
- **501 paved fill/border split** — chce vlastní fill-only template symbol (kolize 501.1).
- **Disk C: kriticky plný** (92 M; cache `.dmr_cache` 2,1 G + `.ortofoto_cache` 891 M, regenerovatelné).

### KPI/KOMPAS
KPI **60,7 %** beze změny (split = kvalita ořezu, ne pokrytí; navíc měření na ntbhej blokováno RAM jako Sez. 141).

### Kudos / Censure
- **Censure 0.** Verify-against-source PŘED kódem odhalil falešný scope (520/521) i footgun (301.1 alias /
  501.1 kolize) → žádné slepé kolo. Rohový bug (klasifikace podle středu) zachycen ruční analýzou před spuštěním.

### Test sken pro rozpoznání ISOM symbolů (výběr uživatele)
Kritéria: nejnovější / dobré rozlišení / věrné barvy / větší / max variety plošných+liniových+bodových /
bez fialového overprintu. Sken nad 265-mapovým korpusem (metadata + detekce fialové na thumbnailech +
vizuál native výřezů). **Vybráno `1127443` „Okruhy" (Branžež, 2.–4. 6. 2026)**: 2026 (nej), mpp 0,89 (nej
rozlišení, native 3344×2365), 0 % fialové (svislá cyan = magn. poledník, ne overprint), čisté OCAD barvy,
pískovcový Český ráj = max variety bodových/plošných (balvany/srázy/skalní detail + multi-zeleň + vrstevnice).
Alternativy: 928644 Pavlovický důl (větší, ale 2025 + open/vesnice), 1167562 Liščí vrch (hi-res, 2025).
Hamr „Tekuté písky" (1198297, největší 19,9 MP) vyřazen — relay tratě (0,04 % fialové). → eval_real na
HAL3000/mrkla (CUDA; ntbhej neutáhne).

### Příště
1. **OOM vizuál reálného Borný** (`Borný_v142.omap` roh Máchova jezera) — HAL3000/mrkla (ntbhej RAM+disk).
2b. **Png2{Area,Point,Line} `eval_real` na testovacím skenu 1127443 Branžež** — HAL3000/mrkla (CUDA).
2. **KPI přeměření 527/525/531** + pseudo body dopad — HAL3000/mrkla (carry Sez. 141).
3. **Přestřel hustoty** (balvany 204/210) — measure-first na `resources/`.
4. **Oplocenky 516–518** (pseudo Etapa 1) — carry.
5. (Backlog: 501 fill/border split vlastní symbol; 523.1; `detect_version` trojí-realita; KOMPAS díry 508/403/409/202.)

## Sezení 143 — %AUDIT:CODE (5 agentů) + quick-win opravy (ntbhej)

%BEGIN: sync OK (`main` = `origin/main`). ROADMAP etapa `Generator()`. Stroj NTBHEJ21. Audit cadence:
**%AUDIT:CODE práh ≥8 DOSAŽEN** (Sez. 135 → 8 sezení + reálný LOC: pseudo body 417/418/419/527/525/531,
`cut` fill/border split, KOMPAS registr) + **%CALIBRATE 16** (od Sez. 127). Volba uživatele: **%AUDIT:CODE**.
Self-check ROADMAP: audit = úklid (ortogonální k fázové závoře); ntbhej + silný model (Opus 4.8 1M) =
ideální audit okno (PROMPTS 33/49-52 — CUDA Příště body tu blokovány).

### Metoda — 5 paralelních agentů + ruční verify proti zdroji (18 103 LOC)
Oblasti: `generator.py` (4678 LOC) / `cut`+`omap_raster`+`omap_export` / `model/` (3 reconstructory) /
`connectors/`+`measure_dod`+`compare_isom`+`separate` / cross-cutting. Agenti = kandidáti, **rozhodnutí
po vlastním verify u zdroje** (lekce Sez. 93/110: agenti generují falešné poplachy).

### Nálezy
- **1 KRITICKÝ — C1** `separate._is_pale_yellow` (separate.py:91-93): broadcast `(N,4,3)` int32 = **1,38 GiB
  @ 30 Mpx** → `ArrayMemoryError`. **Přesně to, co blokovalo měření v Sez. 141/142.** Opravná šablona už
  existovala (`map_gt._classify` row-chunk, Sez. 111).
- **~9 doporučených:** D1 `USED_CODES` nepokrývá emitované 416/416.1 (SSoT dluh, vyhozeno Sez. 30, při
  re-zavedení Sez. 101 nedoplněno) · D2 `detect_version` binární vs trojí realita + tichý default (headline
  imunní, ověřeno 6/6 map) · D3 duplikovaný symbol-id resolver (cut/omap_export, asymetrie fail-loud) ·
  D4 arcgis paging dle délky dávky ne `exceededTransferLimit` · D5 `_line_line_pt` /0 guard · D6 DRY
  pseudo body/boulders · D7 stale benchmark 0,537 (→0,683 MPP fix Sez. 126) · D8 README layout drift ·
  D9 DRY napříč model/ (px/mm, ImageNet, peak-detekce, tiling — triggery už nastaly).
- **~9 kosmetických** (K1-K9). **K7 = FALEŠNÝ POPLACH** — „51 kódů" žije jen v docs, ne v `measure_dod.py`
  kódu (zachyceno ručním verify → potvrzuje hodnotu lekce Sez. 93/110).
- **Bonus verify-against-source:** audit odhalil **nepřesnost diáře Sez. 141** — `Soví vrch.omap` má
  `538=Krmelec` (OCAD), ne `527`; detekováno přes 526-building → crosswalk je správně. Memory aktualizace ne (point-in-time).

### Opraveno (quick wins, volba uživatele) + verify
8 souborů: **C1** (row-chunk, lokální `_CLASSIFY_CHUNK_PX=4M`) · **D1** (`USED_CODES` +416/416.1, total 59) ·
**D5** (`_line_line_pt` guard `abs(d)<1e-12` → fallback koncový bod) · **D7** (0,537→0,683) · **K1**
(`Png2Polygon`→`Png2Area` 3× runnability docstring) · **K3** (530/531 rozliš. „: o"/„: x") · **K4** (docstring
+železnice 509) · **K5** (smazán mrtvý `_TEMPLATE`). Verify: **C1 byte-identický na 4 tvarech vč. 3344×2365**
(rozlišení testovacího skenu Branžež), importy/AST OK, `test_cut`+`test_north_grid` 10/10, `test_purple` 1/1
(`test_checkpoints`/`test_vectorize` blok. torch/skimage = trénink-only, nezměněné moduly). Zbylé nálezy →
TODO sekce „%AUDIT:CODE (Sez. 143)".

### isom_scan ochrana (nález při „ponechat!")
Untracked `isom_scan/` (testovací sken **1127443 Branžež** + 2 PDF) = **Livelox copyright** (B3). `git
check-ignore` MATOUCNĚ tvrdil „ignorováno", ale `git add --dry-run` ukázal pravdu = **přidalo by se** →
do `.gitignore` přidán `isom_scan/` (zůstává na disku, mimo git). Lekce: `add --dry-run` > `check-ignore`.

### KPI/KOMPAS
KPI **60,7 %** beze změny (audit = úklid kódu, ne pokrytí; měření na ntbhej stále blok. — ale **C1 ho příště
odblokuje**).

### Kudos / Censure
- **Censure 0.** Ruční verify u zdroje zachytil falešný poplach (K7) i nepřesnost diáře (Soví vrch 538) →
  žádné slepé přijetí agentích nálezů. C1 oprava reuse hotové šablony (`map_gt`), byte-identičnost doložena.

### Příště
1. **Oplocenky 516–518** pseudo (Etapa 1, vytěžení) — **jde na ntbhej** (render + ČÚZK); fokus #1.
2. **OOM vizuál reálného Borný** (`Borný_v142.omap`) — HAL3000/mrkla (carry Sez. 142).
2b. **eval_real na 1127443 Branžež** + **KPI přeměření 527/525/531** — HAL3000/mrkla (CUDA; C1 odblokoval RAM).
3. **Přestřel hustoty** (balvany 204/210) — measure-first na `resources/` (C1 odblokoval separaci na ntbhej).
4. (Audit carry: D2 detect_version, D3 resolver, D6 pseudo DRY, D9 model/ DRY; off-water helper; A5 smoke.)

## Sezení 145 — %CALIBRATE (od Sez. 127) + KPI přeměření na ntbhej (C1 odblokoval) (ntbhej)

%BEGIN: sync OK (`main` = `origin/main`). ROADMAP etapa `Generator()`. Stroj NTBHEJ21 (CUDA body z Příště
tu nejdou). Audit cadence: **%CALIBRATE práh ≥15 PŘEKROČEN o 3** (naposled Sez. 127 → 18 sezení) → vynucený
první bod (PROMPTS „překročen o ≥2"); navíc ntbhej + Opus 4.8 1M = ideální okno (úsudková práce na silném modelu,
úklid prioritně na ntbhej). Necommitnutý carry z minula: `docs/IDEAS.md` (+3 ř., isom_scan benchmark dodatek).

### %CALIBRATE — meta-audit (context hygiene / process / collaboration)
Materiály: řídicí soubory + diáře 141–144 + TODO/IDEAS + tvrdá data (velikosti, settings, memory, git). Hlavní zjištění:
- **Context hygiene SOLIDNÍ:** projektový CLAUDE.md 90→88 ř. (stabilní, sub-prah ≥50 % netriggerován), memory
  index **31/31 soulad** (žádný drift). Disciplína drží.
- **C1 (akční) — dual-numbering past se OPAKUJE i přes paměť:** Sez. 141 Censure (crosswalk-slepé měření) → Sez. 144
  ZASE (probe porovnal int `516` se string setem `{"516"}` → falešné „0/skip"). Společný jmenovatel = **ad-hoc probe
  obcházející `measure_dod`**. Paměť radila „měř crosswalk-aware", ne „nepiš vlastní probe".
- **B2 (strategický) — měřicí dluh:** KPI **60,7 % beze změny Sez. 138→144** (7 sezení), mezitím 5 nových typů
  (527/525/531/517/518) NEPŘEMĚŘENO, carry „přeměření" 4×. Příčina: ntbhej generuje pokrytí, verify je CUDA/RAM-blokované.
- Drobnosti: A2 (`wc`/`awk` chybí v allow → permission prompty), A3 (README layout drift D8 — chybí soubory).
- Silné stránky: verify-against-source špičkové (Sez. 142–144 Censure 0, 3× chycen falešný poplach).

### Opraveno (4 odsouhlasené, volba uživatele = vše; kód NEMĚNĚN, jen docs/config)
- **C1:** tvrdá konvence *„výskyt/hustotu ISOM měř JEN přes `measure_dod`, ne ad-hoc probe"* → `CLAUDE.md`
  doménové zásady + aktualizace paměti `isom-dual-numbering-oom-ocad` (Sez. 144 case + „nepiš vlastní probe vůbec").
- **A2:** `Bash(wc *)` + `Bash(awk *)` → `~/.claude/settings.json` allow (25→27, JSON ověřen validní).
- **B2:** %END konvence *„přidal-li jsi pokrytí a máš `.pgw`: změř KPI HNED, nečekej na plnou sadu"* → `docs/PROMPTS.md`;
  Velbloud.pgw povýšen na `[!]` (TODO A6 — blokuje měření default sady na ntbhej).
- **A3:** README „Repository layout" doplněn o **15 chybějících souborů** (connectors `ortofoto`/`magnetic`, generator
  `cut`/`omap_export`/`gen_backgrounds`/`measure_dod`/`compare_*`/`stats`/`batch`/`palette`/`project_config`, model
  `mpp`/`purple`/`vectorize`) — popisy **ověřeny z docstringů** (verify-against-source, nehádat). README už deklaruje
  sebe jako SSoT faktů; CLAUDE.md „Klíčové soubory" = „proč/Sez" vrstva (ne duplicita).

### Druhý fokus — KPI přeměření na ntbhej (volba uživatele)
- **✅ C1 fix REÁLNĚ odblokoval `measure_dod` na ntbhej** — rozpor diáře rozřešen empiricky: Sez. 141 svaloval RAM na
  `map_gt.segment_gt`, Sez. 143 C1 opravil `separate._is_pale_yellow` (jiný soubor) → pochybnost „odblokuje to vůbec?".
  **Měření proběhlo bez `ArrayMemoryError`** (downscale na `TARGET_MPP` před `segment_gt` drží peak). Dnešní B2 konvence
  tím prakticky ověřena.
- **KPI 2-mapová sada 55,3 %** (Bedř 49,4 / Blatná 61,2). Velbloud.pgw chybí → `_missing_pgw` ho **hlasitě vynechal**
  (no-silent-fallback funguje). **NESROVNATELNÉ** s historickými 60,7 % (3-mapová; Velbloud byl nejvyšší 67,0 → absence sráží).
- **KOMPAS provedení 5 nových typů (Bedř+Blatná agregát):**

  | kód | jméno | orig | gen | provedení |
  |---|---|---|---|---|
  | 527 | Fodder rack | 7 | **79** | **PŘESTŘEL 11×** |
  | 531 | Special man-made x | 6 | **20** | přestřel 3,3× |
  | 525 | Small tower | 27 | 6 | podstřel |
  | 517 | Ruined fence | 17 | 1 | podstřel |
  | 516 | Fence | 24 | 6 | podstřel |
  | 518 | Impassable fence | 16 | 6 | ok |

- **NÁLEZ — hustoty Sez. 141 jsou VADNÉ (crosswalk-slepé měření):** `PSEUDO_FODDER_PER_KM2=(3,13)` med ~7,7
  vs crosswalk-aware orig Bedř+Blatná **0,6/km²** (10× nad). Medián 5 map je robustní → 7,7 nemůže koexistovat s 0,6
  na 2 z těch map. Přestřel 527/531 sráží **Bedř bod na 40,9 %** (`min` ukrojí přebytek, [[kpi-fill-undershoot-dilutes]]).
- **Rozhodnutí (volba uživatele): NEkalibrovat slepě na 2 mapy** (Goodhart A3 + overfit) → zaznamenat jako akční `[!]`:
  doplnit Velbloud.pgw → přeměřit pseudo hustoty crosswalk-aware přes plnou sadu → PAK kalibrovat (spojit s „Přestřel
  hustoty 204/210"). TODO „KPI přeměření 527/525/531" označen `[x]` (změřeno).

### KPI/KOMPAS
KPI 2-mapová **55,3 %** (Bedř 49,4 / Blatná 61,2; Velbloud chybí — nesrovnatelné s 60,7 % 3-map). Headline 60,7 %
zůstává poslední srovnatelnou hodnotou do doplnění Velbloud.pgw. Díry (agregát): 403/416/508/408/409/410/202 (vegetace
gate + Png2Line dluh, beze změny směru). Nové pokrytí 527/531 = přestřel (kalibrace = carry).

### Kudos / Censure
- **Censure 0.** %CALIBRATE neopravil nic bez odsouhlasení (definice makra); A3 popisy ověřeny z docstringů (nehádány);
  rozpor diáře 141/143 rozřešen empirickým během, ne dohadem; kalibrace odmítnuta slepě (Goodhart).
- **Kudos! metodě:** druhý fokus přesně naplnil dnešní B2 konvenci a odhalil, že hustoty Sez. 141 nesou tutéž
  crosswalk-slepotu, kterou si Sez. 141 sám vytkl — měřicí dluh skrýval kalibrační chybu.

### Příště
1. **Doplnit Velbloud.pgw na ntbhej** (TODO A6 `[!]`) → odblokuje srovnatelný 3-map KPI trend + plnou sadu pro kalibraci.
2. **Kalibrace pseudo hustot** (527/531 dolů, 525/517/516 nahoru) — až po přeměření crosswalk-aware přes plnou sadu.
3. **OOM vizuál reálného Borný** (`Borný_v142.omap`) — HAL3000/mrkla (carry Sez. 142).
4. **eval_real na 1127443 Branžež** — HAL3000/mrkla (CUDA).
5. (Audit carry: D2 detect_version, D3 resolver, D6 pseudo DRY, D9 model/ DRY; off-water helper; A5 smoke.)

## Sezení 144 — Pseudo ploty 516/517/518 (varianta plotu) (ntbhej)

Fokus z Příště #1 Sez. 143 (oplocenky). Self-check ROADMAP: dva nové ISOM typy do generátoru = pokrytí
KOMPASu = čisté vytěžení, Etapa 1 ✓. Stroj NTBHEJ21 (render + ČÚZK jdou). Volba přístupu uživatele:
**varianta plotu (KISS)** — rozšířit existující 516 na tři typy, ne nová umísťovací mašinérie.

### Spec PŘED renderem (paměť isom-spec-before-render) + measure-first
- **Template id 131/132/133:** 516 Fence (plná 210 µm + 1 tick) · 517 Ruined fence (ČÁRKOVANÁ dash
  3,0/break 0,525 mm + 1 tick) · 518 Impassable fence (silnější 375 µm plná + DVOJITÝ tick = 2 mid-elementy).
- **Measure-first výskyt** (crosswalk-aware, paměť isom-dual-numbering-oom-ocad): **dual-numbering past** —
  OOM 516/517/518 ↔ OCAD 522/523/524; v OCAD je `516 = Power line`! Počet OBJEKTŮ v 5 ČR `.omap`:

  | mapa | 516 | 517 | 518 |
  |---|---|---|---|
  | Bedřichovka (OCAD) | 22 | 4 | 11 |
  | Blatná (OOM) | 2 | 13 | 5 |
  | Velbloud (OCAD) | 36 | 18 | 12 |
  | Slovanka (OCAD) | 52 | 6 | 58 |
  | Soví vrch (OCAD) | 1 | 1 | 29 |
  | **výskyt / Σ** | **5/5 / 113** | **5/5 / 42** | **5/5 / 115** |

  → všechny tři vedou 5/5; **518 stejně častý jako 516** (ne marginálie), 517 řidší. Váhy 0,42/0,16/0,42.
  Pozor: `410.2/504.1 živý plot` = vegetace hedge, NE man-made fence — správně vynecháno.

### VERIFY-AGAINST-SOURCE třikrát zachránil před falešným poplachem
1. První probe (`_resolve_targets`) dal **0 pro vše** → vypadalo jako „ploty nevedou" / „měřicí bug v
   measure_dod". Verify (přímý objekt count + přímé volání `_resolve_targets(522,'2000')={516}`) ukázal:
   **bug byl v MÉM probe** (porovnával int klíč `516` se string setem `{"516"}`), measure_dod je SPRÁVNÉ —
   KPI/KOMPAS ploty vidí crosswalk-aware. (Izomorf Sez. 141 past — kdybych probe uvěřil, mylně „skip".)
2. `random.Random(tuple)` v prvním kódu = `TypeError` → smoke odhalil PŘED renderem; opraveno na int
   spatial-hash `(x*73856093)^(y*19349663)` (izomorf vzoru Sez. 57 boulder field, regen-identický).

### Kód (6 změn, izomorf existujícího 516)
- **Registr** (`generator.py`): `ISOM_FENCE_RUINED=517`/`ISOM_FENCE_IMPASSABLE=518`, `FENCE_NAME`→dict,
  `FENCE_TYPES`/`FENCE_TYPE_WEIGHTS`, render dicty `FENCE_WIDTH_PX`/`FENCE_DASH`/`FENCE_TICKS`/`FENCE_TICK_DBL_OFF`.
- **`_draw_fence_line(…, fence_type)`** typ-aware: width/dash/n_ticks dle registru; 517 přes `_draw_dashed`.
- **`_draw_dashed`**: `if mdraw is not None` guard → reuse bez GT masky (plot masku nemá).
- **Fence smyčka**: per pozemek losuje typ z geometrie (spatial-hash) → `fence_features.append((grid, ftype))`.
- **`omap_export.USED_CODES`** +517/518; **`measure_dod.KOMPAS_SOURCE`** 517/518 = (RÚIAN, pseudo).
- Export generický na `str(code)` (řádek 4395) → 517/518 jdou do `.omap` automaticky.

### Verify
Import/registr/USED/KOMPAS OK · distribuce losování 2000 vzorků 42/16/43% ≈ cíl · **render LS bez chyby**
(plot 215 úseků) → `.omap` **516=80 / 517=39 / 518=108** (3 typy, 35/17/48%) · **vizuál** tří typů vedle sebe
render-rozlišitelný (516 tenká plná / 517 čárkovaná / 518 silnější + dvojtick).

### KPI/KOMPAS
KPI **60,7 %** beze změny zde (přeměření blokováno ntbhej: `segment_gt` RAM + chybí `Velbloud.pgw`). 517/518
nově v generátoru (pokrytí KOMPASu ↑); měření je crosswalk-aware správné (ověřeno) → kvantifikace = carry HAL3000/mrkla.

### Kudos / Censure
- **Censure 0.** Verify-against-source zachytil dnes **3× falešný poplach** (K7 audit + první resolve probe +
  random tuple), pokaždé před zápisem/renderem (paměti `verify-data-not-assume` + `isom-dual-numbering-oom-ocad`).
- **Kudos! sobě/metodě:** measure-first + spec-before-render = 0 slepých kol; render hned napoprvé správně.

### Příště
1. **OOM vizuál reálného Borný** (`Borný_v142.omap`) — HAL3000/mrkla (carry Sez. 142).
2. **eval_real na 1127443 Branžež** + **KPI přeměření 527/525/531 + 517/518** — HAL3000/mrkla (CUDA/RAM).
3. **Přestřel hustoty** (balvany 204/210) — measure-first na `resources/`.
4. (Audit carry: D2 detect_version, D3 resolver, D6 pseudo DRY, D9 model/ DRY; off-water helper; A5 smoke.)
