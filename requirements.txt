# AzimutLab — runtime závislosti generátoru (connectors/ + generator/).
# Sys.path skripty (fáze B), ne balík → instalace do .venv v kořeni: py -m pip install -r requirements.txt
# Druhý stroj (git sync, paměť two-machines-git-sync) si tím dorovná prostředí.
numpy
Pillow
contourpy          # marching squares — vrstevnice + vektorizace rock-relief masky
pyproj             # WGS84 → S-JTSK (jen real režimy)
certifi            # jednotný CA bundle pro ČÚZK/Livelox TLS ve urllib konektorech
scipy              # morfologie + connected-components — rock-relief z DMR sklonu (--rocks real, Sez. 63)
pygeomag           # WMM magnetická deklinace (offline, vestavěné koef.) — grivace konektor magnetic.py (Sez. 112)

# UC5 model/ (trénink + inference reconstructorů, Png2Area/Point/Line) běží jen na stroji s CUDA
# (mrkla/HAL3000, RTX 5070) → viz requirements-train.txt (audit 260702-B4b; hranice byla dřív jen
# nepsaný komentář, ne fyzický soubor — 260612-B4 → 260621-B8 → dnes).
