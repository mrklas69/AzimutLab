# Deník — 2026-06-29

## Sezení 176 — Canonical KPI přeměření + watercourse 304/305 klasifikace (délka místo pojmenovanosti) → KPI 67,5→68,7 (HAL3000)

Stroj **HAL3000** (CUDA + `resources/*.pgw` v gitu od Sez. 164 = jediný stroj pro **canonical KPI**).
Sezení začalo `%BEGIN`; git sync ukázal lokální klon **1 commit pozadu** (`ce7ee8b..e3c2c52`) → pull přitáhl
Sez. 173–175 (ntbhej: budovy 521, mapfield crop, scan-vytěžení 419/111/112/115, %AUDIT:CODE+DOCS, celá
`isom_scan/` GT mašinérie + 74 testů). Fokus (volba uživatele): **přeměřit canonical KPI** (stale 67,5 ze
Sez. 164 po 4 změnách generátoru — 210/109-111/budovy/mapfield). Pak směr: „pokračuj autonomně, zastav se až
KPI skutečně vzroste."

### Diskuse / rozhodnutí
- **Canonical KPI nejdřív** — HAL3000 je jediný canonical stroj (`resources/*.pgw`), generátor se 4× změnil →
  PROMPTS krok 6 vyžaduje přeměření. Vyřešil dlouhý carry (Příště #3 visel od Sez. 166).
- **Watercourse 304/305 = klasifikační chyba, ne datová** (verify-against-source): ISOM 304/305 rozlišuje
  **šířku toku** (304 ≥~1 m, 305 < 1 m), NE pojmenovanost. ZABAGED šířku koryta NENESE; pojmenované toky tu
  jsou drobné lesní potoky („Červený/Blatný potok") = 305, ne řeky → dřívější `jmeno → 304` 304 systematicky
  nadhodnocovalo (gen 46 vs kartograf 12).
- **Náhradní diskriminátor = CELKOVÁ délka toku per `idvt`** (identifikátor vodního toku; dlouhá souvislá
  vodoteč napříč mapou = páteřní/širší tok). Práh **3000 m** = přirozený zlom v distribuci délek (probe:
  3 KPI mapy mají skok 47→7 segmentů mezi prahy 2,5 a 3 km), ne ladění na číslo.
- **Zachovat 304 capability** (volba uživatele) — zamítl úplné zrušení 304 (default 305). Délkový práh 304
  zachovává (~11 objektů jen nejdelší toky), proti čistému 305 (304 gen 0) KPI o ~0,1 pb míň (v šumu).
- **Bezpečné pro Png2Line** (verify): `omap_raster.LINE_CLASSES` spojuje 304+305 do JEDNÉ label-třídy
  „watercourse" (komentář „tloušťka ze skenu nerozlišitelná") → přesun 304→305 trénink reconstructoru NEMĚNÍ,
  žádný retrain. ISOM 304 zůstává v `USED_CODES`/capability (umí, jen se řídce generuje).

### Kód
- **`connectors/zabaged.py` `map_water_to_isom`:** stálý tok → 305 default (zrušeno `jmeno → 304`); občasný
  → 306, podzemní → None beze změny. Docstring přepsán s ISOM kritériem šířky + důvodem.
- **`generator/generator.py`:** konstanta `WATERCOURSE_MAIN_LEN_M = 3000.0` + `_generate_real_water`
  předpočítá `idvt → Σ Shape_Length` (plain dict, bez importu) a povýší úseky dlouhých toků (≥ práh) 305→304.

### KPI (measure-first, governance před/po na canonical 3-map sadě)
- **Přeměření baseline:** canonical 67,5 (Sez. 164) → **68,0 %** (Bedř 63,6 / Blatná 69,0 / Velbloud 71,5;
  plocha 79,4 / linie 66,8 / bod 65,0). ntbhej zisky se přenesly tlumeně (68,0 < ntbhej 68,4, separace
  stroj-citlivá). Velbloud mírná regrese vs Sez. 164 (74,7→71,5).
- **Past prahu 1000 m:** generate_map fetchuje větší bbox než probe → většina toků > 1 km → 304 gen 46→**53**
  (přestřel ROSTL), 305 44→37, KPI **67,9** (kleslo). Probe distribuce délek odhalil zlom → práh 3000 m.
- **Po opravě (práh 3000 m):** **KPI 68,7 %** (+0,7 pb vs baseline 68,0; +1,2 vs stale 67,5). Linie
  66,8 → **68,7** (+1,9 pb), plocha/bod beze změny (žádná regrese). Velbloud 71,5→71,9.
  KOMPAS watercourse: **304 gen 46→11** (orig 12, přestřel→~přesně), **305 gen 44→79** (orig 267, podstřel→ok),
  306 25 beze změny (občasné toky = vzácná data, separátní díra). 304/305 vypadly ze žebříčku děr.
- **Nález (verify-against-source, odloženo):** 308 Small marsh (158/0 „chybí") = **prázdná ZABAGED data**
  (1 rašeliniště na 3 mapy), ne regrese — kartografových 158 mokřadů ZABAGED nevede → ne ZABAGED páka.

### Verify
- `py_compile` OK · `unittest discover` **74 OK + 1 skip** · `tests/smoke.py` **OK** (305 stále kreslen).
- `git status`: jen `connectors/zabaged.py` + `generator/generator.py` (maps/ gitignored přegenerovány v cache).

### Kudos / Censure
- **Kudos!** Uživatel zachytil úplné zrušení 304 (capability) → přesměrování na délkový diskriminátor =
  věrnější (304 žije) i jen marginálně horší KPI.
- **Censure!** sobě: první práh 1000 m naslepo (KPI kleslo) — měl jsem distribuci délek změřit PŘED prahem,
  ne po. Probe-first až napodruhé.

### Příště
1. **306 Minor water channel** (294/25 podstřel) — občasné toky vzácná ZABAGED data; zvážit, zda separace
   skenu / pseudo, nebo doložit strop (jako 308 marsh). Nízká priorita (data-gate).
2. **403 / 416** (#1/#2 díry) — plošná separace = kvalita ne pokrytí; páka přes separace ladění (opatrně).
3. **ntbhej + Velbloud mapfield** (carry Sez. 175) — naklikat Velbloud/Blatná mapfield → čistší counts;
   ntbhej přeměření po watercourse opravě (sirotek-disciplína: canonical 68,7 ≠ ntbhej 68,4).
4. **Zbylé code-audit dluhy** D6 `_place_points_in_mask`, D9 model DRY.

## Sezení 177 — Vynucený pruning + KPI/KOMPAS analýza (gen=0 díry = data-gate zeď) + plná regenerace maps/ (HAL3000)

Stroj **HAL3000** (canonical KPI, plná výbava). `%BEGIN`: git sync čistý/up-to-date (`c4abcb6`),
ROADMAP etapa `Generator()`. Uživatel: „pokračuj dle priorit" (odmítl menu otázku).

### Vynucený pruning (cadence ≥12, překročen o 2 — poslední Sez. 163)
Self-check stroj: pruning nepotřebuje CUDA, ale paměť [[cleanup-do-now-not-ntbhej-handoff]] velí udělat HNED
na aktuálním stroji (handoff = úklid nikdy). Ověřeno, že hotové věci jsou v DONE (Sez. 141/144/166/168/170/171/173)
→ pruning informaci neztratí. **TODO 562 → 500 ř. (−62):**
- Smazán **exaktní duplikát** „Nezávislé RNG streamy" (řádky 322-333 byly 2× identické).
- `[x]` blok 521 (HOTOVO Sez. 173) → zúžen na zbylé ZBÝVÁ (503 granularita; 304 už řešen watercourse Sez. 176).
- 8 nabobtnalých `[~]` hotových tahů zúženo na samotné ZBÝVÁ (fokus B guard / circularity Sez. 170 /
  benchmark GT Sez. 168/171 / bodové 523.1 / pseudo hustoty 531 / oplocenky 516-518 / historický kontext Sez. 145).
- Odstraněn stale „521 budovy" ze ZBÝVÁ položky 295. IDEAS čisté (94 ř., nesaháno).
- Obří historický blok „Procedurální generátor přestavba" (ř. 402, ~1500 zn.) NECHÁN — 1500-zn. old_string = vysoké riziko překlepu vs malý zisk.

### Governance ověření (audit A2 [!], měl předcházet scan-mining rozšíření)
Z manifestu + capabilities ověřeno, že **A2 je z velké části vyřešen** reframy Sez. 158-162:
- `capabilities.py ↔ calibration_manifest.json` **konzistentní**: 112/115 **záměrně mimo registr**
  (reconstructor-only, gen nekreslí → [[capability-registry-equals-used-codes]]; audit A2 nález „chybí" tím vyřešen);
  525/527/531 `SCAN_LIVE_POINT` = live signál je **trénovaný Png2Point** (eval_real Sez. 158/159), ne classic-CV
  manifest recall=null → dvě osy, ne rozpor.
- **Nález: 311/312/313 (TODO [!] „další konkrétní krok") jsou KÓDOVĚ HOTOVÉ** — `water_points_poc.py`
  už běžel: 312 `candidate_calibrated` (recall 1.0, 1 GT), 311 = 4 kandidáti / **0 pozitivních GT** (uživatel
  ve 2. průchodu nepotvrdil), 313 bez dat. Blokuje **ruční GT (uživatel) / jiná mapa, ne kód.** Opraven
  zastaralý fakt v TODO (audit C6: řídící docs musí říkat pravdu).

### KPI/KOMPAS analýza (measure-first, volba A = čisté pokrytí)
- **KPI 68,7 %** potvrzeno (identické se Sez. 176, generátor neměnný): plocha 79,4 / linie 68,7 / bod 65,0
  (Bedř 63,7 / Blatná 70,4 / Velbloud 71,9).
- **KLÍČOVÝ NÁLEZ — žebříček `gen=0` děr je systematicky DATA-GATE**, ne nedbalost generátoru:
  202 Passable rock face (195/0, sráz = LINIE, „passable" mírnější než práh 46° → rock_relief ho míjí) ·
  201 Rock pillar/cliff (36/0) · 107 Erosion gully (25/0, má zdroj ale ZABAGED 0 na Liberecku) ·
  108 (155/0) · 205 (24/0, atribut výšky chybí → sloučeno do 204) · 208 (133/0, vrstva 0 na canonical) ·
  308 marsh (158/0, prázdná data). **206 Gigantic boulder = 0/0** (kartograf nepoužívá, gen nekreslí na canonical).
  Závěr: čistá autonomní ČÚZK pokrytí-páka je **VYČERPANÁ** (potvrzuje KPI blok). gen=0 díry = symboly z terénní
  znalosti, již geodata nenesou → buď scan-mining (ruční GT) nebo strop.

### ROADMAP self-check (Censure)
Nabídl jsem „Png2Line gap-bridging u junkcí" jako variantu 1 s nálepkou „legitimní". Při self-checku PŘED kódem
odhaleno, že je **za fázovou závorou**: gap-bridging je čistě vektorizace masky→`.omap` (post-process) →
nezvedá generátor KPI ani eval_real completeness (segmentace), zlepšuje jen **vrcholový produkt `Rekonstruktor()`**.
ROADMAP: dokud KPI < cíl a KOMPAS neplný, nepostupovat do `Rekonstruktor()`. **Uživatel zvolil (A)** = držet
závoru, čisté pokrytí. **Censure sobě:** měl jsem to zachytit už při sestavování menu, ne až po souhlasu.

### Plná regenerace maps/ (požadavek uživatele „kouknu jak jsme na tom")
`temp/regen_batch.py` (Sez. 164, UTF-8 safe, 4 cesty dle [[maps-regen-recipe]]): **21/21 kroků OK** —
KPI(3) dmr+ortho podklady, Livelox(5) `pairs.py map`, DEV(5) `--location`+scan-auto; Buschdörfl ZÁMĚRNĚ
vynechán (special E2E). Vizuál ukázán proaktivně (Bedřichovka/Velbloud/Rovné skály rgb.png): věrný, voda
no-draw drží, vrstevnice nepřetékají, vegetace organická, skalnatost odpovídá lokalitě.

### Verify
- Pruning integrita: žádné `[x]` v aktivních seznamech, RNG duplikát 1×, TODO 500 ř.
- Kód NEMĚNĚN (jen `docs/TODO.md`) → smoke/KPI přeměření není nutné (68,7 % potvrzeno z cache).

### Příště
1. **202 Passable rock face — %THINK extraktor** (největší data-derivovatelná díra 195/1,5 pb): buď DMR sklon
   → hrana strmé oblasti jako sráz linie (mírné srázy <46° = riziko falešných svahů), nebo návrat k opuštěné
   ZABAGED `Skalní_útvary` ploše → obrys jako 202 linie (Sez. 63 ji zavrhl jako „blob"). Velký/nejistý, drží závoru.
2. **Zafixovat strop data-gate děr v KOMPASu** (anti-Goodhart governance) — aby `gen=0` data-gate kódy
   (202/201/107/108/308/205/208) netvrdily „chybí = páka", když je to doložený strop. Nezvedá KPI, čistí kompas.
3. **Scan-mining GT (ruční, uživatel)** — 311/313 na jiné mapě, 417/418 review → odemkne pokrytí z mapařských
   skenů (data tam jsou, ne v ČÚZK). Já doměřím/skóruju.
4. **Carry:** ntbhej + Velbloud mapfield (Sez. 175), code-audit D6/D9.
