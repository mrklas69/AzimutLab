# Diář 2026-05-29

## Sezení 36 — Lesní průseky (ISOM 508 Narrow ride) ze ZABAGED

Navázání na Sez. 35 (audit) bez formálního `%BEGIN` — plynulý přechod. Fokus zvolen z „Příště"
Sez. 35 / [!] TODO priority: poslední „klasické" liniové/bodové OB vrstvy z katalogu. Uživatel
zvolil **Lesní průsek → 508** jako první.

### Foundations před kódem (verify-against-source, paměti `isom-spec-before-render` + `geometric-selfcheck-before-oom`)
- **ISOM 508 = „Narrow ride"** (template id 115): průhled lesem / výrazná stopa BEZ zřetelné vyšlapané
  cesty (ISOM odlišuje od cest 503-506). Render: černá čárkovaná, `line_width=210 µm` (0,21 mm),
  **dash 3000 / break 375 µm** (dlouhé čárky, malé mezery → „skoro plná", odliší od pěšiny 505 7,0/4,0),
  `minimum_length=4875 µm`. Spec poznámka: runnability se ukazuje silnější žlutou/zelenou/bílou linií
  na pozadí.
- **Probe ZABAGED** (`MapServer?f=json` + query na SV): vrstva **`Lesní průsek` id 16**, REST jméno
  **s MEZEROU** (jako tramvaj/lávka, ne escape `Lesní_průsek`); LineString, S-JTSK axis OK; **46 prvků
  na Sovím vrchu**; žádný kategoriální atribut (typ/šířka) → KISS vždy 508.

### Klíčové rozhodnutí — runnability pozadí NEKRESLIT
Spec 508 počítá s žlutým/zeleným pozadím dle prostupnosti. **Necháváme bez pozadí** (ISOM varianta
„without background: same runnability as surroundings") — prostupnost/vegetace **není v datech**
(vegetace gate zavřená Sez. 3) a je to **UC5 predikce, ne projekce**. Vymýšlet barvu = „dekorace nad
rámec dat" (odmítáno jako pramen 312 / vegetace). Uživatel potvrdil: „Ano jen ISOM 508".

### Implementace (izomorfní s railways/powerlines)
- `zabaged.py`: `"Lesní průsek": 16` do `LAYER_IDS`, `RIDE_LAYERS`, `fetch_forest_rides`, `map_ride_to_isom`→508.
- `generator.py`: `ISOM_NARROW_RIDE=508`, `RIDE_NAME/RIDE_CLASS/RIDE_STYLE` (`dashed`, 1 px, dash
  `3,0·PX_PER_MM`/break `0,375·PX_PER_MM`), `_draw_ride` (wrapper nad `_draw_line_symbol`, mode `dashed`
  — žádný nový render mód), `_generate_real_rides`, `--rides` CLI, z-order po cestách/před vedením,
  `mask_rides.png`, meta sekce `rides`, validace (přidáno do DRY smyčky Sez. 35), log řádek.
- `omap_export.py`: `508` do `USED_CODES`, `ride_features` liniový kanál (jako 509/510), návrat `rides`.
- `stats.py`: 508 Narrow ride do symbol-listu + `rides` do agregačních sekcí.
- `batch.py`: `rides="off"` obě větve (čerstvá lekce B1 Sez. 35 — call-sites!).

### Verify
- syntax OK (py_compile ×5); **proc baseline = 65 objektů drží** (rides off → žádná rides sekce v meta,
  čistá zpětná kompatibilita).
- **průseky napříč lokalitami:** SV 46 (= probe) / NL 119 / LS 20 / HS 16 / NV 44 — všech 5 přegenerováno.
- vizuál (paměť `always-show-visual-output`): výřez SV → černá čárkovaná 508 (dlouhé čárky/malé mezery),
  terénní linie lesem přes vrstevnice, odlišná od cest. ✓
- STATISTICS regenerováno (řádek 508: 46/119/20/16/44).

### Propagace (checklist Sez. 34, vč. nového bodu „CALL-SITES" z Sez. 35)
architecture.md UC2, spec §4.9i (nová pod-sekce), katalog ◐→✓ (+ řádek HOTOVO), oba sub-READMEs,
hlavní README (anglický status dorovnán k dnešku — chyběly i skály 30 / mosty 31-33), STATISTICS, batch.py.

### Kudos / Censure
- **Kudos (já)** — foundations-before-curtains: spec 508 + probe layer ID/atributy PŘED kódem
  (`isom-spec-before-render`), runnability rozhodnutí odvozeno z principu „nevymýšlet co v datech není"
  (ne ad-hoc). Žádné slepé iterace.
- **Nález (drobnost)** — `GLOSSARY.md` v repu **neexistuje**, ač ho CLAUDE.md i propagační checklist
  Sez. 34 zmiňují. Buď doplnit (založen Sez. 9 — možná smazán), nebo škrtnout z checklistu.

### Příště (Sez. 37)
1. **Bodové prvky** kříž/boží muka **530** + mohyla **526** + věž **524** (bodové symboly jako 109/110/111;
   probe na atributy → mapování). Pokračování katalogu.
2. **`Zeď` → 513 Wall** (liniová; crossability ověřit v datech).
3. *(drobnost z dneška)* vyřešit chybějící `GLOSSARY.md` (doplnit / škrtnout z checklistu).
4. *(carry-over)* A1 monolit `generator.py`; kapitalizace `DEV_LOCATIONS`; README readability (částečně dorovnáno dnes).

---

## Sezení 37 — Georef výstupu (rgb.pgw + meta) + strojové porovnání s živou mapou

Fokus mimo „Příště" Sez. 36 — nápad uživatele: vzít reálné georeferencované mapy z `resources/`
a porovnat je s generátorovým výstupem téže lokality. Předtucha uživatele („možná to nepůjde,
změna grivace") se ukázala jako přesný zásah.

### Diskuse / %THINK — grivace jako jádro
- **World file (`.pgw`) reálných map nese rotaci = grivaci.** Dekódování rotačních členů: Soví vrch
  **−11,40°**, Blatná −11,9°, Slovanka −3,8° (UTM u 15° poledníku → malá konvergence). Ověřeno proti
  `.omap` atributům `grivation` — sedí na desetinu stupně (SV 11,4 / Blatná 11,9 / Slovanka 3,75).
  **Grivace = konvergence poledníků + magnetická deklinace.** OB mapa je magnetic-north-up, georef
  do S-JTSK nese rotaci.
- **Generátor je grid-north-up, BEZ grivace** (`<projected_crs id="Local"/>`, neemituje `.pgw`).
  Dva framingy verify: (A) pixel-overlay → reálná nakloněná o ~11°; (B) georef-overlay → rotace
  dorovnaná, odhalí *obsahovou* shodu prvků. (B) je ten cenný — vyžaduje, aby gen emitoval georef.

### Kód 1 — emit `rgb.pgw` + georef do `meta.json` (enabler)
- `generator.py`: 3 helpery u `_write_contours_geojson` (SLAP, output writers): `_world_file_coeffs`
  (mapování pixel→S-JTSK je čistý scale+translate → rotační členy 0, +0,5 px na střed UL pixelu),
  `_write_world_file` (6 řádků A,D,B,E,C,F; jen reálný terén), `_georef_meta` (real → S-JTSK bbox +
  `pixel_size_m` + `world_file` + `north:"grid"` + `grivation_deg:null`; noise → `local_m`).
- Georef injektován **do volajícího, ne přes `_build_meta`** (ten má 26 parametrů, A1 monolit —
  nezhoršovat smell). Kotva pro budoucí `--grivation` (`north`/`grivation_deg`).
- Verify: real `.pgw` ručně ověřen (C = xmin+½A, F = ymax+½E, B=D=0, pixel 2,181 m/px); noise =
  žádný `.pgw` + georef `local_m`; **proc baseline 65 drží**. Všech 5 lokalit přegenerováno (mají `.pgw`).
- Reálná mapa Soví vrch CELÁ uvnitř gen bboxu → overlay proveditelný. Vizuální overlay (backward-warp
  reálné mapy do gen mřížky): grivace viditelná (nakloněný obdélník), georef sedí, vrstevnice z DMR
  tvarově kopírují terénní mapování.

### Kód 2 — `compare_real_vs_gen.py` (probe, strojové porovnání)
Dvě statistiky, dependency-light (numpy+PIL+XML; scipy/shapely v prostředí nejsou):
- **STAT 1 — symbolový crosswalk + pokrytí.** **Headline nález: reálná Soví vrch je v ISOM 2000
  číslování, generátor v ISOM 2017-2** → naivní kód-na-kód selže (526=budova vs 521; 508=nevýrazná
  pěšina vs náš průsek; 509=průsek vs naše železnice; 112/113/115 vs 109/110/111). Crosswalk přes
  SÉMANTIKU: z 11 schopností gen má 9 protějšek v mapě (železnice/vedení v lokalitě nejsou). Mezery
  (co mapař kreslí a my ne): **vegetace 262 obj / 70 ha** (UC5), detail skal/srázů 142, ploty/zdi 31
  (= plán 513), bodové umělé 22 (= Sez. 36 Příště 524/526/530), vodní body 8.
- **STAT 2 — prostorová shoda po ISOM barvách** (forward-map reálné mapy do gen mřížky, tol ~4 m):
  hnědá (vrstevnice) **precision 84 % / recall 66 %** — DMR sedí na terénní mapování; voda 71/40 %,
  černá 81/49 % (placement OK, recall půlka — míjíme příkopy/ploty/dots); zelená+žlutá gen 0 %
  (vegetační mezera ~63 % barevné plochy). 2 bugy v probe opraveny při verify: **int16 overflow**
  v klasifikaci barev + ztráta tenkých linií nearest-vzorkováním → **forward-mapování**.

### Závěr
Co umíme z tvrdých dat (vrstevnice/cesty/voda/budovy), **umisťujeme správně a nevymýšlíme si**
(vysoká precision). Doloženo nezávislým zdrojem (GPS terénní mapování uživatele) i metodou (dvojí
`.pgw` georef). Placement ≠ volba symbolu (neověřeno vektor-na-vektor) — recall mezery jsou reálné.
**Nový strategický nález: ISOM 2000↔2017 domain gap** (→ IDEAS).

### Kudos / Censure
- **Kudos (uživatel)** — předtucha „změna grivace" byla přesná diagnóza ještě před měřením; nápad
  porovnat se živou mapou odhalil ISOM2000↔2017 posun (důležitější než další symbol).
- **Kudos (já)** — verify-against-source zlom: world-file rotace ↔ `.omap` grivace na desetinu stupně;
  oba bugy v probe chyceny vlastním verify (overflow + tenké linie), ne až uživatelem.
- **Nález** — `GLOSSARY.md` PŘECE existuje (root `./GLOSSARY.md`), Sez. 36 kontrolovalo špatný path
  (hledalo v `docs/`). Bod „doplnit/škrtnout" → redukováno na „sjednotit, kam checklist odkazuje".

### Příště (Sez. 38)
1. **ISOM 2000↔2017 domain gap** (%THINK z IDEAS) — je cílová doména ISOM2000? Crosswalk vrstva /
   ISOM2000 výstupní režim? Potenciálně důležitější než další symboly.
2. **Bodové prvky 530/526/524** (kříž/mohyla/věž — Sez. 36 Příště) — pozor na kolizi čísel s ISOM2000.
3. *(carry-over)* `Zeď` → 513 Wall; A1 monolit `generator.py`; kapitalizace `DEV_LOCATIONS`.

---

## Sezení 38 — %THINK ISOM 2000↔2017-2 domain gap → deklarace verze ve výstupu (ochrana proti záměně)

Fokus z „Příště" Sez. 37 bod 1 (strategický [!] nález). Čistý %THINK přerostl v drobný bezpečný kód
(deklarace verze) + dvě cenné reference do KB. Vedeno oponenturou uživatele — dvakrát mě zastavila
před chybou.

### %THINK — postupné odlupování (každý krok ověřen, ne hádán)
- **Q: „Nemícháš sprint s klasikou?"** (uživatel) → **ne**, ale trefil sousední past. Empirie použitých
  objektů v `resources/` (6 reálných map, parse `.omap` symbol→object): **4/6 jsou ISOM 2000** (526
  Building, který v 2017-2 neexistuje). Sprint to není (526 je lesní 2000; ISSprOM má budovu 521).
- **Censure! (já / Sez. 37):** detekce verze byla **kontaminovaná** — bral jsem `521 = budova(2017)`,
  jenže v ISOM 2000 je **521 = High stone wall** (ověřeno ze staženého ISOM 2000 PDF spec). Stejně tak
  `112/113` NEjsou exkluzivní 2000 marker (v 2017-2 = Pit/Broken ground) → falešný signál „všechny mapy
  míchají". **Jediný tvrdý diskriminátor verze = `526` Building.** Přesně past z paměti `isom-spec-before-render`.
- **Co Sez. 37 trefilo správně** (ověřeno ze spec 2000): posun `Narrow ride 509→508`, `Railway 515→509`.
- **Pravá osa rozhodnutí** (destilát): rozdíl 2000↔2017-2 je v ČÍSLECH (→ řešitelné crosswalkem,
  triviálně) **nebo** ve VIZUÁLNÍM vzhledu renderu (→ gap, který crosswalk neřeší)? Číslo je jen
  přemapovatelný atribut `.omap`; model UC5 čte pixely.

### Nález, který změnil ekonomiku — crosswalk existuje hotový
- **OOM repo má `ISOM2000-ISOM 2017-2.crt`** (168 ř., GPL, autor Kai Pastor = hl. vývojář OOM) =
  autoritativní cross-reference table. Formát `<kód 2017-2>  <kód 2000>` (ověřeno na 3 jistých párech:
  Building 521↔526, Narrow ride 508↔509, Railway 509↔515) — **nezávisle potvrdil můj ruční crosswalk
  ze spec** (dvojí verify-against-source). → sémantický crosswalk **nevyrábíme, přebíráme**.
- **Postřeh uživatele:** „proč nový OOM nemá poslední ISOM? … ale má Nahrát symboly ze souboru." →
  jádro: symbol set = **vyměnitelný `.omap` soubor**, ne verze SW. Aktuální OOM ISOM 2017-2 **má**
  (ne ale poslední průběžnou revizi Rev 6 — vývoj OOM zpomalil ~2021 → patrně proč je tolik korpusu
  v ISOM 2000).

### Rozhodnutí + Kód — deklarace verze (volba uživatele: „zůstat 2017-2 + deklarovat verzi")
- **Template NEvyměněn za cizí soubor** — verify dokázal, že náš `template_classic.omap` je **100%
  geometricky identický s oficiálním OOM ISOM 2017-2 (1:10000) setem** (všechny `line_width` sedí;
  stažený 15000 set lišil rozměry konzistentně ×1,5 = jen jiné měřítko). Výměna by **rozbila injekci**
  (`<objects count="0">` má cizí set jinak) a zmenšila symboly 1,5×. „Postavit na oficiální set" =
  fakticky už splněno. Lekce CLAUDE.md „měň jen s důkazem" → důkaz mluvil PROTI výměně.
- **„Aktualizace template" provedena bezpečně = deklarace verze** (3 aditivní změny):
  - `generator.py`: helper `_isom_meta()` (izomorfní s `_georef_meta`, injektován mimo `_build_meta`
    kvůli A1) → `meta["isom"] = {version:"2017-2", scale:10000, symbol_set}`.
  - `template_classic.omap` `<notes>`: deklarace verze + **explicitní varování** před číselným konfliktem
    (521=Building ne High stone wall; 509→508; 515→509) + odkaz na crosswalk `.crt`. **Dědí se do
    každého výstupního `.omap`.**
- **Ochrana:** každý náš výstup se sám hlásí k verzi (`.omap` Map Notes pro člověka/OOM, `meta.json`
  pro UC5 pipeline) → konflikt „hádání verze z holých čísel" je u NAŠICH map eliminován.

### Reference do KB (CLAUDE.md: KB je živá, nese licenci)
- `docs/kb/ISOM2000-ISOM 2017-2.crt` (GPL, OpenOrienteering) + `docs/kb/isom-2000-spec.pdf` (IOF,
  withdrawn — archiv ELTE `lazarus.elte.hu/mc/specs/`). Trvalá reference pro pokračování strategie.

### Verify
- proc baseline **65 objektů drží** (aditivní změna nic nerozbila); noise větev má taky `isom` blok.
- `isom` blok 2017-2/10000 ve **všech 5** lokalitách; `map.omap` notes zděděno (SV ověřeno).
- 5 lokalit přegenerováno, počty drží (SV průseky 46 / NL 119 / LS 20 / HS 16 / NV 44 = Sez. 36).
- **Vizuál záměrně beze změny** — deklarace je textová (notes+meta, ukázáno jako text); pixely renderu
  identické (geometrie symbolů se nehnula) → není co okem verifikovat.

### Kudos / Censure
- **Kudos (uživatel) ×2** — (a) oponentura „nemícháš sprint?" odhalila kontaminovaný marker 521
  (lepší teď než v kódu crosswalku); (b) postřeh „Nahrát symboly ze souboru" + tlak na verzi vedl
  k nálezu hotového `.crt` crosswalku a k bezpečnému řešení (deklarace > riskantní výměna template).
- **Censure (já)** — Sez. 37 klasifikovalo verzi podle čísel bez ověření ze spec (proti `isom-spec-before-render`):
  `521` prohlášeno za budovu, ač je v ISOM 2000 zeď.
- **Kudos (já)** — před zásahem do template proveden tvrdý verify (náš == oficiální 1:10000),
  který zabránil zbytečné a rozbíjející výměně.

### Příště (Sez. 39)
1. **Vizuál vs čísla** (otevřená pravá otázka %THINK) — liší se 2000↔2017-2 i graficky (vzhled budov/cest/
   tečkování), nebo jen čísly? Empiricky: `resources/` (2000) vs `template_classic` (2017-2). Rozhodne,
   zda domain gap u verze končí (crosswalk stačí), nebo je i vizuální.
2. **Bodové prvky 530/526/524** (kříž/mohyla/věž — pozor kolize čísel s 2000; teď máme `.crt` na ověření).
3. *(carry-over)* `Zeď` → 513 Wall; A1 monolit `generator.py`; kapitalizace `DEV_LOCATIONS`.

---

## Sezení 39 — Reorganizace kořene: sandbox → generator/ + maps/ + rename generate_map

Fokus mimo „Příště" Sez. 38 — uživatel dal 3 možnosti (1 přeorganizace / 2 plochy ZABAGED / 3 lepší
polygonizace skal) a nechal výběr na mně. **Zvolena (1)** přes foundations-before-curtains: když nesedí
uspořádání, je to základ pod vším — montovat další vrstvy (2/3) na strukturu, kterou stejně měníme, =
dvojí práce. Výtky k (2) a (3) uloženy do IDEAS (vegetace gate u lesa/pastviny; skály proti Sez. 30 raw).

### %BEGIN
Git up-to-date (0/0), čistý. Cadence: %AUDIT:CODE Sez. 35 (3 sez, malé aditivní změny ≪500 LOC),
DOCS/pruning/CALIBRATE od Sez. 34 (4 sez) — **žádný práh překročen**. Stale Příště: nejdéle táhnuté
carry-over (A1 monolit, Zeď 513, bodové prvky) max ~3× — žádný alert.

### %THINK — 3 dílčí rozhodnutí (uživatel)
- **D1 — zrušit `sandbox/` úplně** (A1). Měl jediného obyvatele (`generator-poc`, 2600+ LOC / 24 vrstev =
  dávno ne „PoC"). Izomorfní s `connectors/` (vytaženo Sez. 16). YAGNI: nový experiment se založí ad-hoc.
- **D2 — `generate_map()`** (A2, ne holé `generate()` — „třeba budeme generovat i něco jiného", deštník).
  ⚠ Reverzuje vědomé přejmenování Sez. 23/25 (`generate()`→`synthesize_pseudorealistic_map`). Per
  `stale-todo-verify-rationale` jsem ověřil, že důvod padl: funkce je dnes jediný vstup pro OBĚ větve
  (real i noise přes `terrain=`) → „pseudorealistic" v názvu nepřesné; noise „na zánik" → kolize
  `generate`↔`procedural` teoretická. Podmínka: „pseudorealistic" zůstává v GLOSSARY (vlastnost výstupu).
- **D3 — `maps/<lokalita>/` kotvené v kořeni LAB** (A3, přes `__file__`, ne cwd; default i pro noise).
  Izomorfní: `resources/` (reálné mapy dovnitř) ↔ `maps/` (generované ven).

### Kód (refaktor — 4 fáze)
1. **`git mv`** 9 souborů `sandbox/generator-poc/` → `generator/` (historie zachována, status `R`/`RM`);
   `git rm sandbox/README.md`; fyzicky smazán zbytek `sandbox/` (`.venv`/cache/výstupy = gitignored).
2. **Cesty + rename:** `_REPO_ROOT` jako SSoT (DRY → connectors/asset/maps); cesty o úroveň výš
   (`parent.parent.parent`→`.parent.parent`, `parents[2]`→`[1]`); `generate_map(out_dir=None→maps/output)`;
   `MAPS_DIR`; main/batch/stats/compare kotví výstup pod `maps/`. Rezidua jména v komentářích/docstringu
   propsána (paměť `slap-symbol-rewrite-comments`, grep = 0 nezáměrných).
3. **`.gitignore`** zjednodušen: ~18 ř. (per-lokalita výčet + `sandbox/**`) → jediné `maps/` + DRY cache
   (bez leading-slash matchuje connectors/.X_cache). **`.venv` v kořeni LAB** (sdílený generator+connectors;
   Python 3.12, opraven rozpor README „venv v kořeni").
4. **Docs propagace** 14 živých `.md` + sloučený `generator/README.md` (zrušen PoC framing); diáře/DONE
   ponechány (historie). GLOSSARY/IDEAS/README: poznámka o rename + „pseudorealistic" jako vlastnost výstupu.

### Verify
- **proc baseline 65 objektů drží** (souhrnný log „.omap objektů 65" = 56 vrstevnic + 7 bodů + 2 cesty);
  6 modulů `py_compile` OK; batch import `generate_map`+`MAPS_DIR` OK.
- **5 lokalit přegenerováno do `maps/`**, počty sedí na historii (SV budovy 1078 / průseky 46, NL průseky
  119, LS budovy 8273 / cesty 3995, HS skály 639, NV průseky 44); STATISTICS.md regenerováno z `maps/`.
- SV `rgb.png` ukázán (paměť `always-show-visual-output`) — render pixelově nezměněn (refaktor).

### Nález — kapitalizace DEV_LOCATIONS (verify chytil chybnou „opravu")
Při fázi 2 jsem „opravil překlep" `Soví Vrch`→`Soví vrch` v `compare_real_vs_gen.py`. CLI běh SV ale
ukázal výstup do `maps/Soví Vrch` (Title-Case) — **`DEV_LOCATIONS` skutečně generuje Title-Case**
(`Soví Vrch`, `Nová Louka`), zatímco `resources/` má `Soví vrch.png` (lowercase). Oprava byla chybná
→ vrácena. Potvrzuje carry-over úkol „kapitalizace `DEV_LOCATIONS`" (nekonzistence je reálná) i paměť
`verify-data-not-assume` (neopravuj domnělý překlep bez ověření proti datům).

### Kudos / Censure
- **Kudos (uživatel)** — „takový přesun je dobrý test správně navržených závislostí": refaktor opravdu
  prověřil sys.path/cesty (3 místa o úroveň výš) a kotvení výstupů; argument `generate_map` jako deštník
  „pro budoucí generátory" je čistší než `generate()`.
- **Kudos (já)** — foundations-before-curtains volba (1) před 2/3; oponentura D2 (ověřen padlý důvod
  Sez. 23/25, ne slepá reverzace); verify chytil vlastní chybnou opravu velikosti před commitem.
- **Censure (já)** — chybná „oprava překlepu" `Soví Vrch`→`Soví vrch` bez ověření proti `DEV_LOCATIONS`
  (proti `verify-data-not-assume`); naštěstí zachyceno CLI verify, ne až uživatelem.

### Příště (Sez. 40)
1. **Kapitalizace `DEV_LOCATIONS`** — sjednotit (Title-Case `Soví Vrch` výstup vs lowercase `Soví vrch`
   v `resources/`); rozhodnout směr (najednou je to konkrétní, ne abstraktní carry-over).
2. **Vizuál vs čísla** ISOM 2000↔2017-2 (carry-over Sez. 38 — pořád otevřená pravá %THINK otázka).
3. **Bodové prvky 530/526/524** (kříž/mohyla/věž — pozor kolize čísel s 2000, `.crt` na ověření).
4. *(carry-over)* `Zeď` → 513 Wall; A1 monolit `generator.py` (refaktor „až bolí" — dnes přesun složky,
   ne rozdělení modulu).

---

## Sezení 40 — Kapitalizace DEV_LOCATIONS + %THINK „vizuál vs čísla" ISOM 2000↔2017-2 (verzní gap zavřen)

### %BEGIN
Git čistý, `HEAD == origin/main` (`373ba55`), 0/0. **Cadence:** `%AUDIT:CODE` Sez. 35 (5 sez, **net +494
LOC** — měřeno `git diff --numstat ddff67f HEAD`, těsně pod prahem 500), DOCS/pruning/CALIBRATE od Sez. 34
(6 sez) — žádný práh překročen o ≥2. ⚠ Pozn.: CODE je 494/500, příští kódové sezení ho překlopí. **Stale
Příště:** A1 monolit `generator.py` v „Příště" Sez. 35→39 = **5 sezení → překročen práh ≥5** → návrh DROP.
**Fokus** (volba uživatele): (1) kapitalizace `DEV_LOCATIONS`, pak (2) %THINK vizuál vs čísla. A1 monolit → DROP.

### Kód — kapitalizace `DEV_LOCATIONS` (foundations úklid, carry-over Sez. 39)
Verify-against-source PŘED opravou (proti `verify-data-not-assume` + Sez. 39 chybná „oprava"): zmapoval jsem
**dvojí** nekonzistenci — SV vs `resources/`, ALE i uvnitř `DEV_LOCATIONS` (`Lidové sady` sentence-case vs ostatní
Title-Case). **Hypotéza:** není to chaos, ale **český pravopis pomístních jmen** (sídlo obě velká vs terénní útvar
druhé malé). Předložena uživateli (autorita na názvy, OB kartograf) přes `AskUserQuestion` → **volba A: opravit jen
`Soví Vrch`→`Soví vrch`** (vrch = útvar; `Nová Louka`/`Lidové sady`/`Hrubá Skála` = sídla/čtvrti/obce, správně).
- 3 SSoT: `generator.py DEV_LOCATIONS`, `stats.py LOCATIONS`, `compare_real_vs_gen.py` (GEN_DIR + **zrušen
  3řádkový komentář o nesouladu** — nesoulad zmizel). `resources/` ponecháno (vnější daná jména reálných map).
- Windows NTFS case-insensitive: stará `maps/Soví Vrch/` smazána (gitignored, regenerovatelná) → čistá `maps/Soví vrch/`.

### Verify (kapitalizace)
`py_compile` 3× OK; SV přegenerováno (počty sedí na historii — budovy 1078 / řopíky 70 / průseky 46 / vrstevnice
462 / skály 253 / `.omap` 3502); složka na disku = `Soví vrch`; **`compare` najde obě složky přirozeně bez hacku**
(STAT 1 crosswalk vyjíždí); STATISTICS regen (`Soví vrch/` ř. 8+49); render ukázán (`always-show-visual-output`),
pixelově nezměněn.

### %THINK — „vizuál vs čísla" ISOM 2000↔2017-2 (uzavírá pravou otázku Sez. 38/39)
Verify-against-source nejdřív (přečten crosswalk `.crt` 168 ř. + KB `isom-issprom.md`):
- **Reframe — otázka se rozpadá podle cesty dat:** **vektor** (`.omap` symbol ID → verzi nese ČÍSLO → crosswalk
  `.crt` řeší 1:1) vs **rastr** (pixely → verzi nese VZHLED → čísla úplně irelevantní). **UC5 model čte pixely** →
  pro náš hlavní směr je relevantní JEN vizuální osa; číselná je vyřešená (vektor) nebo bezpředmětná (rastr).
- **Crosswalk jako proxy:** drtivá většina 1:1 posun čísla (sada strukturálně táž); **vrstevnice `101/102/103`
  = identické číslo v obou** = silný signál stabilního vzhledu hnědé kostry.
- **Vizuální sonda** (uživatel: „postav výřezy vedle sebe"): jednorázový skript `temp/build_isom_compare.py` —
  georef **backward-warp** reálné ISOM 2000 Soví vrch (sken 0,095 m/px, grivace −11,4°) × náš 2017-2 render téže
  S-JTSK oblasti, OBĚ grid-north + shodné měřítko (600×450 m okno @ 0,5 m/px) → montáž vedle sebe. **Kartografův
  verdikt: „vše důležité v obou setech, snadno transformovatelné".** Vrstevnice stylově shodné; dominantní
  vizuální rozdíl = **obsahový** (reálná má vegetaci žlutá/zelená, gen bílá = recall gap Sez. 37), NE verzní.

### Rozhodnutí
**Verzní domain gap 2000↔2017-2 ZAVŘEN.** Generátor zůstává **2017-2 + deklarace verze (Sez. 38) + crosswalk pro
vektorovou cestu**; **NEgenerovat zvlášť 2000 variantu** (nemělo by smysl — vizuál transformovatelný). Otevřený
zůstává jen **obsahový (vegetační) gap** = UC5 predikce za zavřenou vegetace gate (jiná osa). Propsáno: KB
`isom-issprom.md` (závěr + oprava driftu názvu `.crt`), IDEAS (→ DONE), TODO ([~] blok zavřen).

### Kudos / Censure
- **Kudos (já)** — kapitalizace: verify-against-source před opravou (zmapoval dvojí nekonzistenci, nehádal směr),
  KISS závěr „jeden překlep ne plošný chaos" opřený o pravopis; rozhodnutí směru nechháno na uživatelově autoritě
  (`AskUserQuestion`) místo domnělé „opravy" — poučení ze Sez. 39 Censure (`verify-data-not-assume`) aplikováno.
- **Kudos (uživatel)** — rychlý jasný kartografický verdikt nad vizuální sondou uzavřel strategickou otázku, která
  visela od Sez. 37; volba „postav výřezy" byla správná (lidská sonda levnější než strojový template-diff a stačila).
- **Proces** — zahltil jsem uživatele dlouhým %THINK výstupem („Co se po mně chce?") → zkráceno na 2 konkrétní
  otázky. Lekce: %THINK závěr destilovat na rozhodovací osy, ne vršit analýzu.

### Příště (Sez. 41)
1. **Bodové prvky 530/526/524** (kříž/mohyla/věž — bodové symboly jako 109/110/111; probe atributů → mapování;
   pozor kolize čísel s ISOM 2000, `.crt` na ověření). **Spustí `%AUDIT:CODE`** (CODE je na 494/500 LOC).
2. *(carry-over)* `Zeď` → 513 Wall (liniová; crossability ověřit v datech).
3. *(strategie, IDEAS)* plochy ZABAGED → ISOM (projekce hřbitov/hřiště vs vegetační dohad les/pastvina za gate)
   — vlastní %THINK před kódem.

---

## Sezení 41 — %AUDIT:CODE + plošný pokryv (surfaces) → mapa dostala barvy

Fokus zvolen z „Příště Sez. 41": nejdřív `%AUDIT:CODE` (LOC práh padl), pak — místo bodových prvků 530/526/524 —
**plochy ZABAGED** (bod 3, eskalováno uživatelem „konečně barvy"). Bodové prvky odsunuty.

### Diskuse
- **%BEGIN:** git up-to-date (HEAD==origin/main), strom čistý. Cadence: jen `%AUDIT:CODE` zralé (LOC 494/500 →
  dnešní kód ho přetne). Stale Příště: nic ≥5. A1 monolit sledován jako podmínka (DROP z Příště Sez. 40).
- **Epizoda „další tool na mrkla"** — uživatel zmínil tool v AzimutLab mimo git. Verify: na hejna nic (ani
  tracked, ani untracked). Vyjasněno = `lasertool/` (externí GPL binárka, vědomě negitovaná) — **plně zachyceno**
  `.gitignore:25` + `tools-models.md:15` + GLOSSARY. Dokumentace fungovala; já ji v %BEGIN nepročetl proaktivně
  (poučení na mé straně). Žádná akce.
- **%THINK plochy → probe (foundations):** verify-against-source dvojí — (a) inventář land-cover ZABAGED (?f=json
  ID + count na 5 lokalit; REST jména s mezerami/čárkami/diakritikou), (b) render struktura z template (plná výplň
  401/403/520 = **tutovka**; pattern 412/413/402 = práce). Dělicí čára: **projekce vs gate** — umělé/kultivované
  plochy mapovatelné deterministicky, vegetace (les→zelená hustník) za gate (UC5). Open land ale NENÍ fatální gate
  (druh je v datech, nejistý jen odstín 401/403).
- **Konzistenční past:** sad 167/703 prvků — když nakreslím louku žlutě a sad bílý (pattern=práce), vznikne matoucí
  mapa. → rozsah je o vyřešení pasti, ne „co je tutovka".

### Rozhodnutí
- **%AUDIT:CODE — opravit vše** (D1–D3, K1–K3; viz DONE). Nález D1: **crt oprava Sez. 40 byla NEúplná** (mezera
  přežila v kódu+template+docs) — typ `slap-symbol-rewrite-comments` (grep-ověřit reziduum). A1 nepovýšen.
- **Plošný pokryv — volba uživatele „open land jako jedna žlutá" (KISS):** louka/park/pole/sad → 401 žlutá;
  hřbitov → 520 olivová; parkoviště → 501 (`--paved`). ISOM-věrné pole 412 / sad 413 (pattern) = vědomá druhá vlna.
  Skupina C (vegetace zelená) NESAHAT (gate, UC5).
- **Z-order: surfaces ÚPLNĚ VESPOD** (podklad pod vrstevnicemi). Meta injekce mimo `_build_meta` (A1, precedent
  Sez. 37/38). `C_OLIVE` aproximace → doladit okem (příští téma „Olivová").

### Kód
- **Audit (8 souborů):** generator.py (crt, footbridge docstring, K2 `_nearest_segment_tangent`→wrapper),
  zabaged.py (202 drift ×2), stats.py, template_classic.omap (crt `<notes>`), GLOSSARY, IDEAS.
- **Surfaces (feature, 8 souborů, izomorfní s plošným kanálem):** zabaged.py (6 layer ID, `OPEN_LAND_LAYERS`/
  `CEMETERY_LAYERS`, `fetch_open_land`/`fetch_cemeteries`, `map_open_land_to_isom`→401 / `map_cemetery_to_isom`→520,
  parkoviště do `PAVED_AREA_LAYERS`), palette.py (`C_OLIVE`), generator.py (`--surfaces`, `_generate_real_surfaces`,
  `_draw_surface_area` outline=None, z-order vespod, `mask_surfaces.png`, meta injekce), omap_export.py (401/520 do
  USED_CODES+AREA_CODES + kanál), batch.py (off obě větve, B1), stats.py (401/520 + sekce). compare_real_vs_gen.py
  (GEN_CAPABILITIES/CROSSWALK/NOTE — „gen nemá" už neplatí).
- **Soubory promíchané audit+surfaces** (generator/zabaged/stats) → per-hunk split nelze (git interaktivní
  nedostupný) → 2 commity místo 3: kód souhrnně + docs.

### Verify
- `py_compile` OK; **proc baseline 65 drží** (regrese, surfaces off čistě vynechán); K2 jednotkově (ekvivalence +
  None-okraj). Real smoke SV 269 ploch, vizuál OK. 5 lokalit regen (pokryv SV 269 / NL 34 / LS 1105 / HS 365 /
  NV 103, sedí na probe), STATISTICS regen.
- **`compare_real_vs_gen` SV: otevřený prostor gen 0 % → 35.8 %** (real 34.7 %) — zaplnil recall mezeru Sez. 37;
  precision/recall ~55 % (projekce ≠ ruční generalizace). Zelená (hustník) 0 % = vegetace gate (UC5), čekané.

### Kudos / Censure
- **Kudos uživatel** — „Test OK" (vizuální verify barev); jasná volba rozsahu (KISS open land) zkrátila %THINK;
  navedení na plochy (bod 3) = velký vizuální skok z tvrdých dat.
- **Kudos uživatel** — disciplína „real data first" → probe před kódem (foundations); žádné zacyklení (kontrast
  Sez. 31/32).
- **Censure já** — v %BEGIN jsem neznal `lasertool` (nepročetl `.gitignore`/`tools-models.md` proaktivně) →
  zbytečné tápání. Dokumentace existovala.

### Příště (Sez. 42)
1. **Olivová** — doladit `C_OLIVE` (dnes aproximace `(150,158,56)`) proti reálné mapě / ISOM spec; crop na hřbitov
   (SV/LS/HS) pro ověření odlišení od žluté.
2. *(druhá vlna pokryvu)* pole → **412 Cultivated** (žlutá + černý vzor) / sad → **413 Orchard** (žlutá + zelené
   tečky) — pattern render, rozliší dnešní KISS „jedna žlutá".
3. *(carry-over)* **Bodové prvky 530/526/524** (kříž/mohyla/věž) + `Zeď` → 513 Wall (odsunuto ze Sez. 41).

---

## Sezení 42 — Olivová 520 z katastru RÚIAN + areály účelové zástavby + audit land-cover

Fokus z „Příště Sez. 41" (Olivová) se rozvinul jinam: uživatel nadhodil, že olivovou (zákaz vstupu)
mapař maluje na **soukromé pozemky u domů** — to nejde z ZABAGED, ale z **katastru**. Sezení se pak
testem na LS rozrostlo o areály účelové zástavby (114) a systematický audit land-cover pokrytí.

### %BEGIN
- Git up-to-date (HEAD==origin/main), strom čistý. Cadence: žádný práh překročen (CODE Sez. 41 čerstvé,
  DOCS/pruning/CALIBRATE od Sez. 34 → 8 sez < prahy). Stale Příště: čisté (Olivová nová Sez. 41→42).

### %THINK — olivová 520 z parcelních dat (foundations před kódem)
- **Reframe „za hranou real fáze"?** Ne — privátní pozemek je **deterministicky odvoditelný z katastru** =
  projekce, ne predikce (osa Sez. 38/40). Není to vegetace gate. Ale **nový datový zdroj** (katastr ≠ topografie
  ZABAGED) → foundations krok (nový konektor + vlastní licence, KB-pravidlo).
- **Pravidlo jako mapař:** český katastr má u parcel **druh pozemku**; zahrada + zastavěná plocha a nádvoří =
  privátní pozemek u domu. Proximita k zástavbě nesena IMPLICITNĚ druhem (pole/louka daleko → jiný druh → žlutá).
- **Probe (verify-against-source, read-only):** RÚIAN MapServer na témže `ags.cuzk.gov.cz` jako ZABAGED, vrstva 5
  `Parcela`, pole `druhpozemkukod` (**codedValue doména přímo ze serveru** — nehádáno: 2 orná/5 zahrada/6 sad/
  7 TTP/10 les/11 voda/13 zastavěná/14 ostatní), `f=geojson` (izomorfní), maxRec 1 000 000 (žádný strop). Licence:
  veřejná open data zák. 111/2009 Sb., bezúplatně. SV: 649 zahrad + 1212 zastavěných. Geometrie reálně přišla.

### Rozhodnutí (3 designová témata, uživatel)
- **Modul:** `ruian.py` sourozenec (katastr ≠ topografie). **Překryv se ZABAGED 135 zahrada:** z-order — olivová
  NAD žlutou (privátní zahrada přemaže žlutý sad). **520 = sjednocení** hřbitov ∪ privátní ∪ (později) areály.
- **Fetch helper (jediná genuine volba — DRY × „negeneralizuj preventivně"):** uživatel zvolil **DRY** → zobecnit
  do sdíleného `arcgis.py`.
- **Test LS — mapování 114:** rozlišit podle `typzast_p` (areály→520, asfalt→501). Rozsah: 114 + drobné stavby 105.

### Kód
- **`arcgis.py`** (nový) — sdílený ArcGIS REST transport: `fetch_geojson_layer(server, layer_id, …)` + geom parsery.
  `zabaged._fetch_layer` + parsery → tenké wrappery (cache-key **zachován** → cache se neinvalidovala). DRY se `ruian`.
- **`ruian.py`** (nový sourozenec) — `fetch_private_land` (`where druhpozemkukod IN (5,13)`) + `map_private_land_to_isom`
  → 520; číselník `DRUH_POZEMKU` pro dokumentaci/druhou vlnu.
- **`generator.py`** — třetí zdroj do `_generate_real_surfaces` (z-order olivová po žluté); `ISOM_CEMETERY`→
  `ISOM_OUT_OF_BOUNDS` (propsáno všude, grep 0 reziduí, paměť `slap-symbol-rewrite-comments`).
- **Jméno mapy** (bod 1 testu) — `out / "map.omap"` → `out / f"{out.name}.omap"` → `Lidové sady.omap`.
- **Areály 114 + kůlny 105** (audit) — `zabaged`: LAYER_IDS 114/105, `UTILITY_AREA_LAYERS` + `PAVED_AREAL_TYPZAST
  {408,409}`, `fetch_utility_areas`, `map_utility_area_to_isom` (asfalt→501 / zbytek→520), `map_building_to_isom`
  DRY přes `BUILDING_AREA_LAYERS` (+ kůlny→521). `generator`: 114 rozděleno dle ISOM kódu — surfaces (520, filtr
  `code in SURFACE_FILL`) + paved (501, filtr `code == ISOM_PAVED`).

### Audit land-cover (test LS, bod uživatele „projdi všechny typy")
- Systematicky **47 plošných ZABAGED vrstev** (MapServer `/layers?f=json` geometryType) vs mapováno. Klíčová
  mezera **114 Areál účelové zástavby** (177/LS, `typzast_p` rozpad: škola 48/garáže 40/hřiště 22/sport 4/stadión/
  kasárna 2/autobus. nádraží 1…) → řeší VŠECHNY 4 testovací nálezy. Drobné: 105 kůlny (850→521), 115 ostatní plocha
  (◐ mezera). Skip: 151 GIA (overlay vlastnictví — IČO/vlastník), 4 CHKO (admin). Vegetace 140/142/144 = vědomě
  bílá (gate, UC5).

### Verify
- `py_compile` celý balík OK; **proc baseline 65 drží**; refaktor behavior-preserving (SV 269 ploch = 267+2, parser
  z `arcgis`). 5 lokalit přegenerováno: pokryv **SV 2140 / NL 174 / LS 20159 / HS 2268 / NV 603** (LS: privátní +
  areály 170 olivová + 7 asfalt; budovy 8273→9123 +850 kůlny); STATISTICS regen; `<lokalita>.omap` ve všech, orphany
  `map.omap` smazány. **Vizuál:** SV — olivová obklopuje budovy u cest (zahrady); LS — centrum souvisle olivové
  s žlutými parky = **přesně test uživatele „střed Liberce olivový s výjimkou parků"**.

### Kudos / Censure
- **Kudos uživatel** — „Super analýza! Go!" (%THINK olivové); „Liberec vypadá báječně, nečekal jsem takový úspěch";
  systematický tah „projdi všechny typy" odhalil 114 = velký vizuální skok.
- **Kudos uživatel (disciplína)** — foundations: probe RÚIAN + audit 47 vrstev PŘED rozhodnutím; žádné hádání
  (číselník druhu pozemku i typzast ze serveru).
- **Censure já** — v %THINK jsem navrhl „doladit `C_OLIVE` okem"; uživatel opravil: **ISOM barvy jsou normované,
  z palety, neladí se okem** („olivové říkají orienťáci zelené"). Paměť `isom-colors-from-palette-not-eye`.

### Příště (Sez. 43)
1. *(nález, „pořešíme")* **Oplocené volné terény** — RÚIAN pravidlo {5,13} chytí ~80 %; oplocené louky/pole u domů
   (druh 2/7) zůstanou žluté, ač za plotem. Možný směr: ZABAGED oplocení/zeď jako indikátor, nebo open-land enklávy
   obklopené olivovou → taky 520. %THINK.
2. *(audit land-cover, drobné mezery)* **115 Ostatní plocha v sídlech** (→501/520?), **zřícenina 103** (→523/521),
   drobné areály (zámek/tribuna/elektrárna/rozvodna…).
3. *(druhá vlna pokryvu)* pole → **412 Cultivated** / sad → **413 Orchard** (pattern render).
4. *(carry-over)* **Bodové prvky 530/526/524** (kříž/mohyla/věž) + `Zeď` → 513 Wall.

---

## Sezení 43 — Systematický audit katalogu: 14 chybějících ZABAGED vrstev → ISOM (zámek/zřícenina/věž/kříž/strom/sráz/zeď/vegetace)

%BEGIN zvolil fokus „bodové prvky + Zeď" (Stale alert ≥7 sez). Hned poté akutní nález uživatele:
**budova domova mládeže (SV/Krompach) chybí na mapě** → spustilo systematický audit celého
transformačního katalogu (volba uživatele „plný balík" + **Censure! potřetí**).

### %BEGIN
- Git up-to-date (HEAD==origin/main), strom čistý. Cadence: žádný práh překročen (CODE Sez. 41 čerstvé;
  DOCS/pruning/CALIBRATE od Sez. 34 → 9 sez < prahy 10/12/15).
- **⚠ Stale Příště ALERT:** bodové prvky 530/526/524 v „Příště" **7 sezení po sobě** (Sez. 37→43) →
  rozhodnuto **DO** (vyřešit provedením, ne potřetí odsunout). Fokus zvolen: bodové prvky + Zeď 513.

### Akutní nález → root cause (verify-against-source)
- Uživatel: „Budova domova mládeže (Krompach) není na mapě, zase nepodchycený typ stavby?" Probe SV:
  budov 1078 (taženo), areály 114 = jen vodojemy/ČOV/tech.služby/**1 areál zámku** → žádné školství.
- **Root cause (potvrzeno daty):** ČÚZK vede **zámek** ve VLASTNÍ vrstvě `Zámek` (id 102), kterou
  netáhneme (jen `Budova_..._plocha_` 99). Domov mládeže = bývalý zámek (potvrdil uživatel). Probe
  historických staveb odhalil DALŠÍ mezery na SV: **8 zřícenin (vč. Milštejna!)**, 1 věžovitá stavba, 5 věží.

### Censure! POTŘETÍ — opakovaný antipattern
- Uživatel: *„Jsme rádi za každé webové podklady, musíme interpretovat VŠECHNA data. Proto máme
  transformační tabulku, aby se nestalo, že něco vypadne. Je to asi potřetí... Já: Chybí tohle. Ty:
  Parkoviště nemapujeme, zámky nemapujeme, kasárna nemapujeme. Zajisti ISOM ekvivalent pro VŠECHNA
  užitečná data."* (parkoviště Sez. 41, areály Sez. 42, zámek/zříceniny Sez. 43). Paměť `geoportal-data-completeness`
  posílena. Lék = data-driven, ne reaktivní.

### Data-driven verify-against-source (jádro sezení)
- **Probe VŠECH 149 vrstev × 5 DEV_LOCATIONS** (`returnCountOnly`, `temp/probe_all_layers.py`) → výskyt
  rozhoduje prioritu, ne odhad od stolu. + probe atributů kandidátů (mapování dle dat).
- **Nalezeno 14 netáhnutých vrstev s ISOM ekvivalentem** (+ chyby konzistence katalogu: tramvaj táhnu od
  Sez. 31 ale řádek ✗; areál duplikát ✓/✗).

### Implementace — 3 dávky (izomorfní s existujícími vrstvami)
- **Dávka 1 — budovové stavby** (do `--buildings`, žádný nový flag): `Zámek`/`Hrad` → 521 (mirror budov);
  `Rozvalina, zřícenina` → **523 Ruin** (čárkovaný obrys bez výplně, 2. třída mask_buildings). Oprava
  bugu: omap export hardcodoval „521" → zřícenina by vypadla jako budova.
- **Dávka 2 — bodové orient. prvky** (nový `--landmarks`, mask_landmarks multi-class): kříž → **530** ring,
  mohyla → **526** cairn, věž/věžovitá stavba/vodojem/silo/těžní/mlýn/motor → **524** (kříž+tečka),
  strom → **417** (zelený kroužek). Nulové vrstvy mapovány pro úplnost (jinde se vyskytnou).
- **Dávka 3 — liniové orient. prvky** (nový `--linefeatures`, mask_linefeatures multi-class): sráz →
  **104 Earth bank** (plná + jednostranné ticky; Σ981 = nejčastější!), zeď/hradba → **513 Wall**,
  liniová vegetace → **416** (zelená čárkovaná). Nová barva `C_GREEN3` do importu.
- Plná integrace každé: zabaged (LAYER_IDS+konstanty+map+fetch) + generator (konstanty+render+gen+
  z-order+maska+meta+omap+CLI+validace) + omap_export + stats + batch (OBĚ větve off, lekce B1).
- **Kótovaný bod (Σ353) — SKIP s odůvodněním:** nese jen `vyska` (virtuální výškopis, ne fyzická
  značka v krajině) → ne ISOM 603. Volba uživatele „virtuální → okomentovat skip".

### Verify
- proc baseline **65 drží** (behavior-preserving). py_compile celý balík OK. 5 lokalit přegenerováno;
  počty sedí na probe: SV orient. 81 (kříž 33/strom 38/věž 6/cairn 4) + liniové 170 (sráz 71/veg 83/zeď 16);
  budovy SV 1179 (+zámek+8 zřícenin), LS 9131. STATISTICS regen (8 nových symbolů). **Vizuál SV:** crop
  domova mládeže = zámek se kreslí jako budova, kříže/zdi/srázy v obci OK.
- Katalog `zabaged-isom-catalog.md` kompletně zrevidován: 14 vrstev ◐/○→✓, konzistence (tramvaj/areál),
  nulové doloženy „0 v 5 výsecích", ✓ count ~27→~40. SSoT „nic užitečného nevypadne".

### Kudos / Censure
- **Censure já (potřetí)** — reaktivní „to nemapujeme" místo systému; transformační tabulka existuje
  právě proto. Napraveno data-driven auditem všech 149 vrstev.
- **Kudos uživatel** — trvání na úplnosti („interpretovat všechna data") = správný princip; konkrétní
  nález (domov mládeže) odhalil celou třídu mezer (historické stavby).

### Příště (Sez. 44)
1. *(akční seznam katalogu ◐)* **bažina 308** (Σ31) + **pramen 312** (Σ65) + **jeskyně 203.2** + **hráz 528**
   + **nádrž 311** + **lom 201** — zbývající kandidáti s výskytem (dávka 4 „vodní/mokřady/terén").
2. *(orientace ticků)* sráz 104 — ticky teď konzistentní pravá normála; věrná orientace na nižší stranu
   svahu chce DMR sklon (TODO).
3. *(carry-over Sez. 43)* **Oplocené volné terény** (RÚIAN {5,13} ~80 %, louky/pole za plotem žluté) — %THINK.
4. *(druhá vlna)* pole → 412 / sad → 413 (pattern); zábrana → 519; tovární komín → 524.
