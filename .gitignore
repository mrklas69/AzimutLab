# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Výstupy / generovaná data (deštníková fáze: zatím nic, ale připraveno)
# Vzor output_* chytá i pojmenované scratch rendery (output_baseline, output_v2, …)
output/
output_*/
sandbox/**/output/
sandbox/**/output_*/
sandbox/**/data/

# Velká binární data (mapy, rastry, LIDAR) — do gitu nepatří, odkaz v KB
*.tif
*.tiff
*.laz
*.las

# Cizí nástroje stažené lokálně (GPL binárky ~12 MB) — odkaz v tools-models.md
lasertool/

# Reálné mapy (páry picture+OMAP) — copyright třetích stran + velká binární data.
# Lokální hold-out / reference, NE do veřejného gitu. Metadata (původ, licence) v KB.
resources/

# Cache stažených DMR 5G dlaždic (reálná ČÚZK data — nepatří do gitu)
.dmr_cache/
sandbox/**/.dmr_cache/

# Claude / IDE lokální
.claude/settings.local.json
.idea/
.vscode/

# OS
Thumbs.db
.DS_Store
