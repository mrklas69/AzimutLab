# Diář 2026-06-07

## Sezení 96 — 210 Stony = bodový gap (ne plošný) + kompas tabulka orig/gen per ISOM kód

Stroj: **HAL3000** (korpus 271 + CUDA). Sync: `main` up-to-date s origin (HEAD `e6c7a3a`), clean.
Cadence: žádný audit zralý (DOCS+pruning Sez. 93/3 zpět, CODE Sez. 89/7 zpět práh 8, CALIBRATE
Sez. 86/10 zpět). Stale check: žádný carry ≥5×. Fokus = bod 1 Příště **210 Stony ground**, přes
optiku measure-first (volba uživatele „A1 go").

### 1. A1 measure-first → 210 NENÍ plošný gap, je BODOVÝ
Než psát DMR-drsnost konektor (analogie `rock_relief` 206), změřena reálná geometrie 210 v 5 mapách:
**VŠECHNY objekty `type=point`** (210.0/210.1 „individual dot"/„jediná tečka"; Slovanka 3473, Velbloud
603, Blatná 340, Soví vrch 193, Bedř 32). Reální kartografové kreslí kamenitou zem jako POLE TEČEK, ne
plochu s pattern výplní → **210 patří na Png2Point dluh, ne plošný generátor.** Volba uživatele:
„překlopit na Png2Point". Měření ušetřilo zbytečný konektor.

### 2. Kořen vady — analytický cut byl nadhodnocen (variant-aware oprava)
Proč Sez. 95 zařadil 210 jako plochu: `symbol_geometry()` bral geom z `template_classic.omap` podle
PRIMÁRNÍHO kódu (210 = area „slow running"), ale `isom_usage` kolabuje integer-prefixem → reálně použitá
point varianta se ztratila → cut počítal 210 (975 obj) do plošného stropu. **Oprava:** `compare_isom.
used_geometry(path)` (geom REÁLNĚ použitého symbolu z mapy, majorita přes objekty) → `coverage()` vrací
`used_geom` → `measure_dod` cut měří geom z reálné mapy, ne template. **Pravý plošný strop = 54 %** (z 58).
GAP: plocha 9/894 obj (Png2Area) · linie 19/1974 (Png2Line) · **bod 21/2213 (Png2Point — 210 sem přibyl)**.
Lekce: měř geometrii symbolu z reálné mapy (co kartograf nakreslil), ne template-primary; stejná past
u 104 Earth bank + 104.1, … .

### 3. Kompas tabulka `measure_dod.py --table` (volba uživatele „toto bude náš kompas")
Tři kapitoly (Png2Area/Png2Line/Png2Point), řádky ISOM 2017-2 kódy, sloupce Σ objektů ORIG (reálné
`.omap`) vs GEN (separační) přes 3 mapy, geom z reálné mapy. Nad rámec DoD % (binární): ukazuje PROPORCE
— blížíme se ve správné četnosti, nebo stojíme. **Nálezy:** Png2Point **gen Σ149 / orig 3960 (~4 %)** =
strukturálně nejhorší (204 Boulder 1064/7, 210 975/0, 417/419 stovky/0); gen **PŘESTŘELUJE** husté ČÚZK
(520 Settlement 838/94 = 9×, 521 626/136, cesty 503/504/502 5–6× — projekce na obal výseku přes natočenou
mapu); **PODSTŘELUJE** vegetaci (403 265/1295); **416** veg boundary 1111/0 = největší missing linie.

### 4. Strategie uživatele (4 body) → IDEAS „Pokrytí do statistické míry četnosti + kompas tabulka"
(A) zastřešující: nedetekovatelné ISOM → generovat věrohodně do obvyklé/ruční míry četnosti (cíl Σ z
kompasu; reframe Sez. 79 — predict nemusí být pravdivá); (B) Png2Point trénink injektováním symbolů na
náhodné ZNÁMÉ souřadnice do reálného podkladu (GT zdarma, vyvážené třídy); (C) 416 z hranic separovaných
ploch, ale JEN do statistické míry (reálné hranice vegetace většinou nejasné); (D) pattern 402/404 splývá
do base 401/403 (separace per-pixel slepá) = kontext over-countu, 401/403 split = pokrok. **Oponentura:**
kompas po statistickém fillu potřebuje provenance rozpad (real-derived vs predict-fill), jinak gen sloupec
klame věrnost. Poznámky: posed/vývrat nejsou vlastní ISOM symbol (531/535, resp. 113/419); „631" mimo
mapové 100-599.

### Výsledek
Měřicí + směrový posun (nepřibyl symbol). Tři keepery: `used_geometry` (variant-aware), `--table` kompas,
IDEAS strategie. 210 překlopen z plochy na bod (Png2Point dluh). Generátor NETKNUTÝ (jen `compare_isom.py`/
`measure_dod.py`) → proc baseline drží triviálně. py_compile OK, standalone + `--proxy` ověřeny. Censure 0.

### Příště (Sez. 97, HAL3000)
1. *(pokrytí, PRAVÝ plošný gap)* ZABAGED **310 indistinct marsh / 413 orchard** na --marsh/--surfaces
   (levné, do plošného stropu 54 %). Ověřit geometrii v reálných mapách (413 = „single dot" → bod? měřit!).
2. *(kompas rozšíření)* **provenance sloupec v `--table`** (real-derived vs predict-fill) — bez něj kompas
   po statistickém doplnění klame; meta nese provenance od Sez. 83.
3. *(strukturální, velký)* **Png2Line** (19 typů/1974 obj) / **Png2Point** (21/2213, vč. 210/204/417) —
   největší zbylé gapy; Png2Point cesta = injektování symbolů (IDEAS B).
4. *(model 1b, odsunuto)* class-balanced expansion (208/501/301.1).

## Sezení 95 — Analytický cut (plošný strop 58 %) + DoD baseline přepnut na separaci (pravdivá produkční cesta)

Stroj: **HAL3000** (korpus 268 + CUDA). Sync: `main` up-to-date s origin (HEAD `368595a`), clean.
Cadence: žádný audit zralý (DOCS+pruning Sez. 93, CODE Sez. 89/5 zpět, CALIBRATE Sez. 86/8 zpět) →
hlavní tah. Uživatel ráno „ztrácím se" → nejdřív připomenuta mapa projektu (UC DAG, smyčka
generator()↔reconstructor(), pokrytí = strop). Fokus = bod 1 Příště: **rozšíření pokrytí**, ale
přes optiku measure-first.

### 1. Analytický cut — strop plošné fáze (volba uživatele „změřit strop nejdřív")
Oponoval jsem „rovnou kreslit pattern plochy": než rozšiřuju plochy, musím vědět, **kam až plošná
cesta sahá**. `compare_isom.symbol_geometry()` (geometrie z OOM `<symbol type>`: 1=bod, 2=linie,
4=plocha, 8=text, 16=kombi — doloženo distribucí v template, sedí s názvoslovím Png2Point/Line/Area)
+ rozpad v `measure_dod.main()`. **Výsledek: plošný strop 58 %** (kdyby gen dokreslil všech 13
chybějících typů ploch). **DoD ≥90 % je plošně NEDOSAŽITELNÉ** — zbytek = linie (18 typů, 1952 obj
→ Png2Line) + body (17 typů, 966 obj → Png2Point), oba modely NEEXISTUJÍ. V rámci plošné cesty jsme
na 43/58 = 74 % plošného stropu.

### 2. 403 integrovat → měření vyvrátilo zadání (volba „403 integrovat")
Cíl uživatele: „aby +403 neznačilo −410" (net-nula Sez. 94). Měření před opravou:
- map_gt **410 detekuje** na všech 3 (0,09–0,31 % px), ale separace vrátí **0 polygonů 410** →
  `MIN_AREA_PX=120` zahodí. Velikosti 410 komponent: **1000+ komponent, největší 27–92 px** =
  rozptýlený tmavě zelený antialiasing, NE souvislá fight plocha (ta by měla stovky+ px).
- **Závěr: souvislé 410 v 3 mapách NEJSOU. forest_age proxy je FABRIKOVAL** (velké 410 z věku
  porostu). `−410` je SPRÁVNÉ; honit ho zpět = míchat fikci s pravdou (Censure pseudorealistic).
Oponoval jsem zadání s daty → uživatel rekalibroval na „přepnout DoD baseline na separaci".

### 3. DoD baseline přepnut na separaci (volba uživatele)
`pairs.build_pair` vyrábí páry **separací** (`predict_areas_sjtsk`), ne forest_age → DoD MUSÍ měřit
reálnou produkční cestu, ne fikci. `measure_dod.main()` teď generuje separační `.omap` (`_gen_sep`,
forest_age='off') jako baseline; forest_age proxy přesunut na `--proxy` (doložení nadhodnocení).
**Baseline 43/37/50 = 43 %** (číselně shodné = net-nula, ale POCTIVÉ): 403 covered na všech 3,
fiktivní 410 missing na všech 3 (ověřeno per-mapa). Plošný gap obj 3231→2126 (403 přešel do covered).

### Výsledek
Měřicí + směrový posun (nepřibyl symbol). Tři domněnky vyvráceny: (1) 90 % plošně dosažitelné →
strop 58 %; (2) 410 poctivá ztráta → šum, proxy fabrikoval; (3) „403 bez −410" → neproveditelné,
místo toho zpravdivěn baseline. Varování do „dotáhnout plošnou fázi": reálný strop < 58 % (pattern
vegetace 404/407/409/113 ~590 obj = dvojitá mezera separace-slepá + vegetace gate). Změněny jen
měřicí nástroje (`compare_isom.py`, `measure_dod.py`) — generátor netknutý, proc baseline drží
triviálně. Censure 0.

### Příště (Sez. 96, HAL3000)
1. *(pokrytí, věrný typ se zdrojem)* **210 Stony ground** (975 obj, druhý největší gap) — probe:
   ZABAGED kamenitou zem nevede → derivace z DMR drsnosti (analogie `rock_relief` 206)? Bez vegetace gate.
2. *(pokrytí, levné)* ZABAGED varianty **310 indistinct marsh / 413 orchard** — napíchnout na --marsh/--surfaces.
3. *(strukturální, velký)* **Png2Line** — největší zbylý gap (18 typů, 1952 obj); generátor linie už kreslí.
4. *(model 1b, odsunuto)* class-balanced expansion (208/501/301.1).

## Sezení 94 — DoD nástroj crosswalk-aware + poctivý matched baseline 43 % + separace = páka kvality

Stroj: **HAL3000** (korpus 268 + CUDA). Sync: `main` up-to-date s origin (HEAD `5dd2870`), clean.
Cadence: žádný audit zralý (%AUDIT:DOCS+pruning právě Sez. 93, CODE 5 zpět, CALIBRATE 8 zpět) →
hlavní tah. Fokus = bod 1 Příště: **Q2 tvrdý DoD na resources mapách** (volba uživatele).

### 1. Matched DoD runner (cesta a) → ale `compare_isom` měří ŠPATNĚ
Postaven `measure_dod.py`: extent z `.pgw` (4 rohy skenu → S-JTSK axis-aligned obal → WGS84
střed+rozměry), `generate_map` matched na obal každé mapy, `compare_isom`. A3 (volba uživatele):
Slovanka (UTM33) + Soví vrch (1/4 domapováno) vynechány → měří Bedřichovka/Blatná/Velbloud.

**Raw baseline 32/36/37 % = PRŮMĚR 35 %** — ale verify-against-source nad číslem odhalil vadu:
chybějící kódy nesly jména **516 „Power line", 509 „Narrow ride", 526 „Building", 518 „Tunnel"**
+ custom CZ („zakop_bunkr", „bílá"). Reálné mapy = **ISOM 2000**, generátor = **2017-2**;
číslování se RECYKLUJE. `compare_isom` (Sez. 91) pároval **naivně integer prefixem** — přesně to,
před čím KB `isom-issprom.md` (Sez. 37-40) varuje a co `.crt` crosswalk řeší. **Tedy i Sez. 91
„38 %" bylo neplatné** (memory `generator-coverage-is-the-ceiling` drží vadné číslo).

### 2. Oprava `compare_isom.py` na crosswalk-aware (volba uživatele: opravit napřímo)
Ověřen směr `.crt` (`<2017-2>  <2000>`) na tvrdých příkladech (526→521, 516→510, 515→509, 518→512,
509→508, 506→505). Verze detekce přes budovu (526 Budova/Building → 2000, 521 → 2017-2; **dřívější
detekce minula Soví vrch — hledala EN „Building", ne CZ „Budova"**). Výsledek: **4/5 map = 2000,
Blatná = 2017** (sedí na KB „4/6 = 2000").

Přidáno: `_load_crosswalk()` (čte `.crt`, integer-prefix, jen 100-599), `detect_version()`,
`coverage()` (verze → 2000 přemapuj na 2017-2 → custom ne-ISOM **vyřaď z jmenovatele** [volba
uživatele] → pokryt = kreslí aspoň 1 z 2017 cílů). `main()` tiskne verzi + per-missing 2017 cíl.

**Crosswalk-aware baseline:** Bedř **43 %** (30/69, raw 32) / Blatná **37 %** (22/60, 2017 → +1 jen
custom exclude) / Velbloud **50 %** (38/76, raw 37) → **PRŮMĚR 43 %**. Crosswalk přidal na 2000
mapách +11/+13 — potvrzuje, že fix opravuje právě podhodnocení 2000 map (Blatná=2017 se nehnula).
`layer_errors` prázdné → baseline důvěryhodný. **Gap je OBSAHOVÝ** (KB Sez. 40 trefa): 416/107/108/507
linie, 418/419/525/527/531 body, 404/407/409 pattern, 210 Stony (ZABAGED nevede), mikroformy.

### 3. Cesta (b) — separace-ze-skenu: páka KVALITY, NE pokrytí (volba uživatele „b")
`map_gt` na downscaled skenu (114 Mpx > ~100 Mpx strop → downscale na TARGET_MPP) → `separate` →
predict plochy v S-JTSK přes `.pgw` afinní → `generate_map(forest_age="off", predict_areas_sjtsk)`.
**Výsledek: net-nula** (43/37/50 beze změny) — **nové [403], ztracené [410]** na všech 3 mapách.
Ověřeno z kódu (generator.py:3140 `if predict_veg: … elif forest_age=="real":` = výlučné): predict
forest_age **úplně nahradí** → AOPK proxy-410 zmizí, separace vrátí věrné 403. **Separace = kvalita
(věrné 403 vs proxy „nesedí"), ne pokrytí.** Metrika kód-presence je navíc shovívavá (proxy-410 počítá
i mispatřené) → věrné pokrytí reálně < 43 %.

### Výsledek
Posun MĚŘICÍ a SMĚROVÝ, ne featurový: nepřibyl symbol, ale dvě „znalosti" (38 % pokrytí; separace
zvedne DoD) se ukázaly mylné. `compare_isom.py` (crosswalk-aware) + `measure_dod.py` (driver) =
keepery. Coverage páka = kreslit nové TYPY symbolů, ne leštit separaci zeleně. Censure 0.

### Příště (Sez. 95, HAL3000/mrkla)
1. *(pokrytí, nový typ — coverage páka)* nejlevnější dopad z gapu: **404/407/409 pattern plochy**
   (generátor kreslit vzor + Y `omap_raster` rozšířit; separace slepá), nebo **210 Stony** (rozhodnout
   zdroj — ZABAGED nevede). Měř `measure_dod.py`.
2. *(analytický cut, levné)* rozbít 90 % DoD podle geometrie (plocha/bod/linie) → realistický strop
   plošného generátoru teď vs co čeká na Png2Line/Png2Point feeder.
3. *(model 1b, odsunuto za pokrytí)* class-balanced expansion (208/501/301.1).
4. *(nový směr)* saský konektor `connectors/saxony.py` → Jonsdorf demo.

## Sezení 93 — Úklidové sezení: %AUDIT:DOCS + pruning + rename forest_age→veg_area

Stroj: **HAL3000** (korpus 268 + CUDA dostupné → plný fokus). Sync: `main` up-to-date s origin
(HEAD `ef01bd2`), working tree clean.

### Volba fokusu (cadence vs hlavní tah)
%BEGIN cadence check: **%AUDIT:DOCS** poslední Sez. 81 → 12 sezení zpět = práh ≥10 **překročen o 2
→ vynucený první bod** (úklid má přednost); **IDEAS/TODO pruning** práh ≥12 **dosažen**. Napětí:
cadence vynucuje úklid, ale hlavní tah je rozšiřování pokrytí generátoru (foundations). Oponoval
jsem „jdeme rovnou na pokrytí" — docs po vlně 82-92 reálně nasbíraly drift. **Volba uživatele:
úklidové sezení** (%AUDIT:DOCS + pruning + forest_age rename, bod 3 Příště). Hlavní tah Q2 = carry.

### 1. %AUDIT:DOCS — 0 kritických / 4 doporučené / 1 kosmetický
3 paralelní Explore agenti (ISOM/vrstvy konzistence · UC5 reframe konzistence · jazyk/broken-links),
nálezy **ověřeny proti zdroji** (verify-against-source — agenti halucinovali kritičnost).

**Zamítnuté agentí falešné poplachy** (proti zdroji):
- `ISOM_FOOTBRIDGE=5122` „export selže" — **ne**, ř. 3653 posílá do `.omap` string `"512.2"`, 5122
  je interní sentinel. Kódová kosmetika pro %AUDIT:CODE, ne docs.
- forest-age „není archivován, rozpor" — **ne**, archivace *jako predikční zdroj vegetace* je doložená
  (separace nahradila Sez. 82), GLOSSARY:375-376 to popisuje správně.
- `[[…]]` cross-ref „broken/neklikatelné" — záměrná wiki-link konvence projektu (i v memory).
- STATISTICS 107 nuly / `predict_areas_sjtsk` mimo CLI — zdokumentováno správně (řídká vrstva / interní kwarg).

**Skutečné nálezy** (jeden vzorec: README + architecture status zaostával za 3 mrkla-sezeními 90/91/92,
jako drift po vlnách Sez. 60/81/89):
- **D1** „16 area tříd" → „16 area kódů + pozadí" (po 403 Sez. 92 je N_AREA 17). Drift širší než agent
  zachytil — sjednoceno: README ×2, architecture, GLOSSARY ×3 (vč. label 0..16), `model/png2area/tile.py`
  komentář (17: pozadí + 16 kódů), generator/README. (TODO milníkové „Sez. 88 = 16 tříd" záznamy ponechány
  — vyřešil je pruning, ne přepis historie.)
- **D2** mIoU **0,621 (Sez. 90) → 0,640 / val 0,654** (stabilizace Sez. 91, cap vah @10 + cosine LR):
  README:125 odstavec, GLOSSARY:282.
- **D3** 403 Rough open (Sez. 92) chyběla v architecture + README → doplněna.
- **D4** Png2Area status zaostával („trénink=mrkla po nočním build_pairs" / Sez. 90) → dopsán první funkční
  reconstructor (test 0,640, datový strop vzácných, DoD pokrytí jako páka): architecture blok, README.
- **K1** GLOSSARY:398 `--forest-age` v přít. čase bez pointeru → doplněno „nahrazen separací Sez. 82".

### 2. IDEAS/TODO pruning (práh ≥12)
- TODO Png2Area blok (22ř. jednoho `[~]` přes Sez. 87-91) → strukturované pod-checkboxy: `Png2Area [x]`
  (pointer DONE Sez. 87/88/90/91) / `class-balanced expansion [~]` / `Png2Point [ ]` / `Png2Line [ ]`.
  Historie zachována v DONE → bezpečné zhuštění. Vedlejší: vyřešil odložený „16 tříd" drift čistě.
- IDEAS „Granularita area tříd" — ODSTÍN bullet → „403 Rough open HOTOVO Sez. 92" značka.
- IDEAS jinak dobře udržovaný (většina bloků už má → DONE / ARCHIVOVÁNO značky) → pruning cílený, ne plošný.

### 3. Rename legacy forest_age → veg_area
TODO úkol (nález Sez. 90) původně řadil „do %AUDIT:CODE". Volba uživatele: **udělat teď, neutrální
`veg_area`** (proti odložení).

**Nález během mapování — rozsah širší než TODO čekal:** `forest_age` proměnné v generate_map jsou
**sdílené nosiče predict (separace) I archiv (AOPK věk)** — na ř. 3140/3146 se stejné `forest_age_*`
plní z `if predict_veg` NEBO `elif forest_age=="real"` a dál zpracovávají jednotně. Navíc kontrakty
mimo generator.py: výstupní soubor `mask_forest_age.png`, meta klíč `real_sections["forest_age"]`,
`omap_export` kwarg `forest_age_features` + counts klíč (čte separate.py + stats.py). → neutrální
`veg_area` (pokrývá obě cesty), ne „predict_veg" (nepřesné).

**Chirurgický řez:**
- **Přejmenováno** (sdílené plošná-zeleň nosiče): `veg_area_features` / `veg_area_info` /
  `veg_area_mask_img` / `veg_area_omap_features` (generator.py) · soubor `mask_veg_area.png` · meta klíč
  `real_sections["veg_area"]` · omap_export kwarg `veg_area_features` + counter `n_veg_area` + counts klíč
  `"veg_area"` · separate.py call+read · stats.py sekce. Komentáře zobecněny (predict separace NEBO archiv věk).
- **Ponecháno legit** (archiv forest-age éra, název správný): konstanty `FOREST_AGE_NAME/FILL/CLASS`
  (archiv-zeleň 406/408/410; superset = `PREDICT_AREA_*`), CLI flag `--forest-age` / param `forest_age`,
  konektor `forest.py` (`fetch_forest_age`/`map_forest_age_to_isom`), funkce `_generate_real_forest_age` /
  `_draw_forest_age_area`, `_try_layer("forest_age")` log label, batch.py `forest_age="off"`.

**Ověření behavior-preserving:**
- py_compile 6/6 · grep čistý (žádný zbylý `forest_age_*` identifikátor mimo legit).
- **noise proc baseline 63=63** (`--terrain noise --paths proc`, seed0/rug0.5/det0.5) — generator +
  omap_export hlavní cesta beze změny.
- **predict E2E `build_pair(1088447)` „Přebor 2025"** (75 s): `mask_veg_area.png` vznikl, meta
  `real_sections["veg_area"]` count 542 provenance `predict` symboly **403/406/408/410**, starý
  `forest_age` meta klíč pryč. Stale `mask_forest_age.png` sirotek (mtime 6.6. 23:58 = Sez. 92) smazán
  → potvrzení, že kód ho už negeneruje.

### Výsledek
12 souborů (README, GLOSSARY, IDEAS, TODO, architecture, generator-procedural, generator/README,
generator.py, omap_export.py, separate.py, stats.py, model/png2area/tile.py). Žádná funkční regrese
(noise 63=63 + predict E2E). Censure 0.

### Příště (Sez. 94, mrkla)
1. *(hlavní tah, `[!]`)* **Q2 — separace na `resources/*.png` → tvrdý DoD.** map_gt na resources sken →
   separace → matched gen `.omap` → `compare_isom` se 403. Dnešní `coverage_union` je schopnostní míra,
   ne matched DoD.
2. *(pokrytí, pokračování rodiny)* další odstínové/snadné: 404 (pattern — rozhodnout přístup), 507 Small
   footpath (linie, ZABAGED?), pak 210 Stony ground (#1 četností, ale ZABAGED nevede — rozhodnout zdroj).
3. *(model 1b, odsunuto za pokrytí)* class-balanced expansion (208/501/301.1).
4. *(nový směr)* saský konektor `connectors/saxony.py` → Jonsdorf demo.
